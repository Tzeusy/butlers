# Environment variables for Butlers AI agent framework
#
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# Credentials approach:
#   Most credentials (Google OAuth, Telegram tokens, etc.) are stored in the
#   butler PostgreSQL database via the dashboard OAuth bootstrap flow.
#   Environment variables below are used only for bootstrap or infrastructure
#   configuration — not for runtime secret storage.

# Required: Your Anthropic API key for Claude Code
ANTHROPIC_API_KEY=your-key-here

# Optional: PostgreSQL connection (required for credential DB storage)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=butlers
# POSTGRES_PASSWORD=your-db-password
# POSTGRES_DB=butlers

# Optional: Google OAuth app credentials (bootstrap only — for the initial
# OAuth flow via the dashboard. After bootstrap, credentials are stored in DB.)
# GOOGLE_OAUTH_CLIENT_ID=your-oauth-client-id
# GOOGLE_OAUTH_CLIENT_SECRET=your-oauth-client-secret
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:40200/api/oauth/google/callback
