name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  session-link-guard:
    # Fast, dependency-free gate (bu-mr5t5): fails the PR when a tool-session
    # link/footer (see scripts/session_link_guard.py) leaks into the PR title,
    # body, review comments, or non-trailer commit text on the current head.
    # Review-comment coverage is best-effort (falls back to an empty set on
    # any API hiccup) so a GitHub API blip never blocks the mandatory
    # title/body/commit checks. Advisory like
    # every other check in this workflow: see docs note below on required-check
    # status.
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Write PR title and body to files
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          printf '%s' "$PR_TITLE" > "${RUNNER_TEMP}/pr_title.txt"
          printf '%s' "$PR_BODY" > "${RUNNER_TEMP}/pr_body.txt"

      - name: Fetch review comments (best-effort)
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          # Each call is independently best-effort: a failed/empty response
          # just means that surface contributes nothing, it never fails the
          # step. Raw (non-jq-filtered) JSON is dumped per surface; the combine
          # script below does the field mapping so no shell/jq quoting games
          # are needed here.
          gh api "repos/${REPO}/pulls/${PR_NUMBER}/comments" --paginate \
            > "${RUNNER_TEMP}/rc_review_comments.json" || true
          gh api "repos/${REPO}/issues/${PR_NUMBER}/comments" --paginate \
            > "${RUNNER_TEMP}/rc_issue_comments.json" || true
          gh api "repos/${REPO}/pulls/${PR_NUMBER}/reviews" --paginate \
            > "${RUNNER_TEMP}/rc_reviews.json" || true
          python3 scripts/combine_review_comment_sources.py \
            --output "${RUNNER_TEMP}/review_comments.json" \
            --input review-comment "${RUNNER_TEMP}/rc_review_comments.json" \
            --input issue-comment "${RUNNER_TEMP}/rc_issue_comments.json" \
            --input review "${RUNNER_TEMP}/rc_reviews.json"

      - name: Scan for tool-session links
        run: |
          python3 scripts/session_link_guard.py \
            --pr-title-file "${RUNNER_TEMP}/pr_title.txt" \
            --pr-body-file "${RUNNER_TEMP}/pr_body.txt" \
            --commit-range "${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}" \
            --review-comments-file "${RUNNER_TEMP}/review_comments.json"

  em-dash-guard:
    # Fast, dependency-free gate (bu-370uj): enforces non-negotiable #6 ("no
    # em-dashes in doctrine or dashboard copy") over the doctrine markdown
    # corpus (about/ essays + roster/*/MANIFESTO.md + roster/*/AGENTS.md). A
    # per-file baseline (scripts/em-dash-baseline.json) freezes pre-existing
    # debt so the gate lands green today while blocking any net-new em-dash and
    # any new dirty file. Frontend user-facing copy is enforced by the eslint
    # rule in the `frontend` job instead. Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check for em-dashes in doctrine + dashboard copy
        run: python3 scripts/check-no-em-dashes.py

  spec-overwrite-guard:
    # Fast, dependency-free gate (bu-s9uv3): `openspec archive` writes the WHOLE
    # requirement block from a change into openspec/specs/, so an unarchived
    # `## MODIFIED Requirements` block authored against an older ancestor
    # silently deletes whatever the baseline gained in between. OpenSpec 1.9.0
    # guards only scenario NAMES, so a block that keeps every name and rewrites
    # the bodies validates clean and still destroys baseline content -- which has
    # happened three times on the same axis. This compares bodies. A digest-keyed
    # ratchet (scripts/spec-overwrite-baseline.json) freezes today's debt and
    # re-fires as soon as an archive moves a requirement under an unarchived
    # block. Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check unarchived MODIFIED spec blocks against the live baseline
        run: python3 scripts/check_spec_overwrites.py

  archived-requirements-guard:
    # Fast, dependency-free gate (bu-966by): the other side of the ledger from
    # spec-overwrite-guard. Archiving a change is supposed to write its
    # requirements into openspec/specs/, and 488 of them across 44 changes never
    # got there while every check stayed green -- `openspec validate` reads delta
    # syntax, `openspec archive` reports what it wrote and not what it skipped,
    # and check_spec_overwrites.py never looks at the baseline side at all. This
    # asserts each archived `## ADDED` requirement exists in the baseline by name
    # and each `## MODIFIED` block's content reached it, failing per requirement
    # so a half-applied archive cannot look green. A digest-keyed ratchet
    # (scripts/archived-requirements-baseline.json) freezes today's debt for
    # bu-tk618 to burn down by hand; there is no re-freeze flag on purpose,
    # and a `## REMOVED` block excuses an absence only once its own change
    # has archived, so a pending proposal cannot mute a gap off-ratchet.
    # Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check archived change requirements reached the baseline
        run: python3 scripts/check_archived_requirements_landed.py

  countable-tasks-guard:
    # Fast, dependency-free gate (bu-h7igs): `openspec archive`'s incomplete-task
    # gate counts markdown checkbox lines only. A tasks.md written as `### N.`
    # sections with per-task acceptance bullets -- richer than most checkbox files
    # here, not lazier -- matches zero of them, so archive reports
    # `Task status: No tasks`, cannot be incomplete, and archives unprompted. The
    # defect is not the missing warning but that the gate's silence is
    # indistinguishable from its pass: "archived with no task warning" reads as
    # evidence the tasks were done. This fails on any unarchived change the gate
    # cannot see, so the difference is restored before the change reaches archive.
    # Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check every change's tasks.md is countable by the archive gate
        run: python3 scripts/check_countable_tasks.py
  cited-requirements-guard:
    # Fast, dependency-free gate (bu-lpwjc): the third side of the ledger. The
    # two guards above read the spec tree against itself and have never opened a
    # test file, so nothing ever checked that a REQ id cited by a test docstring
    # resolves to a definition at all. Measured at introduction: 39 distinct ids
    # are cited, 30 resolve only to a change that has not archived yet, and
    # REQ-cli-runtime-auth-003 resolves nowhere -- there is no cli-runtime-auth
    # capability in the repo. openspec/specs/ and unarchived changes are the two
    # definition sources; openspec/changes/archive/** deliberately is NOT one,
    # because an archived change has already had its chance to write the
    # requirement into the baseline. Counting it would make this blind to
    # exactly the case it exists for (`openspec archive --skip-specs` moves the
    # delta and writes no baseline) and would hide a half-applied archive, since
    # the ids that landed and the ids that did not would read the same.
    # Pending-only citations pass but are printed with the change they lean on:
    # that is the state that rots. A ratchet
    # (scripts/cited-requirements-baseline.json) freezes today's one dangling
    # citation per (test file, id); there is no re-freeze flag on purpose.
    # Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check REQ ids cited by tests resolve to a definition
        run: python3 scripts/check_cited_requirements_resolve.py

  frontend-copy-inventory-guard:
    # Fast, dependency-free gate (bu-erfdj): about/lay-and-land/frontend-copy-inventory.md
    # is generated by scripts/extract-frontend-copy.py, is committed so copy changes
    # surface in review, and tells readers not to edit it by hand -- but nothing ever
    # regenerated it, so it drifted silently while presenting itself as authoritative.
    # Committing the inventory only pays off if it is guaranteed current, so this
    # regenerates it from the checked-out frontend sources and fails on any diff.
    # The generator parses .tsx files directly (no node_modules, no npm install) and
    # its output is a pure function of those files with no timestamp or other
    # nondeterministic field, so this gate cannot flap. Advisory like every other
    # check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Regenerate the frontend copy inventory
        run: python3 scripts/extract-frontend-copy.py

      - name: Check the committed inventory matches the regenerated one
        run: |
          if ! git diff --exit-code -- about/lay-and-land/frontend-copy-inventory.md; then
            echo "::error file=about/lay-and-land/frontend-copy-inventory.md::Frontend copy inventory is stale. Regenerate it with 'python3 scripts/extract-frontend-copy.py' and commit the result."
            exit 1
          fi

  duplicate-name-guard:
    # Fast, dependency-free gate (bu-ayrbg): two branches each added a
    # module-level `_fenced_code_blocks` to one test file with incompatible
    # return types. The definitions were far apart, git auto-merged them, and
    # the later one silently shadowed the earlier one for every caller --
    # including a security guard that then iterated the characters of a string
    # and returned [] for every input, so a runnable pg_restore.sh in a fenced
    # block would have passed REQ-database-security-006 unnoticed.
    #
    # The non-obvious part is that `ruff check` is green on that tree. F811
    # (redefinition of unused name) does not fire when the FIRST definition is
    # USED between the two definitions -- the name is not "unused" at the point
    # of redefinition. That intervening-use condition is F811's blind spot, and
    # it is exactly what a merge produces, since each branch defines the name
    # and uses it. Per-PR CI cannot see it either: both PRs are green alone and
    # CI never builds the union of open PRs.
    #
    # This asserts each module-level name is bound once per file, independent of
    # use. It scans the lint gate's own scope (src/, tests/, roster/,
    # conftest.py) and fails if it finds zero Python files, so it cannot go
    # quiet the way the shadowed guard did. Ratchet:
    # scripts/duplicate-toplevel-names-baseline.json, shipped empty, with no
    # re-freeze flag on purpose. Advisory like every other check here.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Check for duplicate module-level names
        run: python3 scripts/check_duplicate_toplevel_names.py

  # Preserve the historic `check` status as a fail-closed fan-in. The
  # one-time preflight and seven duration-balanced file shards all begin
  # independently; no broad lane serializes the backend critical path.
  check-preflight:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Check lock file is up to date
        run: uv lock --check

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Lint
        run: make lint

      - name: Format check
        run: uv run ruff format --check src/ tests/ roster/ conftest.py -q

      - name: SQL safety check (FOR UPDATE + outer join)
        run: make check-for-update-joins

      - name: Verify CI test shard manifests
        # This replaces the old integration-only path guard. It derives both
        # marker selections with pytest --collect-only and rejects gaps,
        # duplicates, stale files, and zero-selected manifests.
        run: uv run python scripts/check_ci_test_shards.py verify

      - name: Smoke tests (fast gate + release evidence)
        id: smoke
        env:
          SMOKE_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/smoke
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: |
          mkdir -p "$SMOKE_EVIDENCE_DIR"
          SMOKE_CMD="uv run pytest tests/ --ignore=tests/e2e -m smoke -q --tb=short"

          START_TS=$(date +%s)
          set +e
          $SMOKE_CMD --junit-xml="$SMOKE_EVIDENCE_DIR/raw-junit.xml" 2>&1 | tee "$SMOKE_EVIDENCE_DIR/smoke.log"
          SMOKE_EXIT=${PIPESTATUS[0]}
          set -e
          END_TS=$(date +%s)
          DURATION_S=$((END_TS - START_TS))
          [ "$SMOKE_EXIT" -eq 0 ] && STATUS=PASS || STATUS=FAIL

          # Write release-evidence JSON and emit a clearly-formatted log line.
          SMOKE_EXIT_VAL=$SMOKE_EXIT \
          SMOKE_SHA="${{ github.sha }}" \
          SMOKE_DURATION=$DURATION_S \
          SMOKE_STATUS=$STATUS \
          SMOKE_CMD_VAL="$SMOKE_CMD" \
          SMOKE_EVIDENCE_DIR="$SMOKE_EVIDENCE_DIR" \
          python3 << 'PYEOF'
          import json, os, sys, xml.etree.ElementTree as ET

          sha = os.environ["SMOKE_SHA"]
          duration_s = int(os.environ["SMOKE_DURATION"])
          exit_code = int(os.environ["SMOKE_EXIT_VAL"])
          status = os.environ["SMOKE_STATUS"]
          cmd = os.environ["SMOKE_CMD_VAL"]
          evidence_dir = os.environ["SMOKE_EVIDENCE_DIR"]

          try:
              root = ET.parse(os.path.join(evidence_dir, "raw-junit.xml")).getroot()
              skipped_classes = sorted({
                  tc.get("classname") or tc.get("name", "unknown")
                  for tc in root.findall(".//testcase")
                  if tc.find("skipped") is not None
              })
          except (ET.ParseError, OSError) as exc:
              print(exc, file=sys.stderr)
              skipped_classes = ["unavailable"]

          evidence = {
              "cmd": cmd,
              "sha": sha,
              "duration_s": duration_s,
              "status": status,
              "exit_code": exit_code,
              "skipped_classes": skipped_classes,
          }
          with open(os.path.join(evidence_dir, "release-evidence.json"), "w") as f:
              json.dump(evidence, f, indent=2)

          print(f"SMOKE-RELEASE-EVIDENCE cmd={cmd!r} sha={sha!r} duration={duration_s}s status={status!r} skipped_classes={skipped_classes!r}")
          print()
          print(json.dumps(evidence, indent=2))
          PYEOF

          exit $SMOKE_EXIT

      - name: Prepare smoke test duration evidence
        if: ${{ always() && steps.smoke.outcome != 'skipped' }}
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/smoke/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/smoke/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/smoke/top-durations.txt" \
            --limit 25

      - name: Upload smoke release evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-release-evidence
          path: ${{ runner.temp }}/ci-artifacts/smoke/release-evidence.json
          retention-days: 30
          if-no-files-found: warn
          overwrite: true

      - name: Upload smoke test evidence
        if: ${{ always() && steps.smoke.outcome != 'skipped' }}
        uses: actions/upload-artifact@v4
        with:
          name: ci-smoke-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/smoke/junit.xml
            ${{ runner.temp }}/ci-artifacts/smoke/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

  check-unit-1:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 1)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-1.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-1
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 1

      - name: Prepare unit shard 1 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-1/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-1/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-1/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 1 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-1-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-1/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-1/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 1 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-1.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-2:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 2)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-2.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-2
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 2

      - name: Prepare unit shard 2 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-2/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-2/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-2/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 2 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-2-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-2/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-2/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 2 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-2.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-3:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 3)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-3.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-3
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 3

      - name: Prepare unit shard 3 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-3/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-3/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-3/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 3 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-3-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-3/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-3/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 3 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-3.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-4:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 4)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-4.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-4
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 4

      - name: Prepare unit shard 4 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-4/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-4/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-4/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 4 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-4-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-4/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-4/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 4 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-4.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-1:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # ubuntu-latest runners arrive with ~14 GB free. Testcontainers pulls
        # pgvector/pgvector:pg17 (~442 MB compressed) and multiple modules spin
        # up independent containers concurrently under -n auto. Removing
        # pre-installed tools that are unused by this job (Android SDK, .NET,
        # Haskell, Swift, large apt caches) reclaims 10-15 GB and eliminates
        # the intermittent "no space left on device" / asyncpg pool timeout
        # failures seen in PRs #2052 and #2055.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/share/boost
          sudo apt-get clean -q
          docker image prune -f --filter "until=1h" || true
          echo "Disk usage after cleanup:"
          df -h /

      - name: Integration tests (shard 1)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-1.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-1
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 1

      - name: Prepare integration shard 1 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-1/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-1/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-1/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 1 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-1-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-1/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-1/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 1 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-1.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-2:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # ubuntu-latest runners arrive with ~14 GB free. Testcontainers pulls
        # pgvector/pgvector:pg17 (~442 MB compressed) and multiple modules spin
        # up independent containers concurrently under -n auto. Removing
        # pre-installed tools that are unused by this job (Android SDK, .NET,
        # Haskell, Swift, large apt caches) reclaims 10-15 GB and eliminates
        # the intermittent "no space left on device" / asyncpg pool timeout
        # failures seen in PRs #2052 and #2055.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/share/boost
          sudo apt-get clean -q
          docker image prune -f --filter "until=1h" || true
          echo "Disk usage after cleanup:"
          df -h /

      - name: Integration tests (shard 2)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-2.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-2
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 2

      - name: Prepare integration shard 2 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-2/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-2/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-2/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 2 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-2-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-2/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-2/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 2 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-2.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-3:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # ubuntu-latest runners arrive with ~14 GB free. Testcontainers pulls
        # pgvector/pgvector:pg17 (~442 MB compressed) and multiple modules spin
        # up independent containers concurrently under -n auto. Removing
        # pre-installed tools that are unused by this job (Android SDK, .NET,
        # Haskell, Swift, large apt caches) reclaims 10-15 GB and eliminates
        # the intermittent "no space left on device" / asyncpg pool timeout
        # failures seen in PRs #2052 and #2055.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/share/swift
          sudo rm -rf /usr/local/share/boost
          sudo apt-get clean -q
          docker image prune -f --filter "until=1h" || true
          echo "Disk usage after cleanup:"
          df -h /

      - name: Integration tests (shard 3)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-3.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-3
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 3

      - name: Prepare integration shard 3 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-3/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-3/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-3/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 3 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-3-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-3/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-3/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 3 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-3.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check:
    # Do not allow a green historic check status when a preflight or any shard
    # failed or was skipped. The fan-in also requires all seven coverage files.
    needs: [check-preflight, check-unit-1, check-unit-2, check-unit-3, check-unit-4, check-integration-1, check-integration-2, check-integration-3]
    if: ${{ always() && !cancelled() }}
    runs-on: ubuntu-latest

    steps:
      - name: Require preflight and every test shard to pass
        env:
          CHECK_PREFLIGHT_RESULT: ${{ needs.check-preflight.result }}
          CHECK_UNIT_1_RESULT: ${{ needs.check-unit-1.result }}
          CHECK_UNIT_2_RESULT: ${{ needs.check-unit-2.result }}
          CHECK_UNIT_3_RESULT: ${{ needs.check-unit-3.result }}
          CHECK_UNIT_4_RESULT: ${{ needs.check-unit-4.result }}
          CHECK_INTEGRATION_1_RESULT: ${{ needs.check-integration-1.result }}
          CHECK_INTEGRATION_2_RESULT: ${{ needs.check-integration-2.result }}
          CHECK_INTEGRATION_3_RESULT: ${{ needs.check-integration-3.result }}
        run: |
          failed=0
          for named_result in \
            "check-preflight=$CHECK_PREFLIGHT_RESULT" \
            "check-unit-1=$CHECK_UNIT_1_RESULT" \
            "check-unit-2=$CHECK_UNIT_2_RESULT" \
            "check-unit-3=$CHECK_UNIT_3_RESULT" \
            "check-unit-4=$CHECK_UNIT_4_RESULT" \
            "check-integration-1=$CHECK_INTEGRATION_1_RESULT" \
            "check-integration-2=$CHECK_INTEGRATION_2_RESULT" \
            "check-integration-3=$CHECK_INTEGRATION_3_RESULT"; do
            case "$named_result" in
              *=success) ;;
              *)
                echo "::error::$named_result"
                failed=1
                ;;
            esac
          done
          exit "$failed"

      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies for coverage aggregation
        run: uv sync --frozen --dev

      - name: Download unit shard 1 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-1

      - name: Download unit shard 2 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-2

      - name: Download unit shard 3 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-3

      - name: Download unit shard 4 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-4

      - name: Download integration shard 1 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-1

      - name: Download integration shard 2 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-2

      - name: Download integration shard 3 coverage data
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-3

      - name: Combine coverage from all independent test shards
        id: coverage
        env:
          UNIT_1_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-1/coverage-unit-1.data
          UNIT_2_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-2/coverage-unit-2.data
          UNIT_3_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-3/coverage-unit-3.data
          UNIT_4_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-4/coverage-unit-4.data
          INTEGRATION_1_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-1/coverage-integration-1.data
          INTEGRATION_2_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-2/coverage-integration-2.data
          INTEGRATION_3_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-3/coverage-integration-3.data
          COMBINED_COVERAGE: ${{ runner.temp }}/ci-coverage/coverage-combined.data
          COMBINED_REPORT: ${{ runner.temp }}/ci-artifacts/coverage/coverage.json
        run: |
          mkdir -p "$(dirname "$COMBINED_COVERAGE")" "$(dirname "$COMBINED_REPORT")"
          for coverage_file in \
            $UNIT_1_COVERAGE \
            $UNIT_2_COVERAGE \
            $UNIT_3_COVERAGE \
            $UNIT_4_COVERAGE \
            $INTEGRATION_1_COVERAGE \
            $INTEGRATION_2_COVERAGE \
            $INTEGRATION_3_COVERAGE; do
            test -s "$coverage_file"
          done
          uv run coverage combine --data-file="$COMBINED_COVERAGE" \
            $UNIT_1_COVERAGE \
            $UNIT_2_COVERAGE \
            $UNIT_3_COVERAGE \
            $UNIT_4_COVERAGE \
            $INTEGRATION_1_COVERAGE \
            $INTEGRATION_2_COVERAGE \
            $INTEGRATION_3_COVERAGE
          uv run coverage json --data-file="$COMBINED_COVERAGE" -o "$COMBINED_REPORT"
          uv run coverage report --data-file="$COMBINED_COVERAGE" --show-missing
          export COMBINED_REPORT
          COVERAGE=$(python3 -c 'import json, os; print(round(json.load(open(os.environ["COMBINED_REPORT"]))["totals"]["percent_covered"]))')
          echo "percentage=$COVERAGE" >> "$GITHUB_OUTPUT"
          # Color thresholds: green >=80, yellow >=60, red <60.
          if [ "$COVERAGE" -ge 80 ]; then
            echo "color=brightgreen" >> "$GITHUB_OUTPUT"
          elif [ "$COVERAGE" -ge 60 ]; then
            echo "color=yellow" >> "$GITHUB_OUTPUT"
          else
            echo "color=red" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload combined coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-combined-coverage-report
          path: ${{ runner.temp }}/ci-artifacts/coverage/coverage.json
          retention-days: 14
          if-no-files-found: warn
          overwrite: true

      - name: Update coverage badge
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: ${{ secrets.COVERAGE_GIST_ID }}
          filename: butlers-coverage.json
          label: coverage
          message: ${{ steps.coverage.outputs.percentage }}%
          color: ${{ steps.coverage.outputs.color }}

  frontend:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Em-dash copy gate (bu-370uj)
        run: npm run lint:emdash

      - name: Query-result coercion gate (bu-ep4ks.5)
        run: npm run lint:query-coercion

      - name: Import graph (knip)
        run: npm run knip

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test

  frontend-e2e:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npm run test:e2e:install

      - name: Build (required for vite preview)
        run: npm run build

      - name: Run Playwright e2e tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7
