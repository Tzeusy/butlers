{
 "run": "2026-07-04-jarvis-pursuit",
 "workflow": "wf_f430e4fb-8ff",
 "audits": [
  {
   "findings": [
    {
     "title": "Dashboard bypasses the canonical liveness model bu-86c4c.17 built \u2014 fleet state forks into a second dialect",
     "evidence": "frontend/src/components/overview/model.ts:151-153 (local HEALTHY/DEGRADED/OFFLINE status sets), model.ts:271-311 (deriveButlerIndexRow: own 5-min stale threshold from useButlers + useButlerHeartbeats); vs frontend/src/hooks/use-butler-status-board.ts:1-15 (GET /api/butlers/board: server-side canonical activity verb + cron-cadence expectations, 'shared by every consumer'); frontend/src/hooks/event-cache-registry.ts:79-93 (sessionPatch invalidates [\"butlers\",\"board\"] but NOT the dashboard's [\"butlers\"]/[\"system-butler-heartbeats\"] keys; heartbeatPatch:136-138 is a deliberate no-op)",
     "severity": "major",
     "detail": "The epic's judgment-layer bead landed ONE canonical liveness verdict (running/idle/overdue/offline, cadence-aware: 'silent 3d, expected daily') on /butlers and /system \u2014 but the flagship Overview still hand-rolls its own client-side liveness from two separately-polled queries. Consequences: (1) the dashboard and the board can render contradictory verdicts for the same butler at the same instant; (2) the dashboard has zero cadence awareness \u2014 a weekly butler and a broken hourly butler both read 'healthy' while heartbeats beat; (3) the event bus live-patches the board key, so /butlers updates in the same beat while / lags its 30s poll \u2014 the single pane the owner is told to trust is the stalest fleet-health surface in the app."
    },
    {
     "title": "Approve/Deny/Defer on the dashboard fire immediately with neither undo window nor confirm \u2014 two safety contracts for the same verbs",
     "evidence": "frontend/src/pages/DashboardPage.tsx:139-141 (onApprove/onDeny/onDefer call mutate() directly); vs frontend/src/pages/ApprovalsPage.tsx:1308-1325 (scheduleDecision wraps the same mutations in UNDO_WINDOW_MS with a toast Undo action); frontend/src/hooks/use-approval-decisions.ts:71-89 (approve dispatches the tool on success \u2014 irreversible)",
     "severity": "major",
     "detail": "On /approvals a keyboard decision gets a grace window with an Undo toast before the mutation runs; on the dashboard's Needs-attention rows the identical verb executes instantly \u2014 one accidental click on the page the owner lands on first dispatches an irreversible tool execution with no recovery path. Design-bar recovery bias: 'never replace a confirm with an undo that can't undo' \u2014 here there is neither. The shared hook exists (use-approval-decisions.ts) but the undo-window logic stayed page-local in ApprovalsPage instead of moving into it."
    },
    {
     "title": "Cost Today renders a bold $0.00 when the spend-summary fetch fails; Most Expensive Sessions renders 'No session data available' on error",
     "evidence": "frontend/src/pages/DashboardPage.tsx:121 (costQuery.isError \u2192 costData null), :233 (totalCostUsd={costData?.total_cost_usd ?? 0}), :125 (topSessions = isError ? []); frontend/src/components/costs/CostWidget.tsx:8-22,54 (no summary-error prop \u2014 only dailyCostsError for the sparkline); frontend/src/components/costs/TopSessionsTable.tsx:57-68 (empty === error)",
     "severity": "major",
     "detail": "Truth amnesty (bu-86c4c.1/.2) landed for the sparkline (honest 'trend unavailable') and the KPI strip (em-dashes on error) but stopped one prop short: the widget's headline number and the sessions table still let a failed fetch impersonate a zero-spend, zero-session day. NOT-LANDED residue of the fabrication/error-as-empty sin on the exact surface the epic targeted \u2014 a dead spend API reads as 'the fleet spent nothing today', the opposite of earned calm."
    },
    {
     "title": "The cost band is a second visual instrument: shadcn cards, Title Case sans headings, badges and ghost buttons on a page the spec says is card-free",
     "evidence": "openspec/specs/dashboard-design-language/spec.md \u00a7Density and Spacing ('rule-separated rows\u2026 never cards; padding: 24px on a list item is card thinking and forbidden') and \u00a7Eyebrow Section Titles; frontend/src/components/costs/CostWidget.tsx:34-52 (Card/CardHeader/CardTitle 'Cost Today', ghost Button, bg-primary/60 bars, animate-pulse); TopSessionsTable.tsx:40-75 (Card, Badge, Title Case table); DashboardPage.tsx:114-120 comment shows dashboard-domain-pages spec REQUIRES these widgets \u2014 the two specs disagree",
     "severity": "major",
     "detail": "Everything above the fold is Dispatch (mono eyebrows, hairline rules, serif voice); the full-width band below is the pre-redesign shadcn dialect \u2014 cards, bold Title Case CardTitles, secondary badges, en-US-hardcoded timestamps (TopSessionsTable.tsx:31), a primary-tinted sparkline outside the chart tokens. The page visibly reads as two hands. Per design-bar doctrine the spec disagreement itself (dashboard-domain-pages mandates CostWidget/TopSessionsTable; dashboard-design-language forbids cards) is a finding: one of the two specs must yield."
    },
    {
     "title": "Failed-notification signal is event-dead: the bus patches keys the dashboard never reads, and the stats hook has no poll",
     "evidence": "frontend/src/hooks/event-cache-registry.ts:100-104 (notificationPatch invalidates [\"messenger-delivery-stats\"], [\"messenger-queue-depth\"], [\"timeline\"] \u2014 not [\"notification-stats\"] or [\"notifications\"]); frontend/src/hooks/use-notifications.ts:32-37 (useNotificationStats: no refetchInterval, no staleTime); frontend/src/lib/query-client.ts (staleTime 30s, refetch only on focus)",
     "severity": "major",
     "detail": "The dashboard's 'N failed notifications' rows (attention list + Now list, model.ts:499-515,590-603) read [\"notification-stats\"]. A notification that fails after page load emits a live event on the bus, the registry consumes it \u2014 and invalidates only messenger-page keys. On the pane designed to be left open, the delivery-failure signal never appears until the owner blurs and refocuses the tab. Stale state silently presented as current; the declarative-registry promise ('adding a surface means adding one entry') was not kept in sync with the surfaces that already existed."
    },
    {
     "title": "KPI 'Healthy' counts by status string only, contradicting the attention list on the same screen",
     "evidence": "frontend/src/components/overview/model.ts:258 (healthyButlers = butlers.filter(isHealthyStatus(butler.status)) \u2014 heartbeats ignored); vs model.ts:184-186 + 271-311 (attention rows derive stale/offline FROM heartbeats)",
     "severity": "minor",
     "detail": "A butler with status 'ok' but a 20-minute-dead heartbeat produces an '<name> heartbeat is stale' attention row while the KPI strip directly beneath reports it inside 'Healthy: 9/9'. Two verdicts about the same butler, four hundred pixels apart, from the same render pass. Falls out automatically if the KPI consumes the canonical board verdict (finding 1)."
    },
    {
     "title": "Now-list activity rows discard the event identity they hold \u2014 drill-down dead end into an unanchorable /timeline",
     "evidence": "frontend/src/components/overview/model.ts:614-623 (row id is now:activity:${event.id} but href is flat \"/timeline\"; event.data with session_id discarded); frontend/src/pages/TimelinePage.tsx:76-88 (filters are useState only \u2014 the page accepts no deep-link/search params at all)",
     "severity": "minor",
     "detail": "The freshest signals on the page (last two timeline events) link to the generic chronicle, where the owner must re-find the event they just clicked. The drill-down sweep (bu-86c4c.4) gave attention rows real per-butler anchors but the Now column kept flat hrefs; not covered by bu-9d5vp (KPI cells/egress/audit/spend) or bu-ddo0n (attention strip/scrubber). Session-type events could link straight to /sessions/<id> today; the rest need TimelinePage to accept ?event=/?butler= params."
    },
    {
     "title": "The verdict prose is a 5-minute-stale cache with zero clause-doors, and its loading fallback asserts unproven health",
     "evidence": "src/butlers/api/routers/dashboard_briefing.py:17-19 (per-owner LRU+TTL 5-min cache), frontend/src/hooks/use-briefing.ts:23-36 (mirrored 5-min refetch); frontend/src/components/overview/Headline.tsx:33-34 + Elaboration.tsx:32-34 (plain-text render, no links); frontend/src/pages/DashboardPage.tsx:160-161 (fallback: 'Butlers are running.' before any source has loaded)",
     "severity": "minor",
     "detail": "The attention list beneath the headline updates on the live event bus; the judgment above it can lag five minutes \u2014 'All quiet.' over a critical issue row that arrived 3 minutes ago is a coherence break at the page's most prominent surface. The north-star property 'every clause of that prose is a door' is absent: neither headline nor elaboration carries a single anchor. And while everything loads, the fallback elaboration asserts 'Butlers are running.' \u2014 a claim the page cannot yet prove, mildly violating no-fabrication in the very voice meant to embody it."
    }
   ],
   "page": "dashboard",
   "verdict": "solid",
   "jarvis_gap": "The epic's landings genuinely hold on this page \u2014 the attention list, KPI strip, and butler index all distinguish error from empty; the event bus is mounted app-wide; inline decisions exist; the briefing is a real classified verdict, not decoration. What separates it from JARVIS is coherence and follow-through: the page runs TWO liveness models (its own client-side one vs the canonical server verdict the same epic built), TWO visual dialects (Dispatch above, legacy shadcn cards below), TWO safety contracts for the identical approve verb (undo-window on /approvals, instant-fire here), and a live event bus that patches keys the page's notification signal never reads. The verdict prose \u2014 the page's soul \u2014 is a 5-minute cache with no clause-doors sitting above a live-updating evidence list it can contradict. Each seam is small; together they make the single pane read as assembled rather than built by one hand, and earned calm leaks out through exactly those seams.",
   "moves": [
    {
     "title": "Move the Overview onto the canonical board verdict (one liveness model, live by default)",
     "why": "Kills the fleet-state fork (findings 1 and 6) at the root: ButlerIndex, the runtime attention rows, and the Healthy KPI all consume GET /api/butlers/board's server-computed activity verb + cadence status instead of model.ts's private status sets. The dashboard gains cadence judgment ('silent 3d, expected daily' instead of a flat 'healthy'), stops disagreeing with /butlers and /system, and becomes live for free \u2014 the event bus already patches [\"butlers\",\"board\"] on every session event.",
     "cost": "M",
     "integration_points": "frontend/src/components/overview/model.ts (delete deriveButlerIndexRow + status sets; take BoardRow[] as input), frontend/src/pages/DashboardPage.tsx (swap useButlers+useButlerHeartbeats for useButlerStatusBoard), frontend/src/hooks/use-butler-status-board.ts (already exposes activity/cellTone/cadence), event-cache-registry.ts (no change needed \u2014 board key is patched)",
     "slice_plan": "1) model.ts accepts BoardRow[] and maps activity verb \u2192 attention rows + KPI healthy count (tests first, pure function); 2) DashboardPage swaps hooks, deletes the two dead queries; 3) ButlerIndex subtitle renders cadence status ('overdue \u00b7 expected daily') and cost from the board row; 4) delete HEALTHY/DEGRADED/OFFLINE_STATUSES + stale-threshold logic and their tests."
    },
    {
     "title": "One decision safety contract: fold the undo window into useApprovalDecisionMutations",
     "why": "The same verb must carry the same recovery everywhere (finding 2). Approve dispatches an irreversible tool execution; today the flagship page fires it on a single un-undoable click while /approvals gives a grace window. Moving scheduleDecision/UNDO_WINDOW_MS into the shared hook makes every current and future decision surface (dashboard, approvals, upcoming chat widget bu-p6ey8) inherit apply-in-frame optimism WITH the undo toast \u2014 the exact north-star action contract.",
     "cost": "S",
     "integration_points": "frontend/src/hooks/use-approval-decisions.ts (add withUndoWindow wrapper + module-scoped scheduled-decision store lifted from ApprovalsPage.tsx:1280-1325), frontend/src/pages/DashboardPage.tsx:139-141, frontend/src/pages/ApprovalsPage.tsx (delete its local copy), frontend/src/components/overview/AttentionList.tsx (row shows 'Approving in 5s \u00b7 Undo' state)",
     "slice_plan": "1) extract scheduleDecision into the hook behind an option, unit-test timer/cancel/rollback; 2) ApprovalsPage consumes the extracted version (behavior-identical, its tests prove it); 3) dashboard rows opt in; row renders the pending-undo state inline so the toast isn't the only affordance."
    },
    {
     "title": "Retire the shadcn cost band: spend verdict as a Dispatch strip, truth-complete on error",
     "why": "Fixes the page's largest dialect break and its last error-impersonates-empty hole in one move (findings 3 and 4). One Spend (/spend, bu-86c4c.11) already owns cost depth \u2014 the Overview needs a verdict-sized spend surface, not two legacy cards duplicating it in a foreign visual language. Also resolves the dashboard-domain-pages vs dashboard-design-language spec conflict by amending the former.",
     "cost": "M",
     "integration_points": "frontend/src/pages/DashboardPage.tsx:221-242 (delete band), frontend/src/components/costs/CostWidget.tsx + TopSessionsTable.tsx (retire from this page), frontend/src/components/overview/KpiStrip.tsx (add 'Spend \u00b7 today' cell: mega-number, em-dash + source-named note when costQuery.isError, RowLink to /spend), openspec/specs/dashboard-domain-pages (spec delta via spec workflow), frontend e2e specs referencing cost-widget testids",
     "slice_plan": "1) spec delta: Overview's cost obligation becomes 'one honest spend KPI + top-session line, Dispatch dialect, links to /spend'; 2) add the KPI cell with error/empty/loaded states (em-dash on error, never 0); 3) one rule-separated 'most expensive session today' row in the Now column linking to /sessions/<id>; 4) delete the band + update e2e fixtures."
    },
    {
     "title": "Close the event-bus coverage holes with a registry\u2194hook contract test",
     "why": "Finding 5 is one instance of a class: the declarative registry drifts from the query keys surfaces actually use, and nothing fails when it does. Patch notificationPatch to invalidate [\"notification-stats\"]/[\"notifications\"], then add a test that walks every query key the Overview (and each live page) mounts and asserts it is covered by either a registry patch or an explicit poll \u2014 so the next new surface can't silently go event-dead.",
     "cost": "S",
     "integration_points": "frontend/src/hooks/event-cache-registry.ts:100-104, frontend/src/hooks/use-notifications.ts:32-37, new frontend/src/hooks/event-cache-registry.coverage.test.ts (import dashboard hook key constants; assert patch-or-poll), src/butlers/api/routers/events.py EVENT_TYPES comment sync",
     "slice_plan": "1) one-line registry fix + regression test that a notification event invalidates notification-stats; 2) export key constants from hooks that inline them today; 3) coverage test enumerating Overview's nine query keys against the registry + refetchInterval presence."
    },
    {
     "title": "Clause-doored live verdict: the headline judges the same model the page renders",
     "why": "The north star's signature property \u2014 'every clause of that prose is a door' \u2014 is absent at its most prominent location, and the cached verdict can contradict live evidence beneath it (finding 8). Compute state_class client-side from the SAME live triage model (attention rows, board verdicts, pending approvals) so headline and evidence can never disagree; keep the LLM elaboration as flavor but return it as structured clause spans with anchor targets so each clause links to its evidence (approval id, butler, issue group).",
     "cost": "L",
     "integration_points": "frontend/src/components/overview/model.ts (add classifyTriage(state) mirroring briefing/classify.py rules), frontend/src/components/overview/Headline.tsx + Elaboration.tsx (render clause spans as RowLink-styled inline anchors), src/butlers/api/routers/dashboard_briefing.py + briefing/prompts.py (elaboration returns [{text, href}] clauses; voice-lint per clause), src/butlers/api/briefing/classify.py (share thresholds via the API so the two classifiers can't drift)",
     "slice_plan": "1) client-side state_class from the live model drives the headline immediately (templated headline_for strings ported; briefing endpoint keeps serving elaboration only); 2) briefing response gains clauses[] with typed anchors, plain string kept during migration; 3) Elaboration renders clause anchors (keyboard-focusable, Dispatch underline-on-hover); 4) invalidate the server cache on high-severity fleet events so the LLM voice re-writes within the beat, not the TTL."
    }
   ],
   "ideal": "The Overview opens with a verdict computed from the exact same live model as every row beneath it \u2014 \"Good morning. Two things need you.\" \u2014 where \"two things\" is a door to the attention list, each elaboration clause anchors to its evidence (the approval, the stale butler, the issue group), and the prose re-writes itself within a heartbeat of the fleet changing because headline, KPI strip, butler index, and Now column all consume one canonical server verdict (activity verb + cadence expectation) patched by the event bus in the same frame. The attention list is the only place a signal appears (no triple-rendering across KPI/Now/attention), every row is one keystroke from root evidence (j/k roving focus, enter to drill, a/d/x to decide with a 5-second undo toast identical to /approvals), and every decision applies optimistically with visible rollback. Failure states name themselves in the row where the data would have been \u2014 a dead spend source is an em-dash captioned \"spend source unreachable\", never $0.00 \u2014 and the entire page, cost surface included, speaks one Dispatch dialect: hairline rules, mono eyebrows, serif voice, no cards, so the owner verifies fleet health in five seconds, clears the queue in under a minute, and never wonders whether the calm is earned or fabricated."
  },
  {
   "findings": [
    {
     "title": "Restore succeeds but the board keeps showing QUARANTINED for up to 30s",
     "evidence": "frontend/src/hooks/use-general.ts:57-59 \u2014 useSetEligibility's onSuccess invalidates only [\"switchboard-registry\"], a key the board never consumes (board key is [\"butlers\",\"board\"], use-butler-status-board.ts:189). ButlersPage.tsx:133-144 fires the mutation and toasts \"restored\", but no invalidation, no optimistic patch, and no fleet-bus event type covers eligibility transitions (event-cache-registry.ts:148-156 has session/approval/spend/... but no eligibility/registry type), so the red rail + QUARANTINED chip persist until the next 30s poll while the toast claims success.",
     "severity": "major",
     "detail": "Direct violation of 'every owner action applies in the same frame'. After the 5s undo window elapses and the mutation succeeds, isRestorePending goes false and the chip reverts to QUARANTINED (stale cache), inviting a second restore click. Fix is one line of invalidation plus an optimistic row patch; the streaming half is an 'eligibility' event type on the bus so server-side quarantines also arrive live instead of on poll."
    },
    {
     "title": "\"All N butlers healthy\" renders when liveness is unknowable \u2014 error impersonating health",
     "evidence": "Backend rule 3 maps heartbeat-unavailable rows to activity=\"unknown\" (src/butlers/api/routers/butlers.py:568-569; heartbeat_unavailable = registry_source_error OR schema_unreachable, line 643). The frontend's needs-you filter excludes \"unknown\" (use-butler-status-board.ts:174 NEEDS_YOU_ACTIVITIES = offline|quarantined|overdue), so when the switchboard registry query fails fleet-wide (butlers.py:790-792) every row is unknown, needsYou is empty, and NeedsYouStrip.tsx:59-67 renders the calm all-clear line \"All N butlers healthy\".",
     "severity": "major",
     "detail": "The board's lead judgment line asserts health it cannot prove, directly contradicting the amber sources-degraded pill in the same header. Same failure per-entry: one butler with schema_unreachable disappears from the strip entirely (its chip shows \"\u2014\" in the grid, but the verdict line still says all healthy). The all-clear must be gated on zero unknown rows and zero source errors, and unknown rows should surface as 'N unverifiable (registry down)'."
    },
    {
     "title": "Hourly-stripe query failure fabricates a confident zero-activity stripe and SESS\u00b724H = 0",
     "evidence": "src/butlers/api/routers/butlers.py:497-503 \u2014 _fetch_board_hourly_stripe swallows any exception and returns ([0]*24, 0) with no degradation flag. schema_unreachable is only set by pool KeyError or the two session queries (lines 616-641); if those succeed but the stripe query times out, the row ships hourly_total=0 and an all-zero stripe. StatusBoardCell.tsx:281-284 then renders SESS 24H as a confident \"0\" and ActivityStripe paints 24 empty cells; the zeros also fold into aggregates.total_sessions_24h (butlers.py:731) shown in BoardFooter.",
     "severity": "major",
     "detail": "Classic errors-impersonating-empty, one layer below the truth-marking the epic added. The UI already has the honest affordance ('data unavailable', StatusBoardCell.tsx:324-333) \u2014 it just never fires for this failure mode because the backend collapses it to zeros. Needs a per-row stripe_unavailable flag instead of zero-fill."
    },
    {
     "title": "scheduled_tasks query failure fabricates an OVERDUE alarm via the 5-minute fallback",
     "evidence": "src/butlers/api/routers/butlers.py:480-488 \u2014 _fetch_enabled_crons degrades any query failure to [], indistinguishable from 'butler has no cron schedule'. cadence_seconds becomes None and line 672 applies the 5-minute _DEFAULT_STALE_SECONDS rule, so a weekly-cadence butler whose scheduled_tasks query timed out is flagged activity=\"overdue\"/amber and pushed into the needs-you strip after 5 minutes of ordinary silence.",
     "severity": "major",
     "detail": "A transient DB timeout manufactures a false 'needs you' alarm \u2014 the inverse of finding 3 and equally corrosive to earned calm (false alarms train the owner to ignore the strip). Query failure must yield cadence_status=\"unknown\" (a state the model already has, line 664), not the no-schedule fallback."
    },
    {
     "title": "Header pill counts overdue and unknown butlers as \"healthy\", contradicting the strip beside it",
     "evidence": "frontend/src/components/butlers/BoardHeader.tsx:36-38 \u2014 healthyCount = total - offline - quarantined, so overdue and unknown rows count as healthy; pillDotClass (lines 50-55) shows a green dot when healthy===total. With 2 butlers overdue, the header renders a green \"14/14 reporting\" (aria-label \"14 of 14 reporting healthy\", line 110) directly above a strip saying \"2 things need you\". Also pillDotClass returns green for total===0 (line 51) \u2014 an empty fleet reads healthy.",
     "severity": "minor",
     "detail": "Two verdicts on one header disagree. Either the pill should subtract overdue/unknown, or its copy should honestly claim only liveness ('14/14 reachable') and cede the health verdict to the strip."
    },
    {
     "title": "Board ignores the canonical cell_tone it was consolidated onto, and re-derives state colors in a fourth dialect",
     "evidence": "GET /api/butlers/board is documented as THE single source of activity/cell_tone \"for every butler-status surface\" (butlers.py:283-288), but cell_tone's only consumer is SystemPage.tsx:77 \u2014 StatusBoardCell.tsx re-derives chip color from activity (lines 51-66) and rail color from eligibility (lines 106-113) via local maps, using Tailwind stock palette (text-emerald-600, bg-emerald-500, bg-amber-500, text-destructive) while BoardHeader's dot uses bg-green-500 (BoardHeader.tsx:51-54) \u2014 two different greens for 'healthy' on one page, and none of them the spec's exactly-three tokens --red/--amber/--green (openspec/specs/dashboard-design-language/spec.md:81-88, defined in index.css:78-127).",
     "severity": "minor",
     "detail": "Residue of bu-86c4c.17: the consolidation landed for data but the verdict-to-color mapping is still duplicated client-side on the board's home surface, and the status dots bypass the spec-mandated StateDot primitive (spec.md:261). The in-flight bu-86c4c.6 lint would catch the raw-palette classes but not the ignored cell_tone contract \u2014 consuming the server tone is the structural fix that makes drift impossible."
    },
    {
     "title": "Degradation is signaled by an unexplained amber dot but never names itself; overdue/unknown counts computed and rendered nowhere",
     "evidence": "The hook exposes overdue, eligibilityUnavailable, hasPerEntryErrors, heartbeatSourceError, registrySourceError (use-butler-status-board.ts:98-119), yet grep shows zero /butlers renderers \u2014 BoardFooter.tsx:109-146 renders only ACTIVE/OFFLINE/QUARANTINED/SESSIONS/SPEND/AVG LOAD (no OVERDUE, no UNKNOWN cell) and its sole SourceDegradedNote covers the cost source (lines 155-161). A registry outage or per-entry schema failure surfaces only as the header pill flipping amber (BoardHeader.tsx:50-55) with no text anywhere naming which source degraded.",
     "severity": "minor",
     "detail": "Violates 'every degraded source names itself'. /system has SystemVerdictBanner naming these conditions; the roster board \u2014 the surface the owner actually triages from \u2014 has an amber dot and silence. Footer stat cells are also inert divs (no drill-down; bu-9d5vp covers RuntimeSummaryKpi, not this footer)."
    },
    {
     "title": "No page-scoped keyboard or palette surface for the board's own queue",
     "evidence": "ButlersPage.tsx registers zero commands (no useRegisterCommands anywhere under components/butlers/ or the page \u2014 grep shows only ApprovalsPage/IssuesPage register page commands); GlobalActionsRegistrar.tsx offers only \"Trigger <butler>\". The needs-you strip rows are plain links (NeedsYouStrip.tsx:78-85) and the grid is tab-order only \u2014 no j/k or arrow traversal, no 'jump to first needs-you', and a quarantined butler cannot be restored from the palette.",
     "severity": "minor",
     "detail": "North star: clear the queue in under a minute without touching the mouse, one command palette from a single registry. Distinct from in-flight bu-86c4c.15 (inline remedy verbs on signals): this is the navigation/palette half \u2014 reaching and traversing the queue by keyboard. Today the fastest keyboard path to restore is ~N tabs through cells to find the chip."
    }
   ],
   "page": "butlers-roster",
   "verdict": "solid",
   "jarvis_gap": "The board is genuinely close: one round-trip canonical liveness, a needs-you strip that leads, undoable restore, stable roster order, honest '\u2014' chips for per-row outages. What separates it from JARVIS-grade is verdict integrity at the edges and closure of the act loop: the one owner action on the page (restore) doesn't reflect in the same frame; the lead judgment line can assert 'all healthy' precisely when the system cannot know; two backend fetch failures still fabricate calm zeros or false OVERDUE alarms instead of naming themselves; the header pill, strip, and footer can render three disagreeing verdicts at once; and the canonical cell_tone contract the consolidation shipped is unconsumed on its own home surface, leaving color semantics re-derived in stock-Tailwind dialect. Eligibility state changes are also the only liveness transition that never streams \u2014 everything else rides the event bus while quarantine/restore waits for the poll.",
   "ideal": "The board opens with a single Dispatch-voice verdict sentence that is provably derived \u2014 \"All 14 healthy\" only when every row's liveness is verified, otherwise \"12 healthy, finance silent 3d against a daily cadence, registry unreachable for 2\" \u2014 where every clause links to its butler or its degraded source. Below it, the grid renders each tile's rail, chip, and tone from the server's one canonical cell_tone in the spec's three state tokens, with unknown rendered as loudly as offline. Quarantine and restore are events on the fleet bus: an owner pressing r on a strip row (or invoking \"Restore finance\" from the palette) sees the tile flip to eligible in the same frame with a 5s undo, and a server-side quarantine appears within a heartbeat, not a poll. Every count in the footer is a door \u2014 OFFLINE:2 filters the grid \u2014 and every fetch failure anywhere in the pipeline names itself as a source note rather than shipping a zero, so a calm board is always an earned verdict, never a default.",
   "moves": [
    {
     "title": "Same-frame restore: optimistic board patch + eligibility events on the fleet bus",
     "why": "The board's only owner action currently lies for up to 30s after succeeding (finding 1), and eligibility is the sole liveness transition with no stream path \u2014 the exact 'applies in the same frame' and 'live event stream' clauses of the north star.",
     "cost": "M",
     "integration_points": "frontend/src/hooks/use-general.ts (useSetEligibility onMutate/onSuccess: optimistic patch of [\"butlers\",\"board\"] row eligibility/activity/cell_tone + invalidate), frontend/src/hooks/event-cache-registry.ts (new 'eligibility' patch entry), src/butlers/api/routers/events.py EVENT_TYPES + emit on quarantine/restore writes to switchboard.butler_registry.",
     "slice_plan": "Slice 1 (S): invalidate + optimistic patch in useSetEligibility \u2014 fixes the visible lie with no backend change. Slice 2 (M): backend eligibility event emission + registry cache-patch entry so server-side quarantines stream to board and timeline."
    },
    {
     "title": "Truthful all-clear: gate the calm line on proven health and name every degraded source on-board",
     "why": "Findings 2, 5, 7: the strip can claim 'All N healthy' during a registry outage, the pill counts overdue/unknown as healthy, and degradation is an unexplained amber dot. The lead verdict must be earned or absent.",
     "cost": "S",
     "integration_points": "frontend/src/components/butlers/NeedsYouStrip.tsx (all-clear branch requires zero unknown rows and !sourcesPartiallyDegraded; add 'N unverifiable \u2014 <source> unreachable' line), BoardHeader.tsx healthyCount/pillDotClass (subtract overdue+unknown or reword to 'reachable'), BoardFooter.tsx (add OVERDUE and UNKNOWN stat cells from already-computed aggregates; SourceDegradedNote for registry/per-entry errors mirroring the existing cost note).",
     "slice_plan": "One PR: all fields already exist in StatusBoardAggregates \u2014 this is pure rendering of computed-but-dropped truth."
    },
    {
     "title": "Kill the last two zero-fabrication paths in the board endpoint",
     "why": "Findings 3 and 4: stripe-query failure ships a confident empty stripe and SESS\u00b724H=0; cron-query failure manufactures a false OVERDUE alarm via the 5-min fallback. Both violate 'no failed fetch ever impersonates an empty queue' one layer below where the epic's truth amnesty stopped.",
     "cost": "S",
     "integration_points": "src/butlers/api/routers/butlers.py:_fetch_board_hourly_stripe (return availability flag instead of zeros; wire to BoardRow, distinct from schema_unreachable), _fetch_enabled_crons (distinguish query-failure \u2192 cadence_status='unknown' from genuinely-no-schedule \u2192 5-min rule), use-butler-status-board.ts mapRow, StatusBoardCell already renders the 'data unavailable' branch.",
     "slice_plan": "One PR, backend-led with contract test: stripe failure \u2192 stripe_unavailable=true + UI shows existing unavailable affordance; cron failure \u2192 'unknown' chip tooltip, never amber."
    },
    {
     "title": "One verdict, one palette: consume cell_tone everywhere and move board state colors onto the spec tokens",
     "why": "Finding 6: the canonical-liveness consolidation (.17) is only half-consumed \u2014 its home surface re-derives colors from two different fields in stock-Tailwind dialect with two different greens on one page. Consuming the server verdict makes drift structurally impossible and is the consumption-side complement to in-flight bu-86c4c.6's lint enforcement.",
     "cost": "M",
     "integration_points": "frontend/src/components/butlers/StatusBoardCell.tsx (delete activityChipClasses/eligibilityRailClass; single cellTone\u2192token map using --green/--amber-text/--red per openspec/specs/dashboard-design-language/spec.md:81-88), BoardHeader.tsx + BoardFooter.tsx dots \u2192 StateDot primitive (spec.md:261), coordinate with bu-86c4c.6 so the new lint blesses the token map.",
     "slice_plan": "Slice 1: StatusBoardCell consumes cellTone. Slice 2: header/footer dots onto StateDot + token alignment; visual QA against light/dark."
    },
    {
     "title": "Keyboard triage surface for the board: page commands, grid traversal, footer doors",
     "why": "Finding 8 + north star 'clear the queue without touching the mouse': the board has zero page-scoped palette commands, no grid keyboard traversal, and inert footer counts \u2014 the navigation half that bu-86c4c.15's remedy verbs don't cover.",
     "cost": "M",
     "integration_points": "frontend/src/pages/ButlersPage.tsx (register page commands: 'Restore <name>' per quarantined row wired to existing handleRestore, 'Jump to needs-you'), lib/command-registry.tsx, StatusBoardCell/RowLink (roving tabindex or arrow-key handler on the role=group grid), BoardFooter StatCell \u2192 anchor/filter links (OFFLINE scrolls to strip / filters grid).",
     "slice_plan": "Slice 1 (S): palette commands from the existing registry \u2014 restore reuses the undo-window path unchanged. Slice 2 (M): arrow-key grid traversal + footer cells as doors; axe re-run since bu-86c4c.16 tests cover this page."
    }
   ]
  },
  {
   "page": "butler-detail",
   "verdict": "solid",
   "jarvis_gap": "Post-.18 the butler console has one coherent tab vocabulary, a unified run verb, and real doors on the activity stripe and event rows \u2014 the architecture is right. What separates it from JARVIS-grade is truthfulness under failure and voice: the Overview KPI row fabricates calm (\"$0.00\", \"nothing pending\", a flat 24-bar stripe) whenever its sources fail, the Activity analytics \"Errors\" KPI is structurally always zero because it counts a trigger_source value that cannot exist, and the surface opens with a raw KPI grid instead of a synthesized per-butler verdict (the judgment layer landed only on /system). The console also still makes the owner leave it: Run navigates away to /sessions/:id, the 5-event recent feed has no door to the fleet chronicle (which isn't URL-addressable by butler anyway), and none of the page's verbs are registered in the command palette.",
   "findings": [
    {
     "title": "Activity analytics 'Errors' KPI is fabricated \u2014 it can never be nonzero",
     "evidence": "frontend/src/components/butler-detail/ButlerActivityTab.tsx:301-307 derives errorsCount from session kinds where kind === \"error\", but kinds are GROUP BY trigger_source (src/butlers/api/routers/sessions.py:564-568) and the valid trigger_source vocabulary is {tick, external, trigger, route, healing, dashboard, qa} + schedule:/deadline: prefixes (src/butlers/core/sessions.py:25) \u2014 \"error\" is not a possible value. Real failures live in the sessions `success IS FALSE` column (src/butlers/api/read_models/sessions_v1.py:390 already computes failed_count).",
     "severity": "critical",
     "detail": "The KPI confidently renders '0 errors' regardless of how many sessions actually failed, and the code comment even asserts 'zero errors is the correct interpretation'. This is the purest 'fabricated number' sin on the surface: the owner's five-second health read on a butler is answered with a metric that is hardwired to say fine. Fix: expose failed_count (success IS FALSE) from the read model on the session-kinds or latency-stats endpoint and wire the KPI to it; make a nonzero value a door to sessions filtered to failures."
    },
    {
     "title": "Overview KPIs fabricate zeros when their fetches fail (spend, awaiting, stripe, sessions)",
     "evidence": "frontend/src/components/butler-detail/ButlerOverviewTab.tsx:270 (costToday ?? 0), 312-322 (spend panel branches only on isLoading \u2014 isError renders \"$0.00\"), 278+324-331 (awaitingCount falls to 0 on approvalsQuery error \u2192 \"nothing pending\"), 245+273 (only `rows` destructured from useButlerStatusBoard; on board failure stripe = Array(24).fill(0) rendered as 24 clickable \"0 sessions\" bars).",
     "severity": "major",
     "detail": "Violates the state contract landed in bu-86c4c.2 on this page's own opening row: a failed spend fetch impersonates a free day, a failed approvals fetch impersonates an empty queue (the panel below it correctly shows ErrorLine, so the KPI and its detail panel can actively contradict each other), and a failed status board impersonates a silent butler. Each cell needs an explicit isError branch that names the degraded source ('spend unavailable') instead of a value."
    },
    {
     "title": "Sessions section: fetch error impersonates 'No sessions found.'",
     "evidence": "frontend/src/components/butler-detail/ButlerSessionsTab.tsx:73 destructures only { data, isLoading } from useButlerSessions; on error sessions=[] and SessionTable renders its empty state (\"No sessions found.\", frontend/src/components/sessions/SessionTable.tsx:110). Sibling tabs (Logs, Spend, Memory, Activity) all branch on isError.",
     "severity": "major",
     "detail": "Also: the `page` offset state is not reset when the since/until stripe-hour filter changes, so arriving via an activity-stripe door while on page 3 of unfiltered results shows a misleading empty page; and the filter banner says 'Filtered to a single activity-stripe hour' without naming which hour."
    },
    {
     "title": "No judgment layer: the surface opens with a raw KPI grid, not a synthesized verdict",
     "evidence": "frontend/src/components/butler-detail/ButlerOverviewTab.tsx:280-306 opens with a status/sessions/spend/awaiting panel grid; the Dispatch verdict banner from bu-86c4c.17 exists only on /system (frontend/src/components/system/SystemVerdictBanner.tsx \u2014 no butler-detail consumer).",
     "severity": "major",
     "detail": "North-star: 'every surface opens with the system's own synthesized verdict rendered in the Dispatch editorial voice, and every clause of that prose is a door.' The owner landing on /butlers/finance must self-assemble a verdict from six panels. All inputs are already fetched by this page (board row activity/cadence, approvals meta.total, spend, failed sessions once finding 1 is fixed) \u2014 the synthesis layer is simply absent here."
    },
    {
     "title": "Zero page-scoped command-palette surface for the butler console's verbs",
     "evidence": "No useRegisterCommands call anywhere in frontend/src/pages/ButlerDetailPage.tsx or frontend/src/components/butler-detail/ (grep confirms); the only butler command in the palette is the global \"Trigger <name>\" (frontend/src/components/layout/GlobalActionsRegistrar.tsx:26-42). The per-page registration API (src/lib/command-registry.tsx) exists precisely for this.",
     "severity": "major",
     "detail": "Pause/Resume, run-with-custom-prompt, jump-to-tab (Approvals/Spend/System sections), open logs/config are mouse-only. The north star is 'clears that queue in under a minute without touching the mouse'; on this surface every verb except plain trigger requires pointer travel to 10px-font header buttons. New signal distinct from bu-jlhk5 (which is only the invalid tier literal in the global registrar)."
    },
    {
     "title": "Run verb ejects the owner from the console and its failure names nothing",
     "evidence": "frontend/src/components/butler-detail/ButlerDetailActions.tsx:124-126 navigates to /sessions/{id} on success, leaving /butlers/:name entirely; :128-130 catches all errors as toast.error(\"Failed to run butler\") with no reason and no retry affordance.",
     "severity": "major",
     "detail": "Breaks 'detect, diagnose, and act complete on one screen': the owner fires a prompt at the butler and is teleported to a different surface, losing the tab rail, command bar, and context; getting back is a browser-back plus re-orientation. The page already owns SessionDetailDrawer (used by Overview event rows and Sessions tab) \u2014 the spawned session should open in that drawer in place, streaming via the fleet event bus (bu-86c4c.8). The error toast likewise violates 'every degraded source names itself'."
    },
    {
     "title": "Overview drill-down gaps: inert KPI cells and config KVs, capped feed masquerading as a count, cross-window per-session math",
     "evidence": "frontend/src/components/butler-detail/ButlerOverviewTab.tsx \u2014 sessions/spend/awaiting/status panels (308-331) and all config KVs (388-398: port, modules, schedules, skills) are plain text with no links, while sibling elements (stripe bars :99-110, event rows :161-189) are doors; :338 labels the feed sub as `${recentEvents.length} events` on a limit-5 fetch (:252) with no door to the full chronicle; :319 divides today's spend by 24h session count (mismatched windows) and prints \"$0.00 / session\" when sessions24h is 0.",
     "severity": "minor",
     "detail": "New instances on this surface \u2014 bu-9d5vp covers RuntimeSummaryKpi/egress/audit/spend-rule cells on other pages. Natural targets: sessions\u2192?tab=activity&section=sessions, spend\u2192?tab=spend, awaiting\u2192?tab=approvals, modules/schedules/skills\u2192?tab=system&section=\u2026, recent\u2192'view all' into /timeline scoped to this butler (blocked today because TimelinePage filters are component-local useState with no URL param form, frontend/src/pages/TimelinePage.tsx:114-125)."
    },
    {
     "title": "NOT-LANDED: mode-toggle remnants and error-as-loading in SiblingButlerNav",
     "evidence": "frontend/src/components/butler-detail/SiblingButlerNav.tsx:88-99 still reads and carries the dead `?mode=` param (killed by bu-86c4c.18) and SiblingButlerNav.test.tsx:428-446 asserts its propagation; :123-138 renders indefinite Skeleton shimmer on aggregates.isError ('Error state: also render skeleton shape') so a failed board fetch looks permanently loading; atoms.tsx:2 still says 'resident tabs'.",
     "severity": "minor",
     "detail": "The .18 kill holds in the page and tab registry (no mode reads in ButlerDetailPage.tsx/butler-detail-tabs.ts), but the sibling strip both preserves dead machinery and violates 'no failed fetch ever impersonates' \u2014 an errored roster should name itself in one dim mono line, not shimmer forever. Also stale: ButlerApprovalsTab.tsx:134-137 claims the backend cannot filter by butler when GET /approvals/actions has supported ?butler= since src/butlers/api/routers/approvals.py:461."
    }
   ],
   "ideal": "The ideal /butlers/:name opens with one sentence the system wrote itself, in the Dispatch serif voice: \"Nominal. Six ticks on schedule, $0.41 spent, one approval waiting \u2014 expires in 3h.\" Every clause is a door (ticks \u2192 sessions filtered to today, spend \u2192 Spend tab, approval \u2192 that approval's dossier), and when any input to that sentence is degraded the sentence says so by name instead of rounding to calm. Below it, the same KPI grid \u2014 but every cell is a door, every number provably sourced, the Errors count wired to real success=false sessions and red only when true. The command bar is the butler's throat: typing a prompt and hitting Enter spawns a session that streams live into a drawer on this same screen, the owner watching tool calls land without ever leaving the console; Cmd+K carries every verb on the page (pause with quarantine reason shown, resume, run, jump to any tab or System section) so the whole detect\u2192diagnose\u2192act loop is keyboard-complete. The recent feed ends in a door to the fleet chronicle pre-scoped to this butler via a URL-addressable filter, making the butler console a lens over the One Timeline rather than a silo. Failure anywhere \u2014 board, spend, approvals, sessions \u2014 renders as a named, dated degradation line in the exact spot the number would have been, so the owner's trust in every figure that does render stays absolute.",
   "moves": [
    {
     "title": "Truth pass: kill the fabricated Errors KPI and the error-as-zero Overview cells",
     "why": "The surface's opening row currently answers 'is this butler okay?' with numbers that are hardwired or defaulted to 'fine' \u2014 the single worst violation of earned calm on this page, and cheap to fix because sibling components already model the correct isError pattern.",
     "cost": "S",
     "slice_plan": "1) Backend: add failed_count (success IS FALSE, already computed in sessions_v1.py:390) to the butler latency-stats or session-kinds response; wire ButlerActivityTab errorsCount to it and make nonzero a door to failed sessions. 2) Overview: isError branches on spend/awaiting/sessions KPIs and the stripe that render a named 'unavailable' line. 3) ButlerSessionsTab: consume isError + reset page on filter change. 4) SiblingButlerNav: replace the perpetual error-skeleton with a one-line named degradation.",
     "integration_points": "frontend/src/components/butler-detail/ButlerOverviewTab.tsx, ButlerActivityTab.tsx, ButlerSessionsTab.tsx, SiblingButlerNav.tsx; src/butlers/api/routers/sessions.py + read_models/sessions_v1.py; ErrorLine atom in butler-detail/atoms.tsx"
    },
    {
     "title": "Butler verdict opener: extend the judgment layer (bu-86c4c.17) to the butler console",
     "why": "The north star says every surface opens with the system's synthesized verdict; today only /system has one. All inputs (board row activity/cadence, approvals meta.total, spend today, failed sessions) are already fetched by this very page \u2014 the synthesis is pure assembly.",
     "cost": "M",
     "slice_plan": "1) Extract the verdict-derivation pattern from SystemVerdictBanner into a shared helper taking named, individually-degradable inputs. 2) Render a butler-scoped verdict line above the Overview grid, Dispatch voice, each clause a Link (approvals\u2192?tab=approvals&id, spend\u2192?tab=spend, failures\u2192sessions filtered). 3) Degraded inputs surface as clauses ('spend unreadable since 14:02'), never omitted silently.",
     "integration_points": "frontend/src/components/system/SystemVerdictBanner.tsx (pattern source), ButlerOverviewTab.tsx (mount point), useButlerStatusBoard/useApprovalActions/useSpendSummary (existing data), depends on move 1 for honest failure inputs"
    },
    {
     "title": "Register the console's verbs in the command palette",
     "why": "Every verb on this page except global 'Trigger' is mouse-only; the per-page registration API exists and is demonstrably unused here. This is the cheapest large step toward 'clear the queue without touching the mouse'.",
     "cost": "S",
     "slice_plan": "1) A useButlerDetailCommands hook in ButlerDetailPage registering: Pause/Resume (label carries quarantine reason/age), Run tick, Run with prompt\u2026 (focuses the command bar), Go to <each base tab>, Open logs / config / each System section. 2) Commands mount/unmount with the page via useRegisterCommands. 3) Reuse the exact eligibility mutation and trigger call from ButlerDetailActions so palette and buttons share one implementation (and one valid complexity default \u2014 sidestep the bu-jlhk5 literal).",
     "integration_points": "frontend/src/pages/ButlerDetailPage.tsx, frontend/src/lib/command-registry.tsx, ButlerDetailActions.tsx (extract shared handlers), EntityFinder Actions group"
    },
    {
     "title": "Run stays on-console: stream the spawned session into the in-page drawer",
     "why": "Run currently teleports the owner to /sessions/:id, breaking detect\u2192diagnose\u2192act-on-one-screen at the exact moment they most want to watch the butler work; the page already owns SessionDetailDrawer and the fleet event bus (bu-86c4c.8) already streams session state.",
     "cost": "M",
     "slice_plan": "1) On trigger success, open SessionDetailDrawer with the returned session_id instead of navigate(); keep a 'open full page' door inside the drawer. 2) Live-update the drawer via the existing event stream / polling until the session completes. 3) Failure toast names the actual error (status/message from triggerButler) and offers Retry. 4) Apply the same to the palette Run commands from move 3.",
     "integration_points": "frontend/src/components/butler-detail/ButlerDetailActions.tsx:115-133, frontend/src/components/sessions/SessionDetailDrawer.tsx, fleet event bus hooks, GlobalActionsRegistrar.tsx"
    },
    {
     "title": "Chronicle spine: URL-addressable timeline filters + every Overview cell a door",
     "why": "The butler console should be a lens over the One Timeline, but the recent feed dead-ends at 5 events and /timeline cannot be deep-linked by butler because its filters are component-local state; meanwhile four KPI panels and five config KVs are inert text on a page whose doctrine is 'every clause a door'.",
     "cost": "M",
     "slice_plan": "1) TimelinePage: read/write ?butler=&type= search params (mirror the saved-view filter_spec shape) so any scoped view has a URL. 2) Overview recent panel: honest 'last 5' label + 'full chronicle \u2192' door to /timeline?butler=<name>. 3) Doors on KPI cells (sessions\u2192activity&section=sessions, spend\u2192?tab=spend, awaiting\u2192?tab=approvals, status\u2192board row) and config KVs (modules/schedules/skills\u2192?tab=system&section=\u2026). 4) Fix the per-session figure to divide matching windows or drop it.",
     "integration_points": "frontend/src/pages/TimelinePage.tsx:80-140 (filter state \u2192 useSearchParams), ButlerOverviewTab.tsx panels, butler-detail-tabs deep-link vocabulary already honored by ButlerActivitySection/ButlerSystemSection"
    }
   ]
  },
  {
   "page": "sessions",
   "verdict": "functional",
   "jarvis_gap": "The /sessions list page is genuinely close to the bar (window-true KPI strip, URL-as-truth filters, keyboard-operable rows, honest empty/error/truncation states, fleet-bus live updates), but the surface's actual evidence layer \u2014 session detail \u2014 is split across two divergent surfaces, and the canonical one (/sessions/:id, the deep-link hub that ten other surfaces point at) is a pre-redesign relic that drops the trace spine, never updates for running sessions, and renders none of the root-evidence fields (trace_id, request_id, parent session, stderr, cost). Detect works; diagnose dead-ends exactly where the owner arrives asking \"why did this fail.\"",
   "findings": [
    {
     "title": "NOT-LANDED: /sessions/:id \u2014 the fleet-wide deep-link hub \u2014 renders none of the trace-spine fields",
     "evidence": "frontend/src/pages/SessionDetailPage.tsx:134-263 renders only butler/trigger/started/completed/duration/model/tokens/tool_calls/prompt/result/error/cost \u2014 it omits trace_id, request_id, parent_session_id, resolution_source, complexity, and the entire process_log (stderr, exit_code, command) even though SessionDetail carries all of them (frontend/src/api/types.ts:221-268). The trace spine (bu-86c4c.3) landed only in SessionDetailDrawer.tsx:281-296. Meanwhile 10+ surfaces deep-link here: SpendPage.tsx:719, ApprovalsPage.tsx:670, TimelineLedger.tsx:76, MessageThread.tsx:196, notification-feed.tsx:174, EpisodeDetailPage.tsx:142, EntityDetailPage.tsx:165, CalendarWorkspacePage.tsx:1448, TopSessionsTable.tsx:104, EventDrawer.tsx:269.",
     "severity": "critical",
     "detail": "The owner arrives at /sessions/:id from approvals, spend, timeline, or chat asking 'what happened here' and finds a page with no trace link, no request_id, no parent link, no stderr \u2014 the unbroken trace-id spine breaks at its busiest node. For a failed session with a null error string, process_log.stderr is the root evidence and it is shown nowhere on any surface (the drawer shows only process_log.runtime_type, SessionDetailDrawer.tsx:190-194)."
    },
    {
     "title": "Deep-linked session detail is dead to the live event stream \u2014 'Ask butler' lands on a frozen 'Running' page",
     "evidence": "SessionDetailPage.tsx:81-86 uses queryKey [\"session-detail-global\", id] with no refetchInterval; event-cache-registry.ts:88-92 invalidates only [\"session-detail\", butler, sessionId]. GlobalActionsRegistrar.tsx:36 and ButlerDetailActions.tsx:125 navigate to `/sessions/${response.session_id}` (no ?butler=) immediately after spawning a session \u2014 hitting exactly the un-invalidated, non-polling global query.",
     "severity": "major",
     "detail": "The flow the palette itself creates (trigger a butler, get taken to the session) shows StatusBadge 'Running', duration '--', zero tool calls, and never changes until a manual reload. Violates 'state arrives as a live event stream' precisely where the owner is most actively watching. The butler-scoped path (?butler=) IS bus-invalidated, so the bug is the dual-endpoint split, not the bus."
    },
    {
     "title": "Two divergent session-detail surfaces; the page half is off-dialect and tells the owner to hand-edit the URL",
     "evidence": "SessionDetailPage.tsx:139 raw `<h1 className=\"text-2xl font-bold tracking-tight\">` + generic CardTitle sections, no Page archetype, no mono eyebrows \u2014 vs SessionDetailDrawer.tsx:163-296 (uppercase mono section eyebrows, richer field set). formatDuration/formatTokens duplicated in both (SessionDetailPage.tsx:25-38, SessionDetailDrawer.tsx:34-50). Error copy at SessionDetailPage.tsx:119-127: \"Try adding ?butler=name to the URL.\"",
     "severity": "major",
     "detail": "One instrument built by one hand fails here: the same session renders as two different documents with different fields depending on entry path. The recovery instruction 'add ?butler=name to the URL' asks the owner to hand-edit a querystring to work around a backend-topology leak (butler-scoped vs global endpoint) that getSession() already solves."
    },
    {
     "title": "Drawer selection is ephemeral local state \u2014 no shareable/refresh-safe URL, no path from drawer to canonical page",
     "evidence": "SessionsPage.tsx:99 `useState<string | null>(null)` for selectedSessionId while the same file's comment (line 40) declares 'URL is the source of truth' for filters/cursor; SessionDetailDrawer.tsx contains no Link to /sessions/:id (only trace and butler links, lines 155-296); SessionTable.tsx:154-178 rows are click-handler divs with role=button, so middle-click/copy-link is impossible.",
     "severity": "major",
     "detail": "Refresh or share loses the open session; the owner who finds a failure in the drawer cannot hand its URL to anything (or open it in a new tab) without re-finding it. The list page mirrors every filter to the querystring but not the one piece of state that identifies what the owner is looking at."
    },
    {
     "title": "KPI strip swallows aggregate-fetch failure \u2014 permanent dashes impersonate 'still loading'",
     "evidence": "SessionsKpiStrip.tsx:40 `const { data } = useSessionAggregate(filterParams)` discards isError/isLoading; all four cells render DASH (lines 54-77) identically for loading, error, and pre-fetch states, with no retry and no naming of the degraded source. Contrast SessionStripeChart.tsx:141-150 on the same page, which renders an explicit named error state.",
     "severity": "major",
     "detail": "A 500 from /api/sessions/aggregate leaves the strip showing '\u2014' under an honest-looking 'Matching filters' caption forever \u2014 a failed fetch impersonating an empty/loading state, the exact sin the state contract (bu-86c4c.2) purged elsewhere. This strip shipped after that contract, so it is a new instance, not a leftover."
    },
    {
     "title": "parent_session_id is inert text and request_id is absent from both detail surfaces \u2014 drill-down dead ends",
     "evidence": "SessionDetailDrawer.tsx:195-199 renders parent_session_id as a plain mono span (no link to /sessions/:parent, no copy affordance); neither MetadataRow set (drawer lines 168-199) nor SessionDetailPage's dl (lines 152-194) includes request_id, though request_id is a first-class list filter with click-to-filter in the table (SessionTable.tsx:198-216) and SessionDetail carries it (types.ts:229).",
     "severity": "minor",
     "detail": "From a child session you cannot reach its parent in one keystroke; from a session's detail you cannot pivot to its request peers. Both are one-line links to routes that already exist (/sessions/:id, /sessions?request=). bu-9d5vp covers audit request_id, not session detail."
    },
    {
     "title": "ToolCallTimeline uses raw Tailwind palette colors against the tokens-only doctrine StatusBadge documents",
     "evidence": "ToolCallTimeline.tsx:125-129 dot classes bg-emerald-500/bg-amber-500/bg-destructive and lines 320-331 text-emerald-600 for outcome text; SessionDetailDrawer.tsx:75 text-emerald-500 on the copy check icon \u2014 while StatusBadge.tsx:11 in the same directory states the rule: 'Tokens only: --green / --amber via the dot, never raw hex.' Lines 317-333 also re-derive the outcome label by regexing a known 4-value enum (`/error|fail/i.test(tc.outcome)`) and print 'Outcome: unknown' prose under a dot that already carries the same state.",
     "severity": "minor",
     "detail": "Live dialect-drift instances for the bu-86c4c.6 lint to catch (the bead ships enforcement, not these fixes); the redundant enum-regex and duplicate outcome prose are one-file cleanups."
    },
    {
     "title": "Drawer hard-fails for sessions without a butler \u2014 disabled query renders as 'could not be loaded'",
     "evidence": "SessionsPage.tsx:208 `setSelectedSessionButler(session.butler ?? \"\")`; use-sessions.ts:70-76 useSessionDetail is `enabled: !!butler && !!id`, so butler=\"\" never fetches (isLoading false, data null); SessionDetailDrawer.tsx:130 then renders the error branch 'This session could not be loaded' \u2014 even though a global getSession(id) fallback exists and is used by SessionDetailPage.tsx:81-86. SessionSummary.butler is optional (types.ts:206) and SessionTable.tsx:185-192 explicitly renders the null-butler case.",
     "severity": "minor",
     "detail": "The error message also conflates fetch failure with not-found and offers no retry \u2014 the one place on the list surface where a reachable record renders as an unexplained failure."
    }
   ],
   "ideal": "/sessions opens with a one-line synthesized verdict in Dispatch voice \u2014 \"212 sessions since midnight, 96% ok; the 8 failures cluster in finance's 06:00 cron, all the same expired-token stderr\" \u2014 where every clause is a filter link, sitting above the window-true KPI strip and butler-hued stripe chart that already exist. Below it, one keyboard loop: j/k walks rows, Enter opens the session dossier, [/] steps to the prev/next session without closing, y copies the canonical URL, and the open session is always mirrored to the querystring so refresh and share never lose it. There is exactly ONE session dossier \u2014 the same Dispatch-grade component rendered in the drawer and at /sessions/:id \u2014 showing every field the store holds: status verdict sentence, trace-id pill into /timeline, request-id pivot back into the filtered list, parent/child session links, complexity and resolution source, cost, and a disclosure holding process_log stderr/exit_code as the named root evidence for failures. A running session's dossier is alive: elapsed time ticks, tool calls append as the fleet bus streams them, and the moment the session ends the verdict line rewrites itself \u2014 so \"ask a butler, watch it work, read why it failed, retry\" completes on one screen without touching the mouse.",
   "moves": [
    {
     "title": "One session dossier: merge drawer and page into a single Dispatch-grade component rendering every SessionDetail field",
     "why": "Kills three findings at once (spine-less detail page, divergent dialect, hand-edit-the-URL recovery) at the node ten surfaces deep-link into; the trace spine is only as strong as its busiest hub.",
     "cost": "M",
     "slice_plan": "1) Extract SessionDossier component from SessionDetailDrawer content (fields: verdict header, trace_id\u2192/timeline, request_id\u2192/sessions?request=, parent_session_id\u2192/sessions/:id link, complexity, resolution_source, cost, process_log stderr/exit_code disclosure). 2) Rebuild SessionDetailPage as Page-archetype wrapper around it, always using global getSession (delete the ?butler= dual path and the 'try adding ?butler=name' copy). 3) Point the drawer at the same component; delete duplicated formatDuration/formatTokens into a shared module.",
     "integration_points": "frontend/src/pages/SessionDetailPage.tsx, frontend/src/components/sessions/SessionDetailDrawer.tsx, frontend/src/api/index.ts getSession, router-config.tsx:76; all 10 inbound deep-links keep working unchanged."
    },
    {
     "title": "Live session dossier: one bus-invalidated query key + elapsed ticker + streaming tool-call tail",
     "why": "The palette's own 'ask a butler' flow lands on a frozen page today; a running session is the single moment the owner is guaranteed to be watching, and the fleet event bus already exists.",
     "cost": "M",
     "slice_plan": "1) Collapse session-detail-global/session-detail into one key [\"session-detail\", id] and invalidate it in sessionPatch without requiring butler in the event payload. 2) Running sessions: 1s elapsed-time ticker in the dossier header and short-interval refetch (or session events on the bus carrying tool-call deltas) so the timeline appends live. 3) On session end, the bus patch flips the verdict in the same frame.",
     "integration_points": "frontend/src/hooks/event-cache-registry.ts:79-93, frontend/src/hooks/use-sessions.ts, SessionDossier from move 1, src/butlers/api/routers/events.py session event payload."
    },
    {
     "title": "Keyboard triage loop + URL-mirrored selection on the sessions list",
     "why": "The list is where failures are triaged; today each one costs mouse-click, read, Esc, re-find \u2014 and the open session evaporates on refresh, contradicting the page's own URL-as-truth doctrine.",
     "cost": "S",
     "slice_plan": "1) Mirror selection to ?selected= (drawer opens from URL, refresh-safe, shareable). 2) j/k row focus, Enter/Space open (rows already tabbable), [/] prev-next session inside the drawer, o = open full dossier page, y = copy canonical /sessions/:id URL. 3) Make rows real <a> wrappers (RowLink exists from bu-86c4c.16) so middle-click works.",
     "integration_points": "frontend/src/pages/SessionsPage.tsx:97-116 (searchParams), frontend/src/components/sessions/SessionTable.tsx:154-178, existing DisclosureRow/RowLink primitives, GlobalActionsRegistrar for palette registration of the shortcuts."
    },
    {
     "title": "Honest KPI strip: named degraded state instead of silent dashes",
     "why": "A failed aggregate fetch currently impersonates loading forever under an honesty caption \u2014 the exact sin the state contract exists to prevent, on the page's opening element.",
     "cost": "S",
     "slice_plan": "1) Consume isError/isLoading from useSessionAggregate in SessionsKpiStrip; on error render the strip's caption as 'aggregate unavailable \u2014 retry' with the failing source named, cells stay dashed but visually marked degraded. 2) Same treatment for the shared KpiStrip callers if the primitive grows an optional degraded prop.",
     "integration_points": "frontend/src/components/sessions/SessionsKpiStrip.tsx:40-46, frontend/src/components/overview/KpiStrip.tsx (optional degraded/caption slot), existing state-contract patterns from bu-86c4c.2."
    },
    {
     "title": "Sessions verdict opener: judgment layer for /sessions with failure clustering",
     "why": "bu-86c4c.17 gave system/butlers a judgment layer but /sessions still opens with raw aggregates; the owner's five-second question is 'anything failing, and is it one cause?' \u2014 the data (aggregate + stderr/error strings) already exists to answer it.",
     "cost": "L",
     "slice_plan": "1) Backend: extend /api/sessions/aggregate with a failures facet (top error signature, top failing butler+trigger, time cluster). 2) Frontend: one serif Dispatch sentence above the KPI strip synthesizing window + failures, every clause a link that applies the corresponding filter set. 3) Zero-failure window reads as earned calm ('all N sessions completed; quietest hour 03:00'), never fabricated prose when the aggregate is degraded (falls back to move 4's honest state).",
     "integration_points": "src/butlers/api/routers (sessions aggregate endpoint), frontend/src/components/sessions/SessionsKpiStrip.tsx area of SessionsPage.tsx:228-236, Dispatch editorial-voice patterns from the judgment-layer work (StatusBoardCell.tsx/SystemPage.tsx)."
    }
   ]
  },
  {
   "page": "timeline",
   "verdict": "solid",
   "jarvis_gap": "The .10/.9 rebuild is real and holds: server-side heartbeat classification, honest per-source degraded_sources, keyset pagination with SQL-level error filtering, WS-invalidated live tail with an unpin-safe \"N new events\" pill, and a typed drawer with no raw JSON. It moves the surface from weak to solid. What separates it from JARVIS-grade: the URL contract is half-built (drawer is URL-backed, filters are not, and a shared ?event link silently renders nothing); a dead API after first paint impersonates a calm idle fleet; notification rows dead-end at a generic list; trace_id \u2014 which sessions already persist \u2014 never reaches the timeline event, so the chronicle is not on the trace-id spine; and the rows speak a fourth and fifth color outside the three-state Dispatch vocabulary while butler identity is plain text instead of the ButlerMark. Action failures (Load older, Save view) fail silently to console. No synthesized verdict opens the page (attention strip is filed as bu-ddo0n; the editorial verdict line above it is not).",
   "findings": [
    {
     "title": "Dead API after first paint impersonates a quiet fleet",
     "evidence": "frontend/src/hooks/use-timeline-ledger.ts:131-133 \u2014 isError is only true when events.length === 0; use-timeline.ts placeholderData keeps the previous page forever. Once any page has rendered, every subsequent head-poll failure is invisible: rows freeze, LiveStatusBadge (live-status-badge.tsx:53-56) decays to plain 'Idle' \u2014 indistinguishable from a genuinely quiet fleet. degraded_sources only covers per-source SQL failure inside a successful response, not total fetch failure.",
     "severity": "critical",
     "detail": "This is the errors-impersonating-empty sin in its stale-data form: the owner reads calm where the console has actually lost its feed. The surface already has the degraded-banner slot (TimelinePage.tsx:176-184); a 'stream stale since HH:MM \u2014 retry' strip when head.isError && events.length > 0 closes it. Additionally, while unpinned the badge computes freshness from the committed snapshot's first event (TimelinePage.tsx:162), so 'Idle' can render directly above a pill counting live arrivals \u2014 two contradictory liveness signals on one screen."
    },
    {
     "title": "Half-built URL contract: filters unshareable, ?event deep links silently render nothing",
     "evidence": "TimelinePage.tsx:76-78 \u2014 selectedButlers/selectedTypes/activeViewId are plain useState (no useSearchParams), while the drawer IS URL-backed via useEventDrawerState (?event=<id>). TimelineLedger.tsx:302-311 renders the drawer only inline under a matching loaded row, so a reloaded or shared ?event URL whose event is not in the newest 50 (pushed off by new arrivals, or filtered out) shows a plain timeline with zero indication anything was requested.",
     "severity": "major",
     "detail": "The donor ledger this page was rebuilt on URL-backs its range/filters (TimelineTab.tsx:1091,1131 'URL-backed'); the rebuild adopted the drawer param but dropped filter persistence \u2014 a refresh resets every facet and the drawer param survives without its context. Drill-down dead end: the one URL the system hands the owner for 'this exact event' is only valid for ~one page-turn of time."
    },
    {
     "title": "Notification rows dead-end at a generic list \u2014 root evidence unreachable",
     "evidence": "TimelineEventDrawer.tsx:106-111 links 'View in Notifications' to bare /notifications; NotificationsPage.tsx:75 keeps all filters in useState with no useSearchParams \u2014 there is no ?id=, no per-record anchor, no way to land on the specific delivery record. The owner must re-hunt by eye through a paginated list.",
     "severity": "major",
     "detail": "Session/error rows get a hard link to their transcript (/sessions/<id>); notification rows \u2014 the type whose failures most need acting on (bu-6t8ix retry verb pending) \u2014 get a door to a hallway. Needs either ?id= deep-link support on NotificationsPage or a notification-record drawer target."
    },
    {
     "title": "Timeline events drop trace_id the system already persists \u2014 chronicle is off the trace spine",
     "evidence": "src/butlers/core/spawner.py:1298-1312 writes trace_id/request_id onto every session record and src/butlers/api/routers/sessions.py:193 already serves it, but grep of src/butlers/api/read_models/timeline_v1.py finds no trace_id \u2014 TimelineEvent (frontend/src/api/types.ts:420-435) has no trace field, so neither row nor drawer can jump from a chronicle event to its distributed trace or originating ingestion event.",
     "severity": "major",
     "detail": "North star: 'every clause is a door \u2014 one keystroke from any signal to its root evidence via an unbroken trace-id spine.' The fleet chronicle is the surface most obligated to be ON that spine, and the data is one SELECT column away. bu-9d5vp covers other drill-down gaps (KPI cells, egress actors, audit request_id) but not timeline trace_id."
    },
    {
     "title": "Row type dots speak a 4th and 5th color outside the three-state Dispatch vocabulary; butler identity is bare text, not the ButlerMark",
     "evidence": "TimelineLedger.tsx:52-56 \u2014 TYPE_DOT_CLASS maps session\u2192bg-blue-500, notification\u2192bg-purple-500 (raw Tailwind hues, not tokens). openspec/specs/dashboard-design-language/spec.md:81 ('Exactly three state colors exist \u2014 red, amber, green \u2014 and they SHALL appear only when state...') and :270-273 (ButlerMark SHALL be the butler's visual identity, the only place butler hues exist). The butler column (TimelineLedger.tsx:200) renders a plain mono string.",
     "severity": "major",
     "detail": "Type is identity, not state \u2014 blue/purple dots use color where the spec reserves it for severity, and the one identity hue the language does sanction (butler letter-mark) is absent. Fix is subtractive: neutral dot + mono type word for session/notification, bg-destructive only for error (already correct), ButlerMark in the butler cell."
    },
    {
     "title": "Load older and Save view fail silently to console",
     "evidence": "use-timeline-ledger.ts:115-118 \u2014 loadMore .catch() only console.errors; the button re-enables with no inline error, no toast, no retry affordance, so a mid-history pagination failure reads as 'no more events'. TimelinePage.tsx:144-146 \u2014 handleSaveView catch is console.error only; the dialog stays open with zero feedback and the Save button simply doesn't work.",
     "severity": "major",
     "detail": "Violates the state contract (bu-86c4c.2's own standard, met elsewhere on this page via ErrorState/degraded banner) and the design bar's recovery criterion: the error never says what happened or what to do next. An inline error row under the Load older button and an error line in the dialog are both S-size fixes."
    },
    {
     "title": "Saved-view chip stays highlighted after filters diverge; view delete is one-click destructive with no undo",
     "evidence": "TimelinePage.tsx:152-154 \u2014 toggleType/toggleButler mutate filters without clearing activeViewId, so the 'Errors only' chip renders selected (variant=default) while the live filter set includes Notifications; the code comment (:149-151) acknowledges the divergence but the UI still asserts the stale state. TimelinePage.tsx:225-232 \u2014 the \u00d7 button calls deleteSavedView.mutate immediately: no confirm, no undo.",
     "severity": "minor",
     "detail": "A control asserting state that is no longer true is a small fabricated-data instance. Clearing activeViewId (or a 'modified' dot) on divergence is trivial; delete needs undo-toast or confirm per the design bar (destructive actions undoable or explicitly confirmed)."
    },
    {
     "title": "No heartbeat facet \u2014 ticks eat page budget and cannot be excluded; built-in views duplicate the facet row verbatim",
     "evidence": "TimelinePage.tsx:49-53 source facets are session/error/notification only; src/butlers/api/routers/timeline.py:206-258 accepts event_type but has no exclude-heartbeats param despite classifying is_heartbeat server-side (:158). Heartbeats count against the 50-row page, so on a tick-heavy fleet 'Load older' pages through collapsed noise. BUILT_IN_VIEWS (:65-69 \u2014 All / Errors only / Notifications) are exact re-expressions of the facet chips one row below.",
     "severity": "minor",
     "detail": "The owner's question is 'what happened that wasn't routine' \u2014 the server already knows which rows are routine and can't be asked to omit them. Two chip rows expressing one filter vocabulary is redundant control surface; fold built-ins into facets and reserve the view row for custom saved views."
    }
   ],
   "ideal": "The fleet chronicle opens with one synthesized editorial line above the stream \u2014 \"Quiet afternoon: 41 sessions across 9 butlers, one failed delivery to Telegram needs retry\" \u2014 where every clause is a live link (the attention strip of bu-ddo0n supplies the triage rows; the verdict sentence above it is the judgment layer). The stream itself is the honest ledger it already nearly is: hour groups, hairline rows, live tail with the N-new pill \u2014 but every piece of view state (facets, butlers, view, drawer, scroll anchor) lives in the URL so any moment is shareable and restorable, and a ?event deep link either finds its row or says plainly \"this event is older than the loaded view \u2014 jump to its hour\". Every row is on the trace-id spine: session and error rows carry trace_id through to the transcript and back to the originating ingestion event; notification rows land on their exact delivery record with the retry verb inline (bu-6t8ix). Failure never impersonates calm \u2014 a dead poll paints a \"stream stale since 14:32 \u2014 retry\" strip in the same slot the degraded-sources banner already owns. Rows speak pure Dispatch: neutral mono type words, state color only on genuine failure, the ButlerMark as the only hue. Heartbeats are excludable at the server, and the whole surface is keyboard-first (j/k/enter, / to filter \u2014 bu-ddo0n) under the one command palette.",
   "moves": [
    {
     "title": "Truthful staleness: surface post-first-paint fetch failure and reconcile the liveness signals",
     "why": "The single worst honesty gap on the surface \u2014 a dead API currently renders as a calm idle fleet, the exact failure mode the earned-calm doctrine exists to prevent, and it is invisible precisely when the owner most needs to know.",
     "cost": "S",
     "integration_points": "use-timeline-ledger.ts (expose isStale = head.isError && events.length > 0 plus lastSuccessAt), TimelinePage.tsx degraded-banner slot (:176-184) for the 'stream stale since HH:MM \u2014 retry' strip, live-status-badge.tsx or the aside wiring (:162) so 'Idle' is suppressed/annotated while unpinned or stale.",
     "slice_plan": "One PR: hook exposes staleness, page renders the strip with retry, badge input switches to head-page freshness (not committed snapshot); vitest for stale-after-data and unpinned-badge cases."
    },
    {
     "title": "Finish the URL contract: URL-backed filters + honest ?event deep-link resolution",
     "why": "The chronicle's shareability is the spine's currency \u2014 today a shared link loses its filters and its ?event target silently vanishes after one page-turn of new events, turning the system's own permalinks into dead ends.",
     "cost": "M",
     "integration_points": "TimelinePage.tsx filter state \u2192 useSearchParams (pattern: donor ledger's URL-backed filters, TimelineTab.tsx), TimelineLedger.tsx drawer rendering, a resolve path for off-page events (either GET /api/timeline single-event lookup in timeline.py/timeline_v1.py, or a visible 'event not in current view' notice with a jump-to-hour action).",
     "slice_plan": "Slice 1: filters/view into URL params (S). Slice 2: off-page ?event handling \u2014 backend by-id lookup + drawer-above-stream render or explicit not-found notice (M)."
    },
    {
     "title": "Put the chronicle on the trace spine: trace_id through timeline_v1 + notification record deep link",
     "why": "Sessions already persist trace_id; one SELECT column and one link turn every chronicle row into a door to root evidence \u2014 the north star's defining property \u2014 and notification rows stop dead-ending at a generic list.",
     "cost": "M",
     "integration_points": "src/butlers/api/read_models/timeline_v1.py (add trace_id to TimelineSessionRow query), timeline.py _session_dto_to_event data dict, TimelineEvent type (types.ts:420), TimelineEventDrawer.tsx (trace link in SessionDrawerBody; NotificationDrawerBody \u2192 /notifications?id=<id>), NotificationsPage.tsx (accept ?id= and focus/filter to the record).",
     "slice_plan": "Slice 1: trace_id backend\u2192type\u2192drawer link (S/M). Slice 2: NotificationsPage ?id= deep link + drawer target change (S). Coordinate with bu-6t8ix so the retry verb lands where this link points."
    },
    {
     "title": "Dispatch-conform rows: three-state color discipline, ButlerMark identity, loud failures",
     "why": "The rebuilt surface still speaks two off-dialect hues and hides its butler identity; fixing this plus the silent loadMore/save-view failures makes the page read as one instrument that never fails quietly.",
     "cost": "S",
     "integration_points": "TimelineLedger.tsx TYPE_DOT_CLASS (:52-56) \u2192 neutral dot + bg-destructive only for error; butler cell (:200) \u2192 ButlerMark component; use-timeline-ledger.ts loadMore catch \u2192 surfaced loadError; TimelinePage.tsx save-view catch \u2192 inline dialog error; clear activeViewId on facet divergence (:152-157).",
     "slice_plan": "One PR, purely frontend: color/mark swap + error surfacing + view-chip honesty + delete-view undo toast; axe/vitest updates alongside."
    },
    {
     "title": "Server-side heartbeat exclusion + one filter vocabulary",
     "why": "The server already classifies routine ticks; letting the owner ask for 'everything that wasn't routine' makes each 50-row page pure signal, and collapsing the duplicate built-in-views row into the facets removes a redundant control surface.",
     "cost": "M",
     "integration_points": "timeline.py list_timeline (exclude_heartbeats query param pushed into timeline_v1 SQL so pagination/has_more stay correct), TimelineParams/types.ts, TimelinePage.tsx (heartbeat toggle in the facet row; drop BUILT_IN_VIEWS 'Errors only'/'Notifications', keep 'All'), saved-view filter_spec gains the flag.",
     "slice_plan": "Slice 1: backend param + SQL + tests (heartbeat rollup must still report what was excluded, e.g. 'N routine ticks hidden'). Slice 2: frontend toggle + facet-row consolidation. Keep the hidden-tick count visible so exclusion never fabricates silence."
    }
   ]
  },
  {
   "page": "notifications",
   "verdict": "functional",
   "jarvis_gap": "The trace/session out-links (.3) verifiably hold \u2014 every row with a session_id/trace_id now carries a real Link into /sessions/:id and /ingestion?trace= which TimelineTab consumes (notification-feed.tsx:169-188, TimelineTab.tsx:1368). But the surface remains a polled, all-time delivery table rather than a live triage instrument: its own event type in the fleet bus never refreshes it, the just-landed optimistic-ack contract is display-inert because rows render effective_status while the patch flips status, a degraded switchboard fabricates healthy zeros end-to-end (200 + emerald 0.0% failure rate), the 'Retried' filter can never match anything, nothing is deep-linkable inbound (dashboard 'N failed notifications' drops the owner unfiltered), and there is no keyboard path, no detail view, and no synthesized verdict \u2014 the owner detects in Telegram, diagnoses by squinting at 60-char truncations, and acts with a mouse on two buttons that do the same thing.",
   "findings": [
    {
     "title": "NOT-LANDED: 'Retried' status filter can never return a row \u2014 filters the stored column for a value that is only ever computed",
     "evidence": "frontend/src/pages/NotificationsPage.tsx:40-47 (STATUS_OPTIONS includes 'retried') vs src/butlers/api/routers/notifications.py:149-152 (WHERE status = $n on the stored column) and :176-186 ('retried' exists only as the CASE-computed effective_status, never stored)",
     "severity": "critical",
     "detail": "Selecting 'Retried' always yields the empty state 'No notifications match the active filters' \u2014 the owner concludes no retries ever happened. The filter must target effective_status (wrap the CASE in the WHERE, or accept a status_effective param); the same applies to filtering 'failed' which today still returns rows whose effective_status is 'retried', silently double-counting recovered failures."
    },
    {
     "title": "NOT-LANDED: degraded switchboard fabricates a healthy-empty ledger as HTTP 200 \u2014 zeros rendered in success-green",
     "evidence": "src/butlers/api/routers/notifications.py:93-111 (_empty_notification_page/_empty_notification_stats returned when pool is None or table missing, no degraded marker in envelope) + frontend/src/components/notifications/notification-stats-bar.tsx:17-20,74-84 (undefined stats coalesce to 0/0/'0.0%' with the failure-rate tile colored emerald) + frontend/src/pages/NotificationsPage.tsx:92-93,159 (stats error state never read; failedCount 0 hides the ack-all button)",
     "severity": "critical",
     "detail": "Truth amnesty (.1) never reached this API: when the switchboard DB is unreachable the endpoint returns a fabricated all-zeros payload indistinguishable from 'no notifications ever', violating the repo's own degraded-envelope convention (aggregates_available). Downstream, the dashboard attention list only flags on HTTP error (components/overview/model.ts:580-586), so a silently-degraded messenger reads as fleet-wide quiet. The stats bar additionally takes no error prop at all, so even a real fetch failure renders as healthy zeros."
    },
    {
     "title": "Optimistic mark-read/ack-all is display-inert: patches flip `status` but rows render `effective_status`, which the backend always populates",
     "evidence": "frontend/src/hooks/use-notifications.ts:61,75 (updateItems sets {status:'read'} only; 'effective_status' appears nowhere in the file) vs frontend/src/components/notifications/notification-feed.tsx:146 (displayStatus = n.effective_status ?? n.status) and src/butlers/api/routers/notifications.py:186,204 (CASE ... ELSE status END \u2014 effective_status is non-null on every list row)",
     "severity": "major",
     "detail": "The same-frame-apply contract the epic just landed is dead on this page: clicking Mark read/Dismiss/Acknowledge-all changes nothing visually until the invalidation refetch returns; rows keep their badge and their triage buttons for a full network roundtrip. Fix is one line per hook (also set effective_status:'read') or, better, derive display state client-side from a single canonical field."
    },
    {
     "title": "Fleet event bus emits `notification` events but never invalidates the notifications surface's own caches",
     "evidence": "frontend/src/hooks/event-cache-registry.ts:100-104 (notificationPatch invalidates only messenger-delivery-stats, messenger-queue-depth, timeline) vs frontend/src/hooks/use-notifications.ts:18,23,33 (query keys ['notifications'], ['butler-notifications'], ['notification-stats'] \u2014 none patched)",
     "severity": "major",
     "detail": "The one page named after the event type is excluded from the live spine: /notifications learns about new deliveries only via 30s staleTime + remount, so the owner watches Telegram receive a message before the console's own delivery ledger shows it \u2014 precisely the failure the .8 event bus was built to kill. Adding the three keys to notificationPatch is a 3-line fix."
    },
    {
     "title": "NOT-LANDED: inbound drill-down dead-end \u2014 dashboard attention rows link bare /notifications and the page cannot accept any predicate (filters live in useState, not URL)",
     "evidence": "frontend/src/components/overview/model.ts:511,587-599 (both 'N failed notifications' attention rows href='/notifications' with no ?status=failed) + frontend/src/pages/NotificationsPage.tsx:75 (filters/page are local useState; no useSearchParams anywhere in the file)",
     "severity": "major",
     "detail": "The .4 drill-down sweep missed this edge: clicking '3 failed notifications' on the home page lands the owner on the unfiltered all-time stream where the failures may be pages deep. Because filter state is not URL-backed, no surface anywhere (issues, sessions, chat widget) can ever deep-link a pre-filtered notifications view, and the four stat tiles are inert non-anchors. Every clause-is-a-door path into this surface is broken inbound even though outbound trace/session doors now work."
    },
    {
     "title": "NOT-LANDED: zero keyboard surface on a triage queue \u2014 the j/k + verb pattern exists one page over and was not ported",
     "evidence": "frontend/src/pages/NotificationsPage.tsx + notification-feed.tsx contain no onKeyDown/hotkey handling (only mouse Buttons at notification-feed.tsx:199-222) vs frontend/src/pages/ApprovalsPage.tsx:1327-1373 (j/k roving focus + a/d/x decision verbs, active page-wide, landed in .14)",
     "severity": "major",
     "detail": "The north star 'clears that queue in under a minute without touching the mouse' fails outright: acknowledging N failed notifications requires N precise clicks on ghost buttons. The .14 keyboard-triage machinery (roving focus, per-item pending state, verb keys) is already built and tested on /approvals; notifications needs the same j/k + e(ack) + Enter(open evidence) treatment."
    },
    {
     "title": "NOT-LANDED: no detail view \u2014 message truncated at 60 chars, error at 80, `metadata` fetched but never rendered anywhere",
     "evidence": "frontend/src/components/notifications/notification-feed.tsx:83-90 (truncate helpers), :163-166 (hover-only title attribute as the sole path to full text), metadata present on NotificationSummary (frontend/src/api/types.ts ~:290s) and normalized server-side (notifications.py:202) but unused in any component",
     "severity": "major",
     "detail": "Diagnose cannot complete on-screen: a failed Telegram delivery's full message body, its error beyond 80 chars, and its metadata (chat id, retry context) are unreachable except via tooltip hover \u2014 invisible to keyboard and touch entirely. The DisclosureRow primitive from .16 exists precisely for this; each row should expand to full message, error, metadata, and its session/trace doors."
    },
    {
     "title": "NOT-LANDED: page opens with unwindowed all-time counters and no verdict \u2014 one bad week poisons the failure rate forever; by_butler fetched but never shown",
     "evidence": "src/butlers/api/routers/notifications.py:306-312 (stats endpoint takes no window/filter params) + frontend/src/components/notifications/notification-stats-bar.tsx:20 (failureRate = failed/total over all time), :90-99 (only by_channel rendered; by_butler discarded) + no verdict component (SystemVerdictBanner pattern exists at frontend/src/components/system/SystemVerdictBanner.tsx but nothing analogous here)",
     "severity": "major",
     "detail": "The judgment layer (.17) never reached this page: the owner's actual question \u2014 'is delivery healthy right now, and who is failing?' \u2014 is unanswerable from lifetime totals. First glance should be a windowed synthesized sentence ('2 failures in the last 24h, both telegram, both finance \u2014 retried successfully') with each clause a filter-carrying door; the per-butler breakdown the API already returns is the key attribution signal and is thrown away."
    }
   ],
   "ideal": "The page opens with a Dispatch-voice verdict line computed over a live window \u2014 \"Delivery healthy: 41 sent in the last 24h, 2 failed (both telegram, finance), 1 retried successfully, nothing pending\" \u2014 where every clause is a door that applies the corresponding URL-backed filter in place; below it, a keyboard-first ledger (j/k roving focus, e to acknowledge, Enter to disclose, all verbs also in the command palette) streams new deliveries in the same frame the daemon emits them over the fleet event bus, orders failures-first, and renders exactly one triage verb per row plus an inline Retry on failures. Each row discloses in place to the full message, error, and metadata with its session and trace doors, filtered views are shareable URLs that the dashboard's \"N failed notifications\" clause and Telegram alerts land on pre-filtered, acknowledgments apply optimistically in the same frame with undo, and a degraded switchboard names itself in a banner over honest em-dashes \u2014 never zeros, never green.",
   "moves": [
    {
     "title": "Truth pass: degraded envelope + effective_status filtering + honest butler filter",
     "why": "Two critical truth sins live here: a dead switchboard renders as a healthy green ledger, and the Retried filter is a lie that can never match. A console whose delivery page fabricates calm is untrustable at its core.",
     "cost": "M",
     "slice_plan": "1) Add `source_available: false` to the empty-fallback envelopes (notifications.py:93-111) and thread it through NotificationStats/PaginatedResponse types; stats bar takes an error/degraded prop and renders em-dashes + a named degraded banner, never zero-emerald; overview attention marks degraded too. 2) Move status filtering onto the computed effective_status (wrap the CASE into WHERE) so retried/failed filter truthfully. 3) Butler filter becomes a debounced ILIKE substring (or a Select fed from the roster) so partial typing stops returning a false empty.",
     "integration_points": "src/butlers/api/routers/notifications.py:93-111,149-152,171-190; frontend/src/components/notifications/notification-stats-bar.tsx; frontend/src/pages/NotificationsPage.tsx:92-99,159-179; frontend/src/components/overview/model.ts:580-599; follows the aggregates_available convention from the ingestion routers"
    },
    {
     "title": "Wire /notifications into the live spine and un-break same-frame acks (3-line fixes, highest leverage)",
     "why": "The event bus and optimistic-mutation work both landed and both miss this page: notificationPatch skips the page's own query keys, and the optimistic flip sets a field the rows don't render. Two tiny diffs restore the epic's two flagship contracts here.",
     "cost": "S",
     "slice_plan": "1) Add ['notifications'], ['butler-notifications'], ['notification-stats'] to notificationPatch. 2) updateItems in useMarkNotificationRead/useAcknowledgeAllFailed also sets effective_status:'read' (and ack-all matches on effective_status so 'retried' rows aren't mis-flipped). 3) Add a vitest asserting the optimistic flip changes displayStatus.",
     "integration_points": "frontend/src/hooks/event-cache-registry.ts:100-104; frontend/src/hooks/use-notifications.ts:56-78; frontend/src/components/notifications/notification-feed.tsx:146"
    },
    {
     "title": "URL-backed filter state + predicate-carrying inbound doors",
     "why": "Clause-is-a-door is broken inbound: the dashboard's 'N failed notifications' clause dumps the owner on the unfiltered stream, and no surface can ever deep-link a filtered view because filter state is component-local.",
     "cost": "S",
     "slice_plan": "1) Migrate FilterState + page to useSearchParams (pattern: TimelineTab's urlRange/urlTrace). 2) Dashboard attention rows link /notifications?status=failed (+butler when attributable). 3) Stat tiles become anchors applying their own filter (Failed tile \u2192 ?status=failed).",
     "integration_points": "frontend/src/pages/NotificationsPage.tsx:74-131; frontend/src/components/overview/model.ts:511,587-599; frontend/src/components/notifications/notification-stats-bar.tsx; keep bu-9d5vp scope in mind (it covers KPI-cell inertness elsewhere, not this page's URL state)"
    },
    {
     "title": "Keyboard triage + one verb + DisclosureRow detail",
     "why": "The queue-clearing north star is mouse-only today, two buttons per row perform the identical mutation, and the evidence (full message, error, metadata) is hover-tooltip-only \u2014 Detect/Diagnose/Act all stall on this surface.",
     "cost": "M",
     "slice_plan": "1) Port the ApprovalsPage j/k roving-focus + verb-key machinery (.14): e/a = acknowledge, Enter = disclose, with per-row pending state; register verbs in the command registry. 2) Collapse Dismiss/Mark-read into a single 'Acknowledge' verb (they hit the same PATCH \u2014 NotificationsPage.tsx:152). 3) Replace the truncation-only cell with DisclosureRow (.16 primitive) expanding to full message, full error, metadata, and the session/trace doors. Leave failed-row Retry to bu-6t8ix (already filed) but design the verb slot for it.",
     "integration_points": "frontend/src/pages/ApprovalsPage.tsx:1327-1373 (donor pattern); frontend/src/components/ui/DisclosureRow.tsx; frontend/src/components/notifications/notification-feed.tsx:159-225; command registry via GlobalActionsRegistrar; coordinate with bu-6t8ix so the retry verb lands in the same row slot"
    },
    {
     "title": "Judgment header: windowed verdict line over windowed stats with by_butler attribution",
     "why": "The page leads with lifetime counters that one bad week poisons forever and discards the per-butler breakdown it already fetches; the owner's question is 'is delivery healthy now and who is failing', which nothing on the page answers.",
     "cost": "M",
     "slice_plan": "1) Add window param (24h/7d/all) to /notifications/stats reusing its existing since-style predicates. 2) Replace the four stat cards with a Dispatch-voice verdict sentence (SystemVerdictBanner as the donor pattern) whose clauses carry filters, plus compact windowed tiles. 3) Render by_butler chips as filter anchors; failures-first sort default when any failures exist in-window.",
     "integration_points": "src/butlers/api/routers/notifications.py:306-390; frontend/src/components/notifications/notification-stats-bar.tsx (rebuild); frontend/src/components/system/SystemVerdictBanner.tsx (pattern donor); range-toggle.tsx for the window control; depends on Move 3 (clauses need URL predicates to be doors)"
    }
   ]
  },
  {
   "page": "issues",
   "verdict": "functional",
   "jarvis_gap": "Real movement since the WEAK rating: acknowledge-until-recurrence is genuinely landed server-side (issues.py:95-111, public.dismissed_issues watermark), remedy verbs exist (Ping butler / Run schedule now, honest-pending), the View link now emits params AuditLogPage actually reads (actor/action), dismissals are optimistic with rollback, and the fleet event bus makes the feed live. But the surface is still a passive legacy-dialect card list, not the fleet's triage inbox: a grouped issue (\"Seen 47x\") can never be opened to its 47 occurrences anywhere in the product (the audit-log API has no result/error-group predicate), a dead butler still raises no badge outside the page, every fetch live-pings the whole fleet inline (page stalls ~10s exactly when a butler is down), acking an unreachable issue silently bounces back within one poll, there is no verdict line, no time window, no keyboard triage loop, and none of the Dispatch primitives (Row/StateDot/ButlerMark/Mono) that now exist are used here. Detect works; diagnose dead-ends one hop in; act is partial.",
   "findings": [
    {
     "title": "NOT-LANDED: issue groups still have no path to their root evidence \u2014 drill-down dead-ends one hop in",
     "evidence": "src/butlers/api/audit_grouping.py:199-204 builds the link from only actor (single-butler) and action=session (scheduled): a multi-butler warning group emits bare /audit-log with zero predicates; even the best case (actor=X&action=session) lands on ALL of that butler's session rows, successes included, not this group's occurrences. The audit-log API cannot scope tighter: src/butlers/api/routers/audit.py:291-316 accepts only offset/limit/since/actor/action/key/kind \u2014 no result=error, no error-signature predicate. Meanwhile IssuesPanel.tsx:149 renders only the one-line description; issue.error_message, issue.type, and the per-butler breakdown (issue.butlers) are in the payload but never rendered and there is no row expansion.",
     "severity": "critical",
     "detail": "\"Seen 47x \u00b7 First \u2026 \u00b7 Last \u2026\" is a claim the owner can never verify: no surface in the product can list those 47 occurrences or reach the failing session/trace from them. This is the exact defect class bu-86c4c.4 fixed elsewhere (real anchors with full predicates) but the issues\u2192evidence hop needs a backend predicate that was never added, and it is NOT covered by bu-9d5vp (that bead covers KPI cells, egress actors, audit request_id, spend rules, message threads). Fix needs either GET /api/audit-log?result=error&group=<signature> or GET /api/issues/{issue_key}/occurrences reusing the shared CTE, plus inline row expansion (DisclosureRow) showing full error text, ButlerMark chips per affected butler, and session/request_id links into the trace spine."
    },
    {
     "title": "Acknowledging a live 'unreachable' issue silently bounces back within one poll \u2014 the control lies",
     "evidence": "src/butlers/api/routers/issues.py:212-222 stamps reachability issues with first_seen_at = last_seen_at = datetime.now(UTC) on EVERY fetch; the frontend acks with that timestamp as the recurrence watermark (use-issues.ts DismissIssueVariables); _still_acked (issues.py:95-111) then finds last_seen_at > watermark on the very next fetch and un-acks it. The optimistic cache removal (use-issues.ts) makes the button look like it worked.",
     "severity": "major",
     "detail": "For an ongoing condition the ack is a no-op with a \u22645-minute fuse: the issue vanishes, then reappears with no explanation \u2014 and because it is no longer 'still acked' it does not even appear under Show acknowledged for restore. Acknowledge-until-recurrence semantics are right for event groups but wrong for live conditions; unreachable issues need explicit snooze durations (1h / 24h / until-recovery) or an 'acknowledged while down' state, plus a 'recurred after ack' marker when a group resurfaces."
    },
    {
     "title": "Every issues fetch live-pings the entire fleet inline \u2014 the page stalls precisely when a butler is down",
     "evidence": "src/butlers/api/routers/issues.py:32 (_STATUS_TIMEOUT_S = 5.0), :141-170 (per-butler get_client + ping, each with a 5s timeout, retry loop), :205-210 (asyncio.gather over all butlers on every GET /api/issues \u2014 including the include_dismissed=true view, so toggling 'Show acknowledged' re-pings the fleet). Frontend refetch paths: use-issues.ts polls every 5 min, the event bus invalidates [\"issues\"] on every audit error, and both dismiss/undismiss invalidate both views.",
     "severity": "major",
     "detail": "When any butler is down (the moment this page matters most) initial load blocks ~5-10s behind a skeleton, and every ack/restore triggers another fleet-wide ping storm as its invalidation settles. Reachability should be a cached background watcher (daemon-side or a ~15s in-process cache with event-bus push on transitions) so GET /api/issues reads state in milliseconds and can carry an honest 'checked Ns ago' provenance stamp."
    },
    {
     "title": "NOT-LANDED: critical issues surface nowhere globally \u2014 a dead butler raises no badge anywhere",
     "evidence": "frontend/src/components/layout/nav-config.ts:99 \u2014 the /issues nav item has icon and chord 'i' but no badgeKey, while QA (:72, badgeKey 'qa-escalations', red) and Approvals (:74, 'approvals-pending', amber) both carry live badges; Sidebar.tsx:150 renders counts only for items with badgeKey.",
     "severity": "major",
     "detail": "The old dossier's major finding reproduces verbatim on current main. The north star's 'silence means health' contract is inverted: silence currently means 'you haven't visited /issues'. Add badgeKey 'issues-critical' (red) driven by the active feed's critical count \u2014 cheap once reachability is cached (finding above), otherwise the badge poller inherits the ping-storm cost."
    },
    {
     "title": "NOT-LANDED: the panel ignores the Dispatch primitive system \u2014 gray warning badges, plain-text butlers, hidden group members",
     "evidence": "frontend/src/components/issues/IssuesPanel.tsx is built entirely on legacy Card/Badge/Button/EmptyState: :140 renders severity 'warning' as a neutral gray 'secondary' badge (not amber StateDot); :143-147 renders butler names as plain <span> text (not ButlerMark links to /butlers/{name}); :144-146 shows multi-butler groups as only 'N butlers' while issue.butlers identities sit unrendered in the payload; no Row/RowLink/Eyebrow/Mono vocabulary anywhere \u2014 all of these primitives now exist in components/ui/ (Row.tsx, StateDot.tsx, ButlerMark.tsx, Mono.tsx, DisclosureRow.tsx).",
     "severity": "major",
     "detail": "Visual-dialect drift plus an information dead-end in one: 'who is affected?' is unanswerable for grouped issues without leaving the page, and warning-colored-gray breaks the three-state color contract every redesigned surface now honors. Not covered by in-flight bu-86c4c.6 (that is lint/CI token enforcement, not restyling this panel) nor bu-kx3xc (amber text contrast)."
    },
    {
     "title": "Keyboard triage is absent, and the one palette command acks an unnamed issue blind",
     "evidence": "frontend/src/pages/IssuesPage.tsx:75-88 registers a single 'Acknowledge issue' command that silently acks issues[0] (whatever is newest at that instant) with no preview of which issue it will ack; IssuesPanel.tsx:196-204 uses one global isDismissing flag that disables EVERY row's Acknowledge while one ack is in flight, serializing bulk triage; use-issues.ts wires no onError/onSuccess feedback, so a failed ack silently rolls back (the row reappears looking like a UI glitch) and a successful ack has no undo toast.",
     "severity": "major",
     "detail": "The north star is 'clears that queue in under a minute without touching the mouse'. bu-86c4c.14 delivered keyboard triage on /approvals only; the same loop is missing here: no j/k roving row focus, no e=acknowledge / o=open-evidence / u=undo, and the palette command performs a state change on an item the owner cannot see. Per-row pending keyed by issue_key, an undo toast (undismiss already exists as the inverse), and focused-row-scoped commands are all straightforward with existing pieces."
    },
    {
     "title": "NOT-LANDED: unbounded all-time aggregation with zero triage tools \u2014 stale groups live in 'active' forever",
     "evidence": "src/butlers/api/routers/issues.py:64 calls build_audit_group_query() with no where_extra and no limit \u2014 a full-table grouping scan of public.audit_log on every fetch (and every 5-min poll and event-bus invalidation); audit_grouping.py:22 explicitly says 'Callers apply their own window/LIMIT constraints' and this caller applies none. The page (IssuesPage.tsx) offers no time window, severity/butler filter, or search \u2014 only the active/acknowledged toggle.",
     "severity": "major",
     "detail": "A one-off error from months ago sits in the active feed until individually acked, diluting the queue the owner is supposed to clear in a minute, and the query cost grows with audit-log history unboundedly. Default to a windowed view (e.g. 7d, range-toggle to all-time), add severity/butler pills, and cap the CTE with a LIMIT."
    },
    {
     "title": "NOT-LANDED: error state bypasses the bu-86c4c.2 QueryBoundary contract \u2014 no retry, error dressed as empty-state",
     "evidence": "frontend/src/components/issues/IssuesPanel.tsx:77-91 hand-rolls isError as an <EmptyState> ('Could not load issues.') with no retry affordance and no derived error message, while components/ui/query-boundary.tsx exists precisely as the shared three-way contract (loading > error-with-Retry > empty) for nested lists; the loading/error/empty branches each also re-render a redundant Card header repeating the page H1 'Issues'.",
     "severity": "minor",
     "detail": "Copy is honest (so this is not error-impersonating-empty in the data sense) but the presentation is the empty-state visual with no next action, and this surface \u2014 the one the owner opens during incidents \u2014 is exactly where 'retry now' matters. Not covered by bu-mkd5r (that bead scopes topology/education/remaining pills). Swap the three branches for QueryBoundary with onRetry=refetch and drop the inner card shell."
    }
   ],
   "ideal": "/issues is the fleet's triage inbox and reads in the same Dispatch hand as the rest of the console. It opens with a one-line synthesized verdict in the editorial voice (\"2 critical \u2014 pantry unreachable 43m, ping failing; 3 warning groups, none new since Tuesday\") over a summary strip (N critical / N warning / last change), and the shell badges the critical count so the owner only visits when pulled. Below, a severity-first ledger of grouped signals built on Row/StateDot/ButlerMark/Mono: red/amber dots, mono error signatures, butler chips that link, occurrence sparklines, a default 7-day window with range toggle and severity/butler pills. Every row is a door: expand inline to the group's actual occurrences (full error text, per-butler breakdown, each occurrence carrying its session/request_id into the trace spine) and every issue type carries its remedy verb on the row \u2014 ping, run schedule now, open session. Reachability is a cached live stream with a \"checked 12s ago\" provenance stamp, so the page renders in milliseconds even mid-outage. Acknowledgement is a triage state, not a mute: event groups resurface flagged \"recurred after ack,\" live conditions get explicit snooze durations, every ack is one keystroke with an undo toast, and j/k + e/o clears the whole queue mouse-free in under a minute. Nothing is claimed that cannot be opened; nothing stalls; silence genuinely means health.",
   "moves": [
    {
     "title": "Open the group: inline occurrence evidence with a real backend predicate",
     "why": "The surface's core promise \u2014 'Seen 47x' becomes 47 inspectable rows with session/trace links \u2014 is structurally impossible today; this closes the single biggest dead end (finding 1) and delivers the drill-down spine bu-86c4c.4 could not finish without backend support.",
     "cost": "M",
     "slice_plan": "1) Add result= and group= (normalized error signature, computed with the same REGEXP as the CTE) params to GET /api/audit-log, or a new GET /api/issues/{issue_key}/occurrences reusing audit_grouping's normalization; return session/request_id per row. 2) Wrap each issue row in DisclosureRow: expansion shows full error_message (Mono), issue type key, ButlerMark chips per affected butler, and the last ~10 occurrences with links to session/trace. 3) Rewrite audit_grouping.py link building to carry the full predicate (group + actor + since=first_seen) so 'open in audit log' arrives scoped even for multi-butler groups.",
     "integration_points": "src/butlers/api/routers/audit.py:291+ (new params), src/butlers/api/audit_grouping.py:199-204 (link predicates, expose group signature on Issue), frontend/src/components/issues/IssuesPanel.tsx (DisclosureRow expansion), frontend/src/api/types.ts Issue (add group/occurrence fields), trace spine components from bu-86c4c.3/.4."
    },
    {
     "title": "Cached reachability watcher + issues-critical nav badge + verdict strip",
     "why": "Kills the inline fleet ping storm (page stalls ~10s during the exact incidents it exists for), makes a global critical badge affordable, and gives the page the judgment-layer opening every redesigned surface has (findings 3, 4, plus the missing verdict).",
     "cost": "M",
     "slice_plan": "1) Move _check_butler_reachability into a background loop (in-process ~15s cache, event-bus 'reachability-changed' push on transitions); GET /api/issues reads the cache and stamps checked_at. 2) Add badgeKey 'issues-critical' (red) to nav-config.ts:99, fed from the cached active-feed critical count via the existing badge counts hook. 3) Add a verdict line + N critical / N warning / last-change strip at the top of the page, following the SystemVerdictBanner pattern and Dispatch voice.",
     "integration_points": "src/butlers/api/routers/issues.py:133-222, fleet event bus (bu-86c4c.8) for push invalidation, frontend/src/components/layout/nav-config.ts:99 + Sidebar badge counts, components/system/SystemVerdictBanner.tsx as the pattern donor, Provenance.tsx for the checked-ago stamp."
    },
    {
     "title": "Honest ack semantics: snooze for live conditions, 'recurred after ack' markers, undo toast, per-row pending",
     "why": "Acking an unreachable butler currently lies (bounces back \u22645 min with no explanation), resurfaced groups arrive unflagged, failed acks look like UI glitches, and one in-flight ack freezes every other row (findings 2, 6-partial).",
     "cost": "M",
     "slice_plan": "1) Extend public.dismissed_issues with snooze_until; dismiss endpoint accepts a duration for live-condition types; _still_acked honors it. 2) When a watermark lapse resurfaces a group, flag it (recurred_after_ack) and render an amber 'recurred' Pill. 3) In use-issues.ts add onSuccess undo toast (undismiss is the ready-made inverse) and onError toast; key pending state per issue_key instead of the global isDismissing/isRestoring flags.",
     "integration_points": "src/butlers/api/routers/issues.py:95-111 (_still_acked), :262-302 (dismiss), migration for public.dismissed_issues, frontend/src/hooks/use-issues.ts, frontend/src/components/issues/IssuesPanel.tsx:185-205, sonner toasts."
    },
    {
     "title": "Rebuild the ledger in Dispatch dialect with a keyboard triage loop",
     "why": "The panel is the last legacy-dialect surface on a hot path \u2014 gray warning badges, plain-text butlers, hidden group members \u2014 and the owner cannot clear the queue mouse-free; the palette's only command acts blind on issues[0] (findings 5, 6).",
     "cost": "M",
     "slice_plan": "1) Replace Card/Badge rows with Row + StateDot (red critical / amber warning) + ButlerMark chips (all members of issue.butlers, each linking to /butlers/{name}) + Mono error signature. 2) Add roving j/k focus, e=acknowledge (undo toast), o=open/expand evidence, with KbMono hints; scope palette commands to the focused row and name the target in the command label. 3) Replace the error/empty/loading branches with QueryBoundary (onRetry) and drop the redundant inner card header.",
     "integration_points": "frontend/src/components/issues/IssuesPanel.tsx (full rebuild), components/ui/{Row,StateDot,ButlerMark,Mono,KbMono,DisclosureRow,query-boundary}.tsx, lib/command-registry (IssuesPage.tsx:75-88), keyboard pattern donor from the bu-86c4c.14 approvals triage."
    },
    {
     "title": "Window the backlog: default 7-day view, severity/butler pills, capped query, active/acked as a filter",
     "why": "The feed is an unbounded all-time full-table aggregation \u2014 months-old one-off errors pollute the 'active' queue forever and query cost grows with history (finding 7); the dismissed-view mode toggle hides restores behind a context switch.",
     "cost": "S",
     "slice_plan": "1) Pass where_extra (created_at >= now()-interval) and a LIMIT into build_audit_group_query from the issues router, driven by a ?window= param defaulting to 7d. 2) Add a range-toggle (existing component) + severity/butler Pill filters to the page; fold the Show-acknowledged toggle into an active/acked/all pill. 3) Keep reachability issues always visible regardless of window.",
     "integration_points": "src/butlers/api/routers/issues.py:49-69 + :173-186 (query param), src/butlers/api/audit_grouping.py build_audit_group_query (already supports both knobs), frontend/src/pages/IssuesPage.tsx, components/ui/range-toggle.tsx and Pill.tsx."
    }
   ]
  },
  {
   "page": "audit-log",
   "verdict": "functional",
   "jarvis_gap": "The audit log is the fleet's ledger of record, but it opens as a raw, noise-dominated data dump instead of a judgment: no synthesized verdict, heartbeat rows drowning privileged actions, and \u2014 the deepest cut \u2014 the outcome of every audited operation (result/error/metadata, persisted since core_122) is deliberately withheld from the reader, so the one surface meant to prove what the system did cannot say whether any action succeeded. Filters are exact-match text fields that fire per keystroke and produce lying empty states, a URL deep-link silently overrides the visible filter input, the expanded detail row renders at the bottom of the table instead of under the clicked row, and the page has no live-event path, no palette commands, and none of the Dispatch dialect that its sibling Trust Console reel already speaks.",
   "findings": [
    {
     "title": "Audit reader withholds result/error/metadata \u2014 success and failure rows are indistinguishable",
     "evidence": "src/butlers/api/models/audit.py:58-63 (from_record docstring: 'metadata/result/error columns (core_122) are NOT read here... intentionally left to a later PR'); src/butlers/api/routers/audit.py:398,439 (SELECT projects only id/ts/actor/action/target/note/ip/request_id); frontend/src/api/types.ts:1485-1494 (AuditLogEntry omits the trio); frontend/src/components/audit/AuditLogTable.tsx:184-250 (detail row has no outcome/metadata section)",
     "severity": "critical",
     "detail": "The DB stores result, error, and metadata on every audit row, and the /issues feed + fleet event bus are built on exactly the result='error' rows (audit.py:271-281), yet the audit log page \u2014 the root of the evidence spine \u2014 renders error rows identically to success rows and hides the structured context. No open bead covers the deferred reader wiring (bu-e3p1o is transactional append only). The console's 'root evidence' promise dead-ends one hop before the truth."
    },
    {
     "title": "Expanded detail row renders at the bottom of the table, not under the clicked row",
     "evidence": "frontend/src/components/audit/AuditLogTable.tsx:131-175 (entries.map renders all rows) then 177-253 (the single detail TableRow is a sibling appended after the entire map)",
     "severity": "major",
     "detail": "Clicking row 1 of a 20-row page opens its detail at row 21, typically below the fold with zero visual connection to the source row. The owner clicks, sees nothing change in view, and clicks again (collapsing it). Violates design-bar 'Repetition' and 'First glance'; the fix is rendering the detail row adjacent inside the map (a React.Fragment per entry)."
    },
    {
     "title": "?actor deep-link silently overrides the visible Actor input \u2014 the view contradicts its own controls",
     "evidence": "frontend/src/pages/AuditLogPage.tsx:40 (input hydrated from ?actor), 61 (actorFilter read from URL every render), 72 (spread after filters.actor, so URL always wins)",
     "severity": "major",
     "detail": "Arrive from a passport link with ?actor=switchboard, then type 'owner' into the Actor field: the input says owner, but params.actor stays switchboard and the table keeps showing switchboard rows. Two parallel filter states (chip vs input) for the same predicate with a hidden precedence rule. This is an error impersonating a valid view \u2014 the screen shows data the controls say it isn't showing."
    },
    {
     "title": "Default view is heartbeat noise; the existing kind=privileged preset is unexposed, and Trust Console's 'Full audit log' link drops the privileged predicate",
     "evidence": "src/butlers/api/routers/audit.py:308-315,378-381 (kind=privileged implemented server-side); frontend/src/pages/AuditLogPage.tsx:64-73 (page never sends kind); frontend/src/pages/SettingsPermissionsPage.tsx:414,459-462 (audit reel uses kind=privileged, but its 'Full audit log' link is bare /audit-log)",
     "severity": "major",
     "detail": "Page 1 of /audit-log is dominated by *_heartbeat and 'GET /' rows; the signal (permission.set, data.*, webhook.*) is buried. The backend already knows the noise predicate \u2014 only the Trust Console reel uses it. Drilling from that privileged reel into the full log loses the predicate mid-drill: a NEW link-predicate-not-honored instance, cross-page rather than the in-table ones .4 fixed."
    },
    {
     "title": "Exact-match text filters fetch per keystroke and produce lying empty states",
     "evidence": "frontend/src/pages/AuditLogPage.tsx:85-88 (handleFilterChange sets params directly, no debounce; queryKey includes params in use-audit-log.ts:7), 176-201 (free-text inputs 'e.g. owner', 'e.g. model.priority'); src/butlers/api/routers/audit.py:360-368 (actor = $, action = $ exact equality)",
     "severity": "major",
     "detail": "Typing 'model.priority' fires ~14 queries; every intermediate prefix exact-matches nothing, so the table resolves to 'No audit entries found' while the owner is mid-word \u2014 an empty state that means 'you haven't finished typing', not 'no such entries'. There is no vocabulary surface (distinct actors/actions endpoint) and no substring semantics, so the owner must already know exact action verbs to filter at all. Action cells are also inert (no click-to-filter pivot like actor cells got in .4)."
    },
    {
     "title": "NOT-LANDED: no Dispatch dialect or judgment layer on this surface \u2014 generic shadcn cards, no verdict lede",
     "evidence": "frontend/src/pages/AuditLogPage.tsx:118-242 (Card/CardContent/Badge/generic Table; description='Browse audit log entries across all butlers.'); contrast frontend/src/pages/SettingsPermissionsPage.tsx:87-104,336,443 (mono 10px eyebrows, hairline frames, serif voice on the sibling Trust Console)",
     "severity": "major",
     "detail": "The old dossier rated audit-log WEAK; .4 fixed links but the page never received the Dispatch treatment or the judgment layer (.17 landed elsewhere). It opens with a filter bar, not a synthesized verdict ('N privileged actions in 24h, M failures'). Overlaps in kind with in-flight bu-86c4c.6, but .6 is lint/CI token enforcement \u2014 nothing in flight converts this page; enforcement would flag it, not fix it."
    },
    {
     "title": "No live path: audit appends never reach the page except via 30s poll, and issue events don't invalidate the audit-log cache",
     "evidence": "frontend/src/hooks/use-audit-log.ts:9 (refetchInterval 30_000); frontend/src/hooks/event-cache-registry.ts:112-114 (issuePatch invalidates ['issues'] only); src/butlers/api/routers/audit.py:271-281 (emit_event fires only for result='error' rows)",
     "severity": "minor",
     "detail": "The fleet event bus (.8) exists and already carries audit-error events, but the audit page is not subscribed even to those; non-error appends emit nothing at all. Combined with OFFSET pagination on a live append-only table (audit.py:397-402 \u2014 off the house keyset convention used by ingestion events), rows shift under the owner while paging and every fetch pays a full COUNT(*)."
    },
    {
     "title": "No palette commands and no URL form for most filter state \u2014 filtered views are unshareable and unreachable from prose",
     "evidence": "frontend/src/pages/AuditLogPage.tsx:1-12 (no useRegisterCommands import; page registers nothing with the .7 command spine); 64-73 (only ?actor/?key have URL form; action/since/kind/page live in useState only)",
     "severity": "minor",
     "detail": "The one-registry command spine landed, but this page contributes zero verbs (filter by actor, clear filters, next/prev page). A filtered view like action=permission.set since yesterday cannot be linked from a Dispatch clause, bookmarked, or restored on refresh \u2014 the drill-down spine can point INTO /audit-log only for the two predicates that happen to have URL params."
    }
   ],
   "ideal": "The audit log opens as the household's ledger of record in full Dispatch voice: a serif lede synthesizes the period (\"Eleven privileged actions since yesterday \u2014 two failed, both messenger webhook rotations; heartbeats nominal\") with every clause a live link into the pre-filtered view it describes. The default lens is privileged actions with an outcome column in the three state colors; heartbeat/GET noise sits behind a labeled toggle that names its count. Every row is a keyboard-operable disclosure that expands in place to show note, IP, request_id (linked to its trace), structured metadata, and \u2014 for failures \u2014 the error text plus a one-keystroke jump to the matching /issues group and the remedy verb. Filters are honest facets: actor and action are typeahead menus fed by the ledger's own distinct values, matching as you type against known vocabulary so an empty result always means \"zero such entries\"; all filter state (actor, action, since, kind, key, cursor) lives in the URL, so any slice is shareable, palette-addressable, and restored on refresh. New rows arrive over the fleet event bus and slide in live under keyset pagination that never shifts under the reader; every arriving failure pulses once in the state color and settles. Nothing is withheld: what the system recorded is exactly what the owner sees.",
   "moves": [
    {
     "title": "Surface the outcome trio: project result/error/metadata through reader, type, and table",
     "why": "The ledger of record currently cannot say whether any audited operation succeeded \u2014 the single largest honesty gap on the surface, and the columns already exist (core_122).",
     "cost": "M",
     "slice_plan": "1) Project the three columns in both SELECTs (audit.py:398,439) and drop the from_record deferral (models/audit.py:58); 2) extend frontend AuditLogEntry + add an Outcome column (state-colored dot: ok/error/\u2014) and metadata JSON block + error text in the detail row; 3) failure rows link to the matching /issues group (same actor+action grouping as _list_audit_error_issues).",
     "integration_points": "src/butlers/api/models/audit.py, src/butlers/api/routers/audit.py, frontend/src/api/types.ts, frontend/src/components/audit/AuditLogTable.tsx, src/butlers/api/routers/issues.py (grouping key reuse)"
    },
    {
     "title": "Verdict-first privileged default with a Dispatch lede; carry the predicate from Trust Console",
     "why": "First glance must be judgment, not heartbeat noise; the backend predicate already exists and the Trust Console reel already proves the framing.",
     "cost": "M",
     "slice_plan": "1) Default the page to kind=privileged with a 'Show operational noise (N rows)' toggle in URL state; 2) make Trust Console's 'Full audit log' link /audit-log?kind=privileged; 3) add a synthesized serif lede (counts of privileged actions/failures/top actors over the window, each clause deep-linking to its filter); 4) restyle header/filter chrome to mono-eyebrow + hairline dialect, retiring the shadcn Cards (coordinates with, not blocked by, bu-86c4c.6 enforcement).",
     "integration_points": "frontend/src/pages/AuditLogPage.tsx, frontend/src/pages/SettingsPermissionsPage.tsx:459, existing kind=privileged in audit.py; lede aggregates via one small GET /api/audit-log/summary or client-side over the privileged page"
    },
    {
     "title": "One coherent, URL-serialized filter state with honest facet inputs",
     "why": "Fixes the ?actor-overrides-input contradiction, the lying mid-keystroke empty states, and the unshareable-slice gap in one structural change.",
     "cost": "M",
     "slice_plan": "1) Collapse FilterState + deep-link params into a single useSearchParams-backed state (actor/action/since/kind/key/page) \u2014 chips and inputs render the same value, no override shadow; 2) add GET /api/audit-log/facets returning distinct actors + actions (indexed, cheap) and swap the text inputs for typeahead comboboxes; 3) debounce any remaining free-text path 300ms; 4) make action cells pivot to ?action= exactly like actor cells; 5) register palette commands (filter by actor\u2026, clear filters, toggle noise) via useRegisterCommands.",
     "integration_points": "frontend/src/pages/AuditLogPage.tsx, new facets endpoint in src/butlers/api/routers/audit.py, frontend/src/components/audit/AuditLogTable.tsx (action pivot), src/lib/command-registry.tsx"
    },
    {
     "title": "Anchor the detail expansion under its row as a keyboard-operable disclosure",
     "why": "The detail row currently renders below the fold at the table bottom \u2014 a disorienting plain bug \u2014 and row expansion is mouse-only.",
     "cost": "S",
     "slice_plan": "Render the detail TableRow adjacent inside entries.map (Fragment per entry); adopt DisclosureRow semantics for the toggle (coordinate with bu-f310e which owns the a11y half so the two changes land as one edit, not two conflicting ones).",
     "integration_points": "frontend/src/components/audit/AuditLogTable.tsx:131-253, frontend/src/components/ui DisclosureRow (bu-f310e), AuditLogTable.test.tsx"
    },
    {
     "title": "Live ledger: audit events on the fleet bus + keyset pagination",
     "why": "North star requires state as a live event stream; today non-error appends are invisible for up to 30s and OFFSET paging shifts rows under the reader on a live append-only table.",
     "cost": "M",
     "slice_plan": "1) Emit a lightweight 'audit' event from append() for all rows (throttle/batch heartbeat-class actions server-side); 2) add an auditPatch to event-cache-registry invalidating ['audit-log'] (and have issuePatch invalidate it too as an interim S-sized slice); 3) convert the endpoint to cursor pagination (ts DESC, id DESC \u2014 the house convention from /api/ingestion/events), dropping the per-fetch COUNT(*); keep offset accepted during transition only if trivial, else remove (compat waived).",
     "integration_points": "src/butlers/api/routers/audit.py (append + list), src/butlers/api/routers/events.py, frontend/src/hooks/event-cache-registry.ts, frontend/src/hooks/use-audit-log.ts, frontend/src/pages/AuditLogPage.tsx pagination controls"
    }
   ]
  },
  {
   "page": "approvals",
   "verdict": "solid",
   "jarvis_gap": "The Trust Console's forward path (pending queue \u2192 dossier \u2192 decide, ranked by expiry+blast radius, keyboard triage with undo window, per-approval URLs, autonomy ledger with revoke) landed and holds. What is missing is the backward half of trust and the last mile of honesty: a decided approval's dossier cannot prove who decided it, when, why it was denied, or what actually happened when it ran (execution_result never reaches the detail endpoint; deny reason is string-mangled into decided_by and never rendered); the queue can silently render a partial fleet as a complete one; the autonomy-suggestions strip renders fetch errors as nothing; and the page's own defining keyboard surface (j/k/a/d/x) is invisible \u2014 absent from the help sheet, absent from the page, with undo reachable only by mouse. The surface also opens with a static header instead of a synthesized verdict, and autonomy use-counts are numbers without doors.",
   "findings": [
    {
     "title": "Decided dossier omits the decision itself \u2014 no decided_by, decided_at, deny reason, or execution result",
     "evidence": "frontend/src/pages/ApprovalsPage.tsx:660-830 (Dossier body renders title/why/evidence/proposed_action/target_contact only; no branch ever reads detail.decided_by or detail.decided_at) despite the History comment at ApprovalsPage.tsx:1103-1106 claiming \"decided_by/decided_at shown\". Backend: _pending_action_to_detail (src/butlers/api/routers/approvals.py:273-302) omits execution_result entirely (ApprovalDetail in frontend/src/api/types.ts:2751-2778 has no field for it), and the deny reason is HTML-escaped and concatenated into decided_by as \"human:\u2026 (reason: \u2026)\" (src/butlers/modules/approvals/operations.py:267-270) \u2014 unrecoverable as structured data.",
     "severity": "major",
     "detail": "History rows advertise auditability (\"every decided approval opens its read-only dossier\"), but the drill-down dead-ends one hop before the evidence: the owner sees the proposal, a colored status word, and nothing else \u2014 not who decided (human vs system:expiry vs auto-rule), not when, not the reason they themselves typed into the deny box, not whether the executed tool call succeeded or what it returned. For a page whose charter is \"trust is never invisible\", the decision record is the one thing it cannot show. Requires adding execution_result + parsed reason to the detail payload and a Decision section in the Dossier; reason should move out of the decided_by string into its own column."
    },
    {
     "title": "Partial-fleet aggregation impersonates a complete queue \u2014 per-butler pool failures swallowed with no degraded marker",
     "evidence": "src/butlers/api/routers/approvals.py:1546-1548 (list_approvals_flat: `except Exception: logger.warning(\"Failed to query pending_actions for flat list\")` then continues and returns 200 with the remaining butlers' rows) and approvals.py:1608 (list_approvals_history, same pattern). The response envelope (ApiResponse[list[ApprovalSummary]]) carries no sources_degraded/aggregates_available field; frontend/src/pages/ApprovalsPage.tsx:1431-1447 therefore renders a partial queue \u2014 or \"No pending approvals.\" \u2014 indistinguishably from a healthy empty one.",
     "severity": "major",
     "detail": "The truth-amnesty child (.1) fixed fetch-level errors (QueryBoundary checks isError before isEmpty), but this is one layer deeper: the fetch succeeds while N of 9 butler schemas silently failed to answer. An approval waiting in a broken butler's schema vanishes from the console with zero indication \u2014 the exact 'failed fetch impersonating an empty queue' the north star forbids, moved server-side. Fix mirrors the ingestion degraded-mode convention: return the list plus `sources_degraded: [butler,\u2026]`, and render a named-source warning strip above the rail."
    },
    {
     "title": "Autonomy-suggestions strip renders fetch errors as nothing \u2014 error impersonates empty on the autonomy-promotion surface",
     "evidence": "frontend/src/pages/ApprovalsPage.tsx:1144-1161: `const { data } = useAutonomySuggestions(...); const suggestions = data?.data ?? []; if (suggestions.length === 0) return null;` \u2014 isError/isLoading are never read, on the same page that uses QueryBoundary three times, and in direct violation of the contract documented in frontend/src/components/ui/query-boundary.tsx:16-20 (\"a query that has ERRORED must never fall through to the empty branch\").",
     "severity": "major",
     "detail": "When GET /autonomy-suggestions fails, the banner silently disappears; pending promotion/demotion suggestions \u2014 decisions the fleet is waiting on to expand or contract its own autonomy \u2014 are hidden with no degraded-source naming. Because the section is invisible-when-empty by design, the error state needs a distinct minimal treatment (a one-line named-source failure row), not a full empty-state."
    },
    {
     "title": "The page's defining keyboard surface is undiscoverable, its undo is mouse-only, and the palette 'Approve next' bypasses the undo window",
     "evidence": "j/k/a/d/x are bound in ApprovalsPage.tsx:1332-1374 but: (1) the ? help sheet lists only static rows + g-chords generated from the route registry (frontend/src/components/ui/shortcut-hints.tsx:19-41) \u2014 no mechanism exists for page-scoped shortcut rows, so triage keys appear nowhere; (2) no hint text anywhere in ApprovalsPage's rendered output mentions them; (3) undoing a scheduled decision requires clicking the sonner toast action or the dimmed rail row (ApprovalsPage.tsx:1321-1324, 364-369) \u2014 no key (u/Esc/z) cancels, and focus has already advanced to the next item (ApprovalsPage.tsx:1379-1388); (4) the command-palette entry \"Approve next\" (ApprovalsPage.tsx:1400-1411) calls approveMut.mutate(effectiveSelected) directly \u2014 it approves the CURRENT selection, not the next item, and fires instantly with no undo window, unlike the keyboard 'a' it shadows; Deny/Defer have no palette entries at all.",
     "severity": "major",
     "detail": "The north star is clearing the queue in under a minute without the mouse. The keys exist but only a code-reader knows them; the one recovery affordance in a blind-fire flow is the only step that demands the mouse; and the palette variant of the same verb silently drops the safety net and mislabels its target. Fix: a useRegisterShortcuts() page-scoped registry feeding ShortcutHints (mirror of the command registry from .7), a one-line mono hint in the rail footer, a u/Esc binding for 'undo last scheduled decision', and palette approve routed through scheduleDecision with an honest label."
    },
    {
     "title": "Dossier error branch is off the page's own state contract \u2014 bare dead-end with no retry, no alert role, no 404-vs-network distinction",
     "evidence": "frontend/src/pages/ApprovalsPage.tsx:483-489: `if (error || !data?.data) return <div \u2026>failed to load dossier</div>` \u2014 plain muted text, no QueryBoundary, no onRetry, no role=\"alert\", no sourceLabel, and a 404 (pruned/foreign id from a stale notification or bookmark \u2014 the exact deep-link entry the per-approval URLs were built for) renders identically to a network failure.",
     "severity": "minor",
     "detail": "The rest of the page adopted the bu-86c4c.2 contract (QueryBoundary with retry + role=alert); the center pane \u2014 the surface a Telegram notification deep-links into \u2014 did not. A stale /approvals/:id link should say \"this approval no longer exists \u2014 it may have expired or been decided\" with a door to History; a network failure should say the source and offer Retry."
    },
    {
     "title": "Autonomy ledger use-counts and provenance are numbers without doors \u2014 no drill-down from a rule to the actions it auto-approved",
     "evidence": "frontend/src/components/approvals/autonomy-panel.tsx:88-94 renders `{rule.use_count} uses` as inert text; ApprovalRule.created_from (frontend/src/api/types.ts:2804) is never rendered or linked; ApprovalAction.approval_rule_id exists (types.ts:2725) but no endpoint or UI filters history by rule (list_approvals_history at src/butlers/api/routers/approvals.py:1561-1608 supports only since/limit).",
     "severity": "minor",
     "detail": "The panel's charter is \"every active standing rule, its live use count\" \u2014 but 'this rule fired 7 times' cannot be verified: the owner cannot see the 7 actions, so the number is a claim without evidence (drill-down dead end, sin #3). created_from likewise never links back to the originating approval dossier. Needs a rule_id filter on the history endpoint plus links from the count and from created_from. Distinct from bu-9d5vp (dashboard KPI cells / egress / audit request_id / spend rule links \u2014 none touch the autonomy panel)."
    },
    {
     "title": "Stalled approvals (approved-but-never-dispatched) silently fall off the console after 30 newer decisions",
     "evidence": "_WAITING_STATUSES = {\"pending\"} (src/butlers/api/routers/approvals.py:1500) keeps stalled 'approved' actions out of the rail; their only surface is HistorySection's fixed last-30 fetch (frontend/src/pages/ApprovalsPage.tsx:1061-1067, getApprovalsHistory(undefined, 30)) ordered by decided_at DESC with no status filter, no pagination, and no way to load more.",
     "severity": "minor",
     "detail": "The page invests heavily in stalled honesty \u2014 amber-never-green statusColor (ApprovalsPage.tsx:164-179), the 'stalled' relabel, an inline Retry dispatch verb \u2014 yet a stalled approval from two days ago drops off the visible last-30 window and then exists nowhere on the console. An action the owner approved that never ran is arguably the single most attention-worthy state the page owns; it belongs in the actionable queue (rail, ranked above fresh pendings) or a dedicated stalled strip, not the history tail."
    },
    {
     "title": "Expiry countdowns never tick and the rail's ranking rationale is invisible",
     "evidence": "expiryCountdown (frontend/src/pages/ApprovalsPage.tsx:229-245) is computed once per render with no interval; the page's only guaranteed re-render sources are stream events and the 5-minute reconciliation refetch (ApprovalsPage.tsx:1211-1218), so a chip reading \"expires in 3m\" can sit unchanged past the actual expiry. rankScore's blast-radius component (ApprovalsPage.tsx:196-227) reorders the rail by a keyword heuristic (notify/send/delete \u2192 tier 3) but no row shows why it ranked where it did \u2014 only the countdown is visible.",
     "severity": "minor",
     "detail": "A countdown that does not count is a small fabrication: the number was true at render time and quietly stops being true. A 30s interval re-render (or a deadline-scheduled one) fixes it. Separately, the owner cannot see the 'blast radius' half of the ranking \u2014 an 'outbound' / 'internal write' eyebrow chip on rail rows would make the sort self-explaining and expose misclassifications of the regex heuristic instead of hiding them."
    }
   ],
   "ideal": "The Trust Console opens with a one-line synthesized verdict in the Dispatch voice \u2014 \"Three decisions waiting; the nearest expires in 40 minutes. One approved action never ran. The fleet acted unsupervised 12 times today under 4 standing rules.\" \u2014 and every clause is a door: waiting \u2192 the ranked rail, never-ran \u2192 the stalled item, 12 times \u2192 history filtered to auto-approvals, 4 rules \u2192 the autonomy ledger. The rail ranks by expiry and blast radius and says so on each row (an \"outbound\"/\"internal\" eyebrow beside a live-ticking countdown), and it names any butler whose queue could not be read rather than presenting a partial fleet as whole. The dossier is complete in both directions: pending, it shows why/evidence/entities with the floating decision cluster; decided, it shows the full verdict \u2014 who decided (human, expiry, or the named standing rule), when, the deny reason as first-class data, and the execution result with its error payload and an inline retry when dispatch failed. Triage is keyboard-complete and self-teaching: j/k/a/d/x/u are printed in the rail footer and the ? sheet via a page-scoped shortcut registry, undo is one keystroke, and the palette verbs share the same undo window as the keys. Every number on the surface is a door \u2014 a rule's use count opens the exact actions it authorized, created_from opens the approval that spawned the rule \u2014 and every fetch failure names itself, including the autonomy-suggestions strip, so the console's calm is earned rather than assumed.",
   "moves": [
    {
     "title": "Complete the decision record: render verdict + execution result in the dossier",
     "why": "The console's charter is provable trust, but /approvals/:id for a decided action shows the proposal and hides the decision \u2014 no decided_by/decided_at, no deny reason, no execution outcome. This is the highest-leverage honesty gap on the surface and the terminal hop of the drill-down spine (history row \u2192 dossier \u2192 nothing).",
     "cost": "M",
     "slice_plan": "1) Backend: add execution_result, decided_by, decided_at (already present), and a structured `reason` to ApprovalDetail; migrate deny reason out of the decided_by string into its own column (operations.py reject_action), keeping a parse fallback for old rows. 2) Frontend: add a Decision section to Dossier (actor humanized \u2014 'you', 'system: expiry', 'rule: <name>' linking to the autonomy panel; timestamp; reason in serif; execution result with success/error and the returned payload in mono), plus inline Retry dispatch when status=approved. 3) Tests: decided-dossier render for each terminal status.",
     "integration_points": "src/butlers/api/routers/approvals.py:_pending_action_to_detail (273-302); src/butlers/modules/approvals/operations.py:reject_action (228-300, reason column); frontend/src/api/types.ts:ApprovalDetail (2751); frontend/src/pages/ApprovalsPage.tsx Dossier body (660-830) and the existing RetryDispatchButton (1026-1059)."
    },
    {
     "title": "Honest aggregation: degraded-sources envelope on queue, history, and suggestions",
     "why": "Two silent-partial paths (per-butler pool failures swallowed server-side; suggestions fetch error rendered as null) let a broken fleet impersonate a calm one \u2014 the exact defect class the north star names as the console's defining property.",
     "cost": "S",
     "slice_plan": "1) Backend: collect failed butler names in list_approvals_flat/list_approvals_history and return `sources_degraded: [str]` alongside data (same 200-always convention as ingestion's aggregates_available). 2) Frontend: a named-source warning strip above the rail ('finance, health unreachable \u2014 queue may be incomplete') when non-empty. 3) AutonomySuggestionsSection: read isError and render a one-line role=alert failure row with retry instead of null.",
     "integration_points": "src/butlers/api/routers/approvals.py:1507-1560 (flat), 1561-1608 (history); frontend/src/api/types.ts response envelopes; frontend/src/pages/ApprovalsPage.tsx:1144-1161 (suggestions), 1428-1477 (rail); pattern precedent in CLAUDE.md 'Degraded-Mode Response Envelope'."
    },
    {
     "title": "Keyboard-complete triage: page-scoped shortcut registry, undo key, palette parity",
     "why": "The five-second triage loop exists but is a secret: no surface teaches j/k/a/d/x, undo demands the mouse, and the palette's 'Approve next' both mislabels its target and drops the undo window. This closes the 'clear the queue without touching the mouse' promise end-to-end.",
     "cost": "S",
     "slice_plan": "1) Add useRegisterShortcuts() mirroring the command registry (bu-86c4c.7) so pages contribute rows to ShortcutHints; register j/k/a/d/x/u here. 2) Print a one-line mono hint in the rail footer ('j/k move \u00b7 a approve \u00b7 d deny \u00b7 x defer \u00b7 u undo'). 3) Bind u (and Esc when nothing else owns it) to cancel the most recently scheduled decision. 4) Route the palette command through scheduleDecision, rename it 'Approve selected', and add Deny/Defer siblings.",
     "integration_points": "frontend/src/lib/command-registry.ts (pattern to mirror); frontend/src/components/ui/shortcut-hints.tsx:19-57; frontend/src/pages/ApprovalsPage.tsx:1308-1325 (scheduleDecision), 1332-1374 (key handler), 1400-1411 (palette); complements in-flight bu-86c4c.6/.15 without overlapping (neither touches shortcut discoverability)."
    },
    {
     "title": "Trust verdict line: the console opens with judgment, every clause a door",
     "why": "The judgment layer (.17) landed only on SystemPage; Approvals \u2014 the surface most explicitly about the owner's attention \u2014 opens with a static header. The metrics already exist server-side (get_metrics: pending count, auto-approval rate, expired today, active rules) and are already consumed elsewhere, so this is synthesis + wiring, not new data.",
     "cost": "M",
     "slice_plan": "1) Compose a verdict line from getApprovalsFlat + getApprovalMetrics + stalled-approved count: 'N waiting; nearest expires in Xm. K approved actions never ran. The fleet acted autonomously M times today under R rules.' in the SystemVerdictBanner idiom (skeleton, all-clear, problems variants). 2) Wire each clause: waiting \u2192 rail focus, expiry \u2192 that approval's URL, never-ran \u2192 stalled strip/filter, M times \u2192 history filtered to rule-decided, R rules \u2192 autonomy panel. 3) Honesty rule: any clause whose source is degraded names itself or is omitted, never zero-filled.",
     "integration_points": "frontend/src/components/system/SystemVerdictBanner.tsx (idiom to reuse); src/butlers/api/routers/approvals.py:get_metrics (1358-1485); frontend/src/pages/ApprovalsPage.tsx header (1416-1421); depends on move 5's rule-filtered history for the 'M times' door."
    },
    {
     "title": "Every rule auditable: rule\u2192uses and created_from drill-downs; stalled items rejoin the queue",
     "why": "Autonomy is the console's highest-stakes ledger, yet its use counts are unverifiable claims and a stalled approval \u2014 the most attention-worthy state the page owns \u2014 falls off the visible surface after 30 newer decisions.",
     "cost": "M",
     "slice_plan": "1) Backend: add rule_id and status filters to list_approvals_history (actions already carry approval_rule_id). 2) AutonomyPanel: use_count becomes a link opening History filtered to that rule; render created_from as a link to the originating approval's dossier. 3) Move stalled 'approved' items into the actionable surface: either include them in the waiting rail (rank tier above fresh pendings, amber, with Retry inline) or a dedicated 'never ran' strip pinned above History; either way they must never age out invisibly.",
     "integration_points": "src/butlers/api/routers/approvals.py:list_approvals_history (1561-1608) and _WAITING_STATUSES (1500); frontend/src/components/approvals/autonomy-panel.tsx:88-94; frontend/src/pages/ApprovalsPage.tsx:HistorySection (1061-1130) and rankScore (223-227); distinct from bu-9d5vp's dashboard-scoped drill-down list."
    }
   ]
  },
  {
   "page": "calendar",
   "verdict": "solid",
   "jarvis_gap": "The calendar is the fleet's richest workspace \u2014 unified provider+butler projection, drag editing with undo ghosts, recurrence scopes, NL quick-add, find-time, conflict radar, cross-domain overlays, audit trail with session links \u2014 but it is a mouse-driven, polled, verdict-less surface. It opens with a date headline and an event count, not a judgment; everything that needs the owner (butler proposals, duplicates, conflicts, failed syncs) is hidden behind unbadged toggle pills or buried two clicks deep in a dialog; the page contributes zero verbs to the one command spine and has exactly one keydown handler in 6,472 lines; state arrives on 30\u201360s polls instead of the fleet event bus; and the overlay/briefing layer that fuses other butlers' knowledge into time is opt-in-per-URL, non-interactive, and a drill-down dead end. Detect works; diagnose and act require hunting.",
   "findings": [
    {
     "title": "NOT-LANDED: Zero keyboard surface \u2014 calendar registers no commands in the one command spine and has one keydown handler in 6,472 lines",
     "evidence": "frontend/src/pages/CalendarWorkspacePage.tsx:2017 is the ONLY keydown on the page (Enter inside the search input); grep confirms the page never imports src/lib/command-registry.tsx (the per-page verb API that ApprovalsPage.tsx and IssuesPage.tsx already use, built by bu-86c4c.7); the CalendarSearchPalette opens only via a mouse pill (line 4135); Today/prev/next (4217-4238), view/range pills (4188-4212), Create event (4160-4175), quick-add, find-time, sync are all mouse-only. The old dossier flagged this (\"exactly one binding (calendar)\"); the epic landed the global spine but no calendar verbs or keymap.",
     "severity": "critical",
     "detail": "North star: clear the queue \"without leaving the screen or touching the mouse.\" From Cmd+K you can navigate TO /calendar but cannot Create event, Quick add, Find time, Jump to today, or Search events. Grid drag create/move/resize additionally has no keyboard analogue, so the page's flagship interactions are pointer-exclusive."
    },
    {
     "title": "NOT-LANDED: The needs-you queue is invisible \u2014 proposals, duplicates, and conflicts are fetched only after the owner manually opens their panels, with no counts anywhere",
     "evidence": "frontend/src/pages/CalendarWorkspacePage.tsx:2384-2390 (useCalendarProposals gated on `enabled: proposalsPanelOpen`), 2397-2401 (duplicates gated on panel open), toolbar pills at 4241-4292 render bare labels \"Activity / Find time / Proposals / Duplicates\" with no badge; conflict radar is enabled only on week/day (radarEnabled \u2192 conflictsAvailable, line 2322), so month and list ranges show no scheduling issues at all.",
     "severity": "major",
     "detail": "Butler-extracted event proposals awaiting accept/dismiss are the purest \"what needs you\" queue on this surface, and the owner cannot know any exist without clicking an unbadged pill. Violates \"sees exactly what needs them\" \u2014 the five-second health check is impossible here."
    },
    {
     "title": "NOT-LANDED: Conflict radar's degraded mode impersonates a clean week",
     "evidence": "frontend/src/components/calendar/ConflictRadarBanner.tsx:160-163 \u2014 comment reads \"Silent degraded mode and clean windows render nothing\"; `if (!available || issues.length === 0 || dismissed) return null;`. When the conflicts read fails server-side (issues_available:false), the banner is byte-identical to \"no conflicts.\"",
     "severity": "major",
     "detail": "Direct violation of the earned-calm contract (\"no failed fetch ever impersonates an empty queue\") and of the calendar's own shipped degraded-states convention (bu-5rmhbo, PR #2658) which mandates a visible 'context unavailable' rendering. The dossier flagged this at minor; on a safety surface (double-bookings) it is major."
    },
    {
     "title": "Calendar is not on the fleet event bus \u2014 state arrives by 30\u201360s polling, and butler-made mutations appear up to a minute late with no live signal",
     "evidence": "frontend/src/hooks/event-cache-registry.ts:148-156 \u2014 EVENT_CACHE_REGISTRY covers approval/spend/session/notification/issue/ingestion/heartbeat only; no calendar event type. frontend/src/hooks/use-calendar-workspace.ts:108 (workspace refetchInterval 30_000), :134,:157,:201 (60_000 polls for meta/accounts/etc.); the page never imports use-event-stream.ts.",
     "severity": "major",
     "detail": "The bus (bu-86c4c.8) was built exactly so surfaces stop polling; the registry's own docs say adding a surface means one registry entry. A butler creating/moving an event via MCP, a sync completing, or an audit row landing should patch the workspace/conflicts/audit caches in the same frame \u2014 instead the grid is stale for up to 30s while the Live indicator claims the app is live."
    },
    {
     "title": "Overlay pills and briefing chips are drill-down dead ends \u2014 fused cross-domain signals cannot reach their root evidence",
     "evidence": "frontend/src/pages/CalendarWorkspacePage.tsx:964-1000 \u2014 OverlayPill is documented and built as \"Non-interactive context, so it renders as a static chip, not a button\"; a finance bill overlay (with amount badge) has no path to /spend or the bill record, a travel leg no path to the trip, a relationship date no path to /entities. handleBriefingSelect (2692-2699) only navigates the grid to the chip's day. lib/calendar-overlays.ts contains no link/ref plumbing; the overlay contribution contract carries no target reference.",
     "severity": "major",
     "detail": "\"Every clause of that prose is a door \u2014 one keystroke from any signal to its root evidence.\" The fusion layer (RFC-0020, epic bu-1ajgg9) surfaces other butlers' judgments into time but severs the trace spine at exactly the moment the owner wants to act on one. Not covered by bu-9d5vp (which lists dashboard KPI cells, egress actors, audit request_id, spend rules \u2014 not calendar overlays)."
    },
    {
     "title": "A failed sync source never names itself on the canvas \u2014 the grid silently thins while degradation hides two clicks deep",
     "evidence": "frontend/src/pages/CalendarWorkspacePage.tsx \u2014 per-source sync_state/staleness renders ONLY inside the Sources dialog (5669-5673, StateDot + formatStaleness) and as a per-entry chip in the detail panel (1777-1782). The masthead shows just \"N events\" + a \"Sync now\" pill (4126-4141); the Sources pill annotates hidden count but not failed count (4149-4157). No canvas-level banner, no masthead dot, nothing in the eyebrow when a Google source's sync_state is \"failed\" or 26h stale.",
     "severity": "major",
     "detail": "\"Every degraded source names itself.\" A dead OAuth token means the week grid is quietly missing events \u2014 the most dangerous failure a calendar can have \u2014 and the owner has to suspect it, open Sources, and scan rows to find out. The sync-health cockpit (bu-wwftzj) built the diagnosis room but no alarm."
    },
    {
     "title": "NOT-LANDED: Calendar satellites still speak pre-Dispatch dialect, including undefined CSS tokens that render invisible backgrounds",
     "evidence": "frontend/src/components/calendar/ConflictRadarBanner.tsx:92,125,173,178,190 \u2014 raw Tailwind amber-500/emerald-600, rounded-lg card, rounded-full count pill, \u26a0 emoji, shadcn text-muted-foreground/hover:bg-muted on the calendar's most safety-critical banner. frontend/src/pages/calendar/QuickAddBar.tsx:21 \u2014 rounded-full pills vs Design Language \u00a74c (\"4px 10px / 1px border / 3px radius / mono 11px \u2026 The system has no rounded[-full pills]\", openspec/specs/dashboard-design-language/spec.md:242-245). CalendarAgendaView.tsx:63,71 and CalendarPortabilityDialog.tsx:228 use var(--surface), which is defined nowhere in index.css \u2014 hover/file-input backgrounds silently resolve to nothing.",
     "severity": "major",
     "detail": "Distinct from in-flight bu-86c4c.6 (which is the lint/CI ENFORCEMENT mechanism): these are the concrete component fixes the old dossier named that the epic did not land. Accept-proposal renders as a green emerald button \u2014 a fourth 'green' meaning \u2014 inside a surface whose page-level chrome (PILL_BASE, StateDot, KindTag) is otherwise exemplary Dispatch."
    },
    {
     "title": "NOT-LANDED: Two timezones on one surface \u2014 masthead and search palette use browser tz while the grid renders workspace tz; no now-line to anchor either",
     "evidence": "frontend/src/pages/CalendarWorkspacePage.tsx:2125 (`timezone` = browser Intl zone) vs 2144 (`defaultTimezone` = workspace zone used by all grid bucketing/labels); masthead eyebrow prints browser tz at 4118; CalendarSearchPalette and detail panel receive browser `timezone` at 5371/5381; moved-ghost day bucketing uses browser-local isSameDay at 4951-4952 while every other bucket uses tzDayKey(defaultTimezone); the time grid draws no current-time line and always scrolls to fixed 7.5h (528, 2666-2669).",
     "severity": "major",
     "detail": "When browser and workspace zones differ, the eyebrow labels the grid with the wrong zone, search-result times disagree with grid times for the same event, and the undo ghost can render on the wrong day \u2014 quiet fabrication of temporal fact on the one surface whose whole job is time. A now-line is also the cheapest possible orientation signal and is absent."
    }
   ],
   "ideal": "The JARVIS-grade calendar opens with a verdict, not a grid: one Dispatch-voice sentence above the masthead \u2014 \"Quiet week: 14 events, one collision Thursday 15:00, 3 butler proposals waiting, travel calendar stale 26h\" \u2014 where every clause is a door (collision \u2192 radar expanded on the event pair, proposals \u2192 panel with accept/dismiss under j/k+Enter, stale source \u2192 sources row with a re-auth verb). Counts for proposals, duplicates, and conflicts are always fetched and worn as badges; degraded reads say \"conflicts unavailable\" in amber text rather than vanishing; a failed sync source names itself in the masthead the moment it fails. State arrives over the fleet WebSocket \u2014 a butler booking dinner via MCP materializes on the grid in the same frame with a provenance flash, and the audit trail scrolls live. The entire surface is keyboard-native: t/\u2190\u2192 move time, d/w/m/l switch ranges, c creates, q quick-adds, / searches, and every one of those verbs also lives in the global Cmd+K spine so \"find time with Sarah next week\" is reachable from any page. Every fused signal is traversable: a finance-bill overlay chip opens the bill in /spend, a travel ribbon opens the trip, a briefing chip opens its source record \u2014 the trace spine unbroken from a pixel on the grid to the session that put it there. One timezone (the workspace's) governs every label, a now-line anchors the day, and the whole thing is drawn in one hand \u2014 hairline rules, mono eyebrows, three state colors \u2014 including the conflict banner and quick-add chips that today still wear another product's clothes.",
   "moves": [
    {
     "title": "Calendar joins the one command spine + full page keymap",
     "why": "Fixes the critical finding: the fleet's richest workspace is 100% mouse-driven while the registry API (bu-86c4c.7) sits unused one import away. This is the single highest-leverage move on the surface \u2014 every existing feature (create, quick-add, find-time, sync, search, today, range) becomes a one-keystroke verb.",
     "cost": "M",
     "slice_plan": "1) Register page verbs via useCommandMenuActions: Create event, Quick add\u2026, Find time, Sync now, Jump to today, Toggle overlays, Open activity/proposals/duplicates. 2) Add a page-scope keymap (t today, \u2190/\u2192 or n/p range nav, d/w/m/l ranges, c create, q quick-add focus, / opens the ONE Finder). 3) Fold calendar event search into the global Finder (it already federates butlers/sessions/state) or open CalendarSearchPalette from a Finder verb \u2014 kill the mouse-only pill as sole entry. 4) Surface the keymap in the '?' help sheet.",
     "integration_points": "frontend/src/lib/command-registry.tsx (existing API, see ApprovalsPage.tsx usage), frontend/src/components/layout/EntityFinder.tsx, CalendarWorkspacePage.tsx toolbar handlers (updateQuery/openUserCreateDialog/handleSyncAll already exist as callable functions)."
    },
    {
     "title": "Attention masthead: always-on counts, judgment line, and honest degradation",
     "why": "Makes the five-second health check possible: proposals/duplicates/conflicts counts always fetched and badged, a one-sentence Dispatch-voice verdict synthesized from data already on the client (conflict scan, proposal count, source freshness), degraded reads and failed sync sources naming themselves inline instead of hiding in a dialog or rendering as a clean week.",
     "cost": "M",
     "slice_plan": "1) Un-gate useCalendarProposals/useCalendarDuplicates (cheap reads, fetch always; keep panels lazy) and badge the toolbar pills with counts. 2) Enable the conflict scan on all ranges; replace ConflictRadarBanner's `return null` degraded path with an amber 'conflicts unavailable \u2014 <reason>' line. 3) Derive a masthead judgment sentence template (no LLM: n events, first conflict, proposals waiting, worst source staleness) with each clause a click/keystroke target. 4) Lift worst-source sync state into a masthead StateDot + 'source failing' clause linking into the Sources dialog row.",
     "integration_points": "CalendarWorkspacePage.tsx:2313-2401 (query gating), 4110-4180 (masthead), ConflictRadarBanner.tsx:160-163; degraded envelope fields already exist (issues_available, sync_state per bu-5rmhbo convention)."
    },
    {
     "title": "Put calendar on the fleet event bus",
     "why": "\"State arrives as a live event stream\" \u2014 butler-made bookings, sync completions, and audit rows should patch the grid in the same frame instead of a 30\u201360s poll; the bus and declarative registry were built (bu-86c4c.8) precisely so this is one daemon emit + one registry entry.",
     "cost": "M",
     "slice_plan": "1) Backend: emit `calendar` events (mutation applied, sync cursor advanced, proposal created) onto the existing /api/events multiplexed stream from the calendar module's mutation/sync paths. 2) Frontend: add a `calendar` entry to EVENT_CACHE_REGISTRY invalidating/patching workspace, conflicts, proposals, audit query keys. 3) Drop refetchInterval polls to a slow safety net (5m). 4) Provenance flash: reuse the existing flashedEntryId mechanism to flash entries created by butler sessions as they arrive.",
     "integration_points": "frontend/src/hooks/event-cache-registry.ts:148-156, hooks/use-calendar-workspace.ts polling options, backend event stream producer (same path approvals/spend events use), CalendarWorkspacePage.tsx flash logic at 2704-2721."
    },
    {
     "title": "Overlay and briefing drill-down: every fused signal carries a door to its root record",
     "why": "Closes the trace-spine break: the RFC-0020 fusion layer surfaces finance/travel/relationship/health signals on the grid but strands the owner at a static chip; detect-diagnose-act currently dies at diagnose for ~every overlay.",
     "cost": "M",
     "slice_plan": "1) Contract: add an optional `link_ref` (route or {domain, record_id}) to the overlay-contribution shape each butler's precompute job writes; spec delta against calendar-cross-domain-overlays. 2) Populate it in the finance (bill/renewal \u2192 /spend anchor), travel, relationship (\u2192 /entities/{id}), health contribution jobs. 3) FE: OverlayPill and DayBriefingCard chips become real buttons (DisclosureRow-grade a11y) navigating to link_ref when present, falling back to day-jump. 4) Keyboard: Enter on a focused chip follows the door.",
     "integration_points": "lib/calendar-overlays.ts (overlayMetadata), CalendarWorkspacePage.tsx:964-1000 (OverlayPill) and 2692-2699 (handleBriefingSelect), components/calendar/DayBriefingCard.tsx, the per-butler overlay contribution jobs shipped under bu-1ajgg9 (PRs #2624/#2626), openspec calendar-cross-domain-overlays spec."
    },
    {
     "title": "One-hand, one-timezone sweep: Dispatch-native satellites, workspace-tz everywhere, now-line",
     "why": "Erases the last visible seams: the safety-critical conflict banner and quick-add chips in foreign dialect (amber-500/emerald/rounded-full/undefined --surface), two timezones disagreeing on one surface, ISO-8601 text inputs in the quick-add confirm, and a time grid with no 'now'. Together these are why the calendar reads as several hands.",
     "cost": "M",
     "slice_plan": "1) Rewrite ConflictRadarBanner in the page's own Dispatch kit (StateDot + --yellow, PILL_BASE, hairline rules, no emoji; accept = CommitButton). 2) QuickAddBar: PILL_BASE geometry; replace 'ISO 8601' text inputs with datetime-local rendered in workspace tz. 3) Replace var(--surface) uses in AgendaView/PortabilityDialog with defined tokens. 4) Single tz: thread defaultTimezone into masthead eyebrow, search palette, ghost bucketing (tzDayKey); label it once. 5) Draw a now-line in week/day grids and scroll-to-now instead of fixed 7.5h. 6) Swap globalThis.confirm delete for the page's existing undo-ghost pattern.",
     "integration_points": "components/calendar/ConflictRadarBanner.tsx, pages/calendar/QuickAddBar.tsx, CalendarAgendaView.tsx, CalendarPortabilityDialog.tsx, CalendarWorkspacePage.tsx:2125/2144/4118/4951/528, lib/calendar-grid.ts helpers (tzDayKey/minuteOfDayInTz already exist); design authority openspec/specs/dashboard-design-language/spec.md \u00a74c."
    }
   ]
  },
  {
   "page": "health",
   "verdict": "functional",
   "jarvis_gap": "The health suite has real bones \u2014 a Voice briefing with an honesty pill, a genuinely deterministic KPI/trend/adherence backend, QueryBoundary-wrapped trackers that now fail loudly \u2014 but it still behaves as seven loosely-federated filing cabinets rather than one instrument. The landing page cannot reach its own six evidence surfaces except through palette search or transient insights; its vitals are undated, delta-less dead ends; the leading surface of /health/measurements (trend list + chart) and the meals nutrition rail still render outages as calm emptiness, surviving the .2 sweep; the owner must re-specify the same type/time intent up to three times on one page; the most frequent operations in the whole product (log a reading, a dose, a meal) have zero keyboard/palette path while ApprovalsPage and IssuesPage already demonstrate the registration API; and three of six sub-pages still speak pre-Dispatch shadcn (bold h1, Card error chrome) with a semantically inverted green-for-active-illness condition dot. Judgment exists (briefing + insights) but never chains to root evidence \u2014 no signal deep-links to the specific row, and rows carry no provenance (the Measurement API model drops the fact's source metadata), so the trace spine ends at the page boundary.",
   "findings": [
    {
     "title": "NOT-LANDED: Errors still impersonate empty on measurements trend, chart, and nutrition totals",
     "evidence": "frontend/src/components/health/MeasurementChart.tsx:168 destructures only {data, isLoading} from useMeasurements; :253-260 renders 'No trend for {type} in the last {N} days.' whenever buckets.length===0 with trendQuery.isError never read; :343-346 renders 'No {type} readings for this range.' on chart fetch failure. frontend/src/pages/MealsPage.tsx:71-73 destructures only {data, isLoading} from useNutritionSummary, so :117-120 renders 'No nutrition data for this window.' when the endpoint is down.",
     "severity": "critical",
     "detail": "The bu-86c4c.2 state-contract sweep landed QueryBoundary on all six trackers and the overview briefing/KPI/sources, but missed these three queries \u2014 the leading surface of /health/measurements and the meals rail. An API outage on exactly the page whose product is trust-in-the-record still reads as an empty health record. Fix is mechanical: thread isError/refetch into the existing EmptyLine branches (SourceDegradedNote or the trackers' serif 'Couldn't reach the health record \u2014 retry' line)."
    },
    {
     "title": "NOT-LANDED: /health still cannot reach its own six evidence pages",
     "evidence": "frontend/src/pages/HealthOverviewPage.tsx:383-392 \u2014 right column renders only AttentionList (hrefs exist only while insight candidates exist); frontend/src/components/layout/nav-config.ts:88 \u2014 sidebar has a single flat '/health' item with no children; route-registry.ts:61-66 indexes the six sub-pages for palette search only.",
     "severity": "major",
     "detail": "The old dossier's critical finding partially landed: bu-86c4c.7's route-registry makes /health/symptoms etc. palette-searchable, and g-h now targets /health. But from the landing page there is zero visible affordance to measurements/medications/conditions/symptoms/meals/research \u2014 the proposed six-row ledger index (each row a live truth statement + door) never shipped, and the sidebar never grew children. The owner's 'reach any concerning signal in one click' criterion still fails on the butler's own front page."
    },
    {
     "title": "NOT-LANDED: Vitals KPI cells are undated, delta-less dead ends",
     "evidence": "frontend/src/pages/HealthOverviewPage.tsx:286-296 builds cells as {eyebrow, value} only; KpiStrip.tsx:22 has an unused delta?: string slot; LatestMeasurementEntry.measured_at (api/types.ts) is fetched and dropped; cells are plain divs, not links.",
     "severity": "major",
     "detail": "A weight logged four months ago renders identically to this morning's blood pressure under a 32px 'Vitals' framing \u2014 a stale number presented as current on a health surface is a soft fabrication. The data (measured_at, prior reading for delta) is already on the wire. Not covered by bu-9d5vp, which scopes RuntimeSummaryKpi/egress/audit/spend surfaces, not health."
    },
    {
     "title": "No keyboard or palette path for the suite's most frequent actions (log measurement/dose/meal/symptom)",
     "evidence": "grep: useRegisterCommands is consumed by ApprovalsPage.tsx:1411 and IssuesPage.tsx:88 but by zero health pages; GlobalActionsRegistrar.tsx:29-30 registers only 'Trigger {butler}'; MeasurementTracker.tsx:296-298 'Log measurement' is a mouse-only Button opening a Dialog.",
     "severity": "major",
     "detail": "The per-page command registration API from bu-86c4c.7 shipped and has live precedent, yet the highest-frequency write operations in the product (daily logging) require mouse \u2192 button \u2192 dialog on every use. Distinct from in-flight bu-86c4c.15 (inline remedy verbs on signals): this is palette/keyboard reachability of primary page actions. 'Log weight 82' as a palette action with argument capture is the JARVIS-grade version; even bare 'Log measurement\u2026' commands opening the prefilled dialog clears the floor."
    },
    {
     "title": "NOT-LANDED: Condition status colors remain semantically inverted",
     "evidence": "frontend/src/components/health/ConditionTracker.tsx:44-47 \u2014 STATUS_DOT maps active \u2192 'bg-[var(--severity-low)]' (green, comment says 'green \u2014 currently ongoing') and managed \u2192 amber '--severity-medium'; resolved \u2192 neutral.",
     "severity": "major",
     "detail": "An ongoing, possibly unmanaged illness gets the system's healthy green while an under-control condition gets warning amber \u2014 exactly the old dossier's major finding, byte-for-byte unfixed. Breaks one-meaning-per-color (green = healthy everywhere else: StateDot ok, BriefingStatus llm) and the 'state color only when health demands it' rule; every condition row is permanently colored."
    },
    {
     "title": "NOT-LANDED: /health/measurements runs three time scopes and two type vocabularies on one page",
     "evidence": "MeasurementChart.tsx:143-149 trend windows (7/14/30/90d) + :304-338 chart From/To dates + MeasurementTracker.tsx:204-217 the log's own From/To and type select; chart tabs (MeasurementChart.tsx:33-41) include spo2/steps which the log filter (MeasurementTracker.tsx:42-49) and the create form cannot express (roster/health/api/models.py:27 _MEASUREMENT_TYPE Literal omits them).",
     "severity": "major",
     "detail": "Selecting a type or range in the chart changes nothing in the trend list or the log; the owner re-states the same intent up to three times. Connector-ingested spo2/steps readings are chartable but cannot be isolated in the reading log \u2014 the two halves of the page disagree about what a measurement even is."
    },
    {
     "title": "NOT-LANDED: the six sibling pages still speak two dialects",
     "evidence": "ConditionsPage.tsx/SymptomsPage.tsx/ResearchPage.tsx use Page archetype='list', rendering h1 'text-3xl font-bold tracking-tight' (ui/page.tsx:106) with Card-chrome error region (page.tsx error branch uses Card border-destructive) and no mono eyebrow \u2014 vs Eyebrow+Display-500 headers on Measurements/Medications/Meals; eyebrow casing forks ('Health \u00b7 Measurements' MeasurementsPage.tsx:12 vs 'health \u00b7 medications' MedicationsPage.tsx:17); control language forks (shadcn Buttons in MeasurementTracker.tsx:153-169/Condition/Symptom/Research vs RowAction mono-underline in MedicationTracker.tsx:65/MealTracker.tsx:117).",
     "severity": "major",
     "detail": "One butler's suite uses a font-bold display (banned by the Dispatch language's Display-500 rule), two header compositions, two action idioms, and two eyebrow casings. Verified live on main; the .19 orphan purge and .2 sweep did not touch it. Related enforcement work (bu-86c4c.6) is lint tokens, not this structural migration."
    },
    {
     "title": "NOT-LANDED: nutrition rail shows raw window totals; daily_avg still dropped",
     "evidence": "frontend/src/pages/MealsPage.tsx:75-96 renders total_calories/total_protein_g/etc. over a default 30-day window ('Calories 58,400 kcal'); api/types.ts:2274 daily_avg: NutritionDailyAverage is typed and returned by the backend but consumed only by a test fixture (HealthPages.view-only.test.tsx:214).",
     "severity": "major",
     "detail": "The owner's actual question \u2014 am I eating in range per day? \u2014 is answered by daily_avg, which the backend computes and the UI throws away. A 5-figure kcal sum is data theater. Lead with daily_avg vs today-so-far; keep window totals as the secondary line."
    }
   ],
   "ideal": "/health opens with the butler's verdict \u2014 a Voice headline whose every clause is a door (\"Blood pressure drifting up over 14 days\" links to /health/measurements?type=blood_pressure&window=14) \u2014 above a four-cell vitals strip where each number carries its measured-at age and delta and is itself a link, and a six-row living ledger index (measurements / medications / conditions / symptoms / meals / research), each row stating its current truth (\"adherence 86% \u00b7 next dose 21:00\", \"today 1,540 kcal vs 1,820/day avg\", \"last symptom 4d ago \u00b7 severity 6\") and doubling as navigation, mirrored as sidebar children and palette entries. Every sub-page leads with trajectory under a single page-level type+window scope that drives trend, chart, and log together; every row discloses its provenance (manual / butler session / connector) one keystroke from the originating session; every signal \u2014 insight, KPI, condition \u2014 deep-links to pre-filtered evidence with the query applied. Logging is keyboard-native: \"log weight 82.4\" from the palette applies optimistically in-frame. Failure is unmistakable everywhere \u2014 a named degraded line with retry, never an empty record \u2014 and green never decorates an active illness. The page answers \"am I okay, and what changed?\" in one glance, and reaches the exact rows behind that answer in two keystrokes.",
   "moves": [
    {
     "title": "Finish the state contract: isError on measurements trend/chart and nutrition totals",
     "why": "The three surviving error-impersonates-empty branches sit on the suite's leading surfaces; an outage currently renders as an empty health record \u2014 the exact failure the manifesto and .2 sweep exist to prevent.",
     "cost": "S",
     "slice_plan": "One PR: destructure isError/refetch from useMeasurementTrend and useMeasurements in MeasurementChart.tsx and useNutritionSummary in MealsPage.tsx; replace the three EmptyLine/Voice empty branches with the trackers' existing degraded pattern (SourceDegradedNote + retry). Add the three error-state cases to HealthPages.view-only.test.tsx.",
     "integration_points": "frontend/src/components/health/MeasurementChart.tsx:168,253-260,343-346; frontend/src/pages/MealsPage.tsx:71-73,117-120; components/ui/query-boundary.tsx (SourceDegradedNote)"
    },
    {
     "title": "Ship the ledger index and health palette verbs",
     "why": "Closes both discoverability findings at once: the Overview finally reaches its own evidence pages, and daily logging gains a keyboard path using the already-shipped useRegisterCommands API (precedent: ApprovalsPage, IssuesPage).",
     "cost": "M",
     "slice_plan": "Slice 1: right-column six-row index on /health \u2014 each row eyebrow + live one-line truth (reuse useMeasurementsLatest, adherence, useConditions, useSymptoms, useNutritionSummary, useResearch counts) + RowLink; mirror as nav-config children. Slice 2: useRegisterCommands on each health page ('Log measurement', 'Log dose', 'Log meal', 'Log symptom', 'Add condition', 'Add research note') opening the existing dialogs; register the six from /health too, scoped.",
     "integration_points": "HealthOverviewPage.tsx:383-392; nav-config.ts:88; lib/command-registry.tsx:104; the six pages' existing form dialogs"
    },
    {
     "title": "Make vitals doors: age + delta + link on every KPI cell",
     "why": "Undated stale numbers under a 'Vitals' heading are the closest thing to fabrication left on this surface; the delta slot, measured_at field, and target routes all already exist.",
     "cost": "S",
     "slice_plan": "One PR: compute age string from LatestMeasurementEntry.measured_at and delta vs previous reading (extend /measurements/latest to return prev value, or derive from trend endpoint); pass into KpiStrip delta slot; wrap each cell in a Link to /health/measurements?type=X; support the type query param on MeasurementsPage (feeds move 4's unified scope).",
     "integration_points": "HealthOverviewPage.tsx:286-296; components/overview/KpiStrip.tsx:22; roster/health/api/router.py /measurements/latest; MeasurementsPage.tsx query-param plumbing"
    },
    {
     "title": "One scope on /health/measurements",
     "why": "The owner re-specifies the same intent three times on one page and cannot isolate connector-ingested spo2/steps in the log \u2014 the page's two halves disagree about the type vocabulary.",
     "cost": "M",
     "slice_plan": "Lift a single {type, window|since/until} scope to MeasurementsPage state (URL-persisted); drive trend query, chart params, and tracker params from it; delete the duplicate filter toolbars; unify the type list from one constant including spo2/steps (read-only for create \u2014 keep the backend Literal, disable those options in MeasurementForm with 'connector-sourced' hint).",
     "integration_points": "MeasurementsPage.tsx; MeasurementChart.tsx:143-149,304-338 (remove local scopes, accept props); MeasurementTracker.tsx:204-217,42-49; roster/health/api/models.py:27 stays as-is"
    },
    {
     "title": "One dialect for six siblings + honest condition colors + daily_avg lead",
     "why": "Three pages still render bold-h1 shadcn chrome inside a Dispatch butler suite, green decorates active illness, and the nutrition rail hides the one number that answers the owner's question \u2014 all consistency debts on the same six files, cheapest fixed together.",
     "cost": "M",
     "slice_plan": "Slice 1: migrate Conditions/Symptoms/Research off Page archetype='list' onto the Eyebrow+Display+Voice header (normalize eyebrow casing to one form across all six); converge all trackers on the RowAction mono idiom. Slice 2: ConditionTracker STATUS_DOT \u2014 neutral for managed/resolved, attention color only for active; fix aria labels. Slice 3: DailyTotals leads with daily_avg per macro ('1,820 kcal/day avg'), window totals demoted to the footer line.",
     "integration_points": "ConditionsPage/SymptomsPage/ResearchPage.tsx; ConditionTracker.tsx:44-47; MeasurementTracker/SymptomTracker/ResearchTracker action rows; MealsPage.tsx:75-96 + api/types.ts:2274 daily_avg"
    }
   ]
  },
  {
   "findings": [
    {
     "title": "Live-stream MTD double-counts against the polled baseline \u2014 the headline number inflates over time",
     "evidence": "frontend/src/pages/SpendPage.tsx:1243-1260 adds the monotonic `streamedCostUsd` (never reset; frontend/src/hooks/use-spend-stream.ts:98) on top of `forecast.mtd_usd`, while the forecast query refetches every 120s (SpendPage.tsx:1235) and each refetch's server-computed MTD already includes those same streamed calls.",
     "severity": "critical",
     "detail": "Every 2-minute forecast poll re-absorbs events the counter still holds, so a long-lived tab overstates MTD by the cumulative streamed spend since mount; projected EOM is derived from the inflated MTD (SpendPage.tsx:1254) so the red over-ceiling alert (1281-1335) can fire on a fabricated number. The comment's claim 'No subtraction needed' is only true until the first baseline refetch. Fix: reset the counter when the forecast query resolves, or have the server return an `as_of` timestamp and count only events newer than it. Related: the hook's `status` is never consumed anywhere (only `streamedCostUsd` is destructured at 1243), so when the WS is closed the 'live' page silently degrades to 2-minute-stale numbers with no indicator \u2014 violates 'every degraded source names itself'."
    },
    {
     "title": "NOT-LANDED: errors impersonate empty in four /spend sections despite the shipped QueryBoundary primitive",
     "evidence": "frontend/src/pages/SpendPage.tsx:1232-1237 (forecast query destructures only isLoading; error renders 'No forecast data is available yet.' at 1368-1371 and the KPI strip silently vanishes), 600-604\u2192637-640 (breakdown error \u2192 'No spend has been recorded yet.'), 1152-1156\u2192896-900 (rules error \u2192 'No routing rules are configured'), 1303-1312 (movers summaries, no isError). QueryBoundary exists exactly for this (frontend/src/components/ui/query-boundary.tsx:7-21, bu-86c4c.2) and is used by ApprovalsPage/HealthOverviewPage but nowhere on SpendPage.",
     "severity": "critical",
     "detail": "Old-dossier critical ('no isError on useSpendSummary') reproduces on the brand-new merged page. Worst case is rules: a failed fetch renders the calm config statement 'No routing rules are configured; rules evaluate top-to-bottom\u2026', telling the owner their spend caps are gone when the fetch merely failed \u2014 and the backend compounds it by returning HTTP 200 with [] when the DB manager is absent (src/butlers/api/routers/spend.py:1269-1270). Only TopSessions/BySchedule handle isError, and even those render a bare muted sentence with no retry or role=alert."
    },
    {
     "title": "Movers strip fabricates '+$X \u00b7 new' deltas when one of its two summary queries fails",
     "evidence": "frontend/src/pages/SpendPage.tsx:1377-1381 passes `currentSummary?.data?.by_butler ?? {}` and `priorSummary?.data?.by_butler ?? {}` with no isError; computeMovers (476-489) treats a missing prior map as prior=0 and MoverChip (511) labels it '\u00b7 new'.",
     "severity": "critical",
     "detail": "Asymmetric failure \u2014 prior-window query errors while current succeeds \u2014 renders every butler as a red positive mover marked 'new', a synthesized ranked signal built from a failed fetch (sin #1, fabrication, not mere empty-conflation). Symmetric failure renders the serif 'No spend change vs the prior window.' The movers strip must render only when both windows resolved, and name the degraded window otherwise."
    },
    {
     "title": "NOT-LANDED: backend zero-fills unreachable butlers into summary/breakdown totals with no degraded marker in the envelope",
     "evidence": "src/butlers/api/routers/spend.py:430 (`return (info.name, 0.0, 0, 0, 0, {})` on ButlerUnreachableError/timeout); get_cost_summary and get_spend_breakdown (spend.py:980-1035) fold these zeros into totals; the response envelope carries no `aggregates_available`/unavailable-butlers field, unlike the repo's own ingestion degraded-mode convention (CLAUDE.md API Conventions).",
     "severity": "major",
     "detail": "Old dossier critical #3 lineage (per-butler failures return zeros) survives the .11 merge on the DB-fallback path: when the DB path is down and a butler MCP is unreachable, MTD, breakdown bars, and movers all silently under-report with no way for the frontend to say 'health butler's spend is missing from these totals'. On a money surface, an under-count is indistinguishable from savings."
    },
    {
     "title": "Butler identity hue broken on the spend chart \u2014 same butler wears different colors on /spend vs dashboard, and reshuffles per window",
     "evidence": "frontend/src/components/costs/CostStripeChart.tsx:206-212 fills each butler stack with `chartColor(i)` where i is the cost-descending rank (collectButlers, 49-59), while SessionStripeChart uses the canonical `butlerHueVar(name)` (frontend/src/components/dashboard/SessionStripeChart.tsx:198) per the ButlerMark doctrine (frontend/src/components/ui/ButlerMark.tsx:14-17: butler hue as identity).",
     "severity": "major",
     "detail": "Visual-dialect drift: finance is one color on the dashboard session chart and another on the spend chart, and its spend-chart color changes whenever the ranking changes with the TimeWindowPicker \u2014 the exact 'color carries one meaning everywhere' violation the chart-color fix (.5) and ButlerMark were built to end. Movers/TopSessions/BySchedule rows also render bare mono names with no ButlerMark, unlike every other butler-listing surface."
    },
    {
     "title": "Money controls have no keyboard surface: drag-only rule reorder, Enter-dead ceiling edit, zero palette verbs registered",
     "evidence": "frontend/src/pages/SpendPage.tsx:917-924 (rules reorder via draggable/onDragStart/onDrop only \u2014 no keyboard alternative), 429-460 (CeilingEdit is a div, not a form; the number input has no onKeyDown, so Enter does nothing and Escape doesn't cancel), and SpendPage never calls useRegisterCommands (only ApprovalsPage, IssuesPage, GlobalActionsRegistrar do) so the cmdk spine has no 'Set ceiling' / 'Add routing rule' verbs.",
     "severity": "major",
     "detail": "Sin #4 on the surface that governs money. bu-f310e covers DisclosureRow/light-red elsewhere and bu-5o22a is a keydown-guard extraction \u2014 neither covers these. The header comment claims 'noticing a spike and capping the schedule that caused it is one continuous motion', but that motion requires a mouse at every step."
    },
    {
     "title": "projection_confidence is computed, fetched, typed \u2014 and never rendered; low-confidence projections look identical to firm ones",
     "evidence": "src/butlers/api/routers/spend.py:1049,1163 computes projection_confidence ('low'|'normal'); frontend/src/pages/SpendPage.tsx:80 types it on ForecastData; no other reference in SpendPage \u2014 the Projected EOM KPI (229-235), forecast chart, and the red over-ceiling alert (1318-1335) all render regardless of confidence.",
     "severity": "major",
     "detail": "Early in the month a 2-day linear extrapolation can trip the page's only role=alert red banner while the system itself has flagged the number as low-confidence. 'No number is ever fabricated' includes not presenting a known-shaky projection with the same weight as a firm one \u2014 the KPI sub-line and alert copy should carry the confidence ('projected $87, low confidence \u2014 2 days elapsed')."
    },
    {
     "title": "Deleting a routing rule is instant, unconfirmed, and un-undoable \u2014 one mis-click silently lifts a spend cap",
     "evidence": "frontend/src/pages/SpendPage.tsx:936-944 ('Remove' button onClick fires deleteMutation.mutate(id) directly; mutation at 1158-1165 has no confirm step and success toast offers no undo).",
     "severity": "major",
     "detail": "The rule's full definition (condition/action/position) is in hand at click time, so an undo toast that re-creates it at the same position is cheap; alternatively an explicit confirm. Design-bar recovery criterion: destructive actions are undoable or confirmed \u2014 this is neither, on the control that enforces max_cost_per_call caps."
    }
   ],
   "ideal": "The ideal /spend opens with one deterministic Dispatch verdict sentence \u2014 \"On pace: $41.20 through 12 days against a $100 ceiling, projected $87 with normal confidence; finance (+$6.10) drove the week\" \u2014 where every clause is a door (ceiling \u2192 inline edit, mover \u2192 butler spend tab, confidence \u2192 forecast chart), followed by a posture strip whose MTD ticks live from a WS stream that is provably single-counted (server as_of anchor), wears a visible LIVE/RECONNECTING eyebrow, and whose fourth cell is today's burn rather than calendar trivia. Every aggregate names its gaps (\"health unreachable \u2014 totals exclude it\") via a degraded envelope, every section renders through the three-way QueryBoundary contract so a dead backend can never impersonate $0, and the movers strip only speaks when both comparison windows resolved. Butlers wear their canonical identity hue and letter-mark in the stacked chart, movers, sessions, and schedules \u2014 the same color as everywhere else in the console. Governance is one continuous keyboard-first motion: a schedule row's \"cap this\" prefills the rule form, rules reorder with Alt+arrows, ceiling saves on Enter, rule deletion undoes from the toast, and \"Set ceiling\"/\"Add routing rule\" live in the cmdk spine; saved_7d receipts stay on each rule so the owner sees the money their judgment saved.",
   "jarvis_gap": "The .11 merge genuinely landed: one nav-visible surface with the right posture\u2192what-changed\u2192why\u2192controls spine, an honest per-butler-per-day chart (real by_butler data, no re-fabrication), real saved_7d receipts computed by a scheduled job, and live drill-downs to butler spend tabs and sessions. What separates it from JARVIS-grade is truth discipline on the numbers themselves: the live MTD double-counts against its own polling baseline, four sections still let errors impersonate empty despite the epic having shipped QueryBoundary for exactly this, movers can fabricate a ranked signal from a failed fetch, unreachable butlers zero-fill totals with no degraded marker, and a computed confidence rating is thrown away before render. Add that the money controls are mouse-only and butlers lose their identity hue on this one chart, and the surface reads as a well-structured report rather than a trustworthy instrument \u2014 the structure of earned calm without its proof obligations.",
   "moves": [
    {
     "title": "Truth pass: QueryBoundary everywhere + degraded envelope + both-windows-or-silent movers",
     "why": "Three of the four criticals are the same root failure \u2014 /spend presents unproven numbers as calm truth. A money surface that can render 'no rules configured' for a failed fetch or invent '+$X new' movers from a half-failed comparison forfeits the console's core property.",
     "cost": "M",
     "slice_plan": "1) Wrap forecast/KPI, breakdown, rules, movers in QueryBoundary with retry + sourceLabel; movers renders only when both window queries resolved, else names the failed window. 2) Backend: add `unavailable_butlers: []` (or aggregates_available) to /spend/summary and /spend/breakdown envelopes instead of silent zero-fill at spend.py:430; 404/503 instead of 200-[] when db is None on /spend/rules. 3) Frontend renders the gap inline: 'totals exclude health (unreachable)'.",
     "integration_points": "frontend/src/pages/SpendPage.tsx (all four sections), frontend/src/components/ui/query-boundary.tsx, src/butlers/api/routers/spend.py (get_cost_summary, get_spend_breakdown, list_spend_rules), frontend/src/hooks/use-spend.ts"
    },
    {
     "title": "Fix the live layer: single-counted MTD + visible stream state",
     "why": "The headline number on the page drifts upward the longer the tab lives, and the page never admits when its live channel is down \u2014 both directly violate 'no number is ever fabricated / every degraded source names itself'.",
     "cost": "S",
     "slice_plan": "1) Server returns `as_of` (or the forecast query's resolution resets the client counter): count only WS call events newer than the latest baseline. 2) Consume `status` from useSpendStream and render a mono eyebrow next to MTD \u2014 LIVE dot when open, amber 'STREAM RECONNECTING \u00b7 data as of HH:MM' when closed. 3) Regression test: streamed events + forecast refetch must not double-count.",
     "integration_points": "frontend/src/pages/SpendPage.tsx:1243-1260, frontend/src/hooks/use-spend-stream.ts, src/butlers/api/routers/spend.py (forecast endpoint as_of)"
    },
    {
     "title": "Spend verdict line + honest confidence + today-burn KPI",
     "why": "The judgment layer (.17) stopped at the monitoring core; /spend still opens with counters, not judgment, discards its own projection_confidence, and spends a KPI slot on 'Days in Month' while the header comment promises a live today-burn that doesn't exist.",
     "cost": "M",
     "slice_plan": "1) Deterministic serif verdict composed from data already fetched (forecast + movers): pace vs ceiling, confidence, top mover \u2014 each clause a link (ceiling\u2192CeilingEdit, mover\u2192butler tab). 2) Thread projection_confidence into the Projected-EOM sub-line and gate/soften the red over-ceiling alert copy when 'low'. 3) Replace the Days-in-Month cell with Today burn fed by the (fixed) stream. No LLM cost \u2014 pure composition, same pattern as SystemVerdictBanner.",
     "integration_points": "frontend/src/pages/SpendPage.tsx (KpiStrip, alert row, new verdict component), pattern reference frontend/src/components/system/SystemVerdictBanner.tsx, openspec/specs/dashboard-design-language/spec.md (system voice scenarios)"
    },
    {
     "title": "Butler identity dialect on all spend surfaces",
     "why": "One instrument, one hand: the same butler must be the same color everywhere. The spend chart's rank-indexed palette breaks cross-surface identity and even self-consistency across time windows.",
     "cost": "S",
     "slice_plan": "1) CostStripeChart: fill stacks and legend swatches with butlerHueVar(name) (matching SessionStripeChart), keeping chartColor() only for the single-total fallback bar. 2) Add ButlerMark to MoverChip, TopSessions butler cells, BySchedule butler cells. 3) Verify tooltip swatches inherit the identity hue.",
     "integration_points": "frontend/src/components/costs/CostStripeChart.tsx:206-212, frontend/src/components/ui/ButlerMark.tsx (butlerHueVar), frontend/src/pages/SpendPage.tsx (MoverChip, tables)"
    },
    {
     "title": "Keyboard-complete money controls with recoverable deletes",
     "why": "Clearing the queue 'in under a minute without touching the mouse' is impossible on /spend today: rules reorder only by drag, ceiling ignores Enter, rule delete is irreversible, and the cmdk spine has no spend verbs.",
     "cost": "M",
     "slice_plan": "1) useRegisterCommands on SpendPage: 'Set monthly ceiling', 'Add routing rule', window presets. 2) CeilingEdit becomes a form (Enter saves, Escape cancels). 3) Rules rows focusable; Alt+\u2191/\u2193 reorders (same PUT position path as drag); delete gets an undo toast that re-creates the rule at its old position from the in-hand definition. 4) e2e: create\u2192reorder\u2192delete\u2192undo entirely via keyboard.",
     "integration_points": "frontend/src/pages/SpendPage.tsx (CeilingEdit, RulesTable, SpendRulesSection), frontend/src/lib/command-registry.tsx, existing keyboard patterns in frontend/src/pages/ApprovalsPage.tsx (bu-86c4c.14) and shared guard follow-up bu-5o22a"
    }
   ],
   "page": "spend",
   "verdict": "functional"
  },
  {
   "page": "memory",
   "verdict": "solid",
   "jarvis_gap": "The house-ledger redesign gives /memory an honest templated voice, a deep-linked attention rail, and a real provenance spine (fact \u2194 episode \u2194 session), but the surface still lies quietly at the aggregation layer (every fleet-wide number silently deflates when a pool fails, with HTTP 200 and no marker), its remedy loop measures the fleet but repairs one butler's pool, its search orders by recency instead of relevance and truncates with counterfeit counts, and its most prominent numerals \u2014 the KPI strip, the pipeline band, the red dead-letters figure, every recent-activity row \u2014 are prose without doors.",
   "findings": [
    {
     "title": "Memory fan-out swallows per-pool failures \u2014 every fleet-wide number silently deflates with no degradation marker",
     "evidence": "src/butlers/api/routers/memory.py:118-131 \u2014 `_run` catches bare `Exception` and returns None (logged at DEBUG as 'pool lacks memory tables or query failed'), conflating 'butler has no memory module' with 'query errored'. Every /api/memory endpoint (stats at :182, facts/rules/episodes lists, inspect at :2150, reembed/pending at :2205) sums the surviving pools and returns HTTP 200 with no pools_failed/sources_unavailable field, unlike the repo's aggregates_available convention.",
     "severity": "major",
     "detail": "If one butler's pool times out, 'Active facts 3,182' becomes a smaller confident number and the Voice sentence narrates it as truth. This is invisible to the frontend even after bu-mkd5r threads isError \u2014 the request succeeds. Violates 'no number is ever fabricated; every degraded source names itself'. NEW signal: bu-mkd5r covers only frontend isError threading, not partial-truth 200s."
    },
    {
     "title": "Re-embed act loop measures the fleet but remedies one pool \u2014 and its dry run can POST an empty butler",
     "evidence": "frontend/src/components/memory/HousekeepingBand.tsx:346-355 \u2014 comment claims the run 'targets the same pool the counts were measured on', but the counts come from useReembedPending() with NO butler (use-memory-reembed.ts:21-27 \u2192 memory.py:2205-2214 fans out and sums ALL pools) while the POST targets only the alphabetically-first butler (HousekeepingBand.tsx:352-354, 419-420; memory.py:2252-2254 `db.pool(body.butler)` single pool). handleDryRun (line 392) posts `butler: defaultButler ?? \"\"` \u2192 404 when the butlers list hasn't loaded.",
     "severity": "major",
     "detail": "Owner sees 'AttentionRail: 137 rows on an old embedding' (fleet-wide), clicks re-embed, one pool is fixed, completion line says 're-embedded 12 rows', the rail count barely moves \u2014 an act loop that cannot complete. Fix: per-pool drift breakdown + per-pool (or fleet-wide) run endpoint; delete the stale comment. Not covered by bu-6t8ix (retry-consolidation/retire-rule) \u2014 this verb EXISTS and mis-aims."
    },
    {
     "title": "Memory search orders by recency not relevance, truncates per-kind, and renders counterfeit group counts",
     "evidence": "src/butlers/api/routers/memory.py:2013 (`row_limit = offset + limit` per kind per pool), :2036-2041/:2070-2074/:2104-2108 (all three tiers `ORDER BY created_at DESC LIMIT` \u2014 plainto_tsquery match with no ts_rank), :2132-2133 (`total = len(merged)` \u2014 bounded by row_limit, not a true total). frontend/src/components/memory/SearchResults.tsx:44 (SEARCH_LIMIT=50, no pagination in results mode), :62-68 + :81/:94/:107 (GroupHeader count = truncated group.length, rendered as 'FACTS \u00b7 50' as if complete), :141 (meta.total ignored).",
     "severity": "major",
     "detail": "A query matching 400 facts shows the 50 most RECENT matches labelled 'FACTS \u00b7 50'; the best-matching older fact is unreachable by any keystroke. The page's one search affordance \u2014 the primary signal\u2192evidence path \u2014 cannot be trusted to find things, and its counts are fabricated-by-truncation."
    },
    {
     "title": "Overture KPI cells and pipeline numerals are inert \u2014 including the red dead-letters figure",
     "evidence": "frontend/src/components/memory/MemoryOverture.tsx:70-93 (KpiStrip: Pending / Active facts / Proven rules / Last write-up are plain <Mega> spans, no links), :115-151 (PipelineBand: episodes/pending/facts/fading/rules/proven are plain PipeStat spans), :140-147 (the `dead letters N` fragment turns --red when N>0 but is a non-interactive span). Exact deep-links already exist for each: /memory?register=episodes&status=pending|dead_letter, ?validity=fading, ?register=rules&maturity=proven (use-memory-url-state.ts vocabulary; AttentionRail uses them at :201, :227).",
     "severity": "major",
     "detail": "The band that answers 'is remembering working' offers zero doors; the only route from the red dead-letters signal to evidence is scrolling to the rail below. NEW: bu-9d5vp's 'inert KPI cells' is butler-detail RuntimeSummaryKpi scoped, and unlike those, these cells HAVE existing routes to link to."
    },
    {
     "title": "Recent-activity rows are drill-down dead ends by design",
     "evidence": "frontend/src/components/memory/AttentionRail.tsx:15-17 (header comment: 'No color, no severity, no links.') and :127-149 \u2014 ActivityRow renders mono time \u00b7 ButlerMark(tone=neutral) \u00b7 truncated summary with no anchor, though useMemoryActivity items carry `type` and `id` (keyed at :285 as `${item.type}-${item.id}`) that map directly onto /memory/facts/:id, /memory/rules/:id, /memory/episodes/:id.",
     "severity": "major",
     "detail": "Twenty live signals of what the house just learned, none of them a door \u2014 the row's own key proves the route exists. The graduated redesign spec mandated linkless quiet rows, but backward compatibility is waived and the north star ('every clause is a door') supersedes; the fix is a whole-row link identical to LedgerRow's pattern."
    },
    {
     "title": "/memory contributes zero verbs to the command palette",
     "evidence": "frontend/src/lib/command-registry.tsx:104 exposes useRegisterCommands for page-scoped verbs; repo-wide, only src/pages/ApprovalsPage.tsx and src/pages/IssuesPage.tsx call it (rg over src/). No memory page or component registers commands; GlobalActionsRegistrar.tsx contains no memory entries. Only the nav chord 'm' exists (nav-config.ts:75).",
     "severity": "minor",
     "detail": "The one-registry spine (bu-86c4c.7) landed, but memory's frequent paths \u2014 focus search, switch register, filter validity/maturity/status, jump to housekeeping, Confirm/Retract on a fact detail, dry-run re-embed \u2014 are mouse-or-scroll only. `/` focuses search (MemorySearch.tsx:82-95) but is undiscoverable from the palette."
    },
    {
     "title": "'N important facts fading' rail action drops its own predicate on click",
     "evidence": "frontend/src/components/memory/AttentionRail.tsx:234-243 \u2014 the condition counts validity=fading \u2227 importance_min\u22658 (useFacts at :173-178), but the action links to /memory?register=facts&validity=fading, and use-memory-url-state.ts supports no importance param, so the target lists ALL fading facts with no way to isolate the N high-importance ones (FactsRegister rows don't display importance either).",
     "severity": "minor",
     "detail": "The signal names a precise set; the door opens onto a superset with no marker. Add an `importance_min` URL param to the ledger state + a corresponding filter chip so the drill-down preserves the full predicate (same class as the bu-86c4c.4 'full predicates' rule, new instance)."
    },
    {
     "title": "The facts ledger hides authorship: no butler mark and no butler filter on the mixed-pool register",
     "evidence": "frontend/src/components/memory/FactsRegister.tsx:93-153 \u2014 LedgerRow renders subject\u00b7predicate / content / belief with no source-butler mark, though facts are fanned out from 9 per-butler schemas and Fact.source_butler is in the payload; EpisodeRow DOES carry ButlerMark (EpisodesRegister.tsx:169-172, 'the sole carrier of butler hue'). MemoryBrowser.tsx:51-54 accepts a butlerScope prop but MemoryPage.tsx:21 never passes one, and no butler filter exists in use-memory-url-state.ts.",
     "severity": "minor",
     "detail": "Butler hue is the system's identity axis (Dispatch), yet the owner cannot see or ask WHOSE belief a ledger row is without clicking into the detail page \u2014 'Wei \u00b7 favorite_coffee' could be the relationship butler's or the health butler's fact. Add a quiet source-butler mark to LedgerRow and a `butler` URL filter shared by all three registers."
    }
   ],
   "ideal": "The ideal /memory is the house's epistemology console: it opens with the templated Voice verdict it already has, but every numeral in the KPI strip and pipeline band is a door into a pre-filtered register, and the verdict is provably complete \u2014 any butler pool that failed to answer names itself in a degraded note rather than silently vanishing from the sums. The one search affordance is relevance-ranked with true per-kind totals and pagination, so the owner is genuinely one `/`-keystroke and one Enter from any belief the house holds; every activity row, fact row, and attention condition carries its full predicate through the click, and the provenance spine (episode \u2192 fact \u2192 superseding fact \u2192 session trace) that the detail pages already run stays unbroken from the overture down. Remedies aim where the measurement was taken: re-embed shows per-butler drift and repairs the pools it counted, dead-lettered episodes retry inline (bu-6t8ix), and Confirm/Retract, register switches, and filters all live in the single command palette. Each ledger row wears its author's hue, the register can be leaned to one butler's lane, and a fully healthy page still renders zero state-color pixels above the fold \u2014 earned calm, now earned all the way down to the aggregation layer.",
   "moves": [
    {
     "title": "Honest fan-out: partial-source accounting on every /api/memory aggregate",
     "why": "The surface's foundational promise \u2014 'no number is ever fabricated' \u2014 is currently unenforceable: memory.py's _fan_out_memory_queries swallows pool failures (118-131) so stats, registers, search, and reembed counts return confident 200s that undercount the fleet; no frontend sweep (bu-mkd5r) can detect it.",
     "cost": "M",
     "integration_points": "src/butlers/api/routers/memory.py (_fan_out_memory_queries \u2192 return per-pool outcome; distinguish 'no memory schema' from 'query raised'), PaginationMeta/ApiResponse envelope (add pools_failed: [butler...] following the aggregates_available convention), MemoryOverture.tsx + AttentionRail.tsx (render a named degraded note when pools_failed is non-empty).",
     "slice_plan": "1) Backend: _run returns (name, result|error-marker); endpoints thread pools_failed into meta. 2) Frontend: one shared <SourceDegradedNote pools={...}> on overture + rail. 3) Tests pinning that a raising pool yields pools_failed, not a silent smaller sum."
    },
    {
     "title": "Every numeral a door: link the overture, pipeline, activity rows, and full-predicate fading filter",
     "why": "Four KPI cells, seven pipeline stats, the red dead-letters figure, and twenty activity rows are prose without doors even though every target route already exists \u2014 the cheapest, highest-visibility drill-down win left on this surface.",
     "cost": "S",
     "integration_points": "MemoryOverture.tsx (wrap KpiCell/PipeStat/dead-letters in react-router Links to /memory?register=\u2026&status=\u2026/validity=\u2026/maturity=\u2026), AttentionRail.tsx ActivityRow (whole-row link keyed off item.type\u2192route, ButlerMark \u2192 butler console), use-memory-url-state.ts + FactsRegister.tsx (new importance_min param + chip so the 'important facts fading' action keeps its predicate).",
     "slice_plan": "1) Overture/pipeline links. 2) Activity-row links. 3) importance_min URL param + rail action update. Each slice independently shippable and testable with existing register deep-link tests as templates."
    },
    {
     "title": "Search that finds: relevance ranking, true totals, and paginated results mode",
     "why": "The page's primary signal\u2192evidence path orders by created_at instead of ts_rank and truncates at 50 with counterfeit 'FACTS \u00b7 50' headers \u2014 the owner cannot reach an older best match at all, which breaks the five-second-to-evidence bar at its root.",
     "cost": "M",
     "integration_points": "src/butlers/api/routers/memory.py inspect_memory (ts_rank(search_vector, plainto_tsquery) ORDER BY rank DESC, per-kind COUNT(*) for true totals in meta), SearchResults.tsx (group headers from true totals, 'showing X of N' + next page), use-memory-url-state.ts (offset already exists \u2014 wire it in results mode), MemorySearch.tsx kind pills (show counts).",
     "slice_plan": "1) Backend rank + true totals (contract-tested against testcontainers suite \u2014 inspect touches bare-table search_vector SQL). 2) Frontend honest headers + pagination. 3) Kind-pill counts."
    },
    {
     "title": "Aim the remedy where the measurement was: per-pool re-embed",
     "why": "The only maintenance verb on the page measures fleet-wide drift but repairs one alphabetically-chosen pool while a stale comment claims otherwise, and its dry run can POST butler:\"\" \u2014 the act loop literally cannot drive the count to zero.",
     "cost": "M",
     "integration_points": "memory.py get_reembed_pending (return per-butler breakdown, not just summed tiers), run_reembed (accept butler:null \u2192 iterate all memory pools, or a new /reembed/all), use-memory-reembed.ts, HousekeepingBand.tsx Embeddings (per-butler drift lines, one run verb per stale pool or one fleet verb; guard the dry-run until butlers load; delete the false comment).",
     "slice_plan": "1) Backend per-pool pending breakdown + fleet run. 2) Frontend drift lines + fixed verb targeting. 3) Rail 'stale embeddings' row names the worst pool in its detail sentence."
    },
    {
     "title": "Memory verbs in the one palette + butler-lane lens on the ledger",
     "why": "The single command spine exists but /memory contributes nothing to it, and the ledger anonymizes its authors \u2014 the two remaining gaps between this surface and 'one instrument, keyboard-first, butler hue as identity'.",
     "cost": "S",
     "integration_points": "command-registry.tsx useRegisterCommands from MemoryPage (focus search, switch register, cycle validity/maturity/status, jump to housekeeping) and FactDetailPage (Confirm / Retract with the same arm semantics), FactsRegister.tsx LedgerRow (quiet source-butler ButlerMark matching EpisodeRow), use-memory-url-state.ts + all three registers (shared `butler` filter param; MemoryBrowser already plumbs butlerScope).",
     "slice_plan": "1) Page + detail palette commands. 2) LedgerRow butler mark. 3) butler URL filter + pills. Keep zero new state colors; marks stay tone=neutral per MEMORY_LANGUAGE \u00a76."
    }
   ]
  },
  {
   "findings": [
    {
     "title": "Owner-plex ranking failure fabricates a calm household: zero capacity meters + \"No one is owed a call.\"",
     "evidence": "PlexPage.tsx:2097-2104 renders AttentionRail whenever isOwnerMode, outside the isError guard. On rankingError, rankingLoading=false so tierCounts=layoutOwnerPlex([],null) all-zeros (PlexPage.tsx:1431-1434) \u2192 capacity meters render 0/5\u20260/500 (1308-1352), and attention=[] with attentionLoading=false renders the serif empty \"No one is owed a call.\" (1262-1266). Meanwhile the canvas shows \"Failed to load the graph.\" with no retry and no source named (1967-1974).",
     "severity": "critical",
     "detail": "The two systemic sins at once: fabricated zeros and an error impersonating an empty queue, on the flagship landing surface. The flanks must key off the same three-way state contract as the canvas (loading/error/data), the error placard must name the degraded source (dunbar ranking endpoint) and carry a Retry, and no meter may render a number derived from a failed fetch."
    },
    {
     "title": "Halo fetch error is silently indistinguishable from \"no organizations/places exist\"",
     "evidence": "PlexPage.tsx:1437 destructures only `data` from usePlexHalo(isOwnerMode); use-entities.ts:203-210 exposes isError but nothing reads it. On a 4xx/5xx (including the endpoint's own owner_required 403, router.py:3783-3785) haloLayout stays null and OwnerPlexCanvas renders exactly the no-satellites layout (halo=null branch, PlexPage.tsx:408).",
     "severity": "major",
     "detail": "The owner cannot tell a degraded dimension halo from a genuinely person-only graph. Minimum fix: a quiet mono placard on the halo band (\"organizations \u00b7 unavailable \u00b7 retry\") when the query errors."
    },
    {
     "title": "Neighbour-mode dossier: failed facts/dates fetches impersonate empty (\"Nothing recorded yet.\")",
     "evidence": "PlexPage.tsx:1103-1106 destructures only data/isLoading from useEntityFacts and only data from useEntityCoreDates. On error, facts=[] hits the !factsLoading && facts.length===0 branch \u2192 \"Nothing recorded yet.\" (1199-1203); a dates error silently removes the Dates section (1169).",
     "severity": "major",
     "detail": "Same class as the flank finding but on the drill-down surface: the dossier is the evidence pane of the hop spine, so a lying empty here corrupts the owner's read of a specific person. Both queries need isError \u2192 named-source error rows with retry."
    },
    {
     "title": "The \"Worth attention\" verdict is fabricated in the view layer with magic constants, not by the relationship butler",
     "evidence": "PlexPage.tsx:1387-1403 hardcodes TIER_CADENCE_DAYS and TIER_URGENCY_WEIGHT in the frontend; 1489-1518 computes overdue/urgency client-side, with the comment \"the contact-keyed overdue endpoint was retired with public.contacts\". The manifesto (roster/relationship/MANIFESTO.md, \"Relationships decay without interaction\") makes decay/health scoring the butler's doctrine that drives its weekly-review recommendations.",
     "severity": "major",
     "detail": "North star: every surface opens with the system's own synthesized verdict. Here the console's judgment can diverge from what the butler actually recommends (its decay model, its weekly review), and each attention row carries no reason or evidence link beyond \"Nd since contact\" \u2014 no per-item explanation of why THIS person outranks that one, and no trace to the interactions the claim rests on. The verdict belongs server-side with reasons attached."
    },
    {
     "title": "Find-as-you-type returns a false \"0 matches\" for anyone in the tier-1500 periphery or beyond the halo cap, with no escape hatch",
     "evidence": "searchSpace is built only from rendered marks (PlexPage.tsx:1704-1715); PLEX_NODE_TIERS excludes 1500 (plex-layout.ts:27) and halo arcs cap at top-20 per type (router.py:3760-3764), so a familiar-face's name yields \"find x \u00b7 0 matches \u00b7 esc clears\" (PlexPage.tsx:2060-2076) even though the entity exists and the same string would hit in the Cmd-K Finder.",
     "severity": "major",
     "detail": "A canvas-scoped search is honest only if it says its scope. The zero-match state should offer \"enter searches all entities\" (reusing useEntityFinderSearch) and hop to the hit via ?center=, so the plex's find covers the whole graph instead of quietly excluding ~the largest tier."
    },
    {
     "title": "Drag-to-retier is not same-frame and has no undo: the node snaps back to the old ring until the ranking refetch lands",
     "evidence": "useUpdateEntityDunbarTier (use-entities.ts:505-520) has no onMutate/optimistic cache write \u2014 only onSuccess invalidation of dunbar-ranking. handleRetier (PlexPage.tsx:1760-1773) awaits mutateAsync then toasts; during mutate+invalidate+refetch the drag state is already cleared (handleDragEnd, 439-446) so the node visually reverts to its pre-drag ring, then jumps again. The success toast has no Undo action even though the API trivially supports it (tier:null / previous tier).",
     "severity": "major",
     "detail": "Violates the north-star action contract (\"every owner action applies in the same frame with undoable rollback\") on the page's single manual write. Fix: onMutate patches the dunbar-ranking cache (tier + dunbar_tier_override), onError rolls back, and the toast gains Undo restoring the prior tier/pin state."
    },
    {
     "title": "Keyboard users have no path to the micro-dossier or to pin/unpin: hover-only card, drag-only retier",
     "evidence": "PlexNode binds onHoverStart/onHoverEnd only to onMouseEnter/onMouseLeave (PlexPage.tsx:312-313) \u2014 no onFocus/onBlur \u2014 so tabbing to a mark never opens the hover card containing \"Open record\" and \"Unpin tier\" (1061-1077). Tier pinning is exclusively pointer-drag (draggable, 746; handlePointerDown 262-289); the legend itself says \"drag a person to pin a tier\" (2091-2094). No arrow-key traversal between marks; Tab order over ~600 absolutely-positioned buttons is the only walk.",
     "severity": "major",
     "detail": "The a11y floor (bu-86c4c.16) landed axe tests but this is a behavioral keyboard gap axe cannot see. Focus should mirror hover (open the dossier card), and a focused mark needs key verbs (Enter=center exists; add o=open record, p=pin-to-ring picker / unpin)."
    },
    {
     "title": "Circles label badges paint white text over arbitrary owner-supplied hex \u2014 AA contrast unguaranteed and un-lintable",
     "evidence": "CirclesPage.tsx:62-65 (BADGE_TEXT=\"#fff\", labelBg returns label.color ?? categoryHueVar), applied at 198 and 230; CreateLabelDialog accepts any hex (109-133) with no luminance check, so a #ffee00 label renders white-on-yellow.",
     "severity": "minor",
     "detail": "The contrast-locked token work (bu-86c4c.16/bu-kx3xc) cannot protect runtime-supplied colors. Either compute foreground (black/white) from the hex's relative luminance at render, or drop free-hex entirely and constrain labels to the categoryHueVar ramp (Dispatch dialect prefers the latter: one hue system)."
    }
   ],
   "ideal": "The ideal /entities is the household's social instrument: the plex opens with the butler's own verdict rendered as a Dispatch-voice line above the rings (\"Two inner-circle ties are fading \u2014 Sarah, 12 days over cadence; Mom's birthday in 6 days\"), every clause a door \u2014 click or keystroke lands on the person centered in the plex with the evidence (interactions, facts, decay curve) in the dossier flank. The canvas is fully keyboard-native: type to find across the WHOLE graph (periphery and beyond-cap satellites included, falling through to the Finder index when off-canvas), arrows walk marks, focus opens the micro-dossier, single keys pin/unpin tiers, and every write applies in the same frame with an Undo in the toast. The four lenses (Plex, Index, Concentration, Circles) are one instrument, not four tabs: any row anywhere offers \"view in plex\" so the hop-trail spine extends from every list, a circle can be projected onto the rings as a highlight lens, and a concentration row's dominance is one hop from the triples that earned it. Failure is always named: a dead ranking endpoint shows an amber source-named placard with retry \u2014 never zeroed capacity meters or a serif \"No one is owed a call\" \u2014 and the attention verdict itself is computed by the relationship butler from its manifesto's decay model, arriving with reasons and trace-ids, so the console reports judgment it can prove rather than judgment it invented.",
   "jarvis_gap": "The plex is the closest thing the console has to a signature JARVIS surface \u2014 real radial ego-graph, halo, hop trail, find-as-you-type, drag-to-retier, all wired to live endpoints \u2014 but it fails the earned-calm test precisely where it matters: a failed ranking fetch fabricates a perfectly calm household (zero meters, \"No one is owed a call\"), its one synthesized judgment (worth attention) is a frontend heuristic with magic constants rather than the butler's own decay-model verdict with evidence, its flagship interaction (retier) is neither same-frame nor undoable, and its keyboard story stops at click-to-center. The sibling lenses are honest and functional but siloed \u2014 no path from Index/Concentration/Circles rows back into the plex spine.",
   "moves": [
    {
     "title": "One state contract across the plex flanks and dossier: no fabricated calm",
     "why": "Fixes the critical finding plus both silent-error siblings (halo, dossier facts/dates) in one pass: error states name their source, carry Retry, and never render zeros or serif-empty copy derived from a failed fetch.",
     "cost": "S",
     "slice_plan": "1) Thread rankingError into AttentionRail/capacity (render amber source-named placard, suppress meters) + add Retry to the canvas error block. 2) Read isError from usePlexHalo and render a halo-band unavailable placard. 3) Read isError from useEntityFacts/useEntityCoreDates in EntityDossier and render error rows instead of \"Nothing recorded yet.\"",
     "integration_points": "frontend/src/components/relationship/PlexPage.tsx (AttentionRail 1233-1371, error block 1967-1974, EntityDossier 1086-1227); hooks already expose isError/refetch (use-entities.ts:186-240)."
    },
    {
     "title": "Move the attention verdict into the relationship butler with reasons and evidence links",
     "why": "The console's only synthesized judgment on this surface is currently view-layer arithmetic that can diverge from the butler's manifesto-bound decay model; JARVIS-grade means the system reports judgment it computed and can prove.",
     "cost": "M",
     "slice_plan": "1) Add GET /api/butlers/relationship/plex/attention to roster/relationship/api/router.py computing overdue-by-cadence server-side (cadence table lives beside the decay model, returns entity_id, tier, since_days, reason string, urgency). 2) Swap PlexPage's client heuristic (1387-1518) for the endpoint; render the reason as the row subtitle. 3) Surface the same list as the plex's opening Dispatch line above the canvas (one sentence, each name a ?center= door). Act verbs stay with bu-86c4c.15.",
     "integration_points": "roster/relationship/api/router.py (beside /plex/halo, 3750), models.py; PlexPage.tsx AttentionRail; delete TIER_CADENCE_DAYS/TIER_URGENCY_WEIGHT from the frontend."
    },
    {
     "title": "Optimistic retier with Undo",
     "why": "The page's single manual write violates the same-frame + undoable-rollback action contract; the fix is entirely local and the API already supports restore (tier:N / tier:null).",
     "cost": "S",
     "slice_plan": "1) Add onMutate to useUpdateEntityDunbarTier patching the dunbar-ranking cache (dunbar_tier + dunbar_tier_override), onError restoring the snapshot. 2) Capture prior tier/pin in handleRetier and add an Undo action to the success toast that replays the previous state. 3) Same for unpin.",
     "integration_points": "frontend/src/hooks/use-entities.ts:505-520; PlexPage.tsx handleRetier/handleUnpin 1760-1788; queryKey [\"dunbar-ranking\"] shape from use-memory.ts:427."
    },
    {
     "title": "Find-as-you-type falls through to the whole graph",
     "why": "Today the find bar returns a false \"0 matches\" for the entire tier-1500 periphery and beyond-cap satellites \u2014 the one place the owner is most likely to search for someone they half-remember.",
     "cost": "S",
     "slice_plan": "1) When canvas matches are 0 (and debounced), run useEntityFinderSearch(query, {limit:3}) and render the top off-canvas hits in the find bar (\"off the canvas: Priya T. \u00b7 enter hops\"). 2) Enter on an off-canvas hit navigates to ?center=<id> (existing hop semantics). 3) Keep the count line honest: \"0 on canvas \u00b7 3 in the full graph\".",
     "integration_points": "PlexPage.tsx find block 1692-1738 + find bar 2060-2076; useEntityFinderSearch (use-entities.ts, already debounced, same ranking as Cmd-K)."
    },
    {
     "title": "Keyboard parity on the canvas: focus opens the dossier, keys pin",
     "why": "Hover-only micro-dossier and drag-only retier lock keyboard users out of the record-open and pin/unpin verbs entirely \u2014 the one behavioral a11y gap axe (.16) cannot detect.",
     "cost": "M",
     "slice_plan": "1) Bind onFocus/onBlur on PlexNode to the same scheduleHover/scheduleHide as mouse (card already contains Open record + Unpin). 2) Arrow keys move a roving focus between marks (nearest-angle within ring, ring-hop on up/down) via the existing canvas keydown map. 3) `p` on a focused person cycles/pins tier (reusing handleRetier), `o` opens the record. 4) Update the legend line.",
     "integration_points": "PlexPage.tsx PlexNode 299-339, handleKeyDown 1852-1893, legend 2091-2094; layout node lists already carry angle/tier for spatial traversal (plex-layout.ts)."
    }
   ],
   "page": "entities-plex",
   "verdict": "solid"
  },
  {
   "page": "entity-detail",
   "verdict": "functional",
   "jarvis_gap": "The .1 truth fixes hold (formatLoanAmount at :1186, no-fallback provenance at :1400), and the page is genuinely wired \u2014 every curation button traces to a real mutation. But it still fails the console's defining properties: duplicate detection is a truncated client-side scan that silently tells the owner nothing is wrong; ~14 subsection queries have no error state, so failed fetches impersonate empty sections and the Workbench KPI strip fabricates zero mega-numbers; the keyboard map is dead until the container is clicked and none of its verbs exist in the one command palette; timeline, thread, and provenance-grid signals still dead-end with no path to root evidence; and the first screenful leads with admin plumbing (open contact drawer, a 428-line inline Telegram OTP wizard) instead of judgment.",
   "findings": [
    {
     "title": "NOT-LANDED: Duplicate detection derived from a truncated 100-item queue scan \u2014 silent false negatives",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:2631 (useRelationshipEntityQueue({ limit: 100 })), :2634-2657 (duplicateEntry/duplicatePeers found by scanning queueData.items; peer names also resolved only from the same window)",
     "severity": "critical",
     "detail": "If this entity's duplicate-candidate row is beyond the first 100 queue items, the duplicate warning panel (:3036), Workbench duplicate panel (:2479), 'shares identifiers with' rail (:2174), merge buttons (disabled via !duplicatePeerId), and the `m` key (:2744-2747) all silently vanish \u2014 the page affirmatively presents 'no duplicate' when one exists. Peer display names likewise fall back to 'another entity' when the peer isn't in the window (:2653). This is fabricated absence on a curation trust signal; the old dossier's prescribed per-entity endpoint (GET /entities/{id}/duplicates) was never built."
    },
    {
     "title": "NOT-LANDED: subsection fetch failures impersonate empty, and the Workbench KPI strip fabricates zeros",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:2219-2259 (WorkbenchKpiStrip destructures only data from three queries \u2014 during load and on error it renders 'relations 0 / touches 90d 0 / sources 0 / channels 0' as authoritative mega-numbers); :1017+1089-1094 (ActivityTimeline ignores error \u2192 'No activity recorded yet.'); :1141-1142, :1204-1205, :1259-1261 (Gifts/Loans/MessageThreads return null on error \u2014 sections silently vanish); :2150-2152 ('No relations yet.' on failed neighbours fetch); :3328-3343 (first/last seen error renders '\u2014'); CoreDatesBlock.tsx:82 (return null while loading)",
     "severity": "critical",
     "detail": "The Page-level error prop (:3020-3021) covers only the main useEntity query; the ~14 per-section queries have no three-way state contract. Failed fetch = empty-state copy or invisible section, and loading = pop-in layout shift where a slow core-dates query reads as 'no birthday'. The .2 state-contract epic did not reach these sections (bu-mkd5r's remaining scope is topology/education pills, not entity-detail). Direct violation of 'no failed fetch ever impersonates an empty queue' and 'no number is ever fabricated'."
    },
    {
     "title": "NOT-LANDED: keyboard map dead on arrival and invisible; NEW: zero commands registered in the one palette registry",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:3026-3031 (detailRootRef tabIndex=0 container, never programmatically focused \u2014 grep confirms detailRootRef only used at :2612 and :3027); :2738-2766 (m/j/k/Esc/e/Shift+Backspace live only inside that container); :2704 (siblings = first 200 index-order entities, no 'n of m' indicator, idx===-1 \u2192 silent no-op); frontend/src/lib/command-registry.tsx:104 (useRegisterCommands exists; only IssuesPage.tsx and ApprovalsPage.tsx import it \u2014 EntityDetailPage registers nothing)",
     "severity": "major",
     "detail": "On fresh navigation, focus is on body, so every shortcut is inert until the user clicks/tabs into the container; only `m` has any on-screen hint (:3052). Post-.7 ('one cmdk command spine'), none of this page's verbs \u2014 merge review, confirm identity, set tier, archive, forget, mode toggle \u2014 exist in the palette, so the surface is invisible to the single-registry contract. The old dossier's fixes (autofocus root, shortcut-hints strip, position indicator) never landed; the palette gap is new signal against the landed .7 spine."
    },
    {
     "title": "NOT-LANDED: drill-down dead ends \u2014 timeline rows, message threads, and the provenance grid terminate with no path to root evidence",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:1106-1134 (TimelineRow renders content/predicate/date with no session or fact link \u2014 only FactRow has a session link at :1564-1572); :1280-1307 (MessageThreadRow is a non-interactive li, snippet and thread_identity are dead text); :1941-1943 (ProvenanceRow src column is plain text; the Workbench 'canonical fact-level read' grid has zero links to authoring sessions)",
     "severity": "major",
     "detail": "The trace-id spine stops at this page: an interaction in the Activity feed, a thread summary, or a stale fact in the provenance grid cannot be walked to the session/ingestion event that produced it. bu-9d5vp covers inert cells on other surfaces (dashboard KPI, egress, audit, spend); these entity-detail dead ends were dossier findings that did not land in .3/.4."
    },
    {
     "title": "NOT-LANDED: first screenful is admin plumbing \u2014 open-by-default contact drawer and a 428-line inline Telegram OTP wizard on a person page",
     "evidence": "frontend/src/components/relationship/PracticalDrawer.tsx:81 (useState(true) \u2014 still open by default, contradicting the page's own comment 'collapsed by default' at :2924); EntityDetailPage.tsx:3347-3355 (drawer rendered above CoreDatesBlock and LatestInteractionsBlock in editorial); :174-602 (TelegramSessionSetup: API-ID/hash/phone/OTP/2FA flow inline) rendered at :2954-2959, directly below the note at :2942-2952 saying credentials are managed in Secrets \u2192 User",
     "severity": "major",
     "detail": "The v3 brief's primary job (latest interactions, core dates, delta) sits below link/unlink plumbing, channel cards, and \u2014 for the owner entity \u2014 a full transport-credential provisioning wizard that contradicts both the adjacent copy and vision.md non-negotiable 7 (transport is connector responsibility). The wizard also uses raw text-green-600 (:365,373,379,385,582) outside the three-state-color token system (instance for the in-flight .6 lint to catch)."
    },
    {
     "title": "NOT-LANDED: curation verbs triplicated with synonym drift and inconsistent destructive-action grammar",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:2511 ('Promote' = confirm-unidentified) vs :2392-2397 ('promote'/'demote' = step Dunbar tier) vs PulseStrip tier dropdown; Forget reachable from page actions :2998, editorial rail :2404, workbench rail :2534, and Shift+Backspace :2760; :625 (window.confirm for unlink \u2014 native browser dialog on a page that uses AlertDialog for forget at :3456); :2902-2911 (archive fires instantly with no confirmation and no undo, then navigates away)",
     "severity": "major",
     "detail": "One word means two operations depending on which rail you're in; tier is settable by explicit picker in one place and blind steppers in two others; unlink/archive/forget each use a different destructive grammar (native confirm / nothing / styled dialog). Violates one-verb-per-operation and the undo-over-confirm recovery bar for the reversible archive."
    },
    {
     "title": "NEW: entity name rendered twice as two h1s, and the headline jumps 44px\u219230px on mode toggle",
     "evidence": "frontend/src/components/ui/page.tsx:151-161 (editorial archetype renders title as 44px Display h1 whenever breadcrumbs/actions are passed \u2014 EntityDetailPage passes both at :3018-3022) and :106 (overview archetype renders a 30px h1); EntityDetailPage.tsx:3090-3092 (identity hero renders a second <h1 className=\"text-2xl\"> with the same canonical_name and the edit pencil)",
     "severity": "major",
     "detail": "In both modes the owner sees the person's name twice within one viewport \u2014 a 44px (or 30px) shell headline and a 24px hero headline \u2014 and the document has two h1 elements (heading-semantics defect the axe floor won't catch since duplicate h1s aren't an axe violation). Toggling editorial\u2194workbench also swaps the shell headline size, so the page visibly re-skins. The hero h1 + shell headline coexistence appears to postdate the dossier (which only flagged the size jump)."
    },
    {
     "title": "NEW: post-.1 provenance reveal is honest but silently incomplete \u2014 string-matched against a 200-row window instead of id-keyed",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:1387-1401 (_provenanceForFact matches by predicate then exact object/content string equality; returns null on any mismatch); :1420-1423 (drill read capped at limit: 200); :1573-1585 (the reveal toggle simply doesn't render when provenance is null \u2014 no indication provenance exists but wasn't matched)",
     "severity": "minor",
     "detail": "The .1 fix correctly removed the wrong-provenance fallback, but chose honesty-by-omission over the dossier's prescribed structural fix (detail endpoint returning the drill-store id per recent_fact). Any fact whose content differs textually from its drill row's object, any fact beyond the 200-row drill window, and any predicate with two identical objects across stores (find() returns the first) silently loses or mis-scopes its provenance affordance. The owner cannot distinguish 'no provenance recorded' from 'provenance exists but the string join failed'. Related workbench truth nits still live: WorkbenchRelationRow hardcodes entityType=\"person\" for every neighbour (:2080), and the 'channels' KPI counts fact rows, not distinct channels (:2242)."
    }
   ],
   "ideal": "The ideal entity page opens with judgment in the Dispatch voice: one 44px Display headline (the only h1), a serif verdict synthesizing tier, trajectory, and open loops \u2014 \"Inner-circle friend, last touched 9 days ago, one unsettled loan, birthday in 12 days\" \u2014 where every clause is a door (loan \u2192 the loan row, birthday \u2192 core date, 9 days \u2192 the interaction's session). Below it, exactly what needs the owner: a duplicate warning backed by a per-entity duplicates endpoint that can never silently miss, delta-since-last-visit rows one keystroke away, and manifesto verbs (log interaction, gift idea, draft reach-out \u2014 the in-flight .15 scope) inline on the signals they answer. Every subsection obeys the three-way state contract \u2014 skeleton, named degraded source, or true empty \u2014 and no KPI ever shows a number the system didn't compute. Every fact, timeline entry, thread, and provenance row carries an id-keyed link to its authoring session on the trace spine. The whole surface is keyboard-first from the moment it mounts: focus lands on the detail root, j/k walk the arrival scope with an 'n of m' position mark, a visible hint strip teaches m/e/Esc, and every curation verb \u2014 one name per operation, undo-toast for the reversible ones \u2014 is registered in the single command registry so \u2318K reaches merge, set-tier, archive, and forget without the mouse. Transport plumbing (Telegram provisioning) lives on the connectors surface where the vision says it belongs; the person page is about the person.",
   "moves": [
    {
     "title": "Per-entity duplicates endpoint \u2014 kill the truncated queue scan",
     "why": "The page's highest-trust signal (is this person double-counted?) currently false-negatives silently past 100 queue items; a console whose defining property is earned calm cannot derive per-entity state from a truncated list scan.",
     "cost": "S",
     "slice_plan": "1) Add GET /api/butlers/relationship/entities/{id}/duplicates returning peers with resolved names + trigger evidence (predicate, shared_value). 2) New useEntityDuplicates hook; replace the queueData memos at EntityDetailPage.tsx:2631-2676. 3) Delete the limit-100 queue fetch from this page.",
     "integration_points": "roster/relationship/api/router.py (duplicate-candidate bucket query already exists for the queue), frontend/src/hooks/use-entities.ts, EntityDetailPage.tsx duplicate memos, MergeCompareDialog pair wiring"
    },
    {
     "title": "Three-way state contract sweep over every entity-detail subsection",
     "why": "Two systemic sins live here today: failed fetches impersonate empty (timeline, gifts, loans, threads, relations, first/last seen) and the KPI strip fabricates zero mega-numbers during load and on error.",
     "cost": "M",
     "slice_plan": "1) WorkbenchKpiStrip: destructure isLoading/error per query; render per-cell skeleton while loading and a named 'source unavailable' mark on error \u2014 never 0. 2) ActivityTimeline/Gifts/Loans/MessageThreads/ContextRail/first-last-seen: adopt the shared state-contract primitive landed in bu-86c4c.2 (loading skeleton, role=alert degraded row naming the source, true-empty copy only on confirmed empty). 3) CoreDatesBlock/LatestInteractionsBlock: fixed-height skeletons instead of return-null pop-in.",
     "integration_points": "EntityDetailPage.tsx:1017-1307,2117-2259,3323-3355; components/relationship/CoreDatesBlock.tsx:82, LatestInteractionsBlock.tsx:63-68; whatever shared degraded-source primitive .2 introduced (reuse, don't fork)"
    },
    {
     "title": "Wire the page into the command spine and make the keyboard map live on mount",
     "why": "A keyboard map that requires a mouse click to activate, teaches nothing on screen, and is invisible to the single command registry contradicts the landed .7 spine and the five-second/one-minute operating bar.",
     "cost": "M",
     "slice_plan": "1) Focus detailRootRef on mount (after entity load). 2) Register merge-review, confirm-identity, promote/demote tier, archive, forget, and mode-toggle via useRegisterCommands with the entity name in labels. 3) Render the existing shortcut-hints strip. 4) Sibling stepping: scope to arrival context (?from=), preserve search params on navigate, show 'n of m' beside the headline, toast at list edges.",
     "integration_points": "frontend/src/lib/command-registry.tsx (useRegisterCommands, pattern per IssuesPage/ApprovalsPage), EntityDetailPage.tsx:2704-2766 and :3026-3031, ui/shortcut-hints"
    },
    {
     "title": "One identity headline, one curation grammar, admin plumbing demoted",
     "why": "The owner currently sees the name twice as two h1s, meets three synonym-drifted curation surfaces, and scrolls past an open contact drawer plus an inline Telegram OTP wizard before reaching what the page is for.",
     "cost": "L",
     "slice_plan": "1) Drop the hero h1 (:3090); the Page Display headline is the only h1, with edit/type/tier badges on its status row; give workbench a stable headline (workspace archetype or same Display). 2) One verb per operation everywhere: 'Confirm identity', 'Set tier' (picker only \u2014 remove blind steppers), 'Merge', 'Archive' (undo-toast, no navigate-away surprise), 'Forget' (dialog, one entry point + shortcut); replace window.confirm unlink at :625. 3) PracticalDrawer default collapsed (PracticalDrawer.tsx:81), moved below facts; TelegramSessionSetup (:174-602) relocated to the connectors/secrets surface with a status line + link left behind.",
     "integration_points": "components/ui/page.tsx archetypes, EntityDetailPage hero + both rails + PulseStrip tier tile, PracticalDrawer.tsx, secrets/connectors surface for the wizard (aligns with bu-rxtyx settings governance fold \u2014 coordinate, don't collide)"
    },
    {
     "title": "Id-keyed provenance join and evidence links on timeline, threads, and the provenance grid",
     "why": "Post-.1 the page no longer lies about provenance but silently omits it wherever the predicate+content string join fails, and three of its four signal feeds still terminate without a path to root evidence \u2014 the trace spine stops here.",
     "cost": "M",
     "slice_plan": "1) Detail endpoint returns the drill-store fact id (or inline src/verified/staleness) per recent_fact; replace _provenanceForFact string matching (:1387-1401) with an id lookup; show a muted 'provenance unavailable' mark when genuinely absent. 2) Timeline rows link to their originating session/fact (session_id already flows on facts). 3) MessageThreadRow links to the ingestion events view filtered by channel + thread identity. 4) ProvenanceGrid src cell links to the authoring session when session_id exists.",
     "integration_points": "relationship detail endpoint (recent_facts serializer), frontend/src/api/types.ts Fact/EntityFact, EntityDetailPage.tsx:1106-1134,1280-1307,1387-1423,1941-1943, ingestion events route query params (cursor-paginated /api/ingestion/events)"
    }
   ]
  },
  {
   "findings": [
    {
     "title": "Header KPI numerals fabricate zeros when subsystems fail",
     "evidence": "src/butlers/api/routers/settings_console.py:137-143, 203-213, 242-247, 272-282 \u2014 every aggregation helper catches its exception and returns 0/0.0/(0,0) alongside an amber attention item; frontend/src/pages/SettingsConsolePage.tsx:254-276 renders those zeros as confident 28px mega-numerals ('$0.00' Spend MTD, '0' Active Butlers, '0/0' Models OK). A dead spend subsystem thus displays $0.00 spend, and an unreachable roster displays '0 Active Butlers' \u2014 which reads as fleet-down, a different (fabricated) verdict.",
     "severity": "critical",
     "detail": "The amber strip item names the degraded source, but the number itself still impersonates real data \u2014 violating the north star's 'no number is ever fabricated'. HeaderCounts (settings_console.py:77-84) has no nullability, so the API cannot express 'unknown'. Fix: nullable header_counts fields, KpiCell renders an em-dash + degraded marker linking to the failing subsystem."
    },
    {
     "title": "Audit reel error impersonates empty",
     "evidence": "frontend/src/pages/SettingsPermissionsPage.tsx:413-457 \u2014 AuditReelSection destructures only {data, isLoading} from useAuditLog (line 414); on fetch failure entries=[] and lines 451-457 render the empty-state prose 'No recent audit entries.' There is no isError branch and no retry.",
     "severity": "major",
     "detail": "On the page whose job is 'who changed what, on whose authority', a failed audit fetch reads as 'nothing privileged happened' \u2014 the exact five-sins pattern (errors impersonating empty) that bu-86c4c.1 amnesty was supposed to eradicate. New instance, not in the 2026-07-03 dossier."
    },
    {
     "title": "NOT-LANDED: inherited permission cells are dead controls \u2014 the matrix is mostly inert, and the spec contradicts itself",
     "evidence": "frontend/src/pages/SettingsPermissionsPage.tsx:377-380 \u2014 <button disabled={inherited}>; since the matrix is dense and every butler\u00d7permission pair without an explicit row is inherited (openspec/specs/dashboard-permissions/spec.md:39,43), a fresh system's entire matrix is unclickable. Spec self-contradiction: spec.md:43 says inherited cells render 'dim/non-editable' while spec.md:59-61 ('Inherited cells become explicit on mutation \u2014 WHEN an inherited cell is flipped THEN the resulting row is explicit') requires them to be flippable \u2014 a scenario unreachable from the UI.",
     "severity": "major",
     "detail": "Flagged in the 2026-07-03 dossier (SettingsPermissionsPage.tsx:366-393) and still reproduces on main. The primary governance control on the governance surface cannot be operated for its most common state. Also no legend explains \u25cf/\u25cb/dim semantics; the only override reason surface is a hover title attribute (line 387), invisible to keyboard users."
    },
    {
     "title": "Model verification failure has no evidence door",
     "evidence": "frontend/src/pages/SettingsModelsPage.tsx:1377-1410 \u2014 a failed verification renders only a bare '\u2717' glyph; frontend/src/api/types.ts:4183-4188 shows last_verified_at and last_verified_latency_ms arrive in the payload but are never rendered; the per-model Test action surfaces its error only in a transient toast (SettingsModelsPage.tsx:1354-1365). No stored error message, no timestamp, no drill-down.",
     "severity": "major",
     "detail": "The console attention item says 'Model verification failed: <aliases>' and routes here \u2014 then the trail goes cold at a glyph. The owner cannot answer 'failed when, with what error, how slow' without re-running the test and catching a toast. Violates the one-keystroke-to-root-evidence spine. Persist the last verification error and expose at/latency/error on row disclosure."
    },
    {
     "title": "State-color discipline violations on /settings/models: background fills, yellow, and a word-badge",
     "evidence": "frontend/src/pages/SettingsModelsPage.tsx:145-149 (barColorClass returns bg-red-500 / bg-yellow-500 / bg-green-500 background fills), :1246-1249 (white-on-bg-red-500 'BLOCKED' word badge), :1401-1404 (text-green-600 verified glyph), :522 (text-amber-600 warning) \u2014 versus openspec/specs/dashboard-design-language/spec.md:80-104: exactly three state tokens (--red/--amber/--green), 'foreground or border only \u2014 never on background fills (no bg-red-500-style patterns)', and yellow is not a state color at all.",
     "severity": "major",
     "detail": "bu-86c4c.6 (in-flight) builds the machine enforcement; these are concrete unfixed violations on current main that the lint will trip on, worth repairing now as its seed corpus. The usage bars can keep their form with a hairline track + token-colored fill within the attention-tint alpha bounds, and BLOCKED should become a glyph+tone, not a word-badge."
    },
    {
     "title": "NOT-LANDED: webhook Delete fires immediately \u2014 destructive, irreversible, unconfirmed",
     "evidence": "frontend/src/pages/SettingsPermissionsPage.tsx:978-989 (handleDelete calls DELETE /api/webhooks/{id} directly) wired to the row button at 1138-1146 \u2014 no confirm dialog, no undo. One mis-click destroys the endpoint registration and its never-recoverable signing secret.",
     "severity": "major",
     "detail": "Dossier finding still reproducing on main. Contrast: model deletion on the sibling page got a proper DeleteConfirmDialog (SettingsModelsPage.tsx:359-405). Design-bar 'Recovery': irreversible actions require explicit confirmation; secret loss makes undo impossible, so confirm is mandatory."
    },
    {
     "title": "PanelShell keyboard hazard: Enter on inner Retry navigates away; Space scrolls",
     "evidence": "frontend/src/pages/SettingsConsolePage.tsx:296-306 \u2014 the panel is a div role=\"button\" whose onKeyDown fires onNavigate for Enter/Space with no e.target check and no preventDefault; real <button> Retry controls are nested inside it (e.g. :344-352). Keydown on Retry bubbles to the panel div, so pressing Enter on 'Retry \u2192' both retries and navigates to the panel's href; Space additionally scrolls the page.",
     "severity": "minor",
     "detail": "Nested-interactive inside role=button is also an axe violation (nested-interactive) that the bu-86c4c.16 a11y floor missed because these panels are divs, not DisclosureRow/RowLink. Fix: make panels real <Link> elements (title as the link), keep inner buttons as siblings-in-DOM or stopPropagation on their keydown."
    },
    {
     "title": "NOT-LANDED residue on the console/permissions shells: truncation dead end, retry-less errors, dateless audit reel, clock leak",
     "evidence": "SettingsConsolePage.tsx:172-181 (attention truncation footer routes to /audit-log, where truncated attention items do not live \u2014 they are unviewable anywhere); SettingsPermissionsPage.tsx:1240-1242 and SettingsModelsPage.tsx:1690-1696 (static 'Failed to load\u2026' prose with no retry affordance, unlike the console panels' Retry \u2192); SettingsPermissionsPage.tsx:435-437 (audit reel prints toLocaleTimeString() only \u2014 yesterday's '14:02 revoke' is indistinguishable from today's across a 15-entry multi-day window); SettingsConsolePage.tsx:197-210 (ConsoleClock's setInterval cleanup is returned from a setTimeout callback and discarded \u2014 the interval leaks past unmount).",
     "severity": "minor",
     "detail": "All four were in the 2026-07-03 dossier and reproduce verbatim on current main; none is covered by bu-rxtyx (which scopes only the sub-nav fold) or any open bead I could find."
    }
   ],
   "page": "settings",
   "verdict": "functional",
   "jarvis_gap": "The Settings suite is functionally deep (live WS console, one-time secret ceremonies, reason-gated permission flips) but fails the console's defining property \u2014 earned calm through honesty \u2014 at its most visible point: when a subsystem dies, the KPI strip confidently prints fabricated zeros ($0.00 spend, 0 active butlers), and the audit reel renders a fetch failure as 'No recent audit entries.' Its judgment surfaces are doors to nowhere: a failed model shows a bare \u2717 with no error, no timestamp, no trail; the permissions matrix \u2014 the sovereignty control at the heart of the governance chamber \u2014 is inert for every inherited cell, which on a default system is the whole grid. And /settings/models still speaks a foreign dialect (bg-yellow-500 fills, a white-on-red BLOCKED word-badge) that the three-token Dispatch language explicitly bans. Detect works; diagnose and act stall one hop past the headline.",
   "ideal": "The ideal /settings is the household's constitution room: it opens with a one-sentence synthesized verdict in the Dispatch voice ('All governance nominal; codex CLI needs re-auth \u2014 one decision pending') where every clause is a keyboard-reachable door, followed by a KPI strip that renders an em-dash plus a named degraded-source marker \u2014 never a fabricated zero \u2014 whenever a subsystem cannot prove its number. The permissions matrix is fully operable: every cell, inherited or explicit, flips through the reason-gated modal (inherited cells show their default provenance and become explicit on flip, exactly as the spec's mutation scenario demands), a legend teaches \u25cf/\u25cb/dim on-surface, and each flip lands as a linked entry in a date-aware audit reel that doubles as the cross-settings changelog with diff drill-down. Every model row carries its verification evidence (when, latency, last error) one disclosure away, with Test/Verify-all as palette verbs; destructive actions (webhook delete, model delete) share one confirm-with-consequences ceremony; the whole chamber lives under one sub-nav shell (bu-rxtyx) speaking pure three-token Dispatch, and every governance verb \u2014 verify all, export data, add webhook, grant permission \u2014 is one Cmd-K away.",
   "moves": [
    {
     "title": "Truth-honest header counts: nullable aggregates, em-dash rendering, named degradation",
     "why": "The governance console currently fabricates $0.00/0/0-0 mega-numerals when subsystems fail \u2014 the single worst violation of 'no number is ever fabricated' on this surface, and it actively misleads (0 active butlers reads as total outage).",
     "cost": "S",
     "integration_points": "src/butlers/api/routers/settings_console.py (HeaderCounts model :77-84, per-helper returns :137-143/:203-213/:242-247/:272-282); frontend/src/pages/SettingsConsolePage.tsx KpiCell/KpiStrip :224-277; frontend/src/hooks/use-settings-console-stream.ts (header_delta merge must tolerate nulls); openspec/specs/dashboard-settings-console spec delta.",
     "slice_plan": "One PR: (1) make HeaderCounts fields Optional, return None on subsystem failure \u2014 the amber attention item stays as the named source; (2) FE type change + KpiCell renders '\u2014' with a muted 'unavailable' eyebrow when null; (3) update WS snapshot/delta tests and stale fixtures in the same PR (new-required-field fixture-skew trap applies)."
    },
    {
     "title": "Operable, honest permissions page: flippable inherited cells + audit reel with dates, errors, and drill-down + delete confirm",
     "why": "The sovereignty control is inert for its default state (spec scenario 'inherited cells become explicit on mutation' is unreachable), the audit reel lies on error and is date-ambiguous, and webhook delete is one unconfirmed click from destroying an unrecoverable secret.",
     "cost": "M",
     "integration_points": "frontend/src/pages/SettingsPermissionsPage.tsx (:377-380 disabled flag, :413-468 AuditReelSection, :978-989/:1138-1146 delete path); openspec/specs/dashboard-permissions/spec.md (resolve the :43 'non-editable' vs :59-61 'flipped' self-contradiction via spec delta); backend PUT /api/permissions already supports the flip.",
     "slice_plan": "Slice 1: remove disabled={inherited}, flip modal shows 'overriding inherited default (granted)', add on-surface \u25cf/\u25cb/dim legend; spec delta rewords :43 to 'dim but clickable'. Slice 2: audit reel isError branch + date-aware timestamps + each row links to /audit-log filtered to that entry. Slice 3: webhook DeleteConfirmDialog reusing the models-page pattern."
    },
    {
     "title": "Model verification evidence spine: persist and surface when/latency/error per row",
     "why": "The console's red 'Model verification failed' item routes to a bare \u2717 glyph \u2014 the trace dead-ends exactly where the owner needs root evidence; last_verified_at/latency already ship in the payload and are discarded.",
     "cost": "M",
     "integration_points": "frontend/src/pages/SettingsModelsPage.tsx ModelRow :1340-1475; frontend/src/api/types.ts:4183-4188 (fields exist); src/butlers/api/routers/model_settings.py + one migration adding last_verified_error TEXT to public.model_catalog (single-migration bead \u2014 serialize with the fleet to avoid the parallel core_NNN collision); catalog error state :1690-1696 gains Retry.",
     "slice_plan": "Slice 1 (FE-only): render last_verified_at relative + latency on every row; \u2717 becomes a DisclosureRow expanding whatever evidence exists; add Retry to the catalog error state. Slice 2 (BE+migration): persist the verification error string from test/verify-all and surface it in the disclosure."
    },
    {
     "title": "Governance verbs in the command palette + real-link panels",
     "why": "The one-command-spine (bu-86c4c.7) landed, but no settings page contributes a single verb \u2014 verify-all, add-model, export-data, add-webhook are mouse-only, and the console panels are role=button divs with an Enter-bubbling hazard instead of links.",
     "cost": "S",
     "integration_points": "frontend/src/lib/command-registry.tsx (useRegisterCommands, proven pattern in ApprovalsPage.tsx); frontend/src/pages/SettingsModelsPage.tsx, SettingsPermissionsPage.tsx, SettingsConsolePage.tsx (PanelShell :283-322 \u2192 RowLink/<Link>); watch bu-jlhk5 (invalid tier in GlobalActionsRegistrar) for adjacent churn.",
     "slice_plan": "One PR: register 'Verify all models', 'Add model', 'Export data (scope\u2026)', 'Add webhook' as page-scoped palette commands; convert PanelShell to an anchor-based component so Enter/Space semantics come free and the nested-Retry bubbling bug (:302-304 vs :344-352) disappears."
    },
    {
     "title": "Dispatch-token repair on /settings/models (seed corpus for bu-86c4c.6)",
     "why": "Four bg-*-500 background fills, a non-existent 'yellow' state color, and a white-on-red BLOCKED word-badge are literal violations of the design-language spec's State Color Discipline on a top-level surface \u2014 fixing them now gives the in-flight lint (bu-86c4c.6) a clean baseline instead of day-one suppressions.",
     "cost": "S",
     "integration_points": "frontend/src/pages/SettingsModelsPage.tsx :145-149 (barColorClass), :1246-1249 (BLOCKED), :1401-1408 (\u2713/\u2717 colors), :522 (text-amber-600); tokens in frontend/src/index.css (--red/--amber/--amber-text/--green are normative); openspec/specs/dashboard-design-language/spec.md:80-104.",
     "slice_plan": "One PR: usage bars become hairline track + var(--green/--amber/--red) fill (thresholds unchanged); BLOCKED word-badge becomes a red glyph + red usage numeral (one affordance per signal); replace text-green-600/text-amber-600/text-destructive with the tokens (--amber-text for text sites per bu-86c4c.16); align the page shell colors ahead of the bu-rxtyx fold."
    }
   ]
  },
  {
   "findings": [
    {
     "title": "WhatBreaks renders a false 'Nothing depends on this credential.' for most credentials (provider-slug mismatch + category misuse + degraded-200)",
     "evidence": "frontend/src/components/secrets/passport/pages.tsx:1314 passes credential.provider ('telegram_bot', 'homeassistant') and pages.tsx:1880 passes credential.category ('llm', 'general') to WhatBreaks; the backend endpoint exact-matches WHERE provider = $1 (src/butlers/api/routers/secrets_v2.py:1985-1997) against catalogue slugs 'telegram'/'home_assistant' (src/butlers/catalogue_bootstrap.py:83-106), so both queries return []. WhatBreaks.tsx:125-131 then renders the confident negative 'Nothing depends on this credential.' Additionally secrets_v2.py:1977-1981 and 2016-2026 degrade pool-unavailable/table-missing to HTTP 200 empty with no degraded flag, so infrastructure failure also impersonates that empty claim.",
     "severity": "critical",
     "detail": "The one component the spec calls the WhatBreaks source-of-truth affordance tells the owner that Telegram, Home Assistant, and every system key (ANTHROPIC_API_KEY included, via category slug) has zero dependents. An owner acting on that claim ('nothing depends on it, safe to delete') gets an outage. The inventory endpoint already alias-resolves via _infer_provider_from_type (secrets_v2.py:1084-1095) \u2014 the breaks-catalogue endpoint just never adopted it. The degraded-200 also violates the repo's own aggregates_available convention (errors impersonating empty): the FE 'unavailable' branch (WhatBreaks.tsx:115-121) can never fire."
    },
    {
     "title": "PageSystem state plaque is hardcoded green for any set credential, contradicting the real derived state shown in the spine",
     "evidence": "frontend/src/components/secrets/passport/pages.tsx:1648-1649: stateColor = isMissing ? 'var(--dim)' : 'var(--green)'; stateLabel = 'shared default'/'local override' \u2014 credential.state is never read. The backend derives real states (warn = set-but-never-probed, failing = last probe failed, expired) in secrets_v2.py:479-505, and the spine row correctly renders them as amber/red dots (spine-builder.ts:64-77, constants.ts:10-20).",
     "severity": "major",
     "detail": "Clicking an amber 'needs probe' or red 'failing' system row opens a detail page whose plaque asserts green health. Green impersonates failure on the exact surface where the owner decides whether to act \u2014 a direct three-way-state-contract violation inside one screen. Fix: plaque derives from STATE_CATALOG[credential.state]; rowState (shared/local) becomes a secondary stateLine, not the headline verdict."
    },
    {
     "title": "The 'test' verb fabricates a 0ms latency figure, discarding the real measured latency the backend returns",
     "evidence": "frontend/src/components/secrets/passport/pages.tsx:1139 and 1762: the optimistic probe-result merge hardcodes `latencyMs: 0`. ProbeResult (atoms.tsx:691-697) renders latency whenever `!= null`, so '0ms' is displayed. The probe response type SecretsProbeResult carries real latency_ms (frontend/src/api/types.ts:7339-7350, populated from public.secret_probe_log per secrets_v2.py:3170-3199), and the inventory adapter explicitly contracts 'never a fabricated 0ms' (use-secrets-inventory.ts:174-183).",
     "severity": "major",
     "detail": "Post-#2886 regression against the page's own truth-amnesty contract: the passive inventory path is honest (null \u2192 hidden), but the moment the owner presses the hottest verb on the page the result panel shows a fabricated 0ms. Fix is one line per merge: latencyMs: d.latency_ms ?? null."
    },
    {
     "title": "PageSystem's audit drill-down link 422s: ?key= is sent without the required scope prefix",
     "evidence": "frontend/src/components/secrets/passport/pages.tsx:1928: ActionArrow href={`/audit-log?key=${credential.key}`} (e.g. ?key=ANTHROPIC_API_KEY). GET /api/audit-log runs normalize_key_param (src/butlers/api/routers/audit.py:337-341) which raises ValueError for any value without a `scope:` prefix (src/butlers/core/credential_keys.py:80-110) \u2192 HTTP 422. PageUser's sibling link correctly uses `u:${provider}` (pages.tsx:1378).",
     "severity": "major",
     "detail": "The 'stamps \u00b7 audit' door on every system credential page lands the owner on an error \u2014 a drill-down dead end on the trace spine. Should be `s:${credential.key}`. Related label dishonesty: pages.tsx:1420-1422 renders the link text '/ingestion/connectors/{provider}' while the href drops the provider and goes to the bare roster."
    },
    {
     "title": "Zero keyboard surface: no palette commands registered, no '/' search shortcut despite the glyph promising one, no spine arrow-navigation",
     "evidence": "No useRegisterCommands call exists anywhere under frontend/src/components/secrets/ or SecretsPage.tsx (the bu-86c4c.7 command-spine API in src/lib/command-registry.tsx is adopted by Approvals/Issues but not secrets). No onKeyDown/keydown handler exists in any secrets component (grep: zero hits outside tests). SpineSearch renders a decorative aria-hidden '/' prefix (Spine.tsx:56-62) with no matching keybinding.",
     "severity": "major",
     "detail": "The north star is clearing the queue without touching the mouse; here every action \u2014 selecting a sick credential, probing, re-authorizing, adding \u2014 is mouse-only. The '/' glyph is a broken promise: it reads as a shortcut hint and does nothing. Page-level verbs (probe focused credential, re-authorize <sick provider>, add credential) belong in the palette via the existing registry."
    },
    {
     "title": "Unknown data rendered as confident claims: 'used by \u00b7 nobody yet', 'expires \u00b7 no expiry'",
     "evidence": "use-secrets-inventory.ts:241 hardcodes usedBy: [] (backend used_by is 'not yet persisted', secrets_v2.py:417-418), and PageSystem renders empty usedBy as 'nobody yet' (pages.tsx:1851-1853); the '*' \u2192 'every butler that talks to a model.' branch (pages.tsx:1854-1865) is dead code. use-secrets-inventory.ts:204-206 sets expires: null for all user credentials (entity_info has no expires_at), and PageUser renders that null as the claim 'no expiry' (pages.tsx:1264-1266) \u2014 false for Google test-mode consent which expires every 7 days (the page's own TestModeExpiryBanner says so, pages.tsx:753-763).",
     "severity": "major",
     "detail": "'Nobody yet' and 'no expiry' are assertions the system cannot prove \u2014 unknowns must render as '\u2014' or 'not tracked'. bu-0oyaw covers *populating* these fields eventually; the dishonest rendering of the current nulls is not filed anywhere. ANTHROPIC_API_KEY showing 'used by \u00b7 nobody yet' directly undercuts the trust the page exists to build."
    },
    {
     "title": "Dead and inert affordances in the header: noop IdentityChip onClick, KPI cells that are not doors",
     "evidence": "DirectionPassport.tsx:303-311: header IdentityChip has onClick={() => {/* handled via spine */}} \u2014 a cursor-inviting control that does nothing. DirectionPassport.tsx:315-335: the three KPI cells ('3 need hand', 'N unset', 'N expiring') are plain divs; the headline verdict 'N credentials need attention.' (lines 265-269) is likewise not a door to the needs-hand group.",
     "severity": "minor",
     "detail": "Every clause of the synthesized verdict should be one keystroke from evidence; here the page's own attention counts are terminal text. Clicking 'need hand' should scroll/filter the spine to the pinned group and focus its first row; the dead chip should either switch identity or be a non-interactive badge. (bu-9d5vp's inert-KPI item covers RuntimeSummaryKpi on other pages, not this surface.)"
    },
    {
     "title": "Failure and empty states are dead ends, and the attention voice line misstates CLI failure modes",
     "evidence": "SecretsPage.tsx:84-101: fetch failure renders only 'Failed to load credentials.' \u2014 no retry, no cause, no status. pages.tsx:2849-2864: PassportEmptyState says 'No page selected.', which on a zero-credential fresh instance is the entire right pane and teaches nothing. DirectionPassport.tsx:291-294: the voice line reads '{n} runtime token expiring.' but kpis.cli.attention counts all needsHand states including failed/revoked/warn (constants.ts:23-25), and has no pluralization.",
     "severity": "minor",
     "detail": "Recovery criterion of the design bar: the error must say what to do next (retry button + named cause). The empty state should teach the add/connect path that already exists one click away. The voice line calls a failed token 'expiring' \u2014 a wrong diagnosis in the editorial verdict; derive the copy per-state."
    }
   ],
   "page": "secrets",
   "verdict": "solid",
   "jarvis_gap": "The passport is structurally excellent \u2014 real inventory, wired verbs, honest sublines after the truth amnesty \u2014 but it still tells confident lies at the decision points: WhatBreaks answers 'nothing depends on this' for most credentials because of an unadopted slug alias, the system-page plaque shows green over a failing state, the test verb fabricates 0ms, and 'nobody yet'/'no expiry' assert unknowns as facts. Its doors are half-hung: the system audit link 422s, KPI counts and the verdict headline are terminal text, and the entire surface is mouse-only with zero palette commands despite the command spine existing. State arrives by 5-minute polling, not the fleet event bus, and green is unearned \u2014 'warn = never probed' rows sit until a human presses test.",
   "ideal": "The ideal /secrets is a credential ledger the owner can trust with deletions: it opens with a verdict whose every clause is a door ('2 need hand' focuses the pinned group; 'Telegram sick' opens that page), the spine walks with j/k and '/', and cmd-K carries the page's verbs (probe, re-authorize, rotate, add) scoped to the focused credential. Every claim is earned: dependency maps come from an alias-resolved catalogue that names itself when degraded, states on the plaque and spine are one contract, latency is measured or absent, and unknowns say 'not tracked' rather than 'nobody'/'no expiry'. A scheduled verification sweep probes every set credential daily so green means 'verified <time>' not 'set once', and a horizon strip forecasts the next thing that will break (test-mode consent in 3d, CLI token in 9d) each with its one-key remedy. Mutations and probe results arrive over the fleet event bus in the same frame, and each stamp deep-links into the audit spine without ever landing on an error.",
   "moves": [
    {
     "title": "Truth pass: fix WhatBreaks slug resolution and retire every unknown-as-claim rendering",
     "why": "The page's core promise is that no claim is unprovable; today its dependency map is false-negative for most credentials (deletion-risk), and 'nobody yet'/'no expiry'/'0ms' assert facts the system does not have.",
     "cost": "M",
     "slice_plan": "1) breaks-catalogue endpoint resolves ?provider= through _infer_provider_from_type (or matches both raw and aliased slugs) and gains a degraded:true flag on pool/table failure; FE 'unavailable' branch keyed off it. 2) PageSystem stops querying by category \u2014 either seed catalogue rows keyed by system key/category or render 'dependency map not tracked for system keys'. 3) usedBy: render 'not tracked' not 'nobody yet'; delete the dead '*' branch. 4) expires: null renders '\u2014', with the test-mode 7d consent surfaced as the real expiry when known. 5) probe merges pass d.latency_ms ?? null (pages.tsx:1139, 1762).",
     "integration_points": "src/butlers/api/routers/secrets_v2.py (breaks-catalogue endpoint, _infer_provider_from_type), src/butlers/catalogue_bootstrap.py, frontend/src/components/secrets/passport/pages.tsx, WhatBreaks.tsx, use-secrets-inventory.ts. Complements (does not overlap) bu-0oyaw, which tracks populating the underlying data."
    },
    {
     "title": "One state contract on PageSystem: plaque derives from credential.state",
     "why": "Green impersonating a failing credential on the detail page \u2014 while the spine dot says amber/red \u2014 is the most direct trust violation on the surface and a one-file fix.",
     "cost": "S",
     "slice_plan": "PageSystem plaque uses STATE_CATALOG[credential.state] for color/label exactly like PageUser/PageCli (pages.tsx:1648-1649); rowState (shared default / local override) demotes to a stateLine and the storage cross-ref. Add a regression test: system credential with state='failed' renders red plaque + 'failed' label.",
     "integration_points": "frontend/src/components/secrets/passport/pages.tsx (PageSystem), constants.ts STATE_CATALOG, existing PageSystem.actions.test.tsx."
    },
    {
     "title": "Adopt the command spine + keyboard pass for the passport",
     "why": "The mouse-only passport contradicts the clear-the-queue-without-the-mouse north star, and the decorative '/' glyph already promises a shortcut that doesn't exist.",
     "cost": "M",
     "slice_plan": "1) Bind '/' to focus SpineSearch; Escape clears/blurs. 2) ArrowUp/Down (and j/k) move spine focus, Enter selects (URL ?focus= as source of truth). 3) SecretsPage registers palette commands via useRegisterCommands: 'Add credential', 'Probe <focused credential>', plus one 'Re-authorize <provider>' per needs-hand credential. 4) Fix the header dead-chip: either opens the identity switcher or becomes a static badge.",
     "integration_points": "frontend/src/lib/command-registry.tsx (bu-86c4c.7 API, already proven on Approvals/Issues), Spine.tsx, DirectionPassport.tsx, SecretsPage.tsx. Distinct from in-flight bu-86c4c.15 (remedy verbs on signals) \u2014 this is palette/keyboard adoption for an already-verbed page."
    },
    {
     "title": "Every count a door: clickable verdict clauses, KPI cells, and repaired deep links",
     "why": "The page synthesizes attention counts but strands the owner at them; meanwhile its one system-audit door lands on a 422.",
     "cost": "S",
     "slice_plan": "1) Fix pages.tsx:1928 to `?key=s:${credential.key}` (and add a unit test asserting the prefix). 2) Make the '/ingestion/connectors/{provider}' label honest \u2014 link text matches href, or deep-link to the roster filtered by provider. 3) KPI 'need hand' cell and the headline clause set sort=severity and focus the first needs-hand row. 4) Per-state voice copy ('1 runtime token failed', '2 expiring') with pluralization.",
     "integration_points": "frontend/src/components/secrets/passport/pages.tsx (CrossRefFooter links), DirectionPassport.tsx (KpiCell \u2192 button, voice line), audit-log ?key= contract in src/butlers/core/credential_keys.py (no backend change needed)."
    },
    {
     "title": "Earned green: scheduled verification sweep + event-bus live state",
     "why": "Today green means 'a human once pressed test' (warn rows sit forever) and state freshness rides a 5-minute poll; the north star requires live streams and verdicts the system re-earns on its own.",
     "cost": "L",
     "slice_plan": "1) A daemon-side probe sweep (cron, reusing the existing probe paths + secret_probe_log + 5s/key rate limit) verifies every set credential daily, eliminating steady-state 'warn = needs probe'. 2) Emit credential.probe/credential.mutated events on the fleet event bus (bu-86c4c.8) and have useSecretsInventory subscribe-then-snapshot instead of refetchInterval. 3) A 'horizon' strip above the spine lists dated upcoming breakage (test-mode consent expiry from google-health status, CLI expires_at, stalest verification) each with its remedy verb \u2014 derived entirely from data already on the page.",
     "integration_points": "src/butlers/api/routers/secrets_v2.py probe internals + public.secret_probe_log, scheduler/tick infrastructure, fleet event bus (subscribe-before-snapshot pattern per PRs #2890/#2891), frontend/src/hooks/use-secrets-inventory.ts, DirectionPassport.tsx. Check bu-24lu6 push-on-park for the notification half; the sweep itself is uncovered by any open bead."
    }
   ]
  },
  {
   "page": "education",
   "verdict": "functional",
   "jarvis_gap": "The epic's two verified landings hold: fabricated mastery is purged (ReviewTimeline.tsx:48-55 now renders the real entry.mastery_score and falls back to a labeled status chip when null) and quiz-history rows gained real keyboard semantics (QuizHistoryList.tsx:113-129 role/tabIndex/keydown). Almost nothing else from the redesign reached this surface. /education is the clearest case of a pre-Dispatch island in a post-Dispatch app: the same education data rendered inside ButlerDetail's education tab (redesigned in .18) uses chartColor(), isError threading, and the shared dialect, while the dedicated page one route over still speaks 2025 shadcn \u2014 bold headings, card grids, nine hardcoded light-only hue families, raw #3b82f6 recharts fills, an uncontrolled tab set hiding the only time-critical signal, and zero commands in the palette registry. The deeper JARVIS gap is unchanged from the old dossier: the education butler is a live agent walking a five-phase teaching state machine and making time-bound promises (spaced reviews, curriculum builds), and the page still shows only its database artifacts \u2014 useTeachingFlows remains exported and imported by nothing, and a curriculum request still evaporates into a toast. The page reads the butler's tables instead of standing in for the butler; there is no verdict, no visible commitment, and no unbroken path from an overdue review to the session that scheduled it. (Error-impersonating-empty on this page is real but already owned by open bead bu-mkd5r and therefore not re-reported.)",
   "findings": [
    {
     "title": "NOT-LANDED: education charts bypass the chart-color spine shipped in bu-86c4c.5",
     "evidence": "frontend/src/components/education/MasteryTrendChart.tsx:55-56 (stroke=\"#3b82f6\" fill=\"#3b82f6\") and CrossTopicChart.tsx:42 (fill=\"#3b82f6\"); contrast frontend/src/components/butler-detail/ButlerEducationReviewsTab.tsx:43,627 which imports chartColor() from src/lib/chart-colors.ts (the \".5 single source of truth for recharts stroke/fill colors\")",
     "severity": "major",
     "detail": "The exact defect .5 was built to eliminate survives on both /education charts: raw hex blue that ignores the tuned --chart-1..5 oklch palette and does not adapt to theme. The same domain's data, rendered in the butler-detail console, already uses the spine \u2014 one dataset, two dialects, on adjacent routes."
    },
    {
     "title": "NOT-LANDED: light-only hardcoded status palettes across every education badge and node \u2014 dark theme broken, three-state-color rule violated",
     "evidence": "frontend/src/components/education/NodeDetailPanel.tsx:9-15, CurriculumActions.tsx:16-20, QuizHistoryList.tsx:8-21 (nine hue families incl. purple/sky/yellow), ReviewTimeline.tsx:18-21 (red/amber/blue/gray group borders), MindMapGraph.tsx:17-23 (five raw hexes); violates openspec/specs/dashboard-design-language/spec.md ('Exactly three state colors exist \u2014 red, amber, green', line ~81)",
     "severity": "major",
     "detail": "Every badge uses bg-emerald-100 text-emerald-800-style classes with no dark: variants \u2014 in dark theme they render as glowing light patches with broken contrast. The page deploys at least nine distinct hue families where the binding spec allows three state colors plus butler hue on letter-marks. None of the Dispatch primitives that now exist (Eyebrow, Pill, DisclosureRow, page.tsx) are imported by any education component."
    },
    {
     "title": "NOT-LANDED: judgment layer (bu-86c4c.17) never reached education \u2014 page opens with a tagline, not a verdict; overdue reviews buried behind an uncounted tab",
     "evidence": "frontend/src/pages/EducationPage.tsx:78-80 (static 'Adaptive learning dashboard: track mastery, review schedules, and curriculum progress.'), :102-107 (Tabs with bare labels Curriculum/Reviews/Analytics, no counts); overdue detection exists only inside ReviewTimeline.tsx:17-27 behind the Reviews tab",
     "severity": "major",
     "detail": "The one time-critical, owner-actionable signal on this surface \u2014 N reviews overdue \u2014 is computed but invisible on first glance. The north star requires every surface to open with the system's synthesized verdict; this page opens with marketing copy that never changes. The data needed for the verdict (pending reviews across all maps, mastery summaries) is already fetched by the page's own hooks."
    },
    {
     "title": "NOT-LANDED: the butler's agency is invisible \u2014 useTeachingFlows is dead code and a curriculum request leaves no visible commitment",
     "evidence": "frontend/src/hooks/use-education.ts:216-222 (useTeachingFlows exported; repo-wide grep shows zero importers outside the hook file); use-education.ts:301-304 success path is toast-only ('The butler will set it up within a few minutes'); EducationPage.tsx renders no flow/phase/pending-request surface",
     "severity": "major",
     "detail": "The education butler's five-phase state machine (diagnosing\u2192planning\u2192teaching\u2192quizzing\u2192reviewing) and its in-flight promises are unrepresented. After 'Request curriculum' the owner has no way to verify the tutor is working \u2014 the 409 'already pending' error remains the only evidence a request exists. This is the old dossier's core agency finding, untouched by the epic."
    },
    {
     "title": "NOT-LANDED: struggling-concept click is still a dead control from the Analytics tab",
     "evidence": "frontend/src/pages/EducationPage.tsx:102 (<Tabs defaultValue=\"curriculum\"> uncontrolled) + :115-119 (NodeDetailPanel rendered only inside the curriculum TabsContent) + :140-143 (StrugglingNodesCard onNodeClick={setSelectedNodeId} in the analytics TabsContent); StrugglingNodesCard.tsx:34-39 renders real buttons",
     "severity": "major",
     "detail": "Clicking the page's highest-value proactive signal ('what am I failing at') sets selectedNodeId but produces zero visible feedback because the detail panel lives in another tab. A real <button> that does nothing observable is a drill-down dead end on exactly the signal an operator would chase."
    },
    {
     "title": "NOT-LANDED: review entries are drill-down dead ends and the concept map opens its dossier only by mouse",
     "evidence": "frontend/src/components/education/ReviewTimeline.tsx:40-62 (ReviewEntryRow is a non-interactive div \u2014 no onClick, no link to node or curriculum); MindMapGraph.tsx:125-130 (panel opens only via NodeMouseHandler onNodeClick); ReviewTimeline.tsx:21 'Later' bucket actually covers only days 8-14 of the 14-day fetch horizon (use-education.ts:73), and :58 + QuizHistoryList.tsx:147 still use toLocaleDateString instead of the shared Time component",
     "severity": "major",
     "detail": "'closures is overdue' cannot be opened, traced to quiz history, or connected to the session that scheduled it \u2014 the trace-id spine stops at a div. Keyboard users cannot open any node dossier at all. The 'Later' label silently hides everything beyond two weeks, a small honesty defect ('no number fabricated' includes labels)."
    },
    {
     "title": "NOT-LANDED: zero education actions in the command palette despite the per-page registration API existing",
     "evidence": "grep of frontend/src/components/layout/GlobalActionsRegistrar.tsx for education/curriculum: no hits; repo-wide, only ApprovalsPage.tsx and IssuesPage.tsx call the per-page command-registration API (src/lib/command-registry.tsx); 'Request curriculum' is reachable only via the mouse button at EducationPage.tsx:82-84",
     "severity": "minor",
     "detail": "The one-command-spine (.7) shipped the registry and navigation to /education exists via the pages group, but none of this page's verbs (request curriculum, jump to curriculum N, open overdue reviews) are registered \u2014 the owner cannot clear the education queue without the mouse, violating the keystroke-to-anything bar."
    },
    {
     "title": "NEW: education is the last poll-only surface \u2014 it grew bespoke poll-backoff machinery instead of joining the fleet event bus (.8)",
     "evidence": "frontend/src/hooks/use-education.ts:115-122 (scaledPollInterval) and 15-60s refetchInterval on all ~12 hooks (:30,:40,:50,:60,:75 etc.); contrast use-sessions.ts:52 and use-spend.ts:95 ('fleet-event-bus-driven, poll is now a safety net'); MindMapGraph.tsx:33-38 also runs an unconditional animate-pulse loop on frontier nodes with no prefers-reduced-motion guard, inside a fixed h-96 canvas (MindMapGraph.tsx:170) with no legend for its five-color/pulse/dash encodings",
     "severity": "minor",
     "detail": "The north star says state arrives as a live event stream; sessions and spend were converted, education was not \u2014 instead ~3 polled queries per active map fire on a hand-tuned backoff curve. The event bus already exists; education events (review completed, flow phase change, curriculum built) would replace the whole scaledPollInterval apparatus. The perpetual pulse animation is the page's only motion and it is decorative state-signaling that never rests."
    }
   ],
   "ideal": "The page opens with the tutor's own verdict in the Dispatch voice \u2014 one serif line synthesized from data already on hand (\"Two reviews overdue on Linear Algebra; the TCP/IP curriculum finished its diagnostic overnight \u2014 teaching resumes at your word.\") \u2014 above a hairline KPI strip (due now / overdue / concepts mastered this week / next scheduled review) where every cell is a door. Below it, the manifesto's \"learning territory\" is the full-height centerpiece: a theme-tokened concept map (three state colors plus neutral, an always-visible legend, butler hue only on the letter-mark) whose every node, review row, and struggling concept opens the same node dossier drawer \u2014 mastery trajectory, full quiz transcript with evaluator notes, SM-2 schedule, and a trace-id link to the butler session that taught or quizzed it. The butler's agency is a first-class surface: each curriculum card carries its live teaching-flow phase chip and every promise (a requested curriculum, a scheduled review) renders as a tracked in-progress commitment until it resolves, updated over the fleet event bus in the same frame the butler acts. Errors name themselves with retry, empties are one serif-italic sentence, and the whole queue \u2014 open overdue review, request curriculum, jump to curriculum \u2014 clears from the command palette without touching the mouse.",
   "moves": [
    {
     "title": "Tutor-status hero: verdict line + phase chips + visible commitments",
     "why": "Closes the three biggest NOT-LANDED gaps at once (judgment layer, invisible agency, buried overdue signal) using data the page already fetches: pending reviews, mastery summaries, and the dead useTeachingFlows hook.",
     "cost": "M",
     "integration_points": "frontend/src/pages/EducationPage.tsx header (replace :75-85), use-education.ts useTeachingFlows (:216-222, first real consumer), useAllPendingReviews for counts; Dispatch Voice surface per openspec/specs/dashboard-design-language/spec.md (Voice section); pending-request row keyed off the curriculum_request POST + 409 state",
     "slice_plan": "Slice 1: overdue/due-today counts lifted into a KPI strip + tab badges. Slice 2: per-curriculum phase chip from useTeachingFlows. Slice 3: serif verdict line composed from counts+phases. Slice 4: pending-curriculum commitment row that appears on request and resolves when the map lands."
    },
    {
     "title": "Dispatch reskin sweep of all nine education components",
     "why": "Eliminates the last pre-Dispatch island: nine hue families \u2192 three state colors + neutral, cards \u2192 hairline rows/eyebrows, raw #3b82f6 \u2192 chartColor(), light-only badges \u2192 Pill tokens, bold heading \u2192 Page primitive. ButlerEducationReviewsTab.tsx is the in-repo reference implementation for the same data.",
     "cost": "M",
     "integration_points": "MasteryTrendChart.tsx:55, CrossTopicChart.tsx:42 \u2192 src/lib/chart-colors.ts; NodeDetailPanel/CurriculumActions/QuizHistoryList/ReviewTimeline STATUS maps \u2192 components/ui/Pill.tsx + state tokens; MindMapGraph STATUS_COLORS \u2192 3-state scheme with legend; EducationPage \u2192 components/ui/page.tsx; the eslint hsl(var()) guard already exists",
     "slice_plan": "Slice 1: charts onto chartColor() (two-line diffs). Slice 2: badge maps onto Pill/state tokens with dark-theme parity. Slice 3: map node colors collapsed to mastered=green / struggling=amber / overdue=red / neutral, legend added, pulse gated on prefers-reduced-motion. Slice 4: page shell onto Page/Eyebrow with hairline KPI strip."
    },
    {
     "title": "One node dossier reachable from every signal",
     "why": "Kills both drill-down dead ends (struggling-node click that does nothing; review rows that are inert divs) by making one drawer the single destination from graph node, review entry, struggling concept, and quiz row \u2014 and gives it the trace link to the originating butler session.",
     "cost": "M",
     "integration_points": "EducationPage.tsx:102 (controlled Tabs or a page-level drawer replacing the in-tab NodeDetailPanel at :115-119); ReviewTimeline.tsx ReviewEntryRow \u2192 DisclosureRow/RowLink (components/ui/DisclosureRow.tsx); quiz_responses already carry session linkage via the education API \u2014 surface a /sessions/{id} link in the dossier",
     "slice_plan": "Slice 1: make Tabs controlled; struggling-node click switches tab and opens the panel (fixes the dead control in isolation). Slice 2: promote NodeDetailPanel to a page-level drawer with keyboard open/close. Slice 3: review rows become buttons opening the drawer. Slice 4: add originating-session deep link to quiz entries."
    },
    {
     "title": "Register education verbs in the command spine",
     "why": "The registry and per-page registration API exist (.7) and two pages prove the pattern; education's whole queue (request curriculum, open overdue reviews, jump to curriculum) is currently mouse-only, failing the clear-the-queue-without-the-mouse bar.",
     "cost": "S",
     "integration_points": "src/lib/command-registry.tsx per-page API (pattern: pages/ApprovalsPage.tsx, IssuesPage.tsx); EducationPage.tsx dialog state (:30) and map selector (:88-99); overdue counts from useAllPendingReviews",
     "slice_plan": "Slice 1: 'Request curriculum' + 'Open overdue reviews' page commands. Slice 2: dynamic per-curriculum jump commands from the live map list. Slice 3: global (non-page-scoped) 'Request curriculum' in GlobalActionsRegistrar so it works from anywhere, like 'Trigger <butler>'."
    },
    {
     "title": "Put education on the fleet event bus; demote polling to safety net",
     "why": "Education is the last surface where state arrives by timer instead of stream \u2014 ~3 polled queries per active map on a hand-tuned backoff curve (scaledPollInterval). Bus-driven invalidation makes a completed review or flow-phase change appear in the same frame and deletes the bespoke machinery.",
     "cost": "M",
     "integration_points": "Backend: emit education events (review_completed, flow_phase_changed, mind_map_created/updated) onto the bu-86c4c.8 fleet event bus alongside session/spend events; frontend: bus-driven queryClient.invalidateQueries(['education', ...]) mirroring use-sessions.ts:52 / use-spend.ts:95, then lengthen refetchInterval to the safety-net cadence and retire scaledPollInterval (use-education.ts:115-203)",
     "slice_plan": "Slice 1: emit + subscribe for mind-map and review events, invalidate on receipt. Slice 2: extend to teaching-flow phase events (feeds the hero's phase chips live). Slice 3: demote all education polls to safety-net intervals and delete the scaledPollInterval apparatus + its tests."
    }
   ]
  },
  {
   "page": "chronicles",
   "verdict": "functional",
   "jarvis_gap": "The page has the right editorial skeleton (Voice, KPI strip, attention, recent-days index, below-fold day detail) and the IEA epic will fix the data model, but as an instrument it fails \"moves as fast as thought\" and \"one keystroke from any signal to evidence\": every day-step blanks the entire page to a skeleton (no placeholderData, no adjacent-day prefetch), there is not a single keyboard or palette route to any archive action, the primary drill-down (Gantt episode \u2192 drawer) is mouse-only and invisible to screen readers, the Refresh control never refreshes the editorial half it sits under, two fetch failures render as empty days, and the source-state strip asserts today's adapter health on any past day. Judgment prose exists but its provenance (templated vs LLM) is silently dropped, and no clause of it is a door.",
   "findings": [
    {
     "title": "Every day-step blanks the whole page to a skeleton; no keepPreviousData, no adjacent-day prefetch",
     "evidence": "frontend/src/hooks/use-chronicles-briefing.ts:33-43 (useQuery with no placeholderData/keepPreviousData; queryKey changes per date) + frontend/src/pages/ChroniclesPage.tsx:218 (`loading={!data && !isError}`) + frontend/src/components/ui/page.tsx:371-409 (loading replaces all children with WorkspaceSkeleton). Stepping prev/next changes the query key, data becomes undefined, and the Voice, KPI strip, attention list, recent-days index, and the entire drilldown unmount into a skeleton until the fetch lands. The Elaboration cross-fade path (isFetching opacity, Elaboration.tsx) is dead code for date changes because data is never retained across keys. prevIsoDay/nextIsoDay are deterministic, yet nothing prefetches the adjacent days.",
     "severity": "critical",
     "detail": "Archive browsing (stepping back through days) is this page's hottest path \u2014 the design-bar 'Pace' and 'Habit' questions both fail. The north star demands owner actions apply in the same frame; here even a read-only navigation flashes the whole instrument away."
    },
    {
     "title": "Refresh button never refreshes the editorial surface it governs \u2014 including the 'stale' pill it exists to clear",
     "evidence": "frontend/src/components/chronicles/ManualRefreshButton.tsx:50-56 invalidates chroniclesKeys.byDay/byCategory/dayClose/sourceState/pointEvents only; the briefing lives under a different key family, chroniclesBriefingKeys = [\"chronicler\",\"briefing\",...] (frontend/src/hooks/use-chronicles-briefing.ts:25-29), which is never invalidated. The 'stale' indicator (ChroniclesPage.tsx:211,254-262) is derived from that briefing query's voice_source.",
     "severity": "major",
     "detail": "Walkthrough 'Repetition' failure: owner sees 'stale', clicks Refresh, spinner completes, headline/voice/KPI/attention/stale pill all unchanged \u2014 feedback failed, repeat clicks follow. Fix is one shared per-day invalidation family covering both halves of the page."
    },
    {
     "title": "Point-events fetch failure impersonates an empty day (no trail, no scrub snaps, no degradation notice)",
     "evidence": "frontend/src/components/chronicles/ChroniclesDrilldownPanel.tsx:95 destructures only `data` from useChroniclesPointEvents; isError is discarded. On failure, Scrubber gets snapMs=[] (line 164) and FloatingMapMinimap gets trailPoints=[] (line 178), rendering exactly like a day with no movement. Contrast: byDay/byCategory correctly pass isError+onRetry to their charts (lines 185-196).",
     "severity": "major",
     "detail": "Five-sins instance (error impersonating empty), new \u2014 not in the 2026-07-03 dossier's landed fixes, which covered chart/list surfaces, not the scrubber/map evidence feed."
    },
    {
     "title": "Source-state strip asserts TODAY's adapter health on any past day, and swallows its own fetch errors into null",
     "evidence": "frontend/src/components/chronicles/SourceStateBadgeStrip.tsx:172-186: useChroniclesSourceState() takes no date; rendered unconditionally inside the day-scoped drilldown (ChroniclesDrilldownPanel.tsx:155). On /chronicles?date=<last month>, badges claim sources are 'active'/'no recent data' as of now, presented as context for that day's reconstruction. Additionally rows fall back to [] on error and the component returns null (lines 186, 209) \u2014 a failed fetch removes the strip entirely. Off-token `bg-yellow-500 text-white` (line 113) is a fourth state color outside the Dispatch triad.",
     "severity": "major",
     "detail": "A retrospective page needs per-day evidence coverage ('which sources contributed to THIS day, which were silent'), not live ops state. IEA plans per-activity evidence chains but no day-level coverage surface, so this is unclaimed territory."
    },
    {
     "title": "Gantt episodes \u2014 the page's main drill-down door \u2014 are mouse-only and invisible to assistive tech",
     "evidence": "frontend/src/components/chronicles/GanttSwimlaneInner.tsx:318-324: `<g role=\"button\" onClick=...>` with no tabIndex and no onKeyDown, nested inside the timeline SVG declared `role=\"img\"` at line ~600-601 \u2014 role=img flattens all descendants for screen readers, so the role=button children are unreachable by both keyboard and AT. EpisodeDrawer (evidence, corrections) is therefore reachable only by pointer.",
     "severity": "major",
     "detail": "New instance beyond bu-f310e (which targets DisclosureRow on facts/rules/audit HTML rows, not SVG interactive marks) and beyond the landed axe floor (bu-86c4c.16), since axe does not flag unfocusable SVG role=button under role=img reliably. IEA's Day Ribbon replacement must not inherit this."
    },
    {
     "title": "Zero keyboard or palette surface for the archive: no arrow-key stepping, no jump-to-date, no chronicles commands",
     "evidence": "grep across frontend/src/pages/ChroniclesPage.tsx, pages/chronicles-date-nav.ts, components/chronicles/*.tsx: no onKeyDown/keydown listeners anywhere; frontend/src/components/layout/GlobalActionsRegistrar.tsx (47 lines) registers only 'Trigger <butler>' commands \u2014 nothing for chronicles. The eyebrow date (ChroniclesPage.tsx:236-244) is static text, not a date picker; the only navigation is two icon-xs buttons and the recent-days list.",
     "severity": "major",
     "detail": "North star: 'one command palette from a single registry' and clearing surfaces 'without touching the mouse'. Reaching a day N weeks back costs N clicks on a 24px target or hand-editing ?date=. Needs Left/Right day stepping, 'latest' key, and palette verbs (previous/next day, jump to date)."
    },
    {
     "title": "Templated fallback voice impersonates the composed Voice \u2014 degraded source does not name itself",
     "evidence": "voice_source has three values \"llm\u00b7cached\" | \"templated\" | \"stale\" (frontend/src/api/types.ts:6089; roster/chronicler/api/router.py:2255-2266, src/butlers/chronicler/editorial.py:134 templated_voice_paragraph). The frontend surfaces only 'stale' (ChroniclesPage.tsx:211 `isStale = data?.voice_source === \"stale\"`); a 'templated' paragraph renders in the identical serif Voice with no provenance marker. The stale marker itself is a 9px non-interactive label whose explanation lives only in a title tooltip (lines 254-262).",
     "severity": "minor",
     "detail": "The north star's earned-calm clause is 'every degraded source names itself'. The data is honest but its provenance is dropped; a one-word mono eyebrow ('composed' vs 'templated') restores the contract."
    },
    {
     "title": "Chronicles KPI cells are inert \u2014 'Longest gap' and 'Longest episode' name spans on the timeline directly below but do not link or scrub to them",
     "evidence": "frontend/src/pages/ChroniclesPage.tsx:100-126 buildKpiCells emits eyebrow/value/delta only; frontend/src/components/overview/KpiStrip.tsx:18-26 has no href/onClick affordance at all. The Gantt + Scrubber that could display the exact gap/episode span render on the same page (ChroniclesDrilldownPanel.tsx:160-175).",
     "severity": "minor",
     "detail": "New instance distinct from bu-9d5vp (which covers RuntimeSummaryKpi on the dashboard drill-down sweep). Unlike those, this page already has the evidence surface below the fold \u2014 the door costs a scrollIntoView + setScrubberMs, no backend."
    }
   ],
   "ideal": "Opening /chronicles lands on yesterday already rendered from cache: the Voice verdict in serif with a mono provenance eyebrow (composed \u00b7 templated \u00b7 stale \u2014 stale carrying an inline \"refresh day close\" verb), and every clause of that prose a door \u2014 \"led by work\" opens the Work lane on the timeline, \"sleep was short\" scrubs to the sleep span. Left/Right arrows step days with zero blanking (previous day's page stays up under a 200ms cross-fade while the prefetched adjacent day swaps in); `g` or the palette offers \"jump to date\", and the eyebrow date is itself a calendar. The KPI cells are doors: \"Longest gap 7h\" scrolls and scrubs the timeline to that exact gap. Below the fold, the day detail opens with a per-day evidence-coverage strip \u2014 which sources testified for THIS day, which were silent, which erred, in the three Dispatch state colors \u2014 never today's ops health, and never null on a failed fetch: a failed feed names itself in-place. The timeline's episodes are focusable marks (roving tabindex, Enter opens the drawer) so detect\u2192diagnose\u2192act completes keyboard-only, and the IEA Day Ribbon inherits that skeleton. One refresh verb invalidates the whole day coherently, editorial and detail alike.",
   "moves": [
    {
     "title": "Instant archive stepping: keepPreviousData + adjacent-day prefetch",
     "why": "Kills the full-page skeleton flash on the hottest path (finding 1). The page's own cross-fade design (Elaboration isFetching opacity) only works once data persists across date keys.",
     "cost": "S",
     "slice_plan": "1) Add placeholderData: keepPreviousData to useChroniclesBriefing and the drilldown queries (episodes/aggregates/pointEvents keyed by window); gate Page loading on first-ever load only. 2) On briefing settle, queryClient.prefetchQuery for prevIsoDay/nextIsoDay briefings (bounded by earliest/latest).",
     "integration_points": "frontend/src/hooks/use-chronicles-briefing.ts, frontend/src/hooks/use-chronicles.ts, frontend/src/pages/ChroniclesPage.tsx loading gate; no backend changes."
    },
    {
     "title": "Keyboard + palette surface for the archive",
     "why": "Zero keyboard routes today (finding 6); north star requires mouse-free operation from one command registry.",
     "cost": "M",
     "slice_plan": "1) Page-level keydown: ArrowLeft/ArrowRight step days, Home/latest key returns to yesterday, guarded by the shared editable-field keydown guard (bu-5o22a). 2) Register chronicles actions in the single cmdk registry: previous/next day, latest day, jump to date. 3) Make the eyebrow date a button opening a calendar popover clamped to [earliest, latest].",
     "integration_points": "frontend/src/pages/ChroniclesPage.tsx, frontend/src/pages/chronicles-date-nav.ts, the cmdk registry from bu-86c4c.7 (GlobalActionsRegistrar pattern), shared keydown guard from bu-5o22a."
    },
    {
     "title": "One coherent per-day refresh + voice provenance eyebrow",
     "why": "Refresh currently skips the entire editorial half and the stale pill it should clear (finding 2); templated voice silently impersonates composed judgment (finding 7).",
     "cost": "S",
     "slice_plan": "1) Introduce a day-scoped invalidation helper covering chroniclesBriefingKeys.byDate plus all drilldown families; ManualRefreshButton and the stale pill both call it. 2) Render voice_source as a mono eyebrow on the Voice (composed / templated / stale), with stale carrying the refresh verb inline.",
     "integration_points": "frontend/src/components/chronicles/ManualRefreshButton.tsx, frontend/src/hooks/use-chronicles-briefing.ts (export key family), ChroniclesPage.tsx stale span; coordinates with bu-86c4c.15 act-loop verbs but is chronicles-scoped."
    },
    {
     "title": "Day-scoped evidence coverage strip (replace live SourceStateBadgeStrip in the drilldown)",
     "why": "Today's adapter health is the wrong truth for a retrospective day (finding 4), and the strip nulls out on error; IEA plans per-activity evidence chains but no day-level coverage summary.",
     "cost": "M",
     "slice_plan": "1) Backend: small endpoint deriving per-source contribution for a day window (episodes/events count per source, last error within window) \u2014 data already in chronicler tables. 2) Frontend: coverage strip in Dispatch tokens (three state colors, no bg-yellow-500) showing contributed/silent/erred-for-that-day, each badge a door to that source's events; failed fetch renders a named degradation row, never null. 3) Move the live ops strip to wherever ops belongs (butler console), not the archive.",
     "integration_points": "roster/chronicler/api/router.py + models.py, frontend/src/components/chronicles/SourceStateBadgeStrip.tsx (replace), ChroniclesDrilldownPanel.tsx:155; align lane names with IEA's 8-lane taxonomy (bu-8whey5) so it lands once."
    },
    {
     "title": "Keyboard-operable timeline marks + KPI cells as timeline doors",
     "why": "The page's central drill-down is pointer-only and AT-invisible (finding 5), and the KPI numbers name spans the timeline below could show (finding 8). The IEA Day Ribbon will inherit whatever skeleton exists \u2014 fix it before the ribbon lands.",
     "cost": "M",
     "slice_plan": "1) Replace SVG role=\"img\" with role=\"group\" + labelled children; give episode <g> elements tabIndex via roving focus, Enter/Space opens EpisodeDrawer, visible focus ring on the bar rect. 2) Also fix the swallowed pointEvents error (surface isError as an in-place degradation line by the scrubber). 3) Make 'Longest gap'/'Longest episode'/'Top lane' KPI cells buttons that scroll to the timeline and set scrubber/zoom to the named span (extend KpiStrip with an optional onActivate per cell).",
     "integration_points": "frontend/src/components/chronicles/GanttSwimlaneInner.tsx, ChroniclesDrilldownPanel.tsx (pointEvents isError, scrubber state lift), frontend/src/components/overview/KpiStrip.tsx (optional interactive cell), ChroniclesPage.tsx buildKpiCells; hand the focus/roving pattern to bu-8whey5's Day Ribbon as its accessibility contract."
    }
   ]
  },
  {
   "page": "qa",
   "verdict": "functional",
   "jarvis_gap": "The QA dossier is structurally the most JARVIS-shaped surface in the product \u2014 claim-anchored diagnosis prose with numbered footnotes cross-highlighting an evidence log is exactly \"every clause is a door.\" But the doors are painted on: cross-highlighting is hover-only and evidence lines terminate in plain text with no trace-id/session/patrol links, so the spine ends at prose. Worse, the surface still fabricates on its default screen \u2014 every in-flight case's fix panel claims \"Escalated to user,\" the new patrol pulse strip renders findings-dispatched patrols as clean-green because it string-matches the wrong status value, and the KPI strip prints \"0 awaiting CI \u00b7 0 escalated\" while the summary fetch is failing. The five-second health verdict is impossible: the API ships staffer_status, last/next patrol, circuit breaker, and credential health, and the page renders none of them. And the whole surface registers zero palette commands \u2014 the one page about the system healing itself cannot be triaged without a mouse.",
   "findings": [
    {
     "title": "NOT-LANDED: 'No PR. Escalated to user.' fabricated for every in-flight case",
     "evidence": "frontend/src/components/qa/PRPanel.tsx:26-31 renders this copy whenever pr is null; CaseDossier.tsx:150-157 mounts PRPanel unconditionally and never passes dossier.state_track_stage, so cases in detect/diagnose \u2014 the default auto-selected case on /qa is often one \u2014 tell the owner they were escalated when diagnosis is still running. The stage is available two lines up (CaseDossier.tsx:87) and unused.",
     "severity": "critical",
     "detail": "Old-dossier critical, verbatim un-landed on current main. Direct violation of 'no number is ever fabricated' on the surface whose whole job is trust in the staffer's own account of itself."
    },
    {
     "title": "Patrol pulse strip colors findings-dispatched patrols clean-green (status string mismatch)",
     "evidence": "frontend/src/pages/QaOverviewPage.tsx:292-296 statusDotClass checks status === \"dispatched\", but the backend emits \"findings_dispatched\" (src/butlers/api/routers/qa.py:1631-1637; src/butlers/modules/qa/__init__.py:1385) \u2014 the amber branch is dead code, so a patrol that found and dispatched problems renders the same emerald dot as a clean patrol; \"running\" and \"skipped_overlap\" also fall through to green.",
     "severity": "major",
     "detail": "NEW bug introduced with the .19 pulse strip. Green-as-verified-clean is the one color promise the console must keep; here it actively hides the patrols most worth clicking."
    },
    {
     "title": "NOT-LANDED: patrol journal hardcoded to UTC beside viewer-local timestamps",
     "evidence": "frontend/src/components/qa/PatrolJournal.tsx:26 formats with timeZone: \"UTC\" while the dossier header's 'detected' time (components/qa/utils.ts:21-41) and every Time component render viewer-local \u2014 same screen, two unlabeled timezones, off by the owner's UTC offset when correlating journal decisions against detection time.",
     "severity": "major",
     "detail": "Old-dossier critical, un-landed. Fix is one line: render via the shared Time component."
    },
    {
     "title": "NOT-LANDED: staffer heartbeat, circuit breaker, and credential health never rendered on /qa",
     "evidence": "QaSummary already returns staffer_status, last_patrol_at, next_patrol_at, circuit_breaker.tripped, credentials_status (frontend/src/api/types.ts:4967-4990), but the /qa header renders only port/model/interval (QaOverviewPage.tsx:239-247). The breaker is visible only on the butler-detail QA tab (ButlerQaInvestigationsTab.tsx:486) and credentials only on the settings card (settings/QaStafferCard.tsx:125). The empty state is a bare 'Nothing in the dossier.' (QaOverviewPage.tsx:463-466, 483-484) that cannot distinguish 'patrols clean' from 'patrols dead'.",
     "severity": "major",
     "detail": "Fails the five-second verdict: zero cases + tripped breaker looks identical to zero cases + healthy staffer. The manifesto's named failure mode (missing GH token) is invisible from the page it breaks."
    },
    {
     "title": "Case rail silently truncates at the backend's 25-row default with no count or pagination",
     "evidence": "QaOverviewPage.tsx:355-360 calls useQaCases without limit/offset; backend defaults limit=25 (src/butlers/api/routers/qa.py:1863); the page reads only cases.data?.data (line 407) and never meta.total; CaseList.tsx renders no count, no 'load more', header just says 'Cases \u00b7 last 7d'.",
     "severity": "major",
     "detail": "NEW (the folded investigations index had explicit shown/loaded/total meta; the fold dropped it). With >25 matching cases the 26th is unreachable from anywhere and the owner has no signal that the list is partial \u2014 completeness fabrication."
    },
    {
     "title": "Zero keyboard/palette surface: no commands registered, claim-evidence spine is hover-only, butler menu unclosable by keyboard",
     "evidence": "No Qa* file calls useRegisterCommands (ApprovalsPage.tsx:1411 and IssuesPage.tsx:88 do) \u2014 Force patrol, severity/since/state filters, and case selection are mouse-only. Claim\u2194evidence cross-highlight fires only on onMouseEnter/onMouseLeave with no tabIndex or focus handlers (ClaimAnchoredBlurb.tsx:66-67, EvidenceLog.tsx:86-91); the claim note lives solely in a title attribute (ClaimAnchoredBlurb.tsx:65). The butler multi-select has role=menu but no Escape/arrow handling, its outside-click ref is created and never wired (QaOverviewPage.tsx:103), and it closes only on onMouseLeave (line 194).",
     "severity": "major",
     "detail": "NEW. The command spine (.7) landed as infrastructure and this page opted out entirely; keyboard triage (.14) covered approvals only. Screen-reader and keyboard users get evidence rows with no way to see which claims they support."
    },
    {
     "title": "Evidence lines and journal entries are drill-down dead ends \u2014 the trace spine ends at prose",
     "evidence": "EvidenceLog rows render ts/lvl/butler/msg as plain spans (EvidenceLog.tsx:102-113) with no link to the session, log stream, or trace id; PatrolJournal entries render ts/step/text/detail only (PatrolJournal.tsx:55-80) and never link a tick to its patrol even though QaJournalEvent carries a data bag (api/types.ts:5075) and /qa/patrols/:id exists; patrol detail links forward to investigations but nothing links backward from a case to the patrol that detected it.",
     "severity": "major",
     "detail": "Partially NOT-LANDED remnant of the old orphan-routes finding (.19 landed the pulse strip but not journal/finding\u2192patrol links) plus new scope: the evidence log is the exact moment of highest owner scrutiny and it offers zero onward doors to root evidence."
    },
    {
     "title": "Errors impersonating data: KPI sub-line fabricates zeros and the pulse strip hides fetch failure; NOT-LANDED minors persist",
     "evidence": "QaKpiStrip.tsx:34-38 formatActiveBreakdown defaults to 0, so during summary load/failure the active-cases cell shows '\u2014' over a confident '0 awaiting CI \u00b7 0 escalated'. PatrolPulseStrip returns null on isError (QaOverviewPage.tsx:302) so an unreachable patrols endpoint renders as 'no strip', indistinguishable from no patrols. Un-landed old minors: dual severity vocabularies with lossy color mapping (QaPatrolDetailPage.tsx:34-40: 0/1 both destructive, 3/4 both muted) vs 3-level cases; drafted and closed PR dots identical (CaseList.tsx:15-20); native window.confirm on force patrol (QaOverviewPage.tsx:394); investigation-detail API errors still render 'Investigation not found.' (QaInvestigationDetailPage.tsx:39-48).",
     "severity": "minor",
     "detail": "Each is small; together they are three instances of the errors-impersonating-empty sin plus the un-landed vocabulary drift the old audit already named."
    }
   ],
   "ideal": "/qa opens with a verdict masthead in the Dispatch voice \u2014 \"Staff is healthy. Last patrol 4m ago, clean; next in 6m. Two cases need you.\" \u2014 synthesized from the summary fields the API already ships (staffer_status, last/next patrol, breaker, credentials), with a red banner and one-keystroke jump to /secrets when the breaker trips or the GH token is missing. The pulse strip tells the truth per status (green only for clean, amber for findings-dispatched, pulsing for running, red for error), the rail states \"25 of 41 \u00b7 load more\" from meta.total, and the empty state radiates earned calm (\"No open cases \u2014 last patrol 4m ago, clean\"). The dossier stays the crown jewel but every clause becomes a real door: claim footnotes and evidence rows are focusable, cross-highlight on focus as well as hover, and each evidence line and journal tick links to its session, trace, and originating patrol in one keystroke. The fix panel is stage-aware \u2014 \"escalated\" appears only when the case is escalated, with the reason and a decide-now verb; in-flight cases say \"diagnosing, started 3m ago.\" Everything is palette-registered (force patrol, filter by severity/state/butler, next/previous case via j/k, dismiss, retry), all timestamps share one viewer-local clock, and no fetch failure ever wears the costume of an empty queue.",
   "moves": [
    {
     "title": "Dossier truth pass: stage-aware fix panel, one timezone, honest error copy",
     "why": "Kills both un-landed criticals and the error/404 conflation in one sweep \u2014 the default screen stops lying about escalation and stops rendering two clocks.",
     "cost": "S",
     "slice_plan": "1) Pass dossier.state_track_stage into PRPanel and branch the no-PR copy (detect/diagnose \u2192 'Diagnosing \u2014 no PR yet'; escalated \u2192 'Escalated: needs your decision'; landed-without-PR \u2192 'No PR captured'). 2) Replace PatrolJournal's UTC formatter with the shared Time component. 3) Split QaInvestigationDetailPage error vs not-found copy; drop window.confirm on force patrol in favor of pending-state + toast.",
     "integration_points": "frontend/src/components/qa/PRPanel.tsx, CaseDossier.tsx:150-157, PatrolJournal.tsx:23-27, QaInvestigationDetailPage.tsx:28-48, QaOverviewPage.tsx:392-405; no backend changes."
    },
    {
     "title": "Staffer heartbeat masthead + earned-calm empty state",
     "why": "Turns the page's first glance into the five-second verdict; the data is already in QaSummary and rendered nowhere on /qa.",
     "cost": "M",
     "slice_plan": "Add a verdict line under the H1: 'last patrol 4m ago \u00b7 next in 6m \u00b7 breaker OK \u00b7 credentials OK', red banner linking to /secrets when circuit_breaker.tripped or gh_token_present is false (surface provisioning_hint verbatim). Rewrite both empty states to include last-patrol recency and cleanliness, linking to the most recent patrol.",
     "integration_points": "QaOverviewPage.tsx PageHeader (239-270) and empty states (463-466, 483-488); fields from api/types.ts:4967-4990 already fetched by useQaSummary; nav badge 'qa-escalations' (nav-config.ts:72) stays the cross-surface signal."
    },
    {
     "title": "Honest strip and rail: fix status mapping, show totals, stop zero-fabrication",
     "why": "Three confirmed fabrication/error-masking bugs (green findings_dispatched dots, silent 25-row cap, '0 awaiting CI' during failed fetch) are each a few lines to fix and directly restore the no-fabrication invariant.",
     "cost": "S",
     "slice_plan": "1) statusDotClass keyed to real enum: clean\u2192green, findings_dispatched\u2192amber, running\u2192pulsing muted, error/failed\u2192red, skipped_overlap\u2192muted; add a visible findings count per dot. 2) Read meta.total, render 'N of M' in the rail header with a load-more (offset) affordance. 3) formatActiveBreakdown returns '\u2014' when active is undefined; PatrolPulseStrip renders a one-line 'patrol history unavailable' on isError instead of null.",
     "integration_points": "QaOverviewPage.tsx:292-330, 355-360, 407; CaseList.tsx header; QaKpiStrip.tsx:34-38; backend already returns meta (routers/qa.py:1864-1867)."
    },
    {
     "title": "Keyboard triage: palette commands, j/k rail, focusable claim-evidence spine",
     "why": "The north star is clearing the queue without touching the mouse; QA is currently the only judgment-heavy surface with zero registered commands and a hover-only evidence spine.",
     "cost": "M",
     "slice_plan": "1) useRegisterCommands on QaOverviewPage: force patrol, set severity/since/state, toggle butler, next/previous case, dismiss, retry (mirroring ApprovalsPage's pattern). 2) j/k + Enter selection in CaseList. 3) Make claim spans and evidence rows tabbable with onFocus/onBlur driving the same hover-state, and render the claim note visibly on focus (not title attr). 4) Butler menu: Escape close, wire the existing outside-click ref, arrow navigation.",
     "integration_points": "lib/command-registry.tsx (landed .7), ApprovalsPage.tsx:1411 as template, ClaimAnchoredBlurb.tsx:56-79, EvidenceLog.tsx:77-92, QaOverviewPage.tsx:102-213; distinct from in-flight bu-86c4c.15 (remedy verbs) \u2014 this is reachability of existing verbs."
    },
    {
     "title": "Trace-spine the evidence: link evidence lines, journal ticks, and findings to sessions and patrols",
     "why": "The claim-anchored dossier is one link-layer away from being the product's best proof surface; today every investigation ends in unlinked prose.",
     "cost": "L",
     "slice_plan": "1) Backend: include session_id/patrol_id/trace_id in evidence_lines and journal event payloads (journal already carries a data bag \u2014 api/types.ts:5075 \u2014 populate and type it). 2) Frontend: each evidence row gets a mono anchor to /sessions/:id or the log stream at that timestamp; each journal tick links to /qa/patrols/:id; patrol findings already link forward \u2014 add the backward case\u2192patrol link in CaseDossierHeader. 3) Register 'open evidence source' as a palette command scoped to the focused row.",
     "integration_points": "src/butlers/api/routers/qa.py (case dossier + journal serializers), src/butlers/modules/qa/__init__.py note capture, EvidenceLog.tsx, PatrolJournal.tsx, CaseDossierHeader.tsx; reuses the drill-down anchor patterns landed in bu-86c4c.3/.4."
    }
   ]
  },
  {
   "findings": [
    {
     "title": "Timeline aggregates freeze at mount: histogram, hour totals, and footer rollup use a window whose 'to' (and 'from') never advances",
     "evidence": "frontend/src/components/ingestion/TimelineTab.tsx:1857-1863 \u2014 rangeWindow is useMemo'd on [range] only, so `now` is captured once at mount/range-change. The ledger query deliberately omits the upper bound (lines 1902-1913, comment explains the freeze hazard), but histogramParams passes the frozen `to` (line 1938) and the footer rollup passes `to: effectiveWindow?.to ?? rangeWindow.to` (lines 1988-1990). Hour-group header counts are sourced from that histogram (lines 1117-1128, labeled 'Honest hour totals').",
     "severity": "major",
     "detail": "The fleet event bus correctly invalidates ['ingestion','events-histogram'] and ['ingestion','window-rollup'] on every new event (event-cache-registry.ts:126-130), but the refetch re-runs with the same frozen window, so any event received after mount is server-side filtered out of the aggregates. Failure scenario: owner leaves /ingestion open (its whole point as a live console); new rows stream into the ledger while the flame strip stays flat, hour headers under-count the very rows below them, and the footer 'events' figure contradicts the visible list. Also the 'from' edge never slides forward, so '1h' silently becomes '1h + time-open'. Fix is small: recompute the window on a coarse tick (e.g. 30-60s, aligned with LiveStatusBadge's wall-clock tick) or send open-ended windows the server clamps to now."
    },
    {
     "title": "Errors impersonate empty on /ingestion/connectors and /ingestion/filters; roster ignores aggregates_available and renders fabricated zeros",
     "evidence": "frontend/src/components/ingestion/connectors/ConnectorsRoster.tsx:101-107,139-147,170-173 \u2014 useConnectorSummariesWithAggregates() destructures only data/isLoading; a failed fetch renders the serif empty state 'No connectors registered.' plus a KPI band of zeros. Line 105-106: the aggregates_available flag is mentioned in a comment but never read \u2014 'events \u00b7 24h' (lines 134-137, 201) and the 24h sparklines render 0 as if real when Prometheus is down, violating the degraded-envelope contract in CLAUDE.md \u00a7Degraded-Mode Response Envelope. Same sin on filters: FiltersPipeline.tsx:97-98 captures rulesError but passes it only to ChannelDefaultsBlock (line 370); GateSection renders 'No rules at this gate. Policy lives in code.' (GateSection.tsx:114) on a rules fetch failure.",
     "severity": "major",
     "detail": "Failure scenario: API restart or transient 500 while the owner checks connectors \u2192 the console asserts the house has zero connectors and zero traffic, the exact 'failed fetch impersonating an empty queue' the north star forbids. The timeline's own ledger has a proper isError + Retry branch (TimelineTab.tsx:2213-2228), so the pattern exists in-surface and just wasn't applied to the siblings. Fix: isError branches with retry on roster and gates, and a 'metrics unavailable' eyebrow + em-dash KPIs on the roster when aggregates_available is false (FiltersPipeline already does this at line 327-330)."
    },
    {
     "title": "Ingestion sub-routes are orphaned from the command spine; the surface registers zero palette verbs",
     "evidence": "frontend/src/lib/route-registry.ts:64-79 \u2014 EXTRA_ROUTES indexes health/settings/entities sub-pages but not /ingestion/connectors or /ingestion/filters; nav-config.ts:73 promotes only /ingestion (chord 'e'). Grep for useRegisterCommands across pages: only ApprovalsPage.tsx:1411 and IssuesPage.tsx:88 \u2014 no ingestion page registers commands. IngestionSubNav.tsx is plain NavLinks with no keyboard accelerator.",
     "severity": "major",
     "detail": "bu-86c4c.7's own doctrine ('a route that only lives in EXTRA_ROUTES is not orphaned') is violated one level down: the palette's Pages group, g-chords, and the '?' help sheet cannot reach Connectors or Filters at all \u2014 the only path is mouse-clicking the sub-nav after landing on /ingestion. And none of the surface's frequent verbs (switch range, toggle status chips, apply saved view, replay selected, clear trace/minute scope, jump to a connector by name) exist in the palette, so the north-star 'clears that queue without touching the mouse' fails on the busiest evidence surface. Not covered by bu-86c4c.14 (approvals-only) or the in-flight .6/.15."
    },
    {
     "title": "NOT-LANDED: timeline still speaks a second dialect \u2014 a fourth state color (blue) plus state-color background fills and raw Tailwind literals",
     "evidence": "frontend/src/components/ingestion/StatusBadge.tsx:66 (`bg-blue-500 text-white` filled badge), :159-164 (emerald-500/600, blue-500/600 literals in ROW_STATUS_STYLES); TimelineTab.tsx:1179 (`text-blue-600` replay count in hour headers), :393,:434,:470,:753 (bg-amber-500/text-amber-600 literals); HourFlameStrip.tsx:191 (`bg-blue-500` stack segment). Spec: openspec/specs/dashboard-design-language/spec.md \u00a7State Color Discipline \u2014 'Exactly three state colors exist \u2014 red, amber, green', foreground/border only, 'raw color literals appear only in the token definition file itself'.",
     "severity": "major",
     "detail": "This is dossier finding ~line 1967 of docs/redesigns/2026-07-03-jarvis-audit-data.json and it still reproduces on current main after the epic closed \u2014 replay_pending remains a fourth state color, ingested/replay badges remain color fills, and the files hardcode divergent oklch fallbacks (EventDrawer.tsx:148,423,763,766 uses var(--green, emerald.500) style fallbacks; AttentionStrip.tsx:57 uses oklch(0.62_0.20_25) vs the canonical 0.627 0.257 29.2). Distinct from in-flight bu-86c4c.6 (the CI lint that would *catch* this) and bu-kx3xc (amber contrast): the actual migration of these components to tokens has not landed, and .6's enforcement will land red on day one unless this is cleaned first."
    },
    {
     "title": "Timeline's connector attention strip renders underlined link-styled text that is inert \u2014 a dead drill-down to the exact thing needing attention",
     "evidence": "frontend/src/components/ingestion/TimelineTab.tsx:804-813 \u2014 attention items are <span className=\"... underline\"> with the diagnosis in a title tooltip only; contrast connectors/AttentionStrip.tsx:72-76 where the identical item is a real <Link to=\"/ingestion/connectors/:type/:identity\">.",
     "severity": "major",
     "detail": "Failure scenario: 'gmail/default \u00b7 degraded' appears above the ledger; the owner clicks the underlined text (underline = link in every other Dispatch surface, including the roster's own strip) and nothing happens; the reason is hidden in a hover tooltip that keyboard and touch users never see. Repair is one keystroke away by doctrine and zero keystrokes available in practice. Fix: reuse deriveConnectorDispatchInfo + Link exactly as the roster strip does (same component, one import)."
    },
    {
     "title": "Trace spine is one-directional: an ingestion event offers no pivot out to its trace",
     "evidence": "frontend/src/api/types.ts:3422-3463 \u2014 IngestionEventSummary carries no trace_id; EventDrawer.tsx:748-771 \u2014 the drawer's request KV block shows id/received/channel/tier/sender/lifecycle but no trace row and no 'scope to trace' action, even though every linked session already returns trace_id (types.ts:3508, IngestionEventSession.trace_id) which DrawerSessionsTab ignores.",
     "severity": "major",
     "detail": "Links INTO ?trace= from sessions/notifications landed (bu-86c4c.3, verified at TimelineTab.tsx:1362-1376), but standing on the event itself \u2014 the spine's root \u2014 you cannot get the trace ID, scope the ledger to it, or jump to the fleet chronicle (/timeline) filtered to it. Failure scenario: owner finds an error row in the ingestion ledger and wants 'everything this signal caused across the house'; today that requires opening a session, finding its trace link, and coming back. Fix: surface trace_id in the summary/detail payload, render it as a KV row with copy + 'scope timeline to this trace' (sets ?trace=) + a /timeline?trace= crosslink. Not in bu-9d5vp's list (inert KPI cells, egress actors, audit request_id, spend rule links)."
    },
    {
     "title": "Spend view fragments the ledger into duplicated hour groups, each repeating the same full-hour histogram totals",
     "evidence": "frontend/src/components/ingestion/TimelineTab.tsx:1889 (spend view sets sort:'cost'), :2049-2062 (hourGroups groups only CONSECUTIVE events by hour key), :1117-1128 + :2253 (every group with the same hour key receives the identical full-hour histogramBuckets and renders the full hour's 'N events \u00b7 M errors' header).",
     "severity": "minor",
     "detail": "With cost-DESC ordering, received_at hours interleave, so the same hour header ('14:00 \u2014 37 events') appears multiple times down the page, each stamped with the whole hour's totals and flame strip \u2014 numbers that are individually true but presented as if each fragment were the hour. Failure scenario: owner switches to the spend view to find expensive events and reads triple-counted hour summaries. Fix: suppress hour grouping (and the flame strips) under cost sort \u2014 a flat cost-ranked register is the honest shape \u2014 or group by cost bands instead."
    },
    {
     "title": "Custom saved-view delete is a one-click destructive action with no confirm and no undo",
     "evidence": "frontend/src/components/ingestion/TimelineTab.tsx:440-459 \u2014 a 10px trash button sits inside each view pill (opacity-60, brightening on hover) and fires onDeleteCustomView on click; :1811-1828 \u2014 handleDeleteCustomView mutates DELETE immediately and toasts 'Saved view deleted' with no undo affordance.",
     "severity": "minor",
     "detail": "The delete target is 2px from the view-select button the owner clicks constantly, and the deleted filter_spec (statuses+range+q+channels) is unrecoverable. Violates the design bar's recovery criterion (undo over confirm where reversible \u2014 and this is trivially reversible by re-creating from the still-known spec). Fix: toast with an Undo action that re-POSTs the cached spec, matching the drawer's existing optimistic patterns."
    }
   ],
   "page": "ingestion",
   "verdict": "solid",
   "jarvis_gap": "The bones are genuinely good \u2014 live fleet-bus invalidation, an honest event drawer with audited payload gating and reasoned empty states, URL-backed trace/minute scoping that survived verification \u2014 but the surface still behaves like a monitoring tool the owner interrogates rather than an instrument that reports. It opens with static copy ('Last 24 hours, newest first.', 'Where signals come from.') instead of a synthesized verdict; its aggregate numbers freeze at mount while the ledger underneath stays live, so the longer the owner trusts it open the more it quietly lies; two of its four pages can render a failed fetch as an empty house; its sub-pages and every frequent verb are invisible to the command palette; and the trace spine only points inward, so the root evidence surface cannot answer 'what did this signal cause?' in one keystroke. It also remains the last surface speaking two visual dialects (blue as a fourth state color, filled state badges), which will trip the incoming language-enforcement CI.",
   "ideal": "The ideal /ingestion opens with a computed Dispatch verdict line \u2014 'Quiet pipeline: 412 signals in 24h, all dispatched; gmail needs reauth; $0.84 spent.' \u2014 where every clause is a door (reauth \u2192 connector detail, $0.84 \u2192 spend view, 412 \u2192 the ledger below), synthesized client-side from the rollup, connector summaries, and filter stats already being fetched. Beneath it, the ledger is a genuinely live instrument: windows slide with the clock, the flame strip grows new minutes as events land, and hour totals always agree with the rows they head. Every number that cannot be proven names its degraded source; every fetch failure says so and offers retry; every state is one of exactly three colors. From any row: j/k to move, Enter for the drawer, and from the drawer one keystroke to the raw payload, the spawned sessions, or the full trace across the house in either direction (event \u2192 trace \u2192 chronicle, chronicle \u2192 trace \u2192 event). Every verb \u2014 replay selected, switch view, scope to trace, jump to connector \u2014 lives in the single palette registry, and connectors/filters are one g-chord away. Failure looks like an amber sentence naming the broken source with a retry verb; empty looks like a serif reassurance that the quiet is real, proven by a live badge that decays on a wall clock.",
   "moves": [
    {
     "title": "Un-freeze the live window: sliding range bounds for histogram, rollup, and hour totals",
     "why": "The flagship live surface currently contradicts itself within minutes of being opened \u2014 ledger rows stream in while every aggregate (flame strip, hour headers, footer band) is pinned to the mount-time window. 'No number is ever fabricated' fails by staleness, silently.",
     "cost": "S",
     "slice_plan": "1) Add a coarse wall-clock tick (60s, reuse LiveStatusBadge's pattern) that recomputes rangeWindow in TimelineTab; 2) thread the ticking window into histogramParams and the rollup params (eventsFilters already handles it correctly); 3) test: mount with fake timers, advance 2 minutes, assert histogram/rollup query keys advanced; 4) soak-verify on butlers-dev that a new event moves the strip and footer within one tick.",
     "integration_points": "frontend/src/components/ingestion/TimelineTab.tsx:1857-1863,1933-1952,1975-1997; frontend/src/components/ui/live-status-badge.tsx (tick pattern); hooks/use-ingestion-events.ts query keys (windows in keys mean each tick re-keys \u2014 round `now` to the tick boundary so keys stay cacheable)."
    },
    {
     "title": "Ingestion judgment line: synthesized verdict opener on all three ingestion pages",
     "why": "bu-86c4c.17 landed the judgment layer on /system only; ingestion still opens with static marketing copy. The north star demands every surface open with the system's own verdict, every clause a door \u2014 and all the inputs (window rollup, connector summaries with health, pipeline stats) are already fetched by these pages.",
     "cost": "M",
     "slice_plan": "1) Extract SystemVerdictBanner's composition pattern into a shared DispatchVerdict primitive; 2) /ingestion: compose from rollup (events/sessions/cost) + attention connectors, clauses linking to ledger anchors, connector detail, and /spend; 3) /ingestion/connectors: verdict from healthy/needs-attention counts + aggregates_available honesty ('metrics unavailable' clause when degraded); 4) /ingestion/filters: verdict from funnel deltas ('gates dropped 61% of 1,204 signals'); 5) replace the static DispatchHeader headline prop with the verdict node; keep the serif voice tokens.",
     "integration_points": "frontend/src/components/system/SystemVerdictBanner.tsx (pattern source); components/ingestion/dispatch/DispatchHeader.tsx (headline slot); pages/IngestionTimelinePage.tsx:66-71, IngestionConnectorsPage.tsx:55-59, IngestionFiltersPage.tsx:72-77; existing hooks useIngestionWindowRollup / useConnectorSummariesWithAggregates / usePipelineStats \u2014 zero new endpoints."
    },
    {
     "title": "Truth pass on connectors + filters: error \u2260 empty, degraded sources name themselves, attention items are real links",
     "why": "Three verified honesty defects cluster on two pages: roster fetch failure claims 'No connectors registered.', roster KPIs/sparklines render Prometheus-down zeros as real, gate sections claim 'no rules' on a rules fetch failure, and the timeline's attention strip styles inert spans as links. All are small, all violate the earned-calm contract at its core.",
     "cost": "S",
     "slice_plan": "1) ConnectorsRoster: add isError branch with serif error + retry (copy the ledger's TimelineTab.tsx:2213-2228 pattern); read aggregates_available and render 'metrics unavailable' eyebrow + em-dash on events\u00b724h and sparklines; 2) FiltersPipeline: pass rulesError into GateSection, render 'Rules unavailable' + retry instead of the code-policy note; 3) TimelineTab's ConnectorAttentionStrip: replace spans with the roster AttentionStrip's Link markup (shared component if trivial); 4) axe + unit tests per state.",
     "integration_points": "components/ingestion/connectors/ConnectorsRoster.tsx:101-147,170-173; components/ingestion/filters/FiltersPipeline.tsx:97-98,338-349 + GateSection.tsx:114; TimelineTab.tsx:784-816; hooks/use-ingestion.ts (expose isError/refetch); degraded-envelope contract in CLAUDE.md \u00a7Degraded-Mode Response Envelope."
    },
    {
     "title": "Complete the command spine for ingestion: index sub-routes, register page verbs, add ledger row keys",
     "why": "The busiest evidence surface is palette-invisible below its root: connectors/filters unreachable by keyboard, zero registered verbs, no row navigation. The 'clear the queue without touching the mouse' property fails exactly where signals are densest.",
     "cost": "M",
     "slice_plan": "1) Add /ingestion/connectors and /ingestion/filters to EXTRA_ROUTES (one-line each, palette+help-sheet coverage is automatic); 2) useRegisterCommands on the timeline page: range 1h/24h/7d, apply each saved view, toggle noise statuses, replay selected (guarded by selection), clear trace/minute scope; 3) connectors page: 'jump to connector\u2026' command per roster row; 4) roving j/k focus on ledger rows with Enter\u2192drawer, r\u2192replay, x\u2192select (mirror ApprovalsPage's landed triage keymap so the vocabulary stays one system); 5) e2e: palette reaches filters from /spend in two keystrokes.",
     "integration_points": "frontend/src/lib/route-registry.ts:64-79; lib/command-registry.tsx useRegisterCommands (pattern at pages/ApprovalsPage.tsx:1411); TimelineTab.tsx handlers already exist for every verb (handleRangeChange, handleViewSelect, handleStatusToggle, handleReplayAll, handleClearTrace, clearMinuteScope) \u2014 this is registration, not new logic; DisclosureRow focus plumbing at TimelineTab.tsx:994-1022."
    },
    {
     "title": "Bidirectional trace spine + retire the fourth state color",
     "why": "The event drawer is the root of the evidence spine yet cannot emit a trace: no trace_id shown, no scope-to-trace verb, no crosslink to the fleet chronicle. And the same drawer/ledger family is the last NOT-LANDED bilingual surface (blue replay color, filled state badges, raw Tailwind literals) \u2014 cleaning it now is the prerequisite for bu-86c4c.6's CI to land green.",
     "cost": "M",
     "slice_plan": "1) Backend: include trace_id on IngestionEventSummary/Detail (it already exists on the joined sessions; expose the event-level one from the ingest path); 2) Drawer KV: trace row with CopyButton + 'scope ledger to trace' (sets ?trace=) + 'view in chronicle' link (/timeline?trace=); 3) session rows: render each session's trace_id chip; 4) dialect: map replay_pending to amber-foreground (in-flight = degraded-toward-resolution), replay_complete to green, replay_failed to red; convert StatusBadge fills to dot+word (ROW_STATUS_STYLES already has the shape), replace all raw emerald/blue/amber literals with var(--green)/var(--amber)/var(--red) tokens; 5) update HourFlameStrip stack + hour-header replay count to the same mapping; visual regression via existing axe/a11y suites.",
     "integration_points": "frontend/src/api/types.ts:3422-3463; src/butlers/api/routers/ingestion event serializers; components/ingestion/timeline/EventDrawer.tsx:748-771,291-334; components/ingestion/StatusBadge.tsx:46-76,159-165; TimelineTab.tsx:1179; HourFlameStrip.tsx:191; openspec/specs/dashboard-design-language/spec.md \u00a7State Color Discipline (spec change NOT needed \u2014 this conforms to it); coordinate with in-flight bu-86c4c.6 so the lint rules land after the cleanup."
    }
   ]
  },
  {
   "page": "system",
   "verdict": "functional",
   "jarvis_gap": "The .17 judgment layer holds at the happy path \u2014 one canonical liveness verdict shared by banner, heartbeat tile, and topology graph, honest empties, drill-down heartbeat rows with an inline tick remedy \u2014 but the verdict itself can assert \"Instance healthy\" while the queries feeding it have errored, connector liveness (the highest-stakes ingress fact) never reaches the judgment at all, four of seven problem-clause kinds are doors to nowhere, and the whole page still speaks pre-Dispatch card dialect with a mouse-only hardcoded-hex topology map.",
   "findings": [
    {
     "title": "Verdict banner asserts 'Instance healthy' while its own sources are errored",
     "evidence": "frontend/src/components/system/SystemVerdictBanner.tsx:61-62 guards only isLoading/isPending; no isError branch exists for instance/backups/insights/posture. When GET /api/system/backups fails, backups.data is undefined (use-system.ts:41-47), so lines 106-111 push no problem and lines 125-143 render the all-clear banner \u2014 directly above a BackupTile showing 'Could not load backup facts.' (BackupTile.tsx:41-55). Same hole for insights.isError (line 98), posture.isError (line 113), instance.isError (line 127). The component's own doctrine comment (lines 10-11: 'Never asserts all clear while any source is still loading/degraded') covers loading but not error.",
     "severity": "critical",
     "detail": "This is the error-impersonates-health sin inside the just-landed judgment layer itself \u2014 the one component whose entire purpose is earned calm. Not covered by bu-mkd5r (which recorded system/topology as 'handled by bu-86c4c.17'). Fix: any feeding query in isError state must emit a 'cannot verify <source>' clause and suppress the healthy assertion."
    },
    {
     "title": "Connector liveness is invisible to the verdict \u2014 a dead telegram/gmail connector produces no problem clause",
     "evidence": "SystemVerdictBanner.tsx:74-123 composes problems from board/insights/backups/posture only. useConnectorSummaries is already fetched on the same page (SystemPage.tsx:62-63) but only tints nodes in the below-the-fold TopologyGraph (TopologyGraph.tsx:217, liveness 'offline'/'stale' \u2192 border color).",
     "severity": "major",
     "detail": "An offline connector means owner messages silently stop arriving \u2014 arguably the single highest-stakes system fact \u2014 yet the page's judgment sentence stays 'Instance healthy: \u2026 all 9 beating'. Butler daemons can all beat while ingress is dead. Add offline/stale-connector clauses with hrefs to /ingestion/connectors/{type}/{identity}."
    },
    {
     "title": "NOT-LANDED: topology graph still bypasses the design system \u2014 hardcoded hex palette, theme-blind slate nodes, colors outside the three-state vocabulary",
     "evidence": "TopologyGraph.tsx:56-99 (STATUS_COLORS/TONE_COLORS as raw hex #22c55e/#eab308/#ef4444), :132-141 and :184-195 (node fills #1e293b, #0f172a, color: white \u2014 unchanged in light theme against the paper-warm background), :85-90 blue '#3b82f6' staffer-healthy, :270 purple '#8b5cf6' connector edges. openspec/specs/dashboard-design-language/spec.md: 'Exactly three state colors exist' (line ~81) and values in frontend/src/index.css are normative (line ~90).",
     "severity": "major",
     "detail": "Old-dossier finding reproduced verbatim on current main: bu-86c4c.17 rewired WHICH tone a node gets but kept the hex dialect and the two extra hues. bu-86c4c.6 (in-flight) is token lint enforcement, not this conversion \u2014 and this file would fail it."
    },
    {
     "title": "Topology drill-down is mouse-only; connectors have zero keyboard path on this page",
     "evidence": "TopologyGraph.tsx:311-324 \u2014 navigation happens solely in onNodeClick (a NodeMouseHandler); no keyboard activation handler exists, and nodesDraggable={true} (line 402) means arrow keys on a focused xyflow node move it rather than navigate. Butler nodes have a keyboard-equivalent path via ButlerHeartbeatTile's RowLink (ButlerHeartbeatTile.tsx:99-104), but connector nodes appear nowhere else on /system.",
     "severity": "major",
     "detail": "Missing-keyboard-surface sin: the only route from 'red gmail node' to its connector detail is a mouse click on a canvas. Fix alongside the Dispatch conversion: render a keyboard-operable node index (butlers + connectors) beside/beneath the graph, making the canvas an enhancement rather than the sole path."
    },
    {
     "title": "Verdict problem clauses are doors to nowhere for 4 of 7 problem kinds",
     "evidence": "SystemVerdictBanner.tsx:98-123 \u2014 the insights-failed, backup-unreachable, backup-never, posture, and sources-degraded problems are pushed without href, and lines 156-165 render href-less problems as plain <li> text. Only offline/quarantined/overdue link (to /butlers).",
     "severity": "major",
     "detail": "North star: 'every clause of that prose is a door.' Even the cheap version \u2014 an on-page anchor to the elaborating tile (SecurityPostureTile, BackupTile, InsightDeliveryTile all have stable testids but no ids) \u2014 is absent. Not covered by bu-9d5vp (KPI cells/egress/audit/spend) or bu-6t8ix."
    },
    {
     "title": "NOT-LANDED: insight 'Failed: N' is still an inert number \u2014 no path to which insights are blocked, no unblock verb",
     "evidence": "InsightDeliveryTile.tsx:130-134 renders the permanently-blocked count (status='filtered' AND delivery_attempt_count >= 3 per src/butlers/api/routers/system.py:205-206) as a bare StatRow; no link, no disclosure, no remedy. Old-dossier dead-end finding survives; bu-6t8ix covers notification/memory retry verbs, not insight-candidate unblocking, and bu-9d5vp does not mention insights.",
     "severity": "major",
     "detail": "Needs a small backend surface: GET failed insight candidates (butler, title, failure reason, attempts) + an unblock/requeue endpoint, then a DisclosureRow list under the tile and a door from the verdict clause."
    },
    {
     "title": "NOT-LANDED: three status-color dialects on one page; whole page still card-mosaic, not Dispatch",
     "evidence": "SecurityPostureTile.tsx:82 (bg-green-600/text-amber-600/border-amber-400 Badge hack) vs BackupTile.tsx:107 (hand-rolled bg-emerald-600 pill) vs ButlerHeartbeatTile.tsx:44-57 (correct contrast-locked bg-severity-* tokens). Page-wide: SystemPage.tsx:39-49 SystemTile and every tile use Card/CardHeader with 'text-sm font-medium' titles, against dashboard-design-language spec.md ('Sections SHALL be titled with eyebrows \u2014 10px/mono/uppercase/0.14em'; lists are hairline-split rows, 'never cards'; state colors align with the normative --severity-* scale).",
     "severity": "minor",
     "detail": "text-amber-600-as-text at :82 is also an AA risk sibling to bu-kx3xc, but that bead scopes itself to secrets/passport inline-style sites \u2014 this raw-utility site is not in its list. The card-vs-Dispatch conversion of /system is not claimed by any epic child (bu-86c4c.6 is lint enforcement only)."
    },
    {
     "title": "NOT-LANDED: tile errors are retry-less dead ends, and the promised DB size-history follow-up bead was never filed",
     "evidence": "All six TileError components are text-only with no retry affordance (DbSizeTile.tsx:48-62, InsightDeliveryTile.tsx:46-60, BackupTile.tsx:41-55, SecurityPostureTile.tsx:46-60, EgressCatalogTile.tsx:44-58, heartbeat error at ButlerHeartbeatTile.tsx:200-211) \u2014 none sit on the shared query-boundary error-with-retry contract from bu-86c4c.2. Separately, DbSizeTile.tsx:8-11 promises 'a follow-up bead should add a time-series size history endpoint' \u2014 bd list has no matching open bead (searched size/growth/sparkline).",
     "severity": "minor",
     "detail": "bu-mkd5r explicitly deferred system/topology to bu-86c4c.17 with a 'verify each got real error states there, else include here' clause \u2014 error states exist but the retry half never landed; flagging so the synthesizer can route it into bu-mkd5r rather than lose it."
    }
   ],
   "ideal": "The owner lands on /system and reads one Dispatch-voice sentence that is provably true: \"Instance healthy \u2014 v0.4.2, up 12 days, backed up 3h ago, all 9 butlers beating, all 4 connectors listening, last verified just now\" \u2014 or a severity-ranked problem list where every clause is a door (offline connector \u2192 its connector page, failed insights \u2192 the exact blocked candidates with a requeue verb, posture regression \u2192 the settings row that fixes it) and every remedy completes inline without leaving the screen. The verdict is structurally incapable of lying: any feeding source in error state converts the healthy assertion into a named \"cannot verify backups \u2014 source unreachable, retry\" clause, and connector ingress health carries the same weight as butler heartbeats. Below the sentence, eyebrow-titled hairline sections (no cards) elaborate: heartbeats as keyboard-navigable rows sharing the one severity token vocabulary, a theme-aware topology map drawn entirely from index.css tokens with a keyboard-operable node index beside it, DB footprint with a real growth sparkline from a size-history endpoint, egress and posture as sovereign ownership facts. State arrives over the fleet event bus \u2014 a backup completing or a connector dying updates the verdict in the same frame \u2014 and the whole page is reachable and operable from the command palette in under five keystrokes.",
   "moves": [
    {
     "title": "Make the verdict structurally unable to lie: thread isError + connector liveness into SystemVerdictBanner",
     "why": "The flagship judgment surface currently asserts all-clear over failed fetches (critical finding 1) and is blind to dead ingress (finding 2) \u2014 both are direct 'earned calm' violations, and both fix cheaply because every needed query already runs on the page.",
     "cost": "S",
     "integration_points": "frontend/src/components/system/SystemVerdictBanner.tsx (add isError clauses per source + suppress all-clear); lift useConnectorSummaries from SystemPage.tsx TopologyTile to page level or call it in the banner; reuse SourceDegradedNote vocabulary from components/ui/query-boundary.tsx; extend existing SystemVerdictBanner tests.",
     "slice_plan": "(a) per-source isError \u2192 'cannot verify <source>' clause, healthy line suppressed; (b) offline/stale connector clauses with hrefs to /ingestion/connectors/{type}/{identity}; (c) tests: killed backup endpoint can never render all-clear."
    },
    {
     "title": "Every verdict clause a door: anchors for on-page tiles, real routes for the rest, failed-insights disclosure + requeue verb",
     "why": "4 of 7 problem kinds are dead text (finding 5) and 'Failed: N' insights is an inert number with no backend surface behind it (finding 6) \u2014 the last drill-down dead ends on the page not already claimed by bu-9d5vp/bu-6t8ix.",
     "cost": "M",
     "integration_points": "SystemVerdictBanner.tsx (hrefs/anchor ids); id anchors on SecurityPostureTile/BackupTile/InsightDeliveryTile; new GET /api/system/insights/failed-candidates + POST requeue in src/butlers/api/routers/system.py reading public.insight_candidates; DisclosureRow (components/ui) list in InsightDeliveryTile.tsx.",
     "slice_plan": "(a) anchor ids + banner hrefs (pure frontend); (b) failed-candidates read endpoint + disclosure list naming butler/title/attempts; (c) requeue/unblock verb with honest-pending toast, mirroring the Trigger-tick pattern."
    },
    {
     "title": "Dispatch conversion of /system: eyebrow-hairline sections, one severity token vocabulary, token-driven theme-aware topology with a keyboard node index",
     "why": "The page is the last pre-Dispatch card mosaic among redesigned surfaces (finding 7), the topology map is hardcoded-hex and theme-blind (finding 3, NOT-LANDED), and graph drill-down is mouse-only (finding 4). One coordinated pass fixes dialect, contrast, and keyboard access together.",
     "cost": "L",
     "integration_points": "frontend/src/pages/SystemPage.tsx, all components/system/*.tsx (kill bg-green-600/bg-emerald-600/text-amber-600 raw utilities \u2192 severity tokens per index.css normative scale), components/topology/TopologyGraph.tsx (colors from CSS custom properties, node fills from --card/--border tokens, drop blue/purple extra hues per spec's three-state rule), new keyboard-operable node index list adjacent to the graph reusing RowLink; will also satisfy in-flight bu-86c4c.6 lint on this file.",
     "slice_plan": "(a) severity-token sweep on tiles; (b) sections \u2192 eyebrow + hairline grammar; (c) topology token/theme rewrite; (d) keyboard node index (butlers + connectors) with the graph demoted to enhancement."
    },
    {
     "title": "DB size history endpoint + growth clause in the verdict",
     "why": "DbSizeTile.tsx:8-11 promises a size-history follow-up that was never filed; growth_rate_bytes_per_day is permanently null, so runaway table growth \u2014 a real sovereignty/ops risk for a JSONB-heavy single-DB system \u2014 is invisible until disk pressure. A trend also gives the verdict a provable 'db growing 400MB/day' clause.",
     "cost": "M",
     "integration_points": "New sampled size-history table + scheduled sampler (src/butlers/api/routers/system.py or a core scheduler task), GET /api/system/database/history; DbSizeTile sparkline via dataviz-conformant mark; SystemVerdictBanner growth-anomaly clause; alembic migration in the core chain (serialize revision numbering per known collision hazard).",
     "slice_plan": "(a) daily sampler + history endpoint; (b) sparkline + growth rate in tile (omit until \u22652 samples \u2014 never fake); (c) verdict clause when growth exceeds a configurable threshold."
    },
    {
     "title": "System facts join the fleet event bus: backup/insight/egress events patch /system in-frame",
     "why": "North star says state arrives as a live event stream; the board is already event-patched via event-cache-registry.ts, but backups (120s), insights (60s), and posture polls mean the verdict can lag a completed backup or a new insight failure by minutes while the rest of the dashboard updates in the same beat.",
     "cost": "M",
     "integration_points": "src/butlers/api/routers/events.py (emit backup_completed / insight_delivery / egress_recorded envelope types where those writes happen); frontend/src/hooks/event-cache-registry.ts (one CachePatch per type invalidating system-backups / system-insight-delivery / system-egress keys); LiveIndicator already in PageHeader names stream state.",
     "slice_plan": "(a) backend event emission for backup + insight-delivery transitions; (b) registry patches (single-file additions by design of bu-86c4c.8); (c) drop the now-redundant aggressive polls to slow fallbacks."
    }
   ]
  },
  {
   "page": "cross:shell-discoverability",
   "verdict": "functional",
   "jarvis_gap": "The one-command-spine (bu-86c4c.7) genuinely landed \u2014 single route registry feeds sidebar, palette Pages, g-chords, and the '?' sheet \u2014 but the shell still fails \"the first use needs no docs, the tenth use needs no mouse\": the console's hottest loop (approvals j/k/a/d/x triage) is advertised nowhere in the product, the command registry carries no keybinding metadata so the palette can never teach shortcuts, the palette is invisible until you type (no browsable verbs, no recents, substring-only matching), and two page-local key handlers (memory '/', chat Ctrl+Shift+arrows) were built outside the spine \u2014 one of them now dead-colliding with the global '/' binding.",
   "ideal": "Every keyboard path in the console flows through one shortcut+command registry: PaletteCommand grows a `binding` field, a shared useRegisterShortcut hook both binds page-scoped keys and feeds the '?' help sheet (with an \"On this page\" section) and the palette's inline kbd column \u2014 so a shortcut cannot exist without being advertised, by construction. The palette is browsable at empty query (recent commands and destinations above the entity-pinned set), fuzzy-matches verbs and nouns, and shows each entry's chord inline. Sidebar tooltips carry their g-chords; every badge-bearing attention surface has one. The Approvals triage interaction (j/k roving focus + undoable verb scheduling) is a shared hook applied to every triage list \u2014 Dashboard attention, Issues, Notifications \u2014 with its verbs visible in a persistent footer hint strip, matching the finder's own \u2191\u2193/\u21b5/\u21e5 footer pattern.",
   "findings": [
    {
     "title": "Approvals j/k/a/d/x triage verbs are advertised nowhere in the product",
     "evidence": "frontend/src/pages/ApprovalsPage.tsx:1332-1374 binds j/k roving focus and a/d/x undoable decisions; grep confirms zero <kbd>, KbMono, aria-keyshortcuts, or hint copy anywhere in the file. The '?' help sheet's STATIC_SHORTCUTS (frontend/src/components/ui/shortcut-hints.tsx:19-23) lists only Ctrl+K, /, ? plus g-chords \u2014 page-scoped shortcuts have no representation at all. The north-star loop ('clear the queue in under a minute without touching the mouse') is invisible to anyone who hasn't read the source.",
     "severity": "critical",
     "detail": "The finder itself proves the house pattern exists \u2014 its footer strip (EntityFinder.tsx:718-735) advertises \u2191\u2193/\u21b5/\u21e5/esc inline. Approvals (and any future triage surface) needs the same persistent hint strip, plus rows in the help sheet."
    },
    {
     "title": "Global '/' (command menu) collides with MemorySearch's '/' focus binding \u2014 both fire on /memory",
     "evidence": "frontend/src/hooks/use-keyboard-shortcuts.ts:41-45 binds bare '/' to open the command menu (window keydown, preventDefault). frontend/src/components/memory/MemorySearch.tsx:82-96 independently binds bare '/' on window to focus the memory search input, with a comment claiming \"'/' focuses the input from anywhere on the page\". Neither stops propagation, so on /memory both handlers run: the palette opens and its requestAnimationFrame focus (EntityFinder.tsx:286) steals focus from the memory input \u2014 MemorySearch's binding is dead code that also documents false behavior.",
     "severity": "major",
     "detail": "Two components claiming the same bare key with no arbitration is exactly what the single-spine work was meant to end. Either delete the MemorySearch handler or route page-scoped key claims through a registry that detects conflicts."
    },
    {
     "title": "PaletteCommand has no binding metadata \u2014 the palette can never display shortcuts inline, and the help sheet can never show page-scoped keys",
     "evidence": "frontend/src/lib/command-registry.tsx:36-45 \u2014 PaletteCommand is {id, label, keywords, perform}; no binding/shortcut field. EntityFinder.tsx:698-715 renders Action rows with label only. shortcut-hints.tsx:19-41 builds the help sheet from a hardcoded STATIC_SHORTCUTS array plus route chords \u2014 there is no channel by which ApprovalsPage's a/d/x, ChatPanel's chords, or any future page shortcut can reach it.",
     "severity": "major",
     "detail": "The discoverability bar says 'the palette is where shortcuts are learned' and 'displays each entry's keybinding inline'. The registry is the right unifying mechanism and it is one field + one render column away."
    },
    {
     "title": "Palette is invisible until the first keystroke: Actions, Pages, butlers all hidden at empty query; no recents anywhere",
     "evidence": "frontend/src/components/layout/EntityFinder.tsx:366-375 (actionMatches gated on lowerQuery.length >= 1), :324-331 (pageMatches same), :344 (butlerMatches same). Empty query renders only the owner-pinned entity set (:509-539). A user opening Cmd+K sees people \u2014 never learns that 'Trigger <butler>', 'Approve next', or 'Acknowledge issue' exist. No recents store exists for commands or destinations.",
     "severity": "major",
     "detail": "Skill contract: 'shows recents first'; empty states teach. The empty-query view should stack recent commands/destinations above the pinned entities, making the verb layer browsable."
    },
    {
     "title": "Finder preview pane and Tab-hop track the first entity even when a Page/Action/Session row is highlighted",
     "evidence": "frontend/src/components/layout/EntityFinder.tsx:390-413 \u2014 activeResult falls back to entityResults[0] whenever activeValue doesn't start with 'entity:' (e.g. 'page:\u2026', 'action:\u2026'). So with an Action row highlighted, the preview pane shows an unrelated entity, and the Tab handler (:449-452) hops the user to /entities?center=<that entity> \u2014 an action unrelated to the highlighted item. The footer advertises '\u21e5 hop' unconditionally (:728-731).",
     "severity": "major",
     "detail": "activeResult should resolve from the actually-highlighted value; Tab (and the footer's \u21e5 hint) should only be live on entity rows."
    },
    {
     "title": "Substring-only matching in the one palette \u2014 no fuzzy, no typo or word-order tolerance",
     "evidence": "frontend/src/components/layout/EntityFinder.tsx:454 sets shouldFilter={false} and all client groups filter with .toLowerCase().includes(query): pages :326-330, butlers :345, actions :369-374. 'aprove', 'approve nxt', or 'spend page' (word order) match nothing; cmdk's built-in scorer is bypassed and no fuzzy scorer replaces it.",
     "severity": "minor",
     "detail": "Server-ranked entity results are fine; the client-side groups (pages/butlers/actions) need a shared fuzzy scorer \u2014 one utility, applied to all three, so match quality stays consistent across groups."
    },
    {
     "title": "g-chord coverage skew: badge-bearing attention surfaces (/approvals, /qa, /spend) have no chord, and chords are advertised only in the '?' sheet \u2014 not in sidebar tooltips or palette rows",
     "evidence": "frontend/src/components/layout/nav-config.ts:66-108 \u2014 chords exist for Overview/Butlers/Ingestion/Memory/Timeline/Notifications/Issues/Sessions/Audit-log/Health, but /approvals (amber badge, the hot queue), /qa (red badge), /spend, /entities, /settings, /secrets have none; low-traffic g-a\u2192/audit-log holds a prime letter. Sidebar tooltips render label only (Sidebar.tsx:218-220); palette Pages rows show label+section, no chord kbd (EntityFinder.tsx:594-611).",
     "severity": "minor",
     "detail": "Assign chords to every badge-carrying route, render the chord as a kbd in the sidebar tooltip and the palette Pages row \u2014 all three read the same RouteEntry.chord field, so this is pure surfacing."
    },
    {
     "title": "ChatPanel's Ctrl+Shift+ArrowUp/Down conversation switcher exists only as a code comment",
     "evidence": "frontend/src/components/chat/ChatPanel.tsx:108-128 binds Ctrl+Shift+Up/Down to cycle conversations; the only 'advertisement' is the file-header comment at :10. No tooltip, no kbd hint in the conversation list, no help-sheet row, no palette command ('Next conversation') \u2014 a third bespoke keydown listener built outside the command spine.",
     "severity": "minor",
     "detail": "Instance of the missing shortcut-registry mechanism (finding 3); the chat epic bu-p6ey8 covers routing/SSE, not shortcut discoverability."
    }
   ],
   "moves": [
    {
     "title": "Shortcut registry: add `binding` to PaletteCommand + a useRegisterShortcut hook that binds AND advertises",
     "why": "Makes an unadvertised shortcut structurally impossible: one hook both installs the page-scoped key handler (with the shared editable-field guard and g-chord arbitration) and publishes the binding to the '?' help sheet (new 'On this page' section) and the palette's inline kbd column. Directly clears findings 1, 2, 3, 8 and the bu-5o22a duplication in one mechanism.",
     "cost": "M",
     "slice_plan": "1) Add optional `binding?: string[]` to PaletteCommand and render it as KbMono in EntityFinder Action rows + a page-scoped section in shortcut-hints.tsx (read from the same registry context). 2) Ship useRegisterShortcut(keys, perform, {label}) wrapping the guard from use-keyboard-shortcuts.ts. 3) Migrate ApprovalsPage j/k/a/d/x, ChatPanel Ctrl+Shift+arrows, and delete MemorySearch's dead '/' handler. 4) ESLint rule or test: no raw window keydown listeners outside the hook (grep-based unit test is enough).",
     "integration_points": "frontend/src/lib/command-registry.tsx, frontend/src/hooks/use-keyboard-shortcuts.ts, frontend/src/components/ui/shortcut-hints.tsx, frontend/src/components/layout/EntityFinder.tsx, ApprovalsPage.tsx:1332-1374, ChatPanel.tsx:108-128, MemorySearch.tsx:82-96. Complements in-flight bu-86c4c.6 (language lint) and bu-5o22a (guard extraction \u2014 subsumes it)."
    },
    {
     "title": "Browsable palette: empty-query verbs + recents store + shared fuzzy scorer",
     "why": "The verb layer of the command spine is currently invisible until the owner already knows what to type. Recents-first empty state and fuzzy matching are the two remaining contract items of the palette bar ('shows recents first', 'fuzzy-matches verbs and nouns'). Clears findings 4 and 6 (partially 5).",
     "cost": "M",
     "slice_plan": "1) localStorage recents store (last 8 executed commands/destinations) keyed by command id / route path, rendered as the first group at empty query above the pinned entities. 2) Show the Actions group at empty query (all registered verbs, recents first). 3) One fuzzy scorer utility (subsequence + word-prefix boost) applied to pages/butlers/actions groups. 4) Fix activeResult to resolve from the highlighted cmdk value's type; gate Tab-hop and the \u21e5 footer hint to entity rows.",
     "integration_points": "frontend/src/components/layout/EntityFinder.tsx (all groups, activeResult :390-413, Tab handler :449), new frontend/src/lib/fuzzy.ts + frontend/src/lib/command-recents.ts, frontend/src/lib/local-settings.ts pattern for storage."
    },
    {
     "title": "Chord completion + in-situ advertisement (sidebar tooltips, palette rows)",
     "why": "The registry already carries RouteEntry.chord; it's simply not surfaced where the action lives and not assigned to the highest-value routes. Cheapest possible discoverability win on the hottest nav loops (approvals badge, qa badge, spend).",
     "cost": "S",
     "slice_plan": "1) Assign chords in nav-config.ts: /approvals, /qa, /spend, /entities, /settings (letters free: p, q, $\u2192d/x, y, ,). 2) Render `g <chord>` as kbd inside Sidebar TooltipContent and MobileFlatLink title. 3) Render the chord in the palette Pages rows. 4) Unit test: every route with a badgeKey must declare a chord (registry-level invariant, prevents future skew).",
     "integration_points": "frontend/src/components/layout/nav-config.ts:66-108, Sidebar.tsx:218-220 and :676, EntityFinder.tsx Pages group :592-614, route-registry.test.ts for the invariant."
    },
    {
     "title": "Generalize the Approvals triage loop into useListTriage and apply it to Dashboard attention, Issues, and Notifications",
     "why": "The best interaction in the console (j/k roving focus + undoable a/d/x verb scheduling + selection advance, ApprovalsPage.tsx:1289-1388) was built once and never generalized \u2014 every other triage list is Tab-Tab-Enter. This is the mechanism half; in-flight bu-86c4c.15 supplies the remedy verbs themselves, so this hook is what makes those verbs keyboard-fast everywhere.",
     "cost": "L",
     "slice_plan": "1) Extract useListTriage({items, selectedId, verbs: {key, label, run}, undoWindowMs}) from ApprovalsPage \u2014 roving focus, scheduling map, undo toast, g-chord/editable-field guards (via move 1's hook). 2) Re-base ApprovalsPage on it (behavior-identical, tests keep passing). 3) Adopt on DashboardPage attention list (approve/deny already share useApprovalDecisionMutations) and IssuesPage (acknowledge). 4) Ship a shared TriageFooterHints strip (KbMono row, same idiom as EntityFinder's footer) rendered by every adopter \u2014 advertisement is part of the hook's contract, not optional.",
     "integration_points": "ApprovalsPage.tsx:1289-1411, DashboardPage attention list (useApprovalDecisionMutations callers), IssuesPage.tsx:75-88, hooks/use-approval-decisions.ts, move 1's shortcut registry for binding advertisement. Coordinate with bu-86c4c.15 (verbs) and bu-6t8ix (deferred retry verbs) so their verbs land pre-wired for keyboard."
    },
    {
     "title": "Registry noun coverage: make approvals, issues, and notifications findable from the palette",
     "why": "North star is 'one keystroke from any signal to its root evidence', but the palette's noun space is entities/pages/butlers/sessions/state only \u2014 the attention objects themselves (a pending approval, an active issue) can't be jumped to by name. Typing a tool name or butler name should surface its waiting approval directly.",
     "cost": "M",
     "slice_plan": "1) Extend GET /api/search (or add client-side groups fed by the already-cached use-approvals/use-issues queries \u2014 zero new endpoints) with 'Approvals' and 'Issues' groups matching on tool_name/butler/issue title. 2) Render them between Butlers and Sessions with their state pill; Enter navigates to /approvals/<id> or /issues?key=\u2026. 3) Cap at 5 per group, same as butlers.",
     "integration_points": "frontend/src/components/layout/EntityFinder.tsx (new groups), hooks/use-approvals.ts and use-issues.ts (cached lists, so matching can be client-side and instant), optionally src/butlers/api search endpoint if server-side matching is preferred."
    }
   ]
  },
  {
   "page": "cross:visual-language",
   "verdict": "functional",
   "jarvis_gap": "The north star demands the console \"reads as one instrument built by one hand\"; today it is two coexisting design systems \u2014 a real, well-tokenized Dispatch layer (minority) sitting on a shadcn base layer that is still the DEFAULT: the default typeface is the spec-forbidden system-ui/Roboto stack, the shared page-shell and empty-state primitives render off-language for 24+ pages each, the same semantic gray has two values depending on dialect, forbidden skeleton-pulse ships on 46 sites, and chart/status color speaks four dialects with state hues carrying second meanings. The in-flight lint bead (bu-86c4c.6) can stop NEW drift but cannot flip the defaults; until the base layer itself is Dispatch, every un-redesigned surface and every new page is born off-language.",
   "findings": [
    {
     "title": "NOT-LANDED: Default typeface is the forbidden system-ui/Roboto stack; Inter Tight is opt-in and flagship pages never opt in",
     "evidence": "frontend/src/index.css:8 sets `:root { font-family: system-ui, -apple-system, ... Roboto, sans-serif }` \u2014 the spec's explicitly forbidden primary faces (openspec/specs/dashboard-design-language/spec.md:143). index.css:247-249 admits it: \"The :root font-family rule above remains the body default; opt-in consumers reference these variables directly.\" The app shell (components/layout/Shell.tsx:41-89) applies no family; only Sidebar.tsx:472,516,809 opts into font-sans. Grep: DashboardPage, ApprovalsPage, SessionsPage, TimelinePage, SpendPage, SystemPage, AuditLogPage, ButlersPage contain ZERO `font-sans` \u2014 their body text renders in system-ui/Roboto. Only 28 of ~400 component files reference font-sans at all.",
     "severity": "critical",
     "detail": "This was in the 2026-07-03 dossier's summary (\"stock shadcn system-ui body font\") and did not land in any merged epic child. It is a runtime-default problem, not a lint problem \u2014 bu-86c4c.6 (diff-time token lint) cannot fix text that renders wrong today. The fix is one line (`font-family: var(--font-sans)` on :root/body) plus deleting the per-component opt-ins."
    },
    {
     "title": "Shared page-shell and empty-state primitives are still old-language, so every page using the shared path is off-spec by default",
     "evidence": "components/ui/page.tsx:106 renders `<h1 className=\"text-3xl font-bold tracking-tight\">` \u2014 33px/700 matches neither Display (44/500) nor Title (24/500); spec.md:157-162 says \"Display weight is 500, never 700\". page.tsx is imported by 24 non-test files. components/ui/empty-state.tsx:10-18 renders icon + `font-semibold` heading + description paragraph + action (24 consumers) vs spec.md:349-353 \"one serif-italic sentence with no trailing explanation... no illustration\". page.tsx:78-79,217 also bakes `animate-pulse` skeleton blocks into the shared loading state. 23 `font-bold` headline sites overall (EducationPage.tsx:46, SessionDetailPage.tsx:92, ButlerDetailHeader.tsx:156, BoardHeader.tsx:97 \u2014 whose comment at line 13 cites \"text-2xl font-bold\" as the 'design-language.md H1 standard', a stale contract contradicting the Dispatch spec).",
     "severity": "critical",
     "detail": "page.tsx has a second, conformant EditorialHeadingBlock (Display 44/500) \u2014 the two heading dialects live in the same file, selected by archetype. The old HeadingBlock path is what new pages get by default. Rewriting these two primitives moves ~48 consumer surfaces into the language in one diff."
    },
    {
     "title": "Skeleton-pulse and spinners ship on 46 sites though the Motion Vocabulary forbids them; the sanctioned FetchingDim pattern has 3 consumers",
     "evidence": "spec.md:319-320: \"Forbidden: ... shimmer, skeleton-pulse, count-up animations\". components/ui/skeleton.tsx uses `animate-pulse` (shared primitive); 46 animate-pulse/spin/bounce/ping sites across components (page.tsx x8, SettingsPermissionsPage x5, EntityDetailPage x5, TimelineTab x5, live-status-badge, PulseStrip, ButlerHeartbeatTile...). Meanwhile components/ui/fetching-dim.tsx (built by bu-86c4c.13 exactly to replace \"the list blanking to a skeleton/spinner\") is imported by only 3 files.",
     "severity": "major",
     "detail": "The global prefers-reduced-motion floor (index.css:673-682) silences these only for users who set the OS flag \u2014 by default the console pulses on every load, violating \"calm is the feature\". Pattern built once (FetchingDim), never generalized: the exact failure mode this lens hunts."
    },
    {
     "title": "Dual token vocabulary renders the same semantic role at two different values \u2014 muted text is a different gray per dialect",
     "evidence": "index.css defines both vocabularies with mismatched values: shadcn `--muted-foreground` light = oklch(0.556 0 0) (index.css:29) vs Dispatch `--mfg` = oklch(0.46 0 0) (index.css:109); shadcn's \"muted\" actually matches Dispatch's TERTIARY `--dim` (0.55, index.css:115). Usage split: 1,378 `muted-foreground` sites vs 174 `var(--mfg)` \u2014 so ~89% of muted text renders at what Dispatch considers tertiary/timestamp contrast, including text the bu-86c4c.16 contrast work deliberately darkened --dim to protect. Same duplication for --card vs --bg-elev, --destructive vs --red.",
     "severity": "major",
     "detail": "This is deeper than \"two dialects mix on one screen\" (known): the SAME role has two rendered values, so adjacent labels on one page differ in contrast by which import their component used. Fix is aliasing, not migration: redefine the shadcn names as `var(--mfg)` etc. in index.css and 1,378 sites snap to canonical values in one diff."
    },
    {
     "title": "NOT-LANDED: Butler hue identity collides (11 butlers into 8 slots) and the spec's canonical mapping table is fiction",
     "evidence": "components/ui/ButlerMark.tsx:51-63 KNOWN_BUTLERS has 11 entries; :104-105 `CATEGORY_VARS[idx % CATEGORY_VARS.length]` with 8 slots \u2192 qa shares chronicler's hue, relationship shares education's, travel shares finance's. spec.md:116-131 canonical table lists butlers that do not exist (memory, calendar, household \u2014 memory is a module per the repo's own notes) and disagrees with code on every row (spec: relationship=cat-1; code: relationship=cat-2-via-wraparound).",
     "severity": "major",
     "detail": "Flagged in the old dossier (\"butler hue identity collides, doc and code mappings disagree entirely\"), not landed by any merged child. Butler-hue-as-identity is a named pillar of the north star; three identity collisions defeat it everywhere ButlerMark and SessionStripeChart render. Fix: explicit per-butler token map sized to the roster (add --category-9..11 or per-butler tokens), spec table generated from the same constant."
    },
    {
     "title": "Chart color language is fragmented across four dialects despite bu-86c4c.5's chart-colors.ts claiming single source of truth",
     "evidence": "lib/chart-colors.ts:4 (\"Single source of truth for recharts stroke/fill colors\") has 7 consumers, but 8+ chart components bypass it: education hardcodes #3b82f6 (MasteryTrendChart.tsx:55-56, CrossTopicChart.tsx:42) and 5 raw hexes (MindMapGraph.tsx:18-27); chronicles AggregatePieChart.tsx:298 and AggregateStackedBar.tsx:238,242 fill from lane-taxonomy raw hexes; hex-heatmap.ts:33-37 builds a red\u2192amber\u2192green ramp for VISIT DENSITY (a rarely-visited place renders in error-red \u2014 all three state colors repurposed for a non-state meaning); MeasurementChart.tsx:54 carries its own oklch literal fallback; SessionStripeChart.tsx:198 uses butlerHueVar (defensible as identity but unregistered as a sanctioned chart channel).",
     "severity": "major",
     "detail": "The .5 fix repaired the hsl(var()) plumbing but never migrated the fleet. The mechanism gap: chart-colors.ts only models one channel (generic series). It needs four semantic channels \u2014 series, butler-identity, category, and a neutral sequential ramp for density \u2014 so every chart has an in-language answer."
    },
    {
     "title": "State hues carry second meanings and hardcoded fills break one theme or the other",
     "evidence": "components/chronicles/lane-taxonomy.ts:122-136: lane 'eat' = #f59e0b \u2014 the EXACT --amber/degraded value \u2014 and 'rest' = #059669 (a fourth green), each lane double-encoded as a tailwind class AND a hex twin (9 unnamed hexes). components/topology/TopologyGraph.tsx:57-99 introduces a SECOND amber (#eab308 yellow-500) for degraded, blue #3b82f6 as a fourth status state (staffer-ok), purple edges (:270), and hardcodes dark-slate panel fills #1e293b/#0f172a (:185,:215) that are wrong in light mode; components/chronicles/GanttSwimlaneInner.tsx:632 zebra-stripes with rgba(0,0,0,0.02/0.04) \u2014 invisible/wrong in the CANONICAL dark theme (dark-primary doctrine, spec.md:17-18). components/chronicles/MapWidgetInner.tsx:40,180 uses hsl() red/blue literals.",
     "severity": "major",
     "detail": "One hue, one meaning is the first rule of the lens: amber currently means both 'degraded' and 'lunch'; two different ambers both mean 'degraded' depending on page. These are exactly the graph/canvas surfaces (topology, gantt, map) that a className-lint like bu-86c4c.6 will miss because colors flow through JS constants and SVG props, not tailwind classes."
    },
    {
     "title": "NOT-LANDED (partial): Dispatch atoms privately forked; micro-typography splinters into unsanctioned scales",
     "evidence": "components/secrets/passport/atoms.tsx re-implements Eyebrow (:39), Voice (:106 \u2014 line-height 1.55 and arbitrary `size` prop vs spec 16px/1.6), Display (:139), PillBtn, plus its own dot/pill atoms \u2014 a full parallel kit shadowing components/ui/*. 69 inline `uppercase tracking-[0.14em]` eyebrow re-implementations vs 16 files importing ui/Eyebrow; tracking fragments into 9 values (0.14/0.12/0.10/0.1/0.08/0.06/0.04/0.02/0.01em). Voice prose renders at 15px in ingestion/timeline/EventDrawer.tsx:201-403, TimelineTab.tsx:2215, timeline/TimelineEventDrawer.tsx:62 and 17px in qa/ClaimAnchoredBlurb.tsx:43. Off-scale sizes: text-[15px] x21, text-[12px] x19, text-[8px] x3, text-[17px] x3. Status dots hand-rolled in 17 files (25 sites) vs 5 StateDot consumers; 44 files still import shadcn Card and 28 shadow-* sites persist in a \"surfaces, not cards / one elevation\" language; notifications/notification-stats-bar.tsx:35-59 is a full KPI fork (Cards + text-2xl font-bold + emerald/red + Title Case) alongside overview/KpiStrip (32px/500) and butler-detail KpiCell (28px mono).",
     "severity": "major",
     "detail": "The dossier counted \"7 Eyebrow definitions, 2 Voices\"; the forks still exist and inline re-implementations grew. The mechanism gap is that primitives exist but nothing makes the fork more expensive than the import \u2014 an eslint no-restricted-syntax on `tracking-[0.14em]`+`uppercase` outside ui/Eyebrow.tsx, and on `font-serif` sizes \u226016px outside ui/Voice.tsx, would make the primitive the cheapest path."
    }
   ],
   "moves": [
    {
     "title": "Flip the base layer: make Dispatch the default, not the opt-in",
     "why": "One diff moves every un-redesigned surface most of the way into the language and stops the drift compounding: set the root font to var(--font-sans) (killing the forbidden system-ui/Roboto default), rewrite ui/page.tsx HeadingBlock to Display/Title 500, rewrite ui/empty-state.tsx as a single Voice-italic sentence. The ~48 consumers of these two primitives inherit conformance for free, and every FUTURE page is born in-language \u2014 which no lint can achieve.",
     "cost": "M",
     "integration_points": "frontend/src/index.css:8 (root font-family); components/ui/page.tsx:106 (HeadingBlock \u2192 Display/Title tiers, collapse the two-dialect fork with EditorialHeadingBlock); components/ui/empty-state.tsx (new API: one sentence, drop icon/description/action); delete Sidebar.tsx font-sans opt-ins; existing axe/a11y tests and e2e specs guard regressions.",
     "slice_plan": "Slice 1: root font default + remove per-component font-sans opt-ins (verify flagship pages render Inter Tight). Slice 2: page.tsx heading unification. Slice 3: empty-state rewrite + sweep of its 24 consumers for copy (serif-italic sentence)."
    },
    {
     "title": "Token aliasing: collapse the dual vocabulary by redefinition, then deprecate",
     "why": "1,378 muted-foreground sites cannot be hand-migrated, but they don't need to be: redefining the shadcn token names as aliases of Dispatch tokens (--muted-foreground: var(--mfg); --card: var(--bg-elev); --destructive: var(--red); --accent et al.) snaps the entire legacy fleet to canonical VALUES in one index.css diff, ending the two-grays-for-one-role defect. The in-flight bu-86c4c.6 lint then only has to ban the legacy NAMES in new code, a far smaller contract.",
     "cost": "S",
     "integration_points": "frontend/src/index.css :root and .dark blocks (lines 18-48, 413-443); src/lib/contrast.test.ts (reads token literals \u2014 update expected values); visual sanity pass on shadcn ui/ components that assumed the old --muted-foreground lightness.",
     "slice_plan": "Slice 1: alias the text/surface tokens + fix contrast tests. Slice 2: alias state tokens (--destructive\u2192--red) and delete now-dead duplicates. Slice 3: hand bu-86c4c.6 the deprecated-name list."
    },
    {
     "title": "One chart registry with four semantic channels",
     "why": "chart-colors.ts models only generic series, so every chart with a different need (butler identity, activity category, density) invents its own dialect \u2014 including repurposing the red\u2192green STATE ramp for visit density. Extending the registry to four named channels (series: --chart-1..5; butlerIdentity: butlerHueVar; category: lane hues promoted to named index.css tokens; densityRamp: a neutral\u2192accent sequential scale replacing hex-heatmap's red\u2192green) gives every chart an in-language answer and makes the JS-constant color paths (topology, gantt, map) auditable in one file.",
     "cost": "M",
     "integration_points": "lib/chart-colors.ts (extend); index.css (add --lane-* named tokens, a sequential ramp, and topology panel tokens); migrate components/education/{MasteryTrendChart,CrossTopicChart,MindMapGraph}.tsx, components/chronicles/{lane-taxonomy.ts,AggregatePieChart,AggregateStackedBar,hex-heatmap.ts,GanttSwimlaneInner,MapWidgetInner}, components/topology/TopologyGraph.tsx; eslint no-restricted-syntax on fill=\"#/stroke=\"# in tsx.",
     "slice_plan": "Slice 1: registry channels + lane tokens in index.css. Slice 2: chronicles migration (taxonomy, pie, stacked bar, gantt zebra via --border-soft, heatmap neutral ramp). Slice 3: topology + education migration (theme-correct panel fills, one amber, StateDot colors). Slice 4: lint rule."
    },
    {
     "title": "Butler-hue registry sized to the roster, spec table generated from it",
     "why": "Identity is the point of butler hues; three modulo collisions (qa=chronicler, relationship=education, travel=finance) defeat it fleet-wide, and the spec's canonical table names butlers that don't exist. An explicit roster\u2192token map with \u226511 distinct slots, consumed by ButlerMark, charts, AND the spec doc (generated table), makes hue-identity a single source of truth that cannot silently wrap.",
     "cost": "S",
     "integration_points": "components/ui/ButlerMark.tsx:51-106 (replace idx%8 with explicit map; add --category-9..11 to index.css light+dark with the same muted-chroma treatment); openspec/specs/dashboard-design-language/spec.md:116-131 (regenerate table from the constant, drop memory/calendar/household); consumers butlerHueVar in SessionStripeChart.tsx:198 unchanged.",
     "slice_plan": "Slice 1: tokens + explicit map + collision test (assert all roster butlers map to distinct vars). Slice 2: spec table sync + doc note that butlerHueVar in charts is the sanctioned identity channel."
    },
    {
     "title": "Retire pulse: FetchingDim and honest static placeholders as the only loading language",
     "why": "The Motion Vocabulary forbids skeleton-pulse yet the shared Skeleton primitive propagates it to 46 sites, while the purpose-built FetchingDim (bu-86c4c.13) has 3 consumers \u2014 the clearest built-once-never-generalized case in the codebase. Making FetchingDim + static dim placeholders the only loading treatment (and deleting ui/skeleton.tsx) is what 'calm is the feature' means at 60fps.",
     "cost": "M",
     "integration_points": "components/ui/skeleton.tsx (delete), components/ui/page.tsx:78-79,217 (loading blocks \u2192 static dim bars, no animation), components/ui/fetching-dim.tsx (promote; document with placeholderData pattern), the 17 files with animate-pulse status dots (live-status-badge, PulseStrip, ButlerHeartbeatTile \u2192 StateDot static states); add animate-pulse/animate-spin to bu-86c4c.6's forbidden-class list so it cannot return.",
     "slice_plan": "Slice 1: page.tsx + skeleton.tsx replacement (covers the majority of sites via the shared shell). Slice 2: status-dot pulse removal folded into a StateDot adoption sweep (25 hand-rolled dots \u2192 StateDot). Slice 3: lint entry."
    }
   ],
   "ideal": "The console has exactly one visual language, enforced by defaults rather than discipline: the root stylesheet IS Dispatch (Inter Tight/Source Serif/JetBrains Mono by inheritance, one token vocabulary where shadcn names are aliases of Dispatch values), the shared primitives (page shell, empty state, Eyebrow, Voice, StateDot, KpiStrip, FetchingDim) are the cheapest possible path so forks are never worth writing, every color in JS or SVG flows through one chart registry with four semantic channels, each of the 11 butlers owns a distinct hue emitted from one generated map that also renders the spec's table, and the only motion anywhere is the four-entry vocabulary \u2014 no pulse, no spin, no second amber, no red that means lunch. A new page written by a tired agent at 3am comes out in-language because there is no other language left in the tree."
  },
  {
   "page": "cross:interaction-speed",
   "verdict": "solid",
   "jarvis_gap": "The speed policy (bu-86c4c.13) and fleet event bus (.8) built the right primitives, but their reach stops at the surfaces that shipped them: every drill-down in the console lands cold (zero prefetch calls in the entire frontend), three legacy per-page WebSockets still run beside the multiplexed bus and double-invalidate the same keys, bus-covered surfaces still poll at 15\u201330s while Approvals alone got the blessed 5-minute-reconciliation demotion, and the optimistic-mutation doctrine is a comment, not a mechanism \u2014 so hot loops like memory-fact triage still round-trip pessimistically while the north star demands same-frame apply and one-keystroke signal-to-evidence.",
   "ideal": "Every navigation the owner is about to make is already warm: RowLink/DisclosureRow/palette-highlight prefetch the destination's detail queries on hover/focus via one route-registry-driven map; exactly one WebSocket exists, exposed as a subscribable bus that pages consume instead of opening their own; refetchInterval values are three named policy tokens (LIVE_FALLBACK for bus-covered, UNCOVERED, OFF) enforced by lint; every useMutation call site is machine-classified OPTIMISTIC or HONEST-PENDING through two blessed wrappers; and no filter, window, or keystroke change ever blanks a list \u2014 placeholderData + FetchingDim is the floor everywhere.",
   "findings": [
    {
     "title": "Zero prefetching anywhere \u2014 every drill-down lands cold",
     "evidence": "grep for prefetchQuery/prefetch across frontend/src returns no fetch-warming call sites (only the placeholderData comment in components/ui/fetching-dim.tsx:5); the shared row-navigation primitives RowLink (components/ui/RowLink.tsx:65) and DisclosureRow (components/ui/DisclosureRow.tsx:60) have no prefetch affordance; the only onMouseEnter handlers in the app set local hover state (e.g. components/qa/EvidenceLog.tsx:86, components/ingestion/timeline/HourFlameStrip.tsx:201).",
     "severity": "major",
     "detail": "The drill-down spine (.3/.4) made every signal a door, but opening any door \u2014 Sessions row \u2192 ['session-detail', butler, id] (hooks/use-sessions.ts:72), fact/entity/approval detail, timeline anchors \u2014 always starts the fetch after navigation. With a 30s staleTime default (lib/query-client.ts:6) a hover-triggered prefetchQuery would make nearly all drill-downs render instantly from cache. The skill's bar: 'preload what the user will probably want next' \u2014 currently nothing is."
    },
    {
     "title": "Three legacy per-page WebSockets survive the fleet event bus, and approvals events double-invalidate",
     "evidence": "ApprovalsPage.tsx:1209 still mounts useApprovalsStream whose handler invalidates ['approvals','flat'|'history'|'detail'] (hooks/use-approvals-stream.ts:147-150) \u2014 the exact keys the bus registry also invalidates on the same event (hooks/event-cache-registry.ts:55-61), so every approval transition triggers two refetches per key. SpendPage.tsx:1243 opens a second socket (useSpendStream), SettingsConsolePage.tsx:524 a third (useSettingsConsoleStream).",
     "severity": "major",
     "detail": "Root cause is a mechanism gap: RootLayout.tsx:23 consumes useEventStream() but exposes only status \u2014 there is no context/subscribe API for pages to receive raw events from the singleton socket, so any page needing event payloads (Spend's live cost ticker, Settings console lines) must open its own connection. The registry's own doc (event-cache-registry.ts:11-15) says the bus exists so the pattern is built 'once instead of a fourth time', yet the first three copies were never retired."
    },
    {
     "title": "Poll demotion applied to Approvals only; bus-covered surfaces still poll at 15\u201330s",
     "evidence": "ApprovalsPage.tsx:1206-1214 demotes its poll to a 5-minute reconciliation sweep ('a safety net\u2026 not the primary update path'). But ['butlers','board'] polls every 30s (hooks/use-butler-status-board.ts:191) despite sessionPatch invalidating it (event-cache-registry.ts:83); ['timeline'] is invalidated by session, notification, AND ingestion patches (event-cache-registry.ts:87,103) yet the ledger head polls at 15s (hooks/use-timeline-ledger.ts:29,64) atop useTimeline's 30s default (hooks/use-timeline.ts:19); ingestion events default to a 30s poll (hooks/use-ingestion-events.ts:113) despite ingestionPatch (event-cache-registry.ts:126-130); SpendPage keeps 60\u2013120s intervals (SpendPage.tsx:603,1155,1235,1293) despite spendPatch.",
     "severity": "major",
     "detail": "Every bus event that invalidates these keys triggers a refetch AND the poll keeps firing \u2014 redundant load, and the inconsistency means the 'live via bus, poll as safety net' contract exists on exactly one page. NOT-LANDED in spirit: the old dossier's polling critique was resolved only where the epic child touched."
    },
    {
     "title": "Optimistic-mutation policy is a doc comment with no enforcement; hot-loop OPTIMISTIC-class mutations remain pessimistic",
     "evidence": "Only 8 files adopted the shared helpers (use-issues, use-notifications, use-approval-decisions, use-priority-contacts, use-contacts, use-qa, use-schedules, ApprovalsPage) vs ~40 files still on plain useMutation. Clear OPTIMISTIC-class stragglers per the module's own classification (hooks/use-optimistic-mutation.ts:10-16): useConfirmFact/useRetractFact (hooks/use-memory.ts:119,137 \u2014 invalidate-on-success only), thread-affinity override upsert/delete (hooks/use-thread-affinity.ts:70,85), saved-view update/delete (hooks/use-timeline-saved-views.ts:82,98), channel-default toggle (hooks/use-channel-defaults.ts:50), ingestion-rule enable/disable (hooks/use-ingestion-rules.ts:74), finance bulk metadata patch (hooks/use-finance.ts:99).",
     "severity": "major",
     "detail": "Confirm/retract fact is the memory-curation triage loop \u2014 the exact keyboard-clearing flow the north star says must apply in the same frame. Nothing distinguishes a call site that was reviewed and classified HONEST-PENDING from one nobody looked at; bu-86c4c.6 covers Dispatch language linting, not mutation classification."
    },
    {
     "title": "Never-blank policy misses filter/window-keyed lists \u2014 day/week/filter navigation blanks the surface",
     "evidence": "placeholderData: (prev)=>prev adopted on ~15 hooks + FetchingDim on 3 pages (SessionsPage, AuditLogPage, NotificationsPage), but absent from: chronicles episodes keyed on params (hooks/use-chronicles.ts:90-95 \u2014 day navigation blanks the Gantt swimlane), calendar workspace keyed on time-range params (hooks/use-calendar-workspace.ts:105-110 \u2014 week/month nav blanks the grid), all 7 finance queries keyed on params (hooks/use-finance.ts:27-83), and education/health equivalents.",
     "severity": "major",
     "detail": "These are exactly the interactions where the previous window's data is the correct skeleton. The FetchingDim doc (components/ui/fetching-dim.tsx:5) names the pairing as the contract, but the contract is opt-in per hook."
    },
    {
     "title": "Global search blanks between debounced keystrokes",
     "evidence": "hooks/use-search.ts:23-27 keys the query on debouncedQuery with no placeholderData \u2014 each debounced keystroke creates a fresh queryKey whose data is undefined, so results flash empty while the user extends their query; only the 300ms debounce (use-search.ts:10) masks it.",
     "severity": "minor",
     "detail": "One-line fix (placeholderData: (prev)=>prev + isFetching dim) on the highest-frequency typing surface in the console."
    },
    {
     "title": "refetchInterval zoo \u2014 every hook hand-picks a number, no policy tiers",
     "evidence": "Intervals in live code span 2s (hooks/use-whatsapp.ts:73), 5s (hooks/use-butler-logs.ts:27, use-backfill.ts:65), 15s (use-memory.ts:72,185; use-education.ts:75; use-messenger.ts:30; use-timeline-ledger.ts:29), 30s (dozens: use-health.ts, use-qa.ts:55, use-general.ts, use-chronicles.ts defaults\u2026), 60s, 120s, and 5min \u2014 with no shared constants or documented rule for which tier a surface belongs to.",
     "severity": "minor",
     "detail": "The skill's bar: 'cache invalidation states its rule.' Without named tiers, bus-covered vs uncovered surfaces are indistinguishable at the call site and every new hook re-decides \u2014 this is how finding 3's drift keeps regenerating."
    }
   ],
   "moves": [
    {
     "title": "Intent prefetch through RowLink/DisclosureRow + route registry",
     "why": "Makes every drill-down in the spine render instantly from cache \u2014 the single biggest perceived-latency win available, and it lands at one integration point instead of per-page.",
     "cost": "M",
     "integration_points": "components/ui/RowLink.tsx and DisclosureRow.tsx (add optional prefetch prop wired to onMouseEnter/onFocus with a ~75ms intent delay); lib/route-registry.ts (register detail-query builders \u2014 e.g. session-detail, memory-fact, approval-detail \u2014 beside their routes so rows AND cmdk palette highlight share one prefetch map); lib/query-client.ts (queryClient.prefetchQuery with existing 30s staleTime).",
     "slice_plan": "Slice 1: usePrefetchOnIntent hook + RowLink prop, wired to SessionsPage rows \u2192 ['session-detail']. Slice 2: timeline ledger rows + approvals rail. Slice 3: palette-highlight prefetch from the route registry."
    },
    {
     "title": "One socket: subscribable event-bus context, retire the three legacy streams",
     "why": "Eliminates duplicate invalidation (2\u00d7 refetch per approval event today), drops to a single WS connection app-wide, and unblocks any future page needing live payloads without a bespoke socket.",
     "cost": "M",
     "integration_points": "layouts/RootLayout.tsx (wrap in EventBusProvider exposing subscribe(type, cb) backed by the existing onEvent param of use-event-stream.ts:34); SpendPage.tsx:1243 (port streamedCostUsd ticker to subscribe('spend')); SettingsConsolePage.tsx:524 (port console lines to a bus event type \u2014 may need the settings-console payload added to src/butlers/api/routers/events.py EVENT_TYPES); ApprovalsPage.tsx:1209 (delete the call); then delete use-approvals-stream.ts, use-spend-stream.ts, use-settings-console-stream.ts.",
     "slice_plan": "Slice 1: provider + subscribe API + delete useApprovalsStream (pure win, no backend change). Slice 2: spend ticker port. Slice 3: settings-console port + backend event type."
    },
    {
     "title": "Poll-policy tokens keyed to bus coverage, lint-enforced",
     "why": "Turns the blessed Approvals pattern (bus-live + 5-min reconciliation) into the default for every bus-covered surface and makes the interval zoo impossible to regrow.",
     "cost": "S",
     "integration_points": "New lib/poll-policy.ts exporting POLL.RECONCILE (5min, bus-covered), POLL.UNCOVERED (30s), POLL.OFF; demote use-butler-status-board.ts:191, use-timeline-ledger.ts:29, use-timeline.ts:19, use-ingestion-events.ts:113, SpendPage.tsx:603/1155/1235/1293 to POLL.RECONCILE; eslint no-restricted-syntax rule banning numeric refetchInterval literals (frontend CI already runs eslint \u2014 the enforcement lane exists).",
     "slice_plan": "Slice 1: tokens + demote the four bus-covered surfaces. Slice 2: sweep remaining hooks onto tokens + lint rule."
    },
    {
     "title": "Mutation classification made mechanical: two blessed wrappers + lint ban on bare useMutation",
     "why": "The policy's own doc demands every call site be classified but nothing checks it; making classification structural converts the remaining ~40 files from 'unreviewed' to 'explicitly OPTIMISTIC or HONEST-PENDING' and can never drift.",
     "cost": "M",
     "integration_points": "hooks/use-optimistic-mutation.ts (add a thin useHonestPendingMutation re-export so intent is declared in code); eslint no-restricted-imports banning useMutation from '@tanstack/react-query' outside the two wrappers; convert the six named stragglers \u2014 use-memory.ts:119/137 (confirm/retract fact, do first: hot triage loop), use-thread-affinity.ts:70/85, use-timeline-saved-views.ts:82/98, use-channel-defaults.ts:50, use-ingestion-rules.ts:74, use-finance.ts:99 \u2014 to useOptimisticListMutation.",
     "slice_plan": "Slice 1: convert confirm/retract fact + saved-views. Slice 2: remaining four. Slice 3: wrapper + lint rule + mechanical rename of all HONEST-PENDING sites."
    },
    {
     "title": "Never-blank floor: shared windowed-list query wrapper",
     "why": "Generalizes placeholderData+FetchingDim from 3 pages to every filter/window-keyed surface so changing a day, week, filter, or search query never blanks what the owner is reading.",
     "cost": "S",
     "integration_points": "New useWindowedListQuery wrapper (or just add placeholderData:(prev)=>prev) in hooks/use-chronicles.ts:90, use-calendar-workspace.ts:105, use-finance.ts:27-83, use-search.ts:23; pair each consuming page with components/ui/fetching-dim.tsx per its documented contract.",
     "slice_plan": "Slice 1: search + chronicles episodes (highest-frequency interactions). Slice 2: calendar workspace + finance + education/health sweep."
    }
   ]
  },
  {
   "page": "cross:accessibility",
   "verdict": "solid",
   "jarvis_gap": "The a11y floor (bu-86c4c.16) is real \u2014 jsx-a11y lint, contrast-locked tokens, a CSS reduced-motion kill-switch, and one gold-standard drawer \u2014 but its reach is a lottery by surface. The two signature JARVIS surfaces are the least accessible: the ONE command spine (EntityFinder) is a fake modal that leaks Tab focus behind its own overlay and never restores focus, and the live event-stream heartbeat degrades silently for anyone not looking at the pixels (\"every degraded source names itself\" is currently visual-only). Each accessibility pattern was built once, correctly, in exactly one place \u2014 modal choreography in the ingestion drawer, reduced-motion gating in one sparkline, real-page axe tests on 3 of 38 pages \u2014 and never generalized into a mechanism, so every new surface re-rolls the dice.",
   "ideal": "One overlay contract, one announcer, one motion policy, one axe gate \u2014 all mechanisms, not conventions. Every modal/drawer/palette in the app gets identical choreography (focus-in, trap, Escape, focus-restore, sr-only status region) from a single shared implementation, enforced by lint so a hand-rolled `fixed inset-0` overlay cannot merge. A single shell-level live region narrates what the pixels already say: stream connected/reconnecting/down, route changes, new-event counts. prefers-reduced-motion governs ALL motion \u2014 CSS, recharts mount animations, smooth scrolls \u2014 via shared helpers with a lint ban on raw escape hatches. And the real-routed-page axe test iterates the route registry, so a page cannot exist without an axe pass. The result: the keyboard-only, screen-reader, and motion-sensitive operator gets the same five-second verdict and under-a-minute queue-clear the sighted mouse user gets.",
   "findings": [
    {
     "title": "The one command spine (EntityFinder) is a fake modal: Tab leaks focus behind the overlay, no dialog semantics, no focus restore",
     "evidence": "frontend/src/components/layout/EntityFinder.tsx:431-453 \u2014 the palette is a raw div overlay + cmdk Command, with no role=\"dialog\"/aria-modal and no focus trap. The onKeyDown at :449 only preventDefaults Tab when `activeResult` (an ENTITY result) exists; when the query matches only pages/butlers/actions (activeResult null, :390-413 derives it exclusively from entity lists), Tab moves focus to the page BEHIND the black/60 overlay while the palette stays open. On close (Esc/select), focus is never returned to the header trigger \u2014 the focused input unmounts and focus falls to <body>. Every Radix Dialog/Sheet in ui/ does all of this correctly; the app's single most important surface is the one exempt from it.",
     "severity": "critical",
     "detail": "Also: input at :468 is outline-none (acceptable inside a single-field palette, but part of the same hand-rolled divergence). Fix is to render through the shared dialog choreography (or cmdk's own Command.Dialog which wraps Radix), keep the Tab=hop override, and preventDefault Tab when no entity is active."
    },
    {
     "title": "Modal/drawer choreography was built once (ingestion EventDrawer) and not generalized \u2014 four overlays, four different a11y qualities",
     "evidence": "Gold standard: frontend/src/components/ingestion/timeline/EventDrawer.tsx:554-575 (focus-in on open, Escape closes + returns focus to triggering row, sr-only role=\"status\" live region at :674). Its sibling built on the SAME useEventDrawerState: frontend/src/components/timeline/TimelineEventDrawer.tsx:129-142 (/timeline, bu-86c4c.10) has NONE of it \u2014 no focus-in, no Escape handler, no focus restore, just an aria-label and a close button. frontend/src/components/calendar/CalendarAgendaView.tsx:44-48 declares role=\"dialog\" aria-modal=\"true\" with no focus trap, no focus-in, no Escape \u2014 aria-modal without focus management hides the background from screen readers while keyboard focus remains in it (worse than no ARIA). frontend/src/components/butler-detail/ButlerManagementTab.tsx:32-53 ModalBackdrop: Escape only, no dialog role, no focus trap/restore.",
     "severity": "major",
     "detail": "The failure scenario for CalendarAgendaView: SR user opens agenda, presses Tab, lands on controls the SR reports don't exist (aria-modal excludes them); keyboard user opens ButlerManagementTab modal, Tab walks out into the page behind it. One shared choreography (Radix or a useModalChoreography hook extracted from EventDrawer) fixes all four."
    },
    {
     "title": "Reduced-motion floor is CSS-only; recharts mount animations and smooth scrolls escape it, with three divergent chart conventions",
     "evidence": "frontend/src/index.css:660-680 is the global floor, but it only reaches CSS animation/transition. Recharts animations are rAF-driven: 7 chart files leave the default mount animation running with no gating (frontend/src/components/costs/CostChart.tsx, education/CrossTopicChart.tsx, education/MasteryTrendChart.tsx, chronicles/AggregatePieChart.tsx, butler-detail/ButlerFinanceFinancesTab.tsx, system/DbSizeTile.tsx, health/MeasurementChart.tsx \u2014 all import recharts, zero isAnimationActive), ~8 files hardcode isAnimationActive={false}, and exactly one gates on the hook (butler-detail/ButlerHealthMeasurementsTab.tsx:336,344 via usePrefersReducedMotion). Five scrollIntoView({behavior:\"smooth\"}) sites also ignore the preference: pages/EntityDetailPage.tsx:2918, pages/CalendarWorkspacePage.tsx:2711, components/ingestion/TimelineTab.tsx:1145, components/chat/MessageThread.tsx:266, components/ingestion/timeline/EventDrawer.tsx:627.",
     "severity": "major",
     "detail": "usePrefersReducedMotion exists (frontend/src/hooks/use-prefers-reduced-motion.ts) and is used by only 2 non-test components. Mechanism, not symptom-fixes: a shared chartAnimationProps() default + a smoothScrollIntoView() helper, plus a no-restricted-syntax lint entry (the codebase already uses this pattern for hsl(var()) in eslint.config)."
    },
    {
     "title": "Live event-stream state changes and live-tail arrivals are never announced \u2014 'every degraded source names itself' is visual-only",
     "evidence": "frontend/src/components/layout/LiveIndicator.tsx:56-69 \u2014 the shell heartbeat swaps its text Live\u2192Reconnecting\u2192Offline inside a plain span (title tooltip only, no role=\"status\"/aria-live), so a screen-reader user never hears the fleet stream drop; the same span is `hidden ... sm:inline-flex`, so on mobile NO user gets the degraded-stream signal at all. frontend/src/components/timeline/NewEventsPill.tsx:22-33 \u2014 the \"N new events\" affordance mounts/updates silently (no live region), so an SR user paging history never learns events accumulated. Contrast: the ingestion EventDrawer already ships an sr-only role=\"status\" region (EventDrawer.tsx:674) proving the pattern exists in-repo.",
     "severity": "major",
     "detail": "One shell-level sr-only aria-live=\"polite\" region fed by useEventStream status transitions (debounced: announce only open\u2192reconnecting\u2192down edges) covers LiveIndicator, and the same region can take route-change announcements (see SPA navigation: page.tsx manages document.title at frontend/src/components/ui/page.tsx:338-343, but title changes are not announced by SRs in SPAs and Shell has no route announcer or skip-to-content link \u2014 frontend/src/components/layout/Shell.tsx:89)."
    },
    {
     "title": "Secrets passport strips keyboard focus visibility on ~10 edit fields (WCAG 2.4.7)",
     "evidence": "frontend/src/components/secrets/passport/pages.tsx:1471,1971,2022,2042,2638,3110,3134,3150,3172 \u2014 textareas/inputs (including the rotate-credential paste field) with `outline-none` and no focus-visible replacement in className or inline style (verified :1460-1480: static border var(--border-strong), no focus style). frontend/src/components/secrets/passport/Spine.tsx:53 \u2014 the '/' credential search input, same. The passport's bespoke design-token system diverged from shared ui/input.tsx, which carries a proper focus ring.",
     "severity": "major",
     "detail": "A keyboard user rotating a credential \u2014 the single most sensitive write on the page \u2014 cannot see which field has focus. Fix inside the passport's token language: a focus-visible ring on var(--fg) or border-strong\u2192fg swap, applied via one shared passport field class."
    },
    {
     "title": "Real-routed-page axe testing covers 3 of 38 pages \u2014 the pattern exists but was never generalized",
     "evidence": "Only frontend/src/pages/ButlersPage.a11y.test.tsx, frontend/src/pages/ButlerDetailPage.a11y.test.tsx, and frontend/src/components/ingestion/TimelineTab.a11y.test.tsx exist (repo-wide grep for jest-axe/vitest-axe), against 38 page components in frontend/src/pages/. The ButlersPage file's own header (:1-19) documents why stub-based axe was theater and how the real-page + mocked-data-layer pattern works \u2014 that rationale applies to every other page, including ones with confirmed violations this sweep found (EntityDetailPage, CalendarAgendaView).",
     "severity": "major",
     "detail": "Generalize via the route registry (frontend/src/lib/route-registry.ts ALL_ROUTES): one parameterized suite that mounts each registered route with the standard data-layer mocks and runs axe across its loading/empty/error/populated states, with an explicit allowlist for pages needing bespoke mocks."
    },
    {
     "title": "Icon-only edit controls with no accessible name on the entity rename flow",
     "evidence": "frontend/src/pages/EntityDetailPage.tsx:3072-3100 \u2014 three ghost icon Buttons (Check=save, X=cancel, Pencil=start-edit) around the canonical-name editor have no aria-label, sr-only text, or title; their only content is a lucide icon. jsx-a11y has no enabled rule for nameless buttons and no axe test covers this page, so the lint+axe floor structurally cannot catch this class.",
     "severity": "minor",
     "detail": "SR announces three blank 'button's on the primary identity-editing control of the entity dossier. Same-page symptom of finding 6 (axe coverage); direct fix is three aria-labels, mechanism fix is the registry-driven axe sweep."
    },
    {
     "title": "Global single-key shortcut guard misses SELECT and contentEditable targets",
     "evidence": "frontend/src/hooks/use-keyboard-shortcuts.ts:20-21 \u2014 the editable-target guard checks only tagName INPUT/TEXTAREA (window-level keydown, 4 window keydown listeners repo-wide). A native <select> or any contentEditable region focused while typing 'g'/'j'/'k'/'/' fires page shortcuts. NOTE: bu-5o22a (shared editable-field keydown guard) is the in-flight home for this mechanism \u2014 this is scope input for it (the shared guard must cover SELECT, contentEditable, and open-modal suspension), not a new bead.",
     "severity": "minor",
     "detail": "Reported only as an extension of known bu-5o22a scope; the guard should also suspend page-level single-key shortcuts while any dialog/palette is open, which the shared overlay contract (move 1) can expose as state."
    }
   ],
   "moves": [
    {
     "title": "One overlay contract: shared modal choreography + lint ban on hand-rolled overlays",
     "why": "Four overlays currently ship four different a11y qualities (findings 1-2); the correct implementation already exists in EventDrawer. A single mechanism makes the next drawer accessible by construction \u2014 including the command spine, the console's front door.",
     "cost": "M",
     "slice_plan": "1) Extract useModalChoreography (focus-in, trap, Escape, focus-restore, optional sr-only status region) from ingestion EventDrawer; 2) migrate EntityFinder (keep Tab=hop but trap Tab when no entity active; restore focus on close; add role=dialog/aria-modal), TimelineEventDrawer, CalendarAgendaView, ButlerManagementTab ModalBackdrop; 3) add no-restricted-syntax lint entry flagging `fixed inset-0` + z-50 overlay JSX outside src/components/ui/, mirroring the existing hsl(var()) guard.",
     "integration_points": "frontend/src/components/ingestion/timeline/EventDrawer.tsx (source of truth), frontend/src/components/layout/EntityFinder.tsx, frontend/src/components/timeline/TimelineEventDrawer.tsx, frontend/src/components/calendar/CalendarAgendaView.tsx, frontend/src/components/butler-detail/ButlerManagementTab.tsx, frontend/eslint.config.js"
    },
    {
     "title": "Shell announcer: one sr-only live region for stream state, route changes, and live-tail arrivals",
     "why": "Earned calm must be perceivable without sight: today the fleet stream degrades, routes change, and new events accumulate in total silence for SR users (finding 4), and on mobile the stream indicator vanishes entirely.",
     "cost": "S",
     "slice_plan": "1) Add a single aria-live=polite sr-only region to Shell; 2) feed it debounced useEventStream status EDGES ('Fleet stream reconnecting', 'Fleet stream offline', 'Fleet stream restored'); 3) announce route changes from the existing Page title effect (page.tsx already owns titles); 4) have NewEventsPill publish its count to the same region; 5) render LiveIndicator on mobile (dot-only) instead of hiding it.",
     "integration_points": "frontend/src/components/layout/Shell.tsx, frontend/src/components/layout/LiveIndicator.tsx, frontend/src/hooks/use-event-stream.ts, frontend/src/components/ui/page.tsx:338, frontend/src/components/timeline/NewEventsPill.tsx"
    },
    {
     "title": "Motion policy for JS: chart animation defaults + smooth-scroll helper honoring prefers-reduced-motion, lint-enforced",
     "why": "The CSS reduced-motion floor gives false confidence \u2014 all recharts mount animations and 5 smooth scrolls bypass it, and chart animation handling has three divergent conventions across 26 chart files (finding 3).",
     "cost": "S",
     "slice_plan": "1) Add chartAnimationProps() beside src/lib/chart-colors.ts returning {isAnimationActive: !prefersReducedMotion}; apply to the 7 ungated chart files (or standardize on isAnimationActive={false} everywhere \u2014 charts on an operator console arguably should never mount-animate); 2) add smoothScrollIntoView(el, opts) that downgrades behavior to 'auto' under reduced motion; migrate the 5 call sites; 3) lint: no-restricted-syntax for `behavior: \"smooth\"` and a rule requiring isAnimationActive on recharts series components.",
     "integration_points": "frontend/src/lib/chart-colors.ts (sibling module), frontend/src/hooks/use-prefers-reduced-motion.ts, frontend/eslint.config.js, 7 ungated chart files + 5 scrollIntoView sites listed in finding 3"
    },
    {
     "title": "Registry-driven axe sweep: every route in ALL_ROUTES gets a real-page axe pass",
     "why": "The axe floor is correct but reaches 3/38 pages; nameless icon buttons on EntityDetailPage prove violations live in the uncovered 35. Route-registry iteration turns per-page tests into a structural gate \u2014 a new page cannot ship without one (finding 6-7).",
     "cost": "M",
     "slice_plan": "1) Build a shared mount harness (MemoryRouter + QueryClient + standard data-layer mocks, following ButlersPage.a11y.test.tsx's documented pattern); 2) parameterized suite over ALL_ROUTES with a per-route mock/skip manifest \u2014 start permissive (skip-with-reason), burn down; 3) fix violations it surfaces (EntityDetailPage aria-labels first); 4) CI treats a route absent from both the suite and the skip manifest as a failure.",
     "integration_points": "frontend/src/lib/route-registry.ts (ALL_ROUTES), frontend/src/pages/ButlersPage.a11y.test.tsx (pattern source), frontend/src/test/setup.ts, frontend CI eslint/vitest job"
    },
    {
     "title": "Passport focus restoration: one focus-visible treatment for all bespoke secrets fields",
     "why": "Keyboard focus is invisible on the credential-rotation and search fields of the most sensitive write surface in the console (finding 5) \u2014 a WCAG 2.4.7 failure introduced by the passport's parallel token system.",
     "cost": "S",
     "slice_plan": "1) Define one passport field focus style inside its token language (e.g. focus-visible border-color var(--fg) + 1px ring) as a shared class/const guarded by the existing passport design-token lint rule; 2) apply to the ~10 outline-none fields in pages.tsx and Spine.tsx; 3) extend src/lib/contrast.test.ts to assert the focus indicator pair meets 3:1 in both themes.",
     "integration_points": "frontend/src/components/secrets/passport/pages.tsx, frontend/src/components/secrets/passport/Spine.tsx, frontend/src/lib/contrast.test.ts, the passport design-token eslint guard"
    }
   ]
  },
  {
   "page": "eco:connectors",
   "verdict": "n/a",
   "jarvis_gap": "The fleet's perception surface is email-heavy and manual-entry-heavy: gmail/calendar/drive/google-health/owntracks/spotify/steam/home-assistant/telegram/whatsapp/discord/live-listener all exist, but three manifesto-level promises are structurally unfulfillable with current sources \u2014 finance sees only spend that emails a receipt, travel promises real-time delay/gate alerts with no flight-status feed, and chronicler/lifestyle have no signal for the largest share of a knowledge-worker's day (screen and browser activity). A JARVIS perceives the owner's money, movement-in-progress, and attention directly; today those arrive late, by email, or not at all.",
   "findings": [
    {
     "title": "Travel manifesto promises real-time delay/gate alerts but no flight-status source exists anywhere",
     "evidence": "roster/travel/MANIFESTO.md ('We will alert you to delays and gate changes before the airlines announce them'; 'surfaced the moment they happen') vs src/butlers/connectors/ containing no flight/airline connector and openspec/specs/ having no connector-flight* spec; travel ingestion is gmail-email-only and travel.legs (roster/travel/migrations/001_travel_tables.py:53) stores static booking data with no live-status columns. The promise is binding per vision.md rule 6 and is currently impossible to fulfill.",
     "severity": "major",
     "detail": "Airlines announce gate changes/delays via their ops APIs long before (or instead of) email. Email-only ingestion means the butler learns of a delay when the owner's inbox does \u2014 often after the owner is already at the wrong gate."
    },
    {
     "title": "Finance butler perceives only spend that emails a receipt; no bank/transaction feed connector",
     "evidence": "roster/finance/MANIFESTO.md ('The answers come from your actual email history'); openspec/specs/finance-data-import/spec.md is manual CSV import (Chase/Amex/CapitalOne header detection); openspec/specs/bulk-transaction-ingestion exists as a normalization path but no connector feeds it \u2014 grep of src/butlers/connectors/ shows zero bank/plaid/simplefin references.",
     "severity": "major",
     "detail": "Card-present transactions, bank transfers, ATM withdrawals, and merchants that don't email receipts are invisible. The manifesto's 'never surprise you with a bill' and 'find any transaction' promises silently degrade to 'any transaction that happened to email you'."
    },
    {
     "title": "Chronicler's evidence base has no digital-activity source despite an adapter-registration path built for it",
     "evidence": "roster/chronicler/MANIFESTO.md ('LLM sessions, the calendar events you actually attended, the Spotify session summaries, and, over time, everything else the system honestly knows'); openspec/specs/chronicler-source-compatibility/spec.md:52-59 defines the chronicler_adapter registration mechanism for new sources; no connector captures screen time, app usage, or browsing.",
     "severity": "major",
     "detail": "For a desk-working owner, screen activity is the majority of waking evidence. Chronicler can currently reconstruct music, meetings, and location, but the 6 hours between them are a blank \u2014 the biggest single hole in retrospective time."
    },
    {
     "title": "Weather is consumed as an input by three butler skills but no weather source exists in the system",
     "evidence": "grep 'weather' hits roster/home/.agents/skills/energy/SKILL.md, roster/travel/.agents/skills/pre-trip-checklist/SKILL.md, roster/switchboard/.agents/skills/message-triage/SKILL.md \u2014 yet src/butlers/modules/ and src/butlers/connectors/ contain no weather module/connector and openspec/specs/ has none (seasonal-awareness is calendar-period-based, not meteorological).",
     "severity": "minor",
     "detail": "Sessions that need weather must ad-hoc web-search inside an ephemeral LLM run or fabricate \u2014 the latter violates the no-fabrication doctrine. A deterministic keyless source (Open-Meteo) makes this a solved, testable input."
    },
    {
     "title": "Lifestyle manifesto covers shows/books/reading but only music (spotify) and gaming (steam) have connectors",
     "evidence": "roster/lifestyle/MANIFESTO.md ('Track what you're consuming: the show you just started, the book you're reading, the game you've been playing') vs connector inventory in src/butlers/connectors/ \u2014 video, reading, and browsing consumption all require manual owner logging.",
     "severity": "minor",
     "detail": "The taste-mirror promise decays to whatever the owner remembers to log. An ActivityWatch web-watcher bucket (proposal 3) covers most of this passively without a per-service (Netflix/YouTube/Kindle) connector zoo."
    },
    {
     "title": "Order confirmations are already perceived via gmail but delivery progress is not \u2014 packages go dark after purchase",
     "evidence": "src/butlers/connectors/gmail.py ingests order/shipping emails (finance manifesto explicitly claims receipts), but no connector polls carrier/tracking APIs; no parcel/tracking spec in openspec/specs/; no butler manifesto currently owns delivery logistics.",
     "severity": "minor",
     "detail": "The system knows a package exists (order email) but cannot answer 'is anything arriving today?' \u2014 a classic daily-briefing item. The tracking number is already in the ingested payload; only the polling leg is missing."
    },
    {
     "title": "Structural templates for every proposed connector class already exist, making build costs genuinely M not L",
     "evidence": "openspec/specs/connector-google-health/spec.md ('pure polling-and-ingest connector and structurally mirrors connector-spotify' with per-account state diffing); openspec/specs/connector-owntracks/spec.md (webhook-server pattern); .claude/skills/adding-connectors-and-modules/SKILL.md codifies the 4-component pattern (account registry, module, connector, dashboard) with reference checklists.",
     "severity": "minor",
     "detail": "Bank-feed and flight-status connectors are poll+state-diff clones of google_health/spotify; a parcel connector is a watch-table poller; weather needs no account registry at all (keyless API). None require new core infrastructure \u2014 all fit rule 2 (modules only add tools) and rule 7 (transport stays in connectors)."
    },
    {
     "title": "Home-domain perception is alive, not dead \u2014 bu-zquce.7's original premise is stale, so no home-sensor re-proposal is warranted",
     "evidence": "bd show bu-zquce.7 notes: 'home_assistant_rest.py is NO LONGER dead code - PR #2740 (bu-2gfh9) wired HARestPoller/HAFallbackController into home_assistant.py _main as the REST polling fallback'; src/butlers/connectors/home_assistant*.py is a live 8-file pipeline.",
     "severity": "minor",
     "detail": "Recorded so the synthesizer does not double-count: smart-home and music domains are covered by live connectors; this audit deliberately proposes nothing there."
    }
   ],
   "moves": [
    {
     "title": "connector-simplefin: sovereign bank-transaction feed for the finance butler",
     "why": "Closes the finance butler's largest perception gap \u2014 spend that never emails a receipt (card-present, transfers, ATM, subscriptions billed silently). SimpleFIN Bridge is the sovereignty-aligned choice: one owner-held access token, no Plaid enterprise contract, user revocable; add GoCardless Bank Account Data as an alternate provider for EU accounts. Transactions land in the same finance.transactions normalization path CSV import already uses, so every downstream promise (subscriptions, monthly overview, anomaly detection per finance-anomaly-detection spec) immediately gets truer data.",
     "cost": "M",
     "integration_points": "New src/butlers/connectors/simplefin.py (poll + state-diff, structurally mirrors connector-spotify/google_health per its spec); public.simplefin_accounts registry migration per .claude/skills/adding-connectors-and-modules/references/account-registry-pattern.md with access token as secured entity_info (RFC 0004 new type simplefin_access_token); RFC 0003 amendment registering channel=simplefin/provider=simplefin; envelopes \u2192 Switchboard \u2192 finance butler, deduped into finance.transactions via the openspec/specs/bulk-transaction-ingestion pipeline; dashboard account CRUD per dashboard-google-accounts pattern (GET/POST/DELETE /api/simplefin/accounts); new openspec/specs/connector-simplefin + simplefin-account-registry. Serves roster/finance/MANIFESTO.md directly (requires a one-line amendment: 'your actual email history' becomes 'your email and bank feeds').",
     "slice_plan": "1) account registry + token validation + dashboard connect; 2) connector poll loop with first-poll baseline (no historical flood) + ingest.v1 envelopes; 3) finance-side dedupe against email-derived transactions (match on amount+date+merchant fuzzy) so the same purchase seen twice becomes one enriched row."
    },
    {
     "title": "connector-flightstatus: live flight watch fulfilling the travel butler's real-time promise",
     "why": "The travel manifesto's most concrete promise ('alert you to delays and gate changes before the airlines announce them') is unfulfillable today \u2014 email-only ingestion learns of changes when the airline mails. A watch-based poller (FlightAware AeroAPI personal tier, or aviationstack) turns booked legs into live-tracked objects for the 48h around departure, emitting delay/gate/cancel transition events the travel butler converts to owner notifications.",
     "cost": "M",
     "integration_points": "New src/butlers/connectors/flightstatus.py; connector-owned connectors.flight_watches table (flight number, date, PNR-free \u2014 populated when the travel butler's email-parsing session calls a new watch_flight MCP tool in roster/travel/tools/, keeping the connector out of the travel schema per vision.md rule 3); migration adding live-status columns (status, gate, delay_minutes, last_status_at) to travel.legs (roster/travel/migrations/001_travel_tables.py:53); RFC 0003 channel=flightstatus/provider=flightaware; API key as secured entity_info; state-diff only emits on change (delay threshold config); new openspec/specs/connector-flightstatus. Serves roster/travel/MANIFESTO.md verbatim \u2014 no amendment needed, this is the missing half of an existing promise.",
     "slice_plan": "1) flight_watches table + watch_flight/unwatch tool + auto-watch on leg creation; 2) poller with adaptive cadence (6h out: hourly; 2h out: every 5min) + transition envelopes; 3) travel butler notification templates (gate change, delay >15min, cancellation with rebooking context) + dashboard trip-timeline live-status chips."
    },
    {
     "title": "connector-activitywatch: passive digital-activity evidence for Chronicler and Lifestyle",
     "why": "The single biggest hole in Chronicler's retrospective record is what the owner did on their machines \u2014 the majority of a desk-worker's day. ActivityWatch is self-hosted, open-source, local-only (REST on localhost:5600) \u2014 perfect sovereignty fit, zero cloud dependency. Window/AFK/browser buckets become honest episodes ('coding on butlers 09:12\u201311:40', 'watched YouTube 40min'), which also passively fulfills Lifestyle's 'track what you're consuming' promise for video/reading without a per-service connector zoo.",
     "cost": "M",
     "integration_points": "New src/butlers/connectors/activitywatch.py polling GET /api/0/buckets + /events with cursor per bucket (cursor_store.py already exists); pre-aggregation in the connector (coalesce raw window events into >=N-minute app/domain episodes \u2014 never ship raw keystroke-level titles) with sensitivity gating via the existing IngestionPolicyEvaluator source-filter path and control.policy_tier; RFC 0003 channel=activitywatch/provider=activitywatch; no account registry needed (local socket \u2014 note the deviation in the spec like owntracks does); Chronicler adapter registered per openspec/specs/chronicler-source-compatibility/spec.md:52 (chronicler_adapter mode) projecting episodes with provenance + retention class; lifestyle butler consumes weekly aggregates for its digest. Serves roster/chronicler/MANIFESTO.md ('everything else the system honestly knows') and roster/lifestyle/MANIFESTO.md; Chronicler needs no amendment, Lifestyle a minor one (passive consumption capture).",
     "slice_plan": "1) connector + episode coalescing + baseline-on-first-poll; 2) Chronicler adapter + retention/sensitivity classes (browser domains only, no full URLs by default); 3) lifestyle weekly-digest integration + dashboard day-ribbon rendering via the in-flight Chronicler redesign's episode surface (bu-jc6htw covers UI, not this source)."
    },
    {
     "title": "module-weather: deterministic keyless weather tools + severe-alert emitter",
     "why": "Three butler skills already assume weather as an input (home energy, travel pre-trip, switchboard triage) with no source behind them \u2014 sessions must web-search or fabricate, and fabrication violates doctrine. Open-Meteo is keyless, free for personal use, and returns structured forecasts; wiring it as a shared module makes weather a solved deterministic input for every butler, plus a tiny alert path for severe conditions keyed to the owner's last-known owntracks location.",
     "cost": "S",
     "integration_points": "New shared module src/butlers/modules/weather.py implementing the Module ABC (src/butlers/modules/base.py) with tools weather_current/weather_forecast/weather_alerts (lat/lon or named-place; default = last owntracks position read via a switchboard-brokered lookup, not cross-schema); enable in roster/home, roster/travel, roster/health, roster/switchboard butler.toml [modules.weather]; optional micro-connector (or a scheduled home-butler task \u2014 cheaper, no new process) emitting severe-weather envelopes; new openspec/specs/module-weather noting the no-account-registry deviation (keyless API). Serves roster/home/MANIFESTO.md (comfort/energy awareness) and roster/travel/MANIFESTO.md (pre-trip prep); no manifesto amendments required \u2014 this backfills promises already made.",
     "slice_plan": "1) module + tools + tests against recorded Open-Meteo fixtures; 2) enable per-butler + update the three skills to call the tool instead of hand-waving; 3) severe-alert scheduled check with notify() on threshold crossings."
    },
    {
     "title": "connector-parcels: delivery tracking that finishes the story gmail starts",
     "why": "The system already perceives every order at purchase time (gmail ingests the confirmation, finance records the spend) then goes blind until a delivery email maybe arrives. A watch-table poller against a unified tracking API (17track; carrier-direct fallback) turns tracking numbers the fleet already possesses into arrival awareness \u2014 'two packages arriving today' is a canonical morning-briefing line and an errand-planning input the owner currently supplies manually.",
     "cost": "M",
     "integration_points": "New src/butlers/connectors/parcels.py polling a connectors.parcel_watches table (tracking_number, carrier, status, eta, terminal-state cleanup); populated via a new track_parcel MCP tool the general butler calls when its email-triage session sees a shipping confirmation (tracking numbers are already in gmail-ingested payload.raw); RFC 0003 channel=parcels/provider=17track; API key as secured entity_info per RFC 0004; transition-only envelopes (out_for_delivery, delivered, exception) \u2192 Switchboard \u2192 general butler; briefing contribution via the existing cross-butler-briefing-contribution spec. No butler currently owns delivery logistics: this implies a small amendment to roster/general/MANIFESTO.md (the 'capture anything' second-brain absorbing logistics awareness) \u2014 flagging that explicitly rather than inventing a new butler for one connector.",
     "slice_plan": "1) parcel_watches table + track_parcel tool + manual-add from dashboard; 2) poller with per-parcel backoff and terminal-state expiry; 3) general-butler extraction skill (shipping email \u2192 track_parcel) + briefing line + notify-on-delivered."
    }
   ]
  },
  {
   "findings": [
    {
     "title": "Every LLM call in the ecosystem \u2014 including 'lightweight' single-turn discretion screening \u2014 is a cold CLI subprocess spawn",
     "evidence": "src/butlers/core/runtimes/claude_code.py:439 (asyncio.create_subprocess_exec per invoke); src/butlers/connectors/discretion_dispatcher.py:226-239 \u2014 DiscretionDispatcher.call() routes through the same RuntimeAdapter.invoke() subprocess path with mcp_servers={} and max_turns=1. No runtime_type in src/butlers/core/runtimes/ (base/claude_code/codex/gemini/opencode) calls a provider API directly. Per-message connector screening (telegram_user_client.py:357, whatsapp_user_client.py:543, live_listener/connector.py:208, home_assistant_pipeline.py:33) each pay full CLI bootstrap for a one-shot yes/no classification.",
     "severity": "major",
     "detail": "An interactive Telegram reply chains TWO serial cold spawns: switchboard classification session (pipeline.py:1976-1990, Complexity.CHEAP, full claude CLI + MCP handshake to call route_to_butler) then the target butler's route session (core_tools/_routing.py:546). CLI process startup + MCP discovery is pure overhead on both hops and on every high-volume screening call."
    },
    {
     "title": "Trigger-to-act latency is unmeasured \u2014 no phase decomposition anywhere in the spine",
     "evidence": "grep for startup/time-to-first-token/spawn-overhead metrics across src/butlers returns nothing; the only timings are total session duration_ms (spawner.py:1741) and switchboard routing_decision_latency_ms (modules/pipeline.py:1994). core/metrics.py has queue-depth gauges but no queue-WAIT, context-assembly, process-startup, or first-stream-event timers.",
     "severity": "major",
     "detail": "The pursuit question 'where does the loop waste latency' is currently unanswerable from production data: global-semaphore wait (spawner.py:678), per-butler semaphore wait (712), context fetches (1341-1380), adapter subprocess boot, and model time are all folded into one number."
    },
    {
     "title": "Switchboard classification sessions are mislabeled trigger_source=\"tick\"",
     "evidence": "src/butlers/modules/pipeline.py:1979-1984 \u2014 dispatch_kwargs = {\"prompt\": routing_prompt, \"trigger_source\": \"tick\", ...}. Spend routing rules match on trigger (model_routing.py:446-495), sessions/audit/ledger joins use trigger_source.",
     "severity": "major",
     "detail": "Per-message classification spend is indistinguishable from scheduler-tick spend in sessions, audit entries, and spend-rule targeting; an operator rule condition {\"trigger\": \"route\"} silently never matches the classifier, and cost-per-inbound-message cannot be computed."
    },
    {
     "title": "Scheduled prompt tasks spawn unconditionally; several pay a workhorse session to conclude 'do nothing'",
     "evidence": "core/scheduler.py:1843-1858 dispatches every claimed prompt-mode occurrence straight to spawner.trigger with no precondition support. Roster prompts encode the no-op check inside the LLM: roster/education/butler.toml:78 ('If there are ZERO pending reviews across all maps: do nothing'), roster/travel/butler.toml:49 ('If nothing is upcoming, do not send a message'), roster/finance/butler.toml:54 (STEP 2 early exit).",
     "severity": "major",
     "detail": "The deterministic emptiness check (pending-review count, upcoming-trip count, unreconciled-bill count) is evaluated by a full workhorse-tier session instead of a SQL query in the daemon. This is recurring daily/6-hourly waste across the roster."
    },
    {
     "title": "Complexity tier is effectively unused by the roster: 1 of ~60 schedule entries sets it, everything else defaults WORKHORSE",
     "evidence": "grep 'complexity' across roster/*/butler.toml matches only roster/chronicler/butler.toml:118 (reasoning, day_close). core/scheduler.py:37 _DEFAULT_COMPLEXITY = Complexity.WORKHORSE.value; spawner.py:530 default WORKHORSE.",
     "severity": "minor",
     "detail": "The six-tier catalog (complexity-classification spec) and the per-segment classifier tiering (pipeline.py:454-459 routing prompt) exist, but the scheduled-work half of the fleet never exercises cheap-tier routing \u2014 trivial status checks and heavy synthesis run on the same tier."
    },
    {
     "title": "Connector discretion spend is a black hole: attributed to '__discretion__' with NULL session_id and no purpose dimension",
     "evidence": "connectors/discretion_dispatcher.py:106 (butler_name default \"__discretion__\"), :251-258 (record_token_usage with session_id=None); token_usage_ledger columns are only catalog_entry_id/butler_name/session_id/tokens (model_routing.py:370-374). All four connector callers use the default butler_name.",
     "severity": "major",
     "detail": "High-volume per-message screening (telegram user client, whatsapp, live listener, home assistant) is invisible in per-butler spend views, cannot be drilled to a source, and cannot be targeted by spend rules (trigger_source=None makes trigger constraints fail-closed per model_routing.py:481-483)."
    },
    {
     "title": "No conversation/session continuity: every interactive turn re-buys the full context as fresh input tokens",
     "evidence": "core/runtimes/claude_code.py has no --resume/--continue support (grep matches only loop keywords); pipeline.py:98-283 reloads and re-serializes conversation history as prompt text per message; core_tools/_routing.py:140-141 re-injects 'CONVERSATION HISTORY:' text into the target session each turn.",
     "severity": "minor",
     "detail": "A 10-turn Telegram exchange pays classification + target-session cold spawn + full history re-tokenization 10 times. _compose_system_prompt (spawner_context.py:109-139) is cache-prefix-ordered, but cross-invocation provider prompt-cache hits are luck-of-TTL, not designed."
    },
    {
     "title": "Model resolution performs an unconditional round-robin counter WRITE on every spawn",
     "evidence": "model_routing.py:262-271 \u2014 the next_counter CTE in _RESOLVE_SQL does INSERT ... ON CONFLICT DO UPDATE on public.model_round_robin_counters whenever a winning tier exists, even when the tier has exactly one candidate (the common case).",
     "severity": "minor",
     "detail": "A hot-path row write + now() bump per dispatch across the whole fleet for a tie-break that almost never has ties; trivially gateable on candidate count."
    }
   ],
   "moves": [
    {
     "title": "API-direct runtime adapter ('api' runtime_type) for single-turn and fixed-tool-schema calls",
     "why": "Removes CLI-subprocess bootstrap from the two highest-volume call classes: connector discretion screening and switchboard routing classification. The classification session needs exactly one tool (route_to_butler) with a fixed schema \u2014 a direct provider tool-use call returns in model-time only, cutting seconds off every inbound message and every screening decision while keeping intelligence in ephemeral LLM sessions (vision rule 4 intact).",
     "cost": "M",
     "integration_points": "New adapter in src/butlers/core/runtimes/ registered via create_adapter (base.py registry); new runtime_type value 'api' in public.model_catalog rows (no schema change \u2014 runtime_type is already free text resolved per tier, model_routing.py:_RESOLVE_SQL); DiscretionDispatcher._get_or_create_adapter (connectors/discretion_dispatcher.py:118) picks it up automatically once catalog entries flip; switchboard classifier path modules/pipeline.py:1976-1990 gains a structured tool-use fast lane that maps tool_use blocks to the existing route_to_butler handler (core_tools/_switchboard.py); spec delta on model-catalog + complexity-classification; Models tab (src/butlers/api/routers/model_settings.py) already edits runtime_type per entry.",
     "slice_plan": "1) Adapter + unit tests behind a catalog entry nobody references; 2) flip discretion-tier catalog entries and verify connector screening; 3) classifier fast lane with fallback to CLI adapter on error; 4) spec deltas.",
     "cost_note_manifesto": "Serves the Switchboard infrastructure contract (roster/switchboard/MANIFESTO.md \u2014 routing backbone, ingress SLA)."
    },
    {
     "title": "Deterministic precondition gates on scheduled tasks (skip the spawn when nothing changed)",
     "why": "Stops paying workhorse sessions to evaluate 'is there anything to do' \u2014 the check education/travel/finance prompts currently perform inside the LLM becomes a daemon-side deterministic query. Owner payoff: visibly lower spend, fewer no-op sessions polluting the timeline, and the vision marker 'the system is boring, it works'.",
     "cost": "M",
     "integration_points": "scheduled_tasks schema (new nullable columns precondition_kind + precondition_ref, core alembic migration); core/scheduler.py cron pass (evaluate before the dispatch at :1843, record outcome 'skipped:precondition' via record_task_dispatched in core/metrics.py); butler.toml [[butler.schedule]] parsing (scheduler.py:_normalize_schedule_dispatch:246) and roster seeds; schedules API (src/butlers/api/routers/schedules.py) + dashboard schedule editor; core-scheduler openspec delta. Precondition kinds: registered deterministic job (reuses dispatch_mode='job' registry) or a read-only SQL count in the butler's own schema.",
     "slice_plan": "1) Schema + scheduler gate + skipped-outcome provenance; 2) API/UI exposure; 3) migrate education review-queue, travel upcoming-trip, and finance bill-reconcile prompts to gated form; 4) spec delta.",
     "cost_note_manifesto": "Serves every domain butler's manifesto (acts only when its domain has signal); core-scheduler spec amendment, no new butler."
    },
    {
     "title": "Thread-continuation routing affinity: skip classification for active conversations, and let routing history feed the existing ingestion_rules engine",
     "why": "The single biggest interactive-latency lever: a continuation message in a thread the switchboard routed to butler X minutes ago re-runs the full CHEAP classification session today. A short-TTL (sender, thread) \u2192 (butler, complexity) affinity map routes continuations deterministically (bypasses_llm=True, same provenance shape as ingestion-policy route_to rules), and consistent long-run history is surfaced as SUGGESTED route_to:<butler> rules for owner approval \u2014 building on, not duplicating, the owner-authored static rule engine in the ingestion-policy spec.",
     "cost": "M",
     "integration_points": "modules/pipeline.py MessagePipeline.process (insert affinity check after _accept_ingress:1175, before build_prompt:1948); switchboard schema table routing_affinity (sender_identity, thread_identity, target_butler, complexity, last_routed_at, TTL) written where routing outcomes are extracted (_extract_routed_butlers:501); safety valve via the existing corrections loop (core/corrections.py + bu-ec5h8's 'correct' tool) which must invalidate the affinity row; suggested-rule minting into switchboard ingestion_rules (ingestion-policy spec, action='route_to:<butler>', enabled=false pending owner approval via the existing /ingestion-rules API); spec delta on butler-switchboard + ingestion-policy.",
     "slice_plan": "1) Affinity table + bypass with PolicyDecision-style provenance (owner can always see WHY it skipped the LLM); 2) correction-invalidation; 3) suggested-rule miner as a switchboard scheduled job; 4) spec deltas.",
     "cost_note_manifesto": "Serves the Switchboard infrastructure contract (classification + routing execution SLAs)."
    },
    {
     "title": "Latency spine: per-phase spawn timing persisted on sessions and streamed to the console",
     "why": "Makes the trigger\u2192spawn\u2192act loop diagnosable: queue-wait (global + per-butler semaphore), context-assembly ms, adapter process-boot-to-first-stream-event ms, model ms. Without it every other optimization here is flying blind; with it the console's judgment layer can say 'replies are slow because spawns queue 40s behind the global cap' with evidence one keystroke away.",
     "cost": "S",
     "integration_points": "spawner.py trigger()/_run() (timestamps already exist at :676-716 semaphore acquisition and :1295 t0; add monotonic marks around context fetches :1341-1380 and adapter first-event); runtime adapters report first_stream_event_at in last_process_info (claude_code.py stream-json loop, codex.py); persist as JSONB phase_timings on sessions via session_complete (core/sessions.py) \u2014 the session lifecycle events at sessions.py:217/319 already flow to the fleet event bus (api/routers/events.py:78), so the console gets it free; Prometheus histograms in core/metrics.py; core-spawner + core-sessions spec deltas.",
     "slice_plan": "1) Spawner-side marks + sessions JSONB + metrics; 2) adapter first-event mark per runtime; 3) sessions API/read-model exposure.",
     "cost_note_manifesto": "Serves the staffer-archetype/QA contract and the console north star (earned calm, evidence spine); no manifesto amendment needed."
    },
    {
     "title": "Honest spend attribution: purpose dimension on the token ledger + correct classifier trigger_source + per-connector discretion identity",
     "why": "Closes the two attribution holes: classification spend masquerading as 'tick' and all connector screening lumped under '__discretion__' with NULL session_id. Owner payoff: /spend and spend rules can finally answer 'what does one Telegram message cost me end-to-end' and cap screening spend per connector.",
     "cost": "S",
     "integration_points": "public.token_usage_ledger migration adding purpose TEXT NULL + record_token_usage(purpose=...) (model_routing.py:370-374, 1079-1125); spawner finally-block ledger write (spawner.py:2209) passes trigger_source-derived purpose; DiscretionDispatcher gains a required purpose kwarg and callers pass connector identity (telegram_user_client.py:357, whatsapp_user_client.py:543, live_listener/connector.py:208, home_assistant_pipeline.py:33, api/calendar/quick_add.py:175, api/briefing/prompts.py:155); fix modules/pipeline.py:1981 trigger_source 'tick' \u2192 'classification' (audit the failover-classifier and spend-rule fixtures that match on trigger); surface purpose in spend stream events (spawner.py:2237 emit_spend_event extra) and /spend drill-down; catalog-token-limits + dashboard-spend-dashboard spec deltas.",
     "slice_plan": "1) Ledger column + plumb purpose through both write paths; 2) trigger_source rename with fixture sweep; 3) spend UI/rules exposure of the new dimension.",
     "cost_note_manifesto": "No single butler manifesto \u2014 implies a small amendment to the Switchboard infrastructure contract (classification is an accounted service) and the spend-governance surface in dashboard-spend-dashboard spec."
    }
   ],
   "page": "eco:inference",
   "verdict": "n/a",
   "jarvis_gap": "A JARVIS-grade household staff answers in conversational time and spends proportionally to task difficulty; Butlers' inference loop today treats every thought \u2014 a yes/no spam screen, a routing decision, a deep synthesis \u2014 as the same heavyweight act: a cold CLI subprocess with MCP handshake, spawned twice in series for every interactive reply, unconditionally for every cron slot, at a default WORKHORSE tier almost nothing overrides. The loop's latency is unmeasured (one duration_ms number), its cheapest calls are its least attributed (discretion spend under '__discretion__'/NULL session, classification labeled 'tick'), and nothing learns: the same sender's fifth message in a thread re-runs the same classification session the first one did. The routing/quota/failover/provenance machinery (model_routing.py, dispatch_attempts) is genuinely strong \u2014 what's missing is a fast lane (API-direct single-turn calls, thread affinity), a skip lane (deterministic preconditions instead of LLM no-op sessions), and an instrument panel (phase timings, purpose-tagged spend) so the console can prove the loop is getting faster and cheaper.",
   "ideal": "Three call classes, each on its cheapest sufficient substrate: (1) deterministic daemon code for anything decidable by SQL \u2014 precondition-gated schedules, thread-affinity continuations, owner route_to rules \u2014 spawning nothing; (2) API-direct single-turn tool-use calls for screening and classification, returning in model-time with purpose-tagged spend; (3) full agentic CLI sessions reserved for genuine domain work, with per-phase latency persisted on every session and streamed live to the console. Every token in the ledger carries butler + session + purpose + trigger; the spend page can price 'one inbound Telegram message' end-to-end; the judgment layer cites queue-wait vs model-time when replies slow down; and the classifier's own routing history continuously proposes deterministic rules that shrink its future workload \u2014 the fleet gets faster and cheaper the longer it runs."
  },
  {
   "page": "eco:knowledge-graph",
   "verdict": "n/a",
   "ideal": "A single memory substrate whose lifecycle actually runs: every fact carries provenance (episode \u2192 session \u2192 ingestion trace), confidence that visibly decays and can be confirmed, entities that stay deduplicated at write time, a cross-butler discovery plane so any butler can find knowledge held elsewhere without an LLM round-trip, and retrieval/archival machinery proven at 100k-fact scale \u2014 so the graph the owner sees at year 3 is as trustworthy as at week 3.",
   "jarvis_gap": "The knowledge-graph machinery is largely SHIPPED BUT DORMANT. Decay/fading/expiry code exists but is wired to nothing (0 fading, 0 expired rows in the live DB); 4 of 9 memory-enabled butlers never consolidate episodes (switchboard: 4,148 pending episodes vs 44 facts); the cross-butler discovery catalog (full spec + table + tools) has 0 rows because enable_shared_catalog is false everywhere; entity dedup is manual and exact-lowercase duplicates already exist at only 787 entities; vector indexes are ivfflat lists=20 built on empty tables, which will silently degrade recall well before 100k facts. The console's 'earned calm \u2014 no fabricated numbers' doctrine is violated at the substrate level: docs/modules/memory.md promises a scheduled decay sweep that has never run, so every confidence number shown is an un-decayed write-time value masquerading as a maintained one.",
   "findings": [
    {
     "title": "Confidence decay/fading/expiry never runs anywhere in the fleet \u2014 run_decay_sweep is dead code",
     "evidence": "src/butlers/modules/memory/storage.py:2354 defines retention-class-aware run_decay_sweep, but it appears in no job registry (_MEMORY_MAINTENANCE_JOB_HANDLERS at src/butlers/scheduled_jobs.py:378-381 has only memory_consolidation, memory_episode_cleanup, memory_purge_superseded), no MCP tool, and no roster/*/butler.toml schedule (grep run_decay_sweep hits only storage.py + archived specs). Live dev DB: relationship.facts has 0 rows with metadata status='fading' and 0 validity='expired'. docs/modules/memory.md:121-131 claims 'A scheduled sweep computes effective confidence\u2026' and openspec/specs/memory-retention-policy/spec.md ('Retention-class-aware decay sweep') REQUIRES it. Read-time scoring does apply exp-decay (search.py:526,747) so ranking decays, but state transitions (fading/expired/archival) never happen \u2014 facts are immortal regardless of permanence class.",
     "severity": "critical",
     "detail": "This is broader than the known 'relationship.facts dormant decay' ledger item: the sweep is unwired for ALL 9 memory-bearing butlers, and the archive_before_delete path for personal_profile/health_log/financial_log classes has never executed. The console showing confidence values implies a maintained lifecycle that does not exist \u2014 a substrate-level violation of the no-fabricated-numbers doctrine."
    },
    {
     "title": "4 of 9 memory-enabled butlers never consolidate or clean episodes \u2014 promotion pipeline silently absent where volume is highest",
     "evidence": "finance (roster/finance/butler.toml:35), travel (roster/travel/butler.toml:28), switchboard (roster/switchboard/butler.toml:34), education (roster/education/butler.toml:98) all enable [modules.memory] but define NO memory_consolidation/memory_episode_cleanup schedule (grep across roster/*/butler.toml: only general, home, health, relationship, lifestyle schedule them). The module does not self-schedule (src/butlers/modules/memory/__init__.py:224 on_startup registers hooks only). Live dev DB: switchboard 4,148 pending episodes vs 44 facts; finance 1,294 pending; education 1,137 pending.",
     "severity": "major",
     "detail": "Lifecycle wiring is hand-copied TOML per butler, so the highest-traffic butler (switchboard, which sees every routed message) accumulates raw episodes forever and promotes essentially nothing to durable facts. Episode TTL cleanup also never runs there, so growth is unbounded."
    },
    {
     "title": "Cross-butler discovery catalog is fully dormant: spec, table, tools all shipped, zero rows",
     "evidence": "openspec/specs/memory-discovery-catalog/spec.md defines public.memory_catalog as the cross-butler searchable index; enable_shared_catalog defaults False (src/butlers/modules/memory/__init__.py:185) and NO roster/*/butler.toml sets it (grep: zero hits); live dev DB: SELECT count(*) FROM public.memory_catalog = 0. memory_catalog_search (src/butlers/modules/memory/tools/reading.py:113) therefore returns empty for every butler.",
     "severity": "major",
     "detail": "There is currently NO way for one butler to read another's facts short of a full Switchboard LLM round-trip; the sanctioned discovery plane exists but is switched off, and even if switched on today the ~3,600 pre-existing facts would be invisible (write-behind only, no backfill job exists)."
    },
    {
     "title": "No entity dedup at creation time \u2014 duplicates already present at 787 entities",
     "evidence": "Live dev DB public.entities: 9 exact lowercase-name duplicate pairs among non-merged rows, e.g. 'citadel' as {organization, person}, 'tzeusii' (owner handle) as {other, person}, 'tzeusy/butlers' twice; 74 entities flagged metadata.unidentified=true; 46 tombstoned merges are all manual (memory_entity_merge). entity_create (src/butlers/modules/memory/tools/entities.py:52) performs no near-duplicate check \u2014 only entity_find_by_canonical exists (exact match, entities.py:130), and nothing calls it as a guard.",
     "severity": "major",
     "detail": "predicate-taxonomy.md \u00a71 mandates resolve-before-create for ingestion paths, but that contract is enforced only by LLM prompt discipline. Each duplicate fragments the graph (facts split across two anchors), and fragmentation compounds \u2014 the 'which Chloe' problem re-enters through the write path."
    },
    {
     "title": "Retrieval will not survive 100k facts: ivfflat lists=20 built on empty tables, unindexable fuzzy-resolve query",
     "evidence": "src/butlers/modules/memory/migrations/001_memory_schema.py:84,203,330 create IVFFlat indexes with lists=20 at schema-creation time (empty tables) on episodes/facts/rules; pgvector ivfflat centroids are computed at build time and never rebuilt, so cluster quality and the lists count (rule of thumb rows/1000 \u2192 ~100 lists at 100k) are both wrong at scale. _fetch_fuzzy_candidates (src/butlers/modules/memory/tools/entities.py:537-575) filters on similarity(canonical_name,$1) > 0.3 plus per-row UNNEST(aliases) subqueries \u2014 a predicate shape that cannot use a trigram GIN index, forcing a seq scan of public.entities per resolve call.",
     "severity": "major"
    },
    {
     "title": "'Archival' is a metadata flag, not an archive \u2014 archive_before_delete classes get no cold tier",
     "evidence": "run_decay_sweep's archive path (src/butlers/modules/memory/storage.py:2443-2456) sets metadata.archived_at + archived_content=true and flips validity='expired' on the SAME row; content never moves anywhere. memory_policies marks personal_profile/health_log/financial_log archive_before_delete=true (openspec/specs/memory-retention-policy/spec.md seeding table), and RFC 0016 (about/legends-and-lore/rfcs/0016-s3-blob-storage-contract.md) provides a blob store that memory never uses.",
     "severity": "minor",
     "detail": "At years-scale, expired+superseded rows either bloat hot tables (where purge is unscheduled \u2014 memory_purge_superseded runs in only 3 of 9 butlers: lifestyle/health/home butler.toml) or get hard-deleted by purge with no recoverable archive, contradicting the fail-closed archive intent."
    },
    {
     "title": "Fact\u2192evidence provenance chain exists in data but has no read surface",
     "evidence": "Consolidation writes memory_links provenance edges from every derived fact/rule back to source episode IDs (src/butlers/modules/memory/consolidation_executor.py:209-251), and episodes carry session_id; but neither memory_get (tools/reading.py) nor the dashboard memory router (src/butlers/api/routers/memory.py) exposes the chain \u2014 no endpoint or tool answers 'why does the system believe this fact'.",
     "severity": "minor",
     "detail": "The console north star demands every clause be a door to root evidence via the trace spine; for memory facts the spine's data exists (fact \u2192 memory_links \u2192 episode \u2192 session_id) but is unreadable, so fact drill-down dead-ends at the fact row."
    },
    {
     "title": "Spec drift: memory-retention-policy still requires the dropped rule_applications table",
     "evidence": "openspec/specs/memory-retention-policy/spec.md ('Rule application audit tracking' requirement: rule_applications MUST be created and written on memory_mark_helpful/harmful) vs src/butlers/modules/memory/migrations/006_drop_rule_applications.py which drops it. docs/modules/memory.md:148 also still lists rule_applications as an owned table.",
     "severity": "minor",
     "detail": "Likely falls under open epic bu-17axsl (doctrine-spec-code reconciliation) \u2014 flagged here only so the synthesizer can route it there rather than file anew."
    }
   ],
   "moves": [
    {
     "title": "Module-owned lifecycle: memory self-registers decay sweep + consolidation + cleanup as default scheduled jobs",
     "why": "The entire memory lifecycle (decay\u2192fading\u2192expiry, episode promotion, TTL cleanup, superseded purge) currently depends on hand-copied butler.toml cron blocks, and the decay sweep is wired to nothing at all. Making the module register its own deterministic maintenance schedule on startup (with butler.toml able to override cadence, not existence) turns 'decay-by-design but dormant' into 'decay actually running' fleet-wide in one change, and drains the 6,500+ pending episodes in switchboard/finance/education. This is what memory-retention-policy already requires; no new spec needed, just a delta stating jobs are module-default.",
     "cost": "M",
     "integration_points": "Add memory_decay_sweep handler to _MEMORY_MAINTENANCE_JOB_HANDLERS (src/butlers/scheduled_jobs.py:378) calling storage.run_decay_sweep (src/butlers/modules/memory/storage.py:2354); have MemoryModule.on_startup (src/butlers/modules/memory/__init__.py:224) register default deterministic schedules for memory_consolidation/memory_episode_cleanup/memory_purge_superseded/memory_decay_sweep via the core scheduler; delete now-redundant cron blocks from roster/{general,home,health,relationship,lifestyle}/butler.toml; spec delta to openspec/specs/memory-retention-policy/spec.md + module-memory; update docs/modules/memory.md \u00a7Decay. Serves every memory-bearing butler's manifesto implicitly (vision.md: 'the system is boring, it works'); no manifesto amendment needed.",
     "slice_plan": "1) Wire memory_decay_sweep job handler + schedule in the 5 butlers that already have memory cron (pure additive, ships alone). 2) Module-default schedule registration in on_startup with toml override + removal of copy-paste blocks. 3) One-shot catch-up: bounded consolidation backfill for the 4,148/1,294/1,137 pending-episode backlogs (batched, respecting dead_letter states)."
    },
    {
     "title": "Turn on the cross-butler fact plane: enable memory_catalog fleet-wide + backfill job + make it the sanctioned cross-butler read",
     "why": "The one-API cross-butler fact read the pursuit asks for already exists as spec+table+tools (public.memory_catalog, memory_catalog_search) and is simply off: 0 rows, no toml enables it. Enabling it plus a backfill of the ~3,600 existing facts/rules gives every butler discovery over the whole graph (health butler finds relationship's 'sister due in March' before a briefing) at zero LLM cost, with provenance pointers keeping canonical data in the owning schema \u2014 fully consistent with RFC 0006 isolation since grants are catalog-only.",
     "cost": "M",
     "integration_points": "Set enable_shared_catalog=true in [modules.memory] for all 9 roster/*/butler.toml (or flip the default at src/butlers/modules/memory/__init__.py:185); new backfill scheduled job (pattern: src/butlers/scheduled_jobs.py memory handlers) that upserts existing facts/rules into public.memory_catalog using the UNIQUE(source_schema,source_table,source_id) key from openspec/specs/memory-discovery-catalog/spec.md; write-behind already implemented (src/butlers/modules/memory/tools/writing.py:381,531); surface memory_catalog_search results in briefing prompts (roster/general eod-tomorrow-prep skill) and the dashboard memory router (src/butlers/api/routers/memory.py) as a 'fleet knowledge' search. Serves the general butler's briefing manifesto and switchboard's routing-context contract; spec delta: default-on + backfill requirement in memory-discovery-catalog.",
     "slice_plan": "1) Flip default + enable in tomls (write-behind starts populating). 2) Backfill job + idempotency test. 3) Consumption: catalog search in briefing context assembly + dashboard cross-butler search tab."
    },
    {
     "title": "Entity hygiene loop: dedup guard at entity_create + nightly merge-candidate queue through approvals",
     "why": "Duplicates exist at 787 entities (citadel as person AND organization; the owner's own handle duplicated) and every duplicate silently splits facts across anchors \u2014 at 10k entities the 'which Chloe' problem returns via the write path. A write-time guard (exact + trigram + alias match \u2192 return existing or create flagged) plus a scheduled detector that emits merge candidates into the existing approvals/pending-actions surface keeps the graph convergent with owner-in-the-loop for ambiguous merges, matching predicate-taxonomy's resolve-before-create contract that is today enforced only by prompt discipline.",
     "cost": "M",
     "integration_points": "Guard in entity_create (src/butlers/modules/memory/tools/entities.py:52) reusing _fetch_fuzzy_candidates logic; fix the fuzzy predicate to the index-friendly `canonical_name % $1` form + add pg_trgm GIN index migration on public.entities(canonical_name, aliases) (new memory migration after 006); nightly merge-candidate job in src/butlers/scheduled_jobs.py writing to the approvals gate (src/butlers/modules/approvals/gate.py) with auto-merge above a confidence bar and owner review below; merge execution via existing entity_merge (entities.py:1175) which already repoints facts/calendar_event_entities/episodes. Serves roster/relationship/MANIFESTO.md ('a living database of the people in your life') and predicate-taxonomy.md \u00a71; no manifesto amendment.",
     "slice_plan": "1) Trigram index + query-shape fix (pure perf, ships alone). 2) Create-time guard + tests (unidentified-entity path from predicate-taxonomy \u00a71.2 kept working). 3) Nightly detector \u2192 approvals queue \u2192 dashboard entities page renders candidates with one-key merge."
    },
    {
     "title": "100k-fact scale hardening: HNSW index migration + synthetic-scale regression harness",
     "why": "ivfflat with lists=20 built on empty tables is a time bomb: centroids computed at creation never rebuild, so recall degrades invisibly as facts grow \u2014 retrieval starts returning confidently wrong context, the worst failure mode for a system whose doctrine is 'no number is ever fabricated'. pgvector HNSW builds incrementally, needs no list tuning, and removes the rebuild obligation permanently. A checked-in synthetic-scale test (100k facts, measured recall@k + latency budget) makes graph-scale a regression-tested property instead of a hope.",
     "cost": "S",
     "integration_points": "New memory migration (src/butlers/modules/memory/migrations/007_*) dropping the three ivfflat indexes (001_memory_schema.py:84,203,330) and public.memory_catalog's ivfflat (per memory-discovery-catalog spec) in favor of HNSW vector_cosine_ops; scale fixture + recall/latency assertions in tests/ using the testcontainers integration path (per butlers-mocked-pool-vs-integration-gap memory, verify against real Postgres); doc update docs/modules/memory.md \u00a7Retrieval. Serves all memory butlers; spec delta only to the index bullet in memory-discovery-catalog.",
     "slice_plan": "Single PR: migration + scale harness; harness marked slow/nightly to keep CI fast."
    },
    {
     "title": "Honest forgetting: cold-tier archive via RFC 0016 blob store + owner-visible provenance chain ('why do I believe this')",
     "why": "Trustworthy-over-years needs both real forgetting and inspectable remembering. Today 'archival' is a metadata flag on a hot row and purge (where it runs) hard-deletes with no recovery, while the fact\u2192episode\u2192session provenance chain already stored in memory_links has no read surface. Shipping (a) an archive writer that snapshots archive_before_delete-class facts to the S3 blob store before expiry/purge with a tombstone pointer, and (b) a provenance-chain read (memory_get?include_provenance + dashboard drill-down fact\u2192episode\u2192session_id) gives the owner both a bounded hot graph and a one-keystroke path from any remembered fact to its root evidence \u2014 extending the console's trace-id spine into the memory substrate.",
     "cost": "M",
     "integration_points": "Archive writer in run_decay_sweep's archive branch (src/butlers/modules/memory/storage.py:2443) and purge_superseded_facts (storage.py, job at scheduled_jobs.py:360) using the RFC 0016 blob contract (about/legends-and-lore/rfcs/0016-s3-blob-storage-contract.md, src/butlers/storage/); provenance read joins memory_links (consolidation writer: src/butlers/modules/memory/consolidation_executor.py:209-251) exposed via tools/reading.py memory_get and a new GET /api/memory/facts/{id}/provenance in src/butlers/api/routers/memory.py; spec deltas: memory-retention-policy (archive semantics) + module-memory (provenance read). Serves the trust doctrine in vision.md and every memory butler; the dashboard side folds into the existing judgment-layer surfaces rather than a new page.",
     "slice_plan": "1) Provenance chain API + fact drill-down (independent, pure read). 2) Blob archive on expiry with tombstone + restore script. 3) Extend purge job to archive-then-delete for archive_before_delete classes and schedule it fleet-wide (rides on move 1's module-owned schedules)."
    }
   ]
  },
  {
   "page": "eco:cross-butler",
   "verdict": "n/a",
   "jarvis_gap": "The north star implies a household staff that confers: a butler that notices something outside its domain asks a colleague, waits for an answer, and returns a synthesized verdict to the owner. Today the fleet is a hub-and-spoke fan-out with no return path \u2014 route_to_butler is switchboard-only and terminates at {\"status\":\"accepted\"} (src/butlers/core_tools/_switchboard.py:497-498), domain butlers have zero tools to reach one another, cross-domain interest exists only as prompt text in the switchboard's extraction skill, the daily brief concatenates pre-rendered per-domain strings, and three separate unbudgeted egress paths (notify send, insight broker, decision-loop pushes) compete for the owner's attention with no arbiter. The staff cannot collaborate; each butler is alone with its schema.",
   "ideal": "A collaboration fabric with four primitives, all brokered by the Switchboard per MCP-only doctrine: (1) correlated ask/answer delegation \u2014 any domain butler can pose a question to a colleague and is re-spawned with the answer (or the timeout) when it lands; (2) durable typed domain-event subscriptions keyed on the entity spine, so relationship learns when finance records a transaction naming a person it tracks without the extraction prompt anticipating it; (3) one attention ledger arbitrating ALL proactive owner egress \u2014 coalescing, budgeting, and prioritizing across butlers so simultaneous pings become one composed message; (4) cross-domain case files that bind a multi-butler investigation (symptom + pharmacy charge + doctor) into a single trace-spined story the console and the daily brief render as one narrative. Judgment stays in ephemeral sessions; the daemon carries only correlation state.",
   "findings": [
    {
     "title": "Cross-butler dispatch is one-way and terminal \u2014 no answer ever returns to a caller",
     "evidence": "src/butlers/core_tools/_switchboard.py:326-498 \u2014 route_to_butler builds a route.v1 envelope, dispatches route.execute on the target, and on success returns only {\"status\": \"accepted\", \"butler\": butler}. The target spawns its own session whose output goes to session logs or notify(); there is no result payload, no caller-visible correlation id, and no mechanism to check back. RFC 0021's decision loop (commit 9f647ab1d) adds owner one-tap decisions but no butler-to-butler answer path.",
     "severity": "major",
     "detail": "\"Delegation with follow-up\" (butler A asks B and checks back) is structurally impossible: the requesting session is ephemeral and gone by the time the target runs, and nothing re-invokes the requester with the outcome. Every multi-domain question must instead be pre-decomposed by the Switchboard at ingestion or answered incompletely by one butler."
    },
    {
     "title": "Domain butlers have no tool to reach another butler at all",
     "evidence": "route_to_butler registers only on the Switchboard (src/butlers/core_tools/_switchboard.py:19 'Register switchboard-only tools'); grep of roster/*/butler.toml shows [butler.permissions] declared only by messenger, qa, and switchboard \u2014 no domain butler has cross-butler access (consistent with about/heart-and-soul/architecture.md 'Domain butlers default to no cross-butler access'). Domain sessions' only outbound surfaces are notify()/remind() (owner-facing) and memory_catalog_search (passive read of summaries).",
     "severity": "major",
     "detail": "A health session holding a symptom report cannot ask finance whether a pharmacy charge cleared, cannot ask relationship who the owner's doctor is (beyond stale catalog summaries), and cannot ask travel whether the owner will be abroad when a prescription runs out. The permissions matrix (CROSS_BUTLER_PERMISSION check at _switchboard.py:360) already exists as the authorization primitive but nothing on the domain side can exercise it."
    },
    {
     "title": "Owner-attention egress is fragmented across three unarbitrated paths",
     "evidence": "notify(intent='send') delivers direct through Messenger with only per-message quiet-hours priority gating (src/butlers/core_tools/_notifications.py:289-296, quiet-hours gate at :576-621); the RFC 0011 broker budget/cooldown/dedup applies only to the insight pipeline (roster/switchboard/modules/insight_broker.py:1-31, daily cycle in roster/switchboard/butler.toml:58-63); RFC 0021 push-on-park adds a third push path. No table or job arbitrates when two butlers want the owner in the same hour.",
     "severity": "major",
     "detail": "Anti-spam is structural for insights but advisory for everything else. Two scheduled tasks firing near-simultaneously produce two Telegram pings with no coalescing \u2014 the already-observed calendar double-notify failure mode is one instance of this missing layer, not its full extent. 'Earned calm' at the console has no counterpart at the phone."
    },
    {
     "title": "No standing cross-domain subscription primitive \u2014 cross-butler interest lives only in one prompt",
     "evidence": "The context bus is a fixed 11-signal user-state vocabulary with per-signal writer permissions (openspec/specs/context-bus/spec.md 'Signal Vocabulary'); cross-domain fan-out happens once, at ingestion, driven by the switchboard's signal-extraction skill prompt (roster/switchboard/.agents/skills/signal-extraction/SKILL.md) and conversation-decomposition (openspec/specs/conversation-decomposition/spec.md). No table lets a butler durably register interest in another domain's events.",
     "severity": "major",
     "detail": "If finance records a transaction that names a tracked contact, or travel creates a trip that overlaps a medication schedule, the interested butler learns nothing unless the extraction prompt happened to anticipate that exact join at ingestion time. Domain-originated events (job scans, connector ingests routed to one butler) never fan out at all."
    },
    {
     "title": "The daily brief is concatenation, not synthesis \u2014 the composing session cannot see any specialist's data",
     "evidence": "Specialist contributions are deterministic SQL snapshot jobs writing pre-rendered summary strings to state (src/butlers/jobs/briefing.py:39-51, contribution envelope :60-77); the General butler's prompt renders 'one concise line using the pre-rendered summary field' per domain and permits a cross-domain 'Heads-up' only when highlight strings happen to collide (roster/general/butler.toml:77-89).",
     "severity": "minor",
     "detail": "The one place the fleet speaks with one voice is assembled from seven strings written before any cross-domain reasoning occurs. The composing session has no tool to pull a detail from a specialist (e.g., the actual departure time behind travel's highlight), so the Heads-up clause can only restate, never investigate."
    },
    {
     "title": "Insight proposals are jobs-only \u2014 the LLM sessions where judgment lives cannot propose insights",
     "evidence": "propose_insight_candidate is registered as an MCP tool only on the Switchboard ([modules.insight_broker] appears only in roster/switchboard/butler.toml:42); domain butlers submit exclusively from deterministic scan jobs via direct in-process import (roster/health/jobs/health_jobs.py:152 'from butlers.tools.switchboard.insight.broker import propose_insight_candidate'; same pattern in roster/finance/jobs/finance_jobs.py, roster/travel/jobs/travel_jobs.py).",
     "severity": "minor",
     "detail": "This inverts the doctrine that 'intelligence is in ephemeral sessions' (about/heart-and-soul/vision.md rule 4): the only agents allowed to propose proactive insights are the deterministic SQL scans, while a session that notices something genuinely insightful mid-conversation has no path into the broker and can only notify() directly (bypassing budget/dedup) or stay silent."
    },
    {
     "title": "memory_catalog is a passive cross-butler index \u2014 discovery without notification",
     "evidence": "public.memory_catalog is explicitly 'a discovery index, NOT a canonical store' (openspec/specs/memory-discovery-catalog/spec.md); the only consumer surface is memory_catalog_search (src/butlers/modules/memory/tools/reading.py:113). Writes to the catalog trigger nothing.",
     "severity": "minor",
     "detail": "The one existing cross-butler knowledge spine (entity_id-keyed catalog rows) is pull-only: relationship never learns that health wrote a fact about a person it tracks unless a future session happens to search for it. The catalog's entity_id/object_entity_id columns are exactly the join keys a subscription fabric needs \u2014 the schema is ready, the delivery mechanism absent."
    }
   ],
   "moves": [
    {
     "title": "Delegation ledger: correlated ask/answer between butlers with scheduled follow-up",
     "why": "Unlocks the defining collaborative behavior \u2014 health asks finance whether the pharmacy charge cleared, finance answers from its schema, health is re-spawned with the answer and reports one complete verdict to the owner. Serves every domain manifesto's 'complete picture' promise (health 'pieces only reveal their meaning when held together'; finance 'the answer is already waiting'). Requires a Switchboard infrastructure-contract amendment adding 'delegation brokering' as a responsibility.",
     "cost": "M",
     "integration_points": "New core tool group 'delegation' in src/butlers/core_tools/ (registered on domain butlers, gated by the existing CROSS_BUTLER_PERMISSION matrix check pattern at _switchboard.py:360): ask_butler(butler, question, deadline_s) and answer_delegation(delegation_id, answer). New public.delegations table (id, requestor_butler, target_butler, question, status pending/answered/expired, answer JSONB, request_id for trace spine, expires_at) in the core Alembic chain. Switchboard carries the ask as a route.v1 envelope (reusing _switchboard_route); on answer or expiry, the requestor's daemon schedules a one-shot prompt dispatch (existing scheduler, dispatch_mode='prompt') carrying the answer. New spec openspec/specs/cross-butler-delegation/; RFC amending roster/switchboard/MANIFESTO.md. Dashboard: delegations render on One Timeline keyed by request_id.",
     "slice_plan": "Slice 1: public.delegations table + ask_butler/answer_delegation tools + Switchboard brokering, answers visible via bd-style status polling only. Slice 2: follow-up re-spawn on answer/expiry. Slice 3: timeline lane + drill-down on the dashboard."
    },
    {
     "title": "Domain-event subscriptions: typed cross-butler pub/sub on the entity spine",
     "why": "Replaces 'anticipate every cross-domain join in one extraction prompt' with durable declared interest: relationship subscribes to any event carrying an entity_id it tracks; health subscribes to travel.trip_created to prep medications for time abroad. This is what genuinely collaborating butlers accomplish that none can alone \u2014 standing awareness across domains without schema access. Serves relationship ('a friend who never forgets'), health, and travel manifestos; the memory_catalog's entity_id columns already provide the join key.",
     "cost": "L",
     "integration_points": "New public.domain_events table (id, event_type e.g. 'finance.transaction_recorded', origin_butler, entity_id nullable FK public.entities, payload JSONB, request_id, created_at) and public.event_subscriptions (subscriber_butler, event_type pattern, predicate JSONB e.g. entity-tracked-only, seeded from a [butler.subscriptions] block in roster/*/butler.toml). Publish is a plain public-schema write from domain jobs/tools (same sanctioned pattern as roster/health/jobs/health_jobs.py:152's broker import). Delivery is a Switchboard scheduled job that batches matched events per subscriber and dispatches via existing route_to_butler (_switchboard.py:326) \u2014 MCP-only doctrine preserved. New spec cross-butler-subscriptions; RFC required (extends RFC 0009/0011 lineage).",
     "slice_plan": "Slice 1: tables + publish helper + 3 pilot event types (finance.transaction_recorded, travel.trip_created, health.symptom_logged). Slice 2: Switchboard matcher/delivery job with per-subscriber batching + daily cap. Slice 3: butler.toml-seeded subscriptions + dashboard 'subscriptions' governance panel under Trust Console."
    },
    {
     "title": "Fleet attention arbiter: one egress ledger for all proactive owner contact",
     "why": "Extends RFC 0011's structural anti-spam from insights-only to every proactive ping. Two butlers wanting the owner in the same window become one composed message in the Dispatch voice; a global interrupt budget makes calm earned at the phone, not just the console. Builds beyond bu-24lu6 (which adds pushes) and the known calendar double-notify mitigation (which patched one butler): this is the missing structural layer. Amends the Messenger and Switchboard infrastructure contracts.",
     "cost": "M",
     "integration_points": "Generalize public.insight_candidates into a public.attention_ledger (or sibling table): notify(intent='send') from non-interactive contexts (no request_context \u2014 the scheduled-task path distinguished today in src/butlers/core_tools/_notifications.py) writes a delivery intent instead of dispatching; a Messenger/Switchboard arbiter job (extending roster/switchboard/modules/insight_broker.py's cycle) coalesces same-window intents, ranks via the RFC 0011 priority convention, and delivers one composed message. priority='high' and interactive reply/react bypass unchanged. Spec deltas: core-notify, proactive-insight-engine, insight-delivery; RFC amending RFC 0011. Dashboard: ledger rows drill down from One Timeline via request_id.",
     "slice_plan": "Slice 1: ledger table + notify send-path park-and-record behind a per-butler opt-in flag, arbiter delivers verbatim (no coalescing) within 5-min windows. Slice 2: coalescing + global daily interrupt budget + composed multi-butler digest. Slice 3: flip default on for all scheduled-task notify sends; retire per-butler ad-hoc dedup patches."
    },
    {
     "title": "Cross-domain case files: one shared object binding a multi-butler investigation",
     "why": "The 'health notices a symptom, finance sees the pharmacy charge, relationship knows the doctor' story needs a durable spine object, or the three threads live and die separately in three schemas. A case binds fan-out segments, delegations (move 1), and events (move 2) into one trace-spined story that the console's One Timeline and the daily brief render as a single narrative with a synthesized outcome. No manifesto owns cross-domain narratives today \u2014 this implies a Switchboard contract amendment (case custody) with the opening butler owning synthesis.",
     "cost": "L",
     "integration_points": "New public.cases (id, title, opened_by_butler, status open/synthesized/closed, outcome TEXT, request_id) + public.case_items (case_id, kind fanout_segment/delegation/domain_event/fact_ref, source_butler, ref_id, request_id). Attach case_id at the Switchboard fan-out point (conversation-decomposition / _process_ingested_message in src/butlers/core_tools/_switchboard.py:280-320) when extraction targets \u22652 butlers; delegation and event rows (moves 1-2) carry case_id forward. Synthesis: the opening butler gets a scheduled check-back prompt when all case items resolve. Dashboard: case lane on One Timeline (epic bu-86c4c.9/.10 spine), case card in the daily brief. New spec cross-butler-case-files.",
     "slice_plan": "Slice 1: tables + auto-open on multi-butler fan-out + read-only dashboard case view over existing request_id joins. Slice 2: delegation/event linkage + synthesis check-back. Slice 3: daily-brief case section replacing the string-collision 'Heads-up' clause."
    },
    {
     "title": "Session-side insight proposals: register propose_insight on every domain butler",
     "why": "Repairs the doctrine inversion (finding 6) with almost no new machinery: the sessions where judgment lives get a governed path into the existing broker, so a mid-conversation realization competes for delivery under budget/cooldown/dedup instead of bypassing via notify() or being lost. Serves every domain manifesto's proactive promise ('we surface what is urgent') and strengthens move 3 by pulling more egress under the broker.",
     "cost": "S",
     "integration_points": "New core tool group 'insights' in src/butlers/core_tools/ registered on domain butlers (butler_type == BUTLER guard, same pattern as _temporal.py), thin wrapper over the existing plain function butlers.tools.switchboard.insight.broker.propose_insight_candidate (already a sanctioned public-schema write, per roster/health/jobs/health_jobs.py:152 precedent) with origin_butler forced to the calling butler. Spec delta: proactive-insight-engine 'Insight Candidate Submission' requirement (currently Switchboard-tool-only). Docstring must teach priority ranges and dedup_key discipline per the RFC 0011 convention.",
     "slice_plan": "Single slice: tool + tests + spec delta; pilot on health and relationship, then fleet-wide via core_groups."
    }
   ]
  },
  {
   "page": "eco:proactivity",
   "verdict": "n/a",
   "jarvis_gap": "The doctrine for proactivity is world-class on paper \u2014 RFC 0011's brokered insight engine (budget, dedup, cooldowns, adaptive one-way ratchet, quiet hours) is exactly the \"earned calm\" architecture \u2014 but on current main it governs a minority of actual proactive traffic. Most proactive owner-facing output is prompt-cron tasks calling notify() directly (finance alone has five such tasks), bypassing budget, dedup, cooldown, and engagement tracking entirely; the sanctioned finance insight-scan runs once a month on day 30. There is no event-driven internal trigger of any kind (scheduler triggers are calendar/deadline only), so anomaly detection on ingested streams is polled at daily/monthly crons and the \"time-critical\" insight class gets exactly one delivery slot per day. Quiet hours exist twice (per-butler delivery_preferences, default open and configured by no butler; public.insight_settings for insights only) and the context bus dnd/sleeping signals gate nothing deterministically. Engagement learning is a crude 60-minute any-message heuristic with no per-category attribution, so the system cannot learn which kinds of nudges the owner values. A JARVIS acts within minutes of a signal, at moments the owner is receptive, through one accountable attention channel, and learns from every dismissal \u2014 the plumbing for all four exists here but is unwired.",
   "ideal": "Every proactive owner-facing byte flows through one attention ledger with one owner-level policy (quiet hours, per-intent budgets, cross-fleet dedup) enforced deterministically at the Switchboard/Messenger boundary; domain butlers detect via fixed manifesto-scoped ingestion-event triggers (minutes, not days, of latency) but only ever propose \u2014 candidates compete for delivery at wake-anchored, context-bus-gated moments; every delivered nudge carries one-tap feedback that reweights its category; consequential preparation (drafted replies, staged events) is speculative but parked behind per-event approval, so the owner wakes to work already done and only ever taps \"send\".",
   "findings": [
    {
     "title": "Most proactive traffic bypasses the insight broker \u2014 two disjoint proactivity systems",
     "evidence": "roster/finance/butler.toml:106-155 \u2014 anomaly-digest (daily 21:00), budget-status-check, subscription-audit-monthly, upcoming-bills-check, subscription-renewal-alerts all instruct 'Use notify(channel=\"telegram\", intent=\"send\", ...)' directly, so they skip the budget/dedup/cooldown/engagement machinery of openspec/specs/proactive-insight-engine/spec.md entirely; RFC 0011's principle that butlers 'never deliver directly' (about/legends-and-lore/rfcs/0019-...md:40-48) is enforced only inside the pipeline that most proactive tasks don't use.",
     "severity": "major",
     "detail": "The anti-spam architecture is structural only for insight-scan jobs. Any prompt-cron task can notify() at will, so the global daily budget, cross-butler dedup, and the adaptive disengagement ratchet measure and govern a fraction of what the owner actually receives. This is the root cause behind noisy-fleet symptoms (e.g., the known calendar double-notify) recurring despite a fully built anti-spam engine."
    },
    {
     "title": "No fleet-wide quiet hours in practice: per-butler gate defaults open and no butler configures it",
     "evidence": "openspec/specs/core-notify/spec.md 'Quiet Hours Delivery Gate' \u2014 'no delivery_preferences row exists ... delivered immediately regardless of time or priority'; grep for delivery_preferences across roster/*/butler.toml and src/butlers/modules returns nothing, so no butler seeds a row; insight quiet hours live separately in public.insight_settings (proactive-insight-engine spec, Quiet Hours Suppression).",
     "severity": "major",
     "detail": "Quiet hours exist twice \u2014 per-butler (time-aware-delivery) and insight-pipeline (insight_settings) \u2014 and neither is authoritative for direct notify() traffic, which is ungated at 3am today. An owner-level single policy is missing."
    },
    {
     "title": "Context bus dnd/sleeping/traveling signals never gate delivery deterministically",
     "evidence": "grep for get_active_context/user_context consumers in src/butlers: only src/butlers/core/spawner_context.py (prompt preamble injection) and api/routers/audit.py + audit models; neither the notify() quiet-hours gate nor _run_switchboard_insight_delivery_cycle_job (src/butlers/scheduled_jobs.py:162) consults the context bus.",
     "severity": "major",
     "detail": "RFC 0009's dnd signal is advisory-to-the-LLM only. Suppressing proactive delivery while the owner sleeps or is in dnd depends on the ephemeral session choosing to care \u2014 the opposite of vision.md Rule 4 (deterministic daemon) for the one place determinism matters most: not waking the owner."
    },
    {
     "title": "Zero event-driven internal triggers \u2014 anomaly detection is cron-polled with days of worst-case latency",
     "evidence": "Scheduler trigger types are calendar_event_end / deadline_passed / deadline_threshold only (openspec/specs/event-chains/spec.md; src/butlers/core/scheduler.py:_parse_deadline_threshold_reference); nothing fires off public.ingestion_events. A spend spike ingested at 09:05 waits for the 21:00 anomaly-digest cron at best; via the sanctioned pipeline it waits for a monthly insight-scan plus the single daily insight-delivery-cycle at 08:00 (roster/switchboard/butler.toml:59-63).",
     "severity": "major",
     "detail": "The spec's own priority taxonomy defines 90-100 as 'time-critical (action needed within 24-48 hours)' yet grants that class exactly one delivery opportunity per day and no detection path faster than the owning butler's cron. RFC 0019 parks the owner-programmable rule engine, but fixed, code-defined, manifesto-scoped triggers that only spawn analysis sessions (no egress) are explicitly on the safe side of that line."
    },
    {
     "title": "Engagement signal is a 60-minute any-message heuristic \u2014 dismissal learning is impossible",
     "evidence": "src/butlers/modules/pipeline.py:1499-1507 \u2014 on each ingress request, all insight_engagement rows delivered in the last 60 minutes flip to engaged=TRUE, regardless of which insight (or whether the message had anything to do with it); adaptation is a global budget shrink only (proactive-insight-engine spec, Adaptive Delivery).",
     "severity": "minor",
     "detail": "There is no per-insight, per-category, or explicit-dismissal signal, so the system can never learn that the owner values finance anomalies but ignores reconnection nudges \u2014 it can only get globally quieter. The one-way ratchet punishes all categories for one boring category."
    },
    {
     "title": "Draft delivery intent is target-state plumbing with no producer anywhere",
     "evidence": "openspec/specs/core-notify/spec.md:256-262 defines [TARGET-STATE] intent=\"draft\" (Gmail draft, never sends); grep for needs_reply/draft producers across butler-switchboard and butler-messenger specs returns nothing \u2014 no capability detects reply-needed inbound messages or stages a draft.",
     "severity": "minor",
     "detail": "RFC 0019 itself notes draft-then-review is 'more conservative than the status quo' and deliberately un-parked it, yet no spec exercises it. Speculative preparation \u2014 the highest-leverage proactivity form \u2014 has sanctioned plumbing and zero producers."
    },
    {
     "title": "Finance insight-scan cron runs only on day 30 of the month \u2014 never in February",
     "evidence": "roster/finance/butler.toml:158-161 \u2014 cron = \"0 7 30 * *\" (07:00 on the 30th day of month), versus relationship daily (0 7 * * *) and health weekly; openspec/specs/butler-finance/spec.md does not mention insight-scan at all, so the cadence is unspecced and unreviewed.",
     "severity": "minor",
     "detail": "The flagship anomaly-detection domain feeds the brokered pipeline roughly 11 times a year; everything timely goes out the direct-notify side door instead."
    },
    {
     "title": "No morning surface exists; briefing contributions are consumed only by the 23:00 SGT prep",
     "evidence": "roster/general/butler.toml:49-100 \u2014 collect_briefing_contributions (cron 58 6) writes state consumed solely by eod-tomorrow-prep (cron 0 15 = 23:00 SGT); no schedule delivers a wake-time brief, and nothing couples delivery timing to the preferences:home_wake_time predicate (openspec/specs/user-preferences/spec.md) or the context-bus sleeping signal.",
     "severity": "minor",
     "detail": "The insight digest fires at a fixed clock hour whether the owner is awake, asleep, or in another timezone; 'brief ready when the owner surfaces' \u2014 the canonical JARVIS moment \u2014 has all its ingredients (ingress hook, context bus, deferred_notifications) unassembled."
    }
   ],
   "moves": [
    {
     "title": "One attention ledger: route ALL proactive owner-facing traffic through the broker with a single owner-level policy",
     "why": "Makes RFC 0011's anti-spam guarantees actually cover what the owner receives. Converts finance/health prompt-cron digests from direct notify() into propose_insight_candidate() calls, and adds a deterministic attention ledger at the Switchboard notify boundary: one owner-level policy row (quiet hours, per-intent daily caps, cross-fleet dedup window) replacing the default-open per-butler delivery_preferences, plus a dashboard 'attention ledger' panel showing every delivery counted against budget \u2014 earned calm made auditable.",
     "cost": "M",
     "integration_points": "roster/finance/butler.toml:106-155 + roster/health/butler.toml prompt tasks (rewrite to propose candidates); src/butlers/modules/pipeline.py insight broker; notify tool in core (src/butlers/core, core-notify spec amendment: notify checks owner policy, not per-butler delivery_preferences); public.insight_settings generalized to an owner attention-policy table; deprecate per-butler delivery_preferences (time-aware-delivery spec amendment); dashboard /api surface for the ledger. Serves the Switchboard and Messenger infrastructure contracts (roster/messenger/MANIFESTO.md already asserts sole delivery ownership \u2014 this gives that ownership teeth).",
     "slice_plan": "Slice 1: attention ledger table + notify-path counting and owner-level quiet hours (deterministic, no butler changes). Slice 2: convert finance's five direct-notify proactive tasks to insight candidates. Slice 3: dashboard ledger panel + retire delivery_preferences."
    },
    {
     "title": "Ingestion-event triggers (fixed, manifesto-scoped, non-egress) plus an urgent delivery sub-cycle",
     "why": "Closes the detect-latency gap without touching RFC 0019's parked rule engine: butler.toml gains [[butler.trigger]] entries with trigger_type=\"ingestion_event\" and a deterministic predicate over public.ingestion_events fields (source_channel, triage_target, e.g. finance transaction over threshold), debounced, dispatching an existing job/prompt whose only outputs are facts and insight candidates \u2014 no egress, not owner-programmable, declared in git like cron entries. Pair with an hourly urgent sub-cycle in insight_delivery_cycle that delivers only priority>=90 candidates (still budget- and quiet-hours-capped), so 'time-critical' means hours, not days.",
     "cost": "L",
     "integration_points": "src/butlers/core/scheduler.py (extend trigger detection beyond calendar/deadline; event_chains pattern as precedent), public.ingestion_events registry (openspec/specs/ingestion-event-registry), new [[butler.trigger]] toml schema + scheduled_tasks materialization, roster/finance/butler.toml + roster/health/butler.toml first adopters, switchboard insight_broker cadence (roster/switchboard/butler.toml:59-63, src/butlers/scheduled_jobs.py:162). Also fix finance insight-scan cron to daily. Serves finance manifesto (anomaly detection) and health manifesto (missed-dose detection: trigger on absence is slice 3 via a deadline-style check); needs a short RFC distinguishing this from RFC 0019's parked engine.",
     "slice_plan": "Slice 1: urgent hourly sub-cycle in the delivery job (pure broker change). Slice 2: single hardcoded finance trigger (large-transaction event \u2192 anomaly_scan job, debounced 15 min). Slice 3: generalize to declared [[butler.trigger]] toml entries + spec."
    },
    {
     "title": "Per-category feedback loop: one-tap 'useful / mute this kind' on every delivered insight",
     "why": "Answers 'learning notification preferences from dismissals' \u2014 the one proactivity dimension the adaptive ratchet cannot express. Digest items and standalone insights carry inline Telegram buttons (decision-loop RFC 0021 / bu-24lu6 provides the callback plumbing); taps write per-category weight rows that the delivery cycle multiplies into priority (and hard-mutes on request), replacing punish-everything-globally with targeted learning. Explicit attribution also supersedes the 60-minute any-message heuristic in pipeline.py:1499-1507 wherever a tap exists.",
     "cost": "M",
     "integration_points": "src/butlers/modules/pipeline.py delivery cycle + insight_engagement table (add category attribution + reaction columns); new public.insight_category_prefs table; telegram module callback handler (src/butlers/modules/telegram) reusing bu-24lu6 inline-keyboard infrastructure; proactive-insight-engine + insight-delivery spec amendments (RFC 0011 addendum). Dashboard: category weights visible on the Trust/insight settings surface. Serves the switchboard staffer contract (insight broker owner); no domain-manifesto change needed since it is owner-facing meta-preference.",
     "slice_plan": "Slice 1: mute-category via existing message reply parsing (no buttons). Slice 2: inline buttons once bu-24lu6 keyboard plumbing lands. Slice 3: weight multipliers in ranking + dashboard surface."
    },
    {
     "title": "Speculative reply drafting: a producer for the sanctioned draft intent, parked behind one-tap approval",
     "why": "The highest-value proactive act \u2014 'the reply is already written, just tap send' \u2014 using only sanctioned machinery: Switchboard triage flags inbound messages as reply-needed, the routed domain butler drafts a reply in its session, and parks it via intent=\"draft\" (core-notify spec:256-262, already TARGET-STATE, currently producer-less) or a pending_action delivering Send/Edit/Discard through the decision loop. Never auto-sends; RFC 0019 explicitly classifies draft-then-review and inline approval buttons as strengthening the per-event approval path.",
     "cost": "L",
     "integration_points": "roster/switchboard/.agents/skills/message-triage + signal-extraction (add reply-needed classification output); switchboard routing to carry the flag (src/butlers/modules/pipeline.py, RFC 0003 path); implement core-notify draft intent in the notify tool + messenger Gmail-draft tool (roster/messenger/modules); module-approvals pending_actions for telegram drafts with bu-24lu6 inline keyboards; new spec delta on butler-switchboard + core-notify. Serves the relationship manifesto (reconnection outreach planning \u2014 reconnect-planner skill can emit drafted openers) and the messenger contract (delivery stays messenger-owned); switchboard manifesto needs a one-line amendment for the reply-needed flag.",
     "slice_plan": "Slice 1: implement the specced Gmail draft intent end-to-end (email only, owner reviews in Gmail \u2014 zero new UX). Slice 2: reply-needed triage flag + relationship/general butlers drafting on it. Slice 3: telegram staged drafts with one-tap Send via decision loop."
    },
    {
     "title": "Wake-anchored delivery: fire the morning digest on first owner activity, gated by the context bus",
     "why": "Replaces clock-time delivery with receptive-moment delivery using parts that already exist: pipeline.py already intercepts every ingress for engagement tracking \u2014 add first-owner-activity-of-day detection (owner identity, owner-timezone calendar day) that triggers the insight delivery cycle and deferred_notifications flush, with a fixed owner-tz cron as fallback ceiling; make both the delivery cycle and the notify gate consult get_active_context() so sleeping/dnd deterministically suppress (fixing the context-bus-gates-nothing finding). The owner wakes, sends one message anywhere, and the brief + digest arrive \u2014 never at 3am, never while dnd.",
     "cost": "M",
     "integration_points": "src/butlers/modules/pipeline.py ingress hook (extend the engagement block at :1499); src/butlers/context_bus.py get_active_context consumed by scheduled_jobs.py:162 insight_delivery_cycle and by the notify quiet-hours gate; deferred_notifications flush path (time-aware-delivery spec); owner-timezone-context spec for day boundaries; optional new general-butler morning-brief schedule consuming briefing/combined state (roster/general/butler.toml) so contributions collected at 06:58 finally have a morning consumer. Serves the general butler's briefing mandate (roster/general/MANIFESTO.md) and RFC 0009's stated purpose for the sleeping/dnd signals.",
     "slice_plan": "Slice 1: delivery cycle + notify gate consult context bus (deterministic dnd/sleeping suppression). Slice 2: first-activity trigger for the insight cycle with cron fallback. Slice 3: morning-brief task consuming existing briefing/combined state."
    }
   ]
  }
 ],
 "synthesis": {
  "headline": "The 07-03 epic genuinely landed \u2014 timeline, issues, audit-log, spend, approvals, butler-detail, and secrets all climbed a tier and nothing broke \u2014 but every mechanism it shipped (judgment layer, command spine, event bus, state contract, keyboard triage) stopped at one or two surfaces, so the console reads as islands of excellence in an unmigrated sea. The deeper truth is one layer down: the frontend truth amnesty never reached the backend, where roughly a dozen aggregation endpoints still zero-fill on failure (butlers board, memory fan-out, notifications, approvals, settings, spend, secrets), letting dead sources render as healthy zeros under the very verdict banners built to prevent it. And the ecosystem's most consequential machinery \u2014 memory decay, the cross-butler discovery catalog, the insight broker's anti-spam \u2014 is fully built and effectively switched off.",
  "tier_board": [
   {
    "page": "dashboard",
    "verdict": "solid",
    "movement": "unchanged \u2014 landings hold (attention list, KPI error states, event bus), but two liveness models, two dialects, and two decision contracts keep it from world-class"
   },
   {
    "page": "butlers-roster",
    "verdict": "solid",
    "movement": "unchanged \u2014 canonical board + needs-you strip hold; edge fabrications (stripe zero-fill, false OVERDUE) and stale restore remain"
   },
   {
    "page": "butler-detail",
    "verdict": "solid",
    "movement": "improved \u2014 .18 console consolidation verified (one tab vocabulary, unified run verb, real doors); Errors-KPI fabrication is the residue"
   },
   {
    "page": "sessions",
    "verdict": "functional",
    "movement": "unchanged \u2014 list page near the bar, but the deep-link hub /sessions/:id is a pre-redesign relic missing the entire trace spine"
   },
   {
    "page": "timeline",
    "verdict": "solid",
    "movement": "improved (from weak) \u2014 .9/.10 rebuild verified: server heartbeat classification, honest degraded_sources, live tail; URL contract half-built"
   },
   {
    "page": "notifications",
    "verdict": "functional",
    "movement": "unchanged \u2014 .3 trace/session out-links verified, but degraded-200 fabrication, dead 'Retried' filter, and event-bus exclusion persist"
   },
   {
    "page": "issues",
    "verdict": "functional",
    "movement": "improved (from weak) \u2014 ack-until-recurrence, remedy verbs, real audit links landed; group\u2192occurrences drill-down still structurally impossible"
   },
   {
    "page": "audit-log",
    "verdict": "functional",
    "movement": "improved (from weak) \u2014 .4 link fixes verified; reader still withholds result/error/metadata so success and failure rows are indistinguishable"
   },
   {
    "page": "approvals",
    "verdict": "solid",
    "movement": "improved (from functional) \u2014 keyboard triage, ranked rail, per-approval URLs, autonomy ledger all hold; decided dossier omits the decision itself"
   },
   {
    "page": "calendar",
    "verdict": "solid",
    "movement": "unchanged \u2014 richest workspace, still mouse-only, poll-only, verdict-less; two timezones on one surface"
   },
   {
    "page": "health",
    "verdict": "functional",
    "movement": "unchanged \u2014 .2 reached the trackers but missed the leading trend/chart/nutrition surfaces; overview still can't reach its six sub-pages"
   },
   {
    "page": "spend",
    "verdict": "functional",
    "movement": "improved (from weak) \u2014 .11 merged One Spend with real by_butler data and saved_7d receipts; live MTD double-counts and four sections still error-as-empty"
   },
   {
    "page": "memory",
    "verdict": "solid",
    "movement": "unchanged \u2014 house-ledger voice and provenance spine hold; fan-out silently deflates fleet numbers and the re-embed verb mis-aims"
   },
   {
    "page": "entities-plex",
    "verdict": "solid",
    "movement": "unchanged \u2014 signature surface; ranking failure still fabricates a calm household and the verdict is a frontend heuristic"
   },
   {
    "page": "entity-detail",
    "verdict": "functional",
    "movement": "unchanged \u2014 .1 truth fixes hold; duplicate detection false-negatives past 100 queue items, ~14 subsections lack error states"
   },
   {
    "page": "settings",
    "verdict": "functional",
    "movement": "unchanged \u2014 KPI strip fabricates zeros on subsystem failure; inherited permission cells still dead controls"
   },
   {
    "page": "secrets",
    "verdict": "solid",
    "movement": "improved (from functional) \u2014 passport structurally excellent post-amnesty; WhatBreaks slug mismatch is the one critical lie left"
   },
   {
    "page": "education",
    "verdict": "functional",
    "movement": "unchanged \u2014 .1/.16 fixes verified but the page remains the clearest pre-Dispatch island: raw hex charts, nine hue families, dead useTeachingFlows"
   },
   {
    "page": "chronicles",
    "verdict": "functional",
    "movement": "unchanged \u2014 right editorial skeleton; every day-step blanks the page, zero keyboard surface, refresh doesn't refresh the half it governs"
   },
   {
    "page": "qa",
    "verdict": "functional",
    "movement": "regressed (from solid) \u2014 old criticals ('Escalated to user' fabrication, UTC journal) never landed AND .19's pulse strip shipped new bugs (findings_dispatched renders clean-green, silent 25-row rail cap)"
   },
   {
    "page": "system",
    "verdict": "functional",
    "movement": "unchanged \u2014 .17 verdict holds at the happy path but asserts 'Instance healthy' over errored sources and is blind to connector liveness"
   },
   {
    "page": "ingestion",
    "verdict": "solid",
    "movement": "unchanged \u2014 live bones verified; aggregates freeze at mount and the trace spine is one-directional"
   },
   {
    "page": "circles",
    "verdict": "functional (thin evidence)",
    "movement": "improved (from weak) \u2014 .19 Circles lens landed; only residue found is arbitrary-hex label badges with unguaranteed contrast"
   },
   {
    "page": "cross:shell-discoverability",
    "verdict": "functional",
    "movement": "unchanged \u2014 one-command-spine landed, but triage keys advertised nowhere, palette invisible at empty query, '/' collides on /memory"
   },
   {
    "page": "cross:visual-language",
    "verdict": "functional",
    "movement": "unchanged \u2014 Dispatch layer real but the DEFAULT base layer (root font, page shell, tokens) is still shadcn; lint (.6) can't flip defaults"
   },
   {
    "page": "cross:interaction-speed",
    "verdict": "solid",
    "movement": "improved \u2014 speed policy + bus are the right primitives; zero prefetch, three legacy sockets, and poll zoo are the adoption gap"
   },
   {
    "page": "cross:accessibility",
    "verdict": "solid",
    "movement": "improved \u2014 .16 floor is real (lint, tokens, reduced-motion CSS); command palette is a fake modal and axe covers 3/38 pages"
   },
   {
    "page": "eco:connectors",
    "verdict": "n/a",
    "movement": "new \u2014 perception audit: finance/travel/chronicler promises structurally unfulfillable with email-only sources"
   },
   {
    "page": "eco:inference",
    "verdict": "n/a",
    "movement": "new \u2014 every LLM call is a cold CLI spawn; latency unmeasured; discretion spend unattributed"
   },
   {
    "page": "eco:knowledge-graph",
    "verdict": "n/a",
    "movement": "new \u2014 lifecycle machinery shipped but dormant: decay never runs, 4/9 butlers never consolidate, catalog has 0 rows"
   },
   {
    "page": "eco:cross-butler",
    "verdict": "n/a",
    "movement": "new \u2014 hub-and-spoke fan-out with no return path; no butler can ask another a question"
   },
   {
    "page": "eco:proactivity",
    "verdict": "n/a",
    "movement": "new \u2014 RFC 0011 anti-spam governs a minority of actual proactive traffic; quiet hours default open fleet-wide"
   }
  ],
  "themes": [
   {
    "title": "The truth amnesty stopped one layer above the backend: aggregation endpoints zero-fill on failure",
    "body": "bu-86c4c.1/.2 fixed frontend error-as-empty, but the fan-out/aggregation layer beneath it still collapses failures into confident zeros returned as HTTP 200 with no degraded marker \u2014 so the very verdict banners the epic built assert calm over dead sources. This is the dominant NEW+NOT-LANDED defect class of the run, reproducing independently on at least 12 surfaces, and it is invisible to any frontend isError sweep because the fetch succeeds.",
    "exemplars": "src/butlers/api/routers/butlers.py:497-503 (stripe failure \u2192 [0]*24, folded into aggregates); src/butlers/api/routers/memory.py:118-131 (pool failures summed away silently); frontend/src/components/system/SystemVerdictBanner.tsx:61-62 ('Instance healthy' rendered while backups/insights/posture queries are errored)",
    "affected": [
     "dashboard",
     "butlers-roster",
     "notifications",
     "approvals",
     "spend",
     "memory",
     "settings",
     "secrets",
     "system",
     "entities-plex",
     "entity-detail",
     "health",
     "qa",
     "ingestion",
     "sessions"
    ]
   },
   {
    "title": "Built once, never generalized: every epic mechanism has an adoption cliff",
    "body": "The epic shipped correct mechanisms \u2014 command registry, keyboard triage, QueryBoundary, FetchingDim, event-cache registry, modal choreography, real-page axe tests \u2014 and each stopped at 1-3 consumers. Only Approvals and Issues register palette commands; the approvals j/k/a/d/x loop exists nowhere else and is advertised nowhere; FetchingDim has 3 consumers vs 46 forbidden animate-pulse sites; the event registry misses the notifications page's own query keys and calendar/education aren't on the bus at all; axe covers 3 of 38 pages. Nothing structural makes the primitive cheaper than the fork, so drift regenerates.",
    "exemplars": "frontend/src/lib/command-registry.tsx:104 (useRegisterCommands: 2 importers repo-wide); frontend/src/hooks/event-cache-registry.ts:100-104 (notificationPatch skips ['notifications']/['notification-stats']); frontend/src/components/ui/fetching-dim.tsx:5 (3 consumers vs 46 animate-pulse sites)",
    "affected": [
     "calendar",
     "memory",
     "health",
     "secrets",
     "qa",
     "spend",
     "education",
     "chronicles",
     "settings",
     "butler-detail",
     "notifications",
     "dashboard",
     "cross:shell-discoverability",
     "cross:interaction-speed",
     "cross:accessibility"
    ]
   },
   {
    "title": "The judgment layer landed on 2 surfaces; 13+ open with static copy or raw counters",
    "body": "bu-86c4c.17 put a synthesized verdict on /system and /butlers only. Approvals, sessions, notifications, qa, spend, ingestion, issues, calendar, education, chronicles, health, butler-detail, and audit-log all open with taglines or KPI grids \u2014 while the data for their verdicts is already fetched and, in several cases, rendered nowhere (QA's staffer_status/breaker/credentials, spend's projection_confidence, notifications' by_butler breakdown are all discarded on the client).",
    "exemplars": "frontend/src/api/types.ts:4967-4990 vs QaOverviewPage.tsx:239-247 (staffer heartbeat fields fetched, never rendered); frontend/src/pages/SpendPage.tsx:80 (projection_confidence typed and discarded); frontend/src/pages/EducationPage.tsx:78-80 (static marketing tagline as the opener)",
    "affected": [
     "approvals",
     "sessions",
     "notifications",
     "qa",
     "spend",
     "ingestion",
     "issues",
     "calendar",
     "education",
     "chronicles",
     "health",
     "butler-detail",
     "audit-log"
    ]
   },
   {
    "title": "The trace spine breaks at its hub nodes",
    "body": "The .3/.4 spine work made signals into doors, but the destinations those doors converge on drop the spine: /sessions/:id (deep-linked from 10+ surfaces) renders no trace_id/request_id/parent/stderr; the audit reader deliberately withholds result/error/metadata (core_122 columns) so the ledger of record cannot say whether any action succeeded; timeline events never carry the trace_id sessions already persist; the ingestion event drawer cannot emit a trace outward; issue groups ('Seen 47x') have no path to their occurrences because the audit API lacks a result/group predicate.",
    "exemplars": "frontend/src/pages/SessionDetailPage.tsx:134-263 (omits trace_id/request_id/parent/process_log); src/butlers/api/models/audit.py:58-63 ('intentionally left to a later PR'); src/butlers/api/audit_grouping.py:199-204 + routers/audit.py:291-316 (no result= predicate \u2014 group evidence unreachable)",
    "affected": [
     "sessions",
     "audit-log",
     "timeline",
     "ingestion",
     "issues",
     "qa",
     "entity-detail",
     "memory"
    ]
   },
   {
    "title": "Dispatch is opt-in, not the default: the base layer is still shadcn",
    "body": "The default typeface is the spec-forbidden system-ui/Roboto stack (Inter Tight is per-component opt-in that flagship pages never take); the shared page shell renders text-3xl font-bold h1s to 24 consumers; empty-state is icon+heading+cta against the one-serif-sentence spec; the same semantic gray has two values (--muted-foreground 0.556 vs --mfg 0.46, 1,378 vs 174 sites); butler hues collide 11-into-8 via modulo; chart color speaks four dialects. The in-flight lint (bu-86c4c.6) stops new drift but cannot flip runtime defaults \u2014 every new page is born off-language.",
    "exemplars": "frontend/src/index.css:8 (root font-family system-ui/Roboto vs spec.md:143); frontend/src/components/ui/page.tsx:106 (text-3xl font-bold, 24 consumers); frontend/src/components/ui/ButlerMark.tsx:104-105 (idx % 8 \u2192 qa=chronicler, relationship=education, travel=finance)",
    "affected": [
     "cross:visual-language",
     "education",
     "system",
     "issues",
     "audit-log",
     "settings",
     "dashboard",
     "sessions",
     "timeline",
     "ingestion",
     "chronicles"
    ]
   },
   {
    "title": "Component-local filter state severs the inbound half of clause-is-a-door",
    "body": "Outbound doors landed, but the pages they point at hold filters in useState with no URL form, so the system's own attention links dump the owner on unfiltered streams: the dashboard's 'N failed notifications' links bare /notifications, Trust Console's 'Full audit log' drops its privileged predicate, a shared timeline URL loses every facet, and no surface can ever deep-link a filtered view of notifications/timeline/issues. Filtered views are also unshareable, un-bookmarkable, and palette-unaddressable.",
    "exemplars": "frontend/src/pages/NotificationsPage.tsx:75 (filters in useState, no useSearchParams anywhere in file); frontend/src/components/overview/model.ts:511 (attention row href='/notifications', no ?status=failed); frontend/src/pages/TimelinePage.tsx:76-78 (facets/view in plain useState while ?event drawer param survives contextless)",
    "affected": [
     "notifications",
     "timeline",
     "issues",
     "audit-log",
     "health",
     "dashboard",
     "settings"
    ]
   },
   {
    "title": "Shipped but dormant: the ecosystem's judgment machinery is built and switched off",
    "body": "The substrate-level counterpart of the UI themes: memory decay/fading/expiry code exists and is wired to nothing (0 fading, 0 expired rows live; the spec-required sweep has never run, so every confidence number shown is an un-decayed write-time value); 4 of 9 memory butlers never consolidate (switchboard: 4,148 pending episodes vs 44 facts); the cross-butler discovery catalog has spec+table+tools and 0 rows; RFC 0011's anti-spam budget/dedup governs only insight-scan traffic while finance alone runs five direct-notify cron tasks around it; quiet hours default open and no butler configures them.",
    "exemplars": "src/butlers/modules/memory/storage.py:2354 (run_decay_sweep in no job registry, no schedule, no tool); src/butlers/modules/memory/__init__.py:185 + public.memory_catalog count=0 (enable_shared_catalog false everywhere); roster/finance/butler.toml:106-155 (five proactive tasks bypassing the broker via direct notify())",
    "affected": [
     "eco:knowledge-graph",
     "eco:proactivity",
     "eco:cross-butler",
     "eco:inference",
     "memory",
     "notifications"
    ]
   }
  ],
  "ranked_moves": [
   {
    "rank": 1,
    "title": "Honest aggregation: purge server-side zero-fill and adopt the degraded envelope fleet-wide",
    "what": "Extend the repo's own aggregates_available convention to every fan-out/aggregation endpoint: butlers board (stripe failure \u2192 stripe_unavailable flag, cron failure \u2192 cadence 'unknown' not false-OVERDUE), memory fan-out (pools_failed in meta), notifications list/stats (source_available), approvals flat/history (sources_degraded), settings header_counts (nullable fields), secrets breaks-catalogue (degraded flag), spend summary/breakdown (unavailable_butlers). Then gate every verdict/all-clear renderer \u2014 SystemVerdictBanner, NeedsYouStrip, plex flanks, spend movers \u2014 on isError + degraded flags so calm is never asserted over failed or partial sources.",
    "why": "North star: 'no number is ever fabricated; every degraded source names itself; no failed fetch ever impersonates an empty queue.' This is the run's dominant defect class \u2014 the truth amnesty (bu-86c4c.1/.2) fixed the frontend while the backend still fabricates calm on ~12 surfaces, invisible to any client-side sweep because the fetch returns 200.",
    "evidence": "src/butlers/api/routers/butlers.py:497-503 ([0]*24 on stripe failure) and :480-488 (cron failure \u2192 false OVERDUE); src/butlers/api/routers/memory.py:118-131; src/butlers/api/routers/notifications.py:93-111; src/butlers/api/routers/approvals.py:1546-1548; src/butlers/api/routers/settings_console.py:137-282; SystemVerdictBanner.tsx:61-62 ('Instance healthy' over errored sources); NeedsYouStrip.tsx:59-67 ('All N healthy' when liveness is unknowable); SpendPage.tsx:1377-1381 (movers fabricate '+$X \u00b7 new' from a half-failed comparison).",
    "cost": "L",
    "kind": "ux",
    "slice_plan": "1) Shared envelope helper + convention doc (extend CLAUDE.md Degraded-Mode section); 2) per-router sweeps as independent PRs (butlers, memory, notifications, approvals, spend, settings, secrets), each with a contract test that a raising source yields degraded flags, never zeros; 3) verdict/all-clear gating on system/butlers/plex/spend renderers; 4) frontend em-dash + named-source rendering via the existing SourceDegradedNote vocabulary."
   },
   {
    "rank": 2,
    "title": "Confirmed-lies hotfix batch: seven verified fabrications, each a few lines",
    "what": "One sprint of \u2264S fixes for lies confirmed at decision points: QA PRPanel 'No PR. Escalated to user.' rendered for every in-flight case + pulse strip coloring findings_dispatched patrols clean-green (status string mismatch) + silent 25-row rail cap; butler-detail Errors KPI structurally hardwired to 0 (counts a trigger_source value that cannot exist); secrets PageSystem green plaque over failing state + fabricated 0ms probe latency + audit link that 422s (missing s: prefix); notifications 'Retried' filter that can never match a row; spend live-MTD double-counting against its own polling baseline; timeline dead-API-impersonates-idle (stale strip when head poll fails after first paint).",
    "why": "Each is a confirmed fabrication \u2014 the purest violation of earned calm \u2014 at the exact moment the owner decides whether to act, and each fix is mechanically small. Doctrine: trust defects outrank everything at this cost.",
    "evidence": "PRPanel.tsx:26-31 + CaseDossier.tsx:150-157 (stage available, unused); QaOverviewPage.tsx:292-296 vs src/butlers/api/routers/qa.py:1631-1637 ('dispatched' vs 'findings_dispatched'); ButlerActivityTab.tsx:301-307 vs core/sessions.py:25; secrets pages.tsx:1648-1649, :1139/:1762 (latencyMs:0), :1928; NotificationsPage.tsx:40-47 vs notifications.py:149-186; SpendPage.tsx:1243-1260; use-timeline-ledger.ts:131-133.",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "File as one batch bead with seven independent commits; each carries a regression test pinning the honest behavior (e.g. state='failed' system credential renders red plaque; findings_dispatched dot renders amber; MTD counter resets on forecast resolution)."
   },
   {
    "rank": 3,
    "title": "Wire the memory lifecycle: decay sweep runs, consolidation is module-default, backlog drains",
    "what": "Register run_decay_sweep as a scheduled-job handler and have the memory module self-register default maintenance schedules (decay, consolidation, episode cleanup, superseded purge) on startup for all 9 butlers \u2014 toml overrides cadence, not existence. Drain the 6,500+ pending-episode backlog (switchboard 4,148, finance 1,294, education 1,137) with a bounded catch-up job, and swap the ivfflat lists=20 indexes (built on empty tables) to HNSW while touching migrations.",
    "why": "Substrate-level fabrication: docs/modules/memory.md and the memory-retention-policy spec promise a decay sweep that has never run anywhere, so every confidence value the console displays is an un-decayed write-time number masquerading as a maintained one, and 4/9 butlers promote nothing to durable facts. Turning on shipped machinery is the highest value-per-cost ecosystem move available.",
    "evidence": "src/butlers/modules/memory/storage.py:2354 (run_decay_sweep absent from _MEMORY_MAINTENANCE_JOB_HANDLERS at scheduled_jobs.py:378-381, no toml schedule, no tool); live dev DB: 0 fading, 0 expired rows; roster grep: only 5 of 9 butlers schedule consolidation; migrations/001_memory_schema.py:84,203,330 (ivfflat lists=20 at empty-table creation).",
    "cost": "M",
    "kind": "ecosystem",
    "slice_plan": "1) memory_decay_sweep handler + schedules in the 5 already-cron'd butlers (ships alone); 2) module-default schedule registration in on_startup + delete copy-paste toml blocks; 3) bounded consolidation backfill respecting dead_letter states; 4) HNSW migration + nightly synthetic-scale recall harness; spec delta to memory-retention-policy declaring jobs module-default."
   },
   {
    "rank": 4,
    "title": "Dashboard coherence: one liveness model, one decision safety contract",
    "what": "Move the Overview onto GET /api/butlers/board's canonical server verdict (delete model.ts's private status sets, 5-minute threshold, and the two dead queries \u2014 the event bus already live-patches the board key), and fold the approvals undo window (scheduleDecision/UNDO_WINDOW_MS) into the shared useApprovalDecisionMutations hook so dashboard rows, /approvals, and the upcoming chat widget inherit the identical grace-window contract.",
    "why": "'One instrument built by one hand': the flagship page currently renders contradictory liveness verdicts against /butlers in the same instant (and its KPI contradicts its own attention list), and the identical approve verb is undoable on /approvals but fires an irreversible tool execution on a single click on the page the owner lands on first.",
    "evidence": "frontend/src/components/overview/model.ts:151-153,271-311 (private HEALTHY/DEGRADED/OFFLINE sets + own stale threshold) vs use-butler-status-board.ts:1-15 (canonical cadence-aware verb); event-cache-registry.ts:79-93 (patches ['butlers','board'] but not the dashboard's keys); DashboardPage.tsx:139-141 (direct mutate) vs ApprovalsPage.tsx:1308-1325 (undo window).",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) model.ts accepts BoardRow[] (pure-function tests first); 2) DashboardPage swaps hooks, deletes dead queries, KPI consumes board verdict; 3) extract scheduleDecision into use-approval-decisions behind an option, ApprovalsPage consumes it behavior-identically; 4) dashboard rows opt in with inline 'Approving in 5s \u00b7 Undo' state."
   },
   {
    "rank": 5,
    "title": "One session dossier on the trace spine, live for running sessions",
    "what": "Merge SessionDetailPage and SessionDetailDrawer into a single Dispatch-grade SessionDossier rendering every SessionDetail field the store holds \u2014 trace_id \u2192 /timeline, request_id \u2192 /sessions?request=, parent_session_id link, complexity, resolution_source, cost, and process_log stderr/exit_code as the named root evidence for failures. Always fetch via global getSession (delete the ?butler= dual path and the 'try adding ?butler=name to the URL' copy), collapse to one bus-invalidated query key so the palette's own trigger\u2192session flow stops landing on a frozen 'Running' page, and mirror list selection to ?selected=.",
    "why": "The trace-id spine is only as strong as its busiest hub: 10+ surfaces deep-link to /sessions/:id, where the owner arrives asking 'why did this fail' and finds no trace, no stderr, no parent, and a page that never updates while a session runs \u2014 detect works, diagnose dead-ends.",
    "evidence": "SessionDetailPage.tsx:134-263 (omits trace_id/request_id/parent/process_log despite types.ts:221-268 carrying all of them); inbound links from SpendPage.tsx:719, ApprovalsPage.tsx:670, TimelineLedger.tsx:76, notification-feed.tsx:174 et al.; event-cache-registry.ts:88-92 invalidates only ['session-detail', butler, id] while GlobalActionsRegistrar.tsx:36 navigates to the un-invalidated global key.",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) Extract SessionDossier from the drawer content with full field set + stderr disclosure; 2) rebuild SessionDetailPage as a Page-archetype wrapper on it, one global query key, bus-invalidated; 3) running-session elapsed ticker + streaming tool-call tail; 4) ?selected= URL mirroring + j/k/[/]/y keyboard loop on the list."
   },
   {
    "rank": 6,
    "title": "The ledger tells the truth: audit outcome trio + issue-group occurrences",
    "what": "Project result/error/metadata (persisted since core_122) through the audit reader, API model, and table \u2014 Outcome column in the three state colors, error text and metadata in the detail row, failure rows linking to their /issues group. Add result= and group= predicates to GET /api/audit-log (or GET /api/issues/{key}/occurrences reusing the shared CTE) so 'Seen 47x' finally opens its 47 occurrences with session/request_id links. Default the page to kind=privileged (backend predicate already exists), fix Trust Console's link to carry it, and render the detail expansion under its row instead of at the table bottom.",
    "why": "The audit log is the root of the evidence spine and it currently cannot say whether any audited operation succeeded \u2014 while the issues feed built ON result='error' rows offers no path to them. 'Root evidence one keystroke away' dead-ends one hop before the truth on both surfaces.",
    "evidence": "src/butlers/api/models/audit.py:58-63 ('intentionally left to a later PR'); routers/audit.py:398,439 (projections omit the trio) and :291-316 (no result/group predicate); audit_grouping.py:199-204 (multi-butler groups emit bare /audit-log); AuditLogTable.tsx:131-253 (detail row appended after the entire map); SettingsPermissionsPage.tsx:459 (privileged predicate dropped mid-drill).",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) Reader/model/type projection + Outcome column + adjacent detail row (coordinate DisclosureRow with bu-f310e); 2) result=/group= predicate + full-predicate links from audit_grouping; 3) issues rows gain DisclosureRow expansion with occurrences + ButlerMark chips; 4) kind=privileged default + noise toggle + Trust Console link fix."
   },
   {
    "rank": 7,
    "title": "Flip the Dispatch base layer: defaults, not opt-ins",
    "what": "Make the language the default in one coordinated pass: root font-family \u2192 var(--font-sans) (killing the spec-forbidden system-ui/Roboto default); rewrite ui/page.tsx headings to Display/Title 500 and ui/empty-state.tsx to the one-serif-sentence spec (~48 consumers inherit conformance); alias the shadcn token names to Dispatch values in index.css (--muted-foreground\u2192--mfg, --card\u2192--bg-elev, --destructive\u2192--red \u2014 1,378 sites snap to canonical values in one diff); explicit 11-slot butler-hue map ending the modulo collisions, spec table generated from the constant; extend chart-colors.ts to four semantic channels (series/butler-identity/category/neutral-density-ramp) and migrate the raw-hex charts; retire skeleton-pulse for FetchingDim + static placeholders.",
    "why": "'One instrument built by one hand' cannot be lint-enforced onto a base layer that defaults off-language \u2014 every un-redesigned surface and every 3am page is born wrong. This single move does more for fleet-wide coherence than any per-page reskin, and it hands bu-86c4c.6 a clean baseline instead of day-one suppressions.",
    "evidence": "index.css:8 (system-ui/Roboto root default vs spec.md:143; index.css:247-249 admits opt-in); ui/page.tsx:106 (text-3xl font-bold, 24 consumers); index.css:29 vs :109 (two grays for one role, 1,378 vs 174 sites); ButlerMark.tsx:51-105 (11 butlers into 8 slots); MasteryTrendChart.tsx:55-56 #3b82f6, hex-heatmap.ts:33-37 (state ramp repurposed for density); 46 animate-pulse sites vs fetching-dim.tsx's 3 consumers.",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) Root font + page.tsx heading unification + empty-state rewrite; 2) token aliasing + contrast-test updates; 3) butler-hue registry + collision test + spec table sync; 4) chart registry channels + education/chronicles/topology migration; 5) skeleton retirement + animate-pulse added to the .6 forbidden list."
   },
   {
    "rank": 8,
    "title": "One attention ledger: all proactive owner egress through the broker",
    "what": "Route every proactive owner-facing byte through RFC 0011's machinery: convert the direct-notify prompt-cron tasks (finance's five, health's digests) into insight candidates; add a deterministic owner-level attention policy (quiet hours, per-intent daily budgets, cross-fleet dedup, same-window coalescing into one composed Dispatch-voice message) enforced at the Switchboard/Messenger notify boundary; make the delivery cycle and notify gate consult the context bus (dnd/sleeping) deterministically; add an hourly urgent sub-cycle so priority\u226590 means hours, not one daily slot.",
    "why": "Earned calm must hold at the phone, not just the console: the anti-spam architecture is structural for a minority of traffic, quiet hours default open and no butler configures them, and the observed calendar double-notify is one instance of the missing arbiter, not its extent. Extends beyond bu-24lu6 (which adds pushes) \u2014 this is the governance layer under them.",
    "evidence": "roster/finance/butler.toml:106-155 (five tasks instructing direct notify(), bypassing budget/dedup/cooldown); core-notify spec quiet-hours gate defaults-open + zero delivery_preferences rows fleet-wide; grep: context-bus consumed only by spawner_context.py \u2014 the notify gate never checks dnd; roster/switchboard/butler.toml:59-63 (one insight delivery slot/day for 'time-critical').",
    "cost": "L",
    "kind": "ecosystem",
    "slice_plan": "1) Attention ledger table + owner-level quiet hours + notify-path counting (deterministic, no butler changes); 2) context-bus gating of delivery cycle + notify; 3) convert finance's five tasks to candidates; 4) same-window coalescing + urgent hourly sub-cycle; 5) dashboard attention-ledger panel under Trust Console; RFC amending 0011 + Messenger/Switchboard contracts."
   },
   {
    "rank": 9,
    "title": "Verdict-opener rollout: a shared, degradation-honest DispatchVerdict primitive",
    "what": "Extract SystemVerdictBanner's composition pattern into one shared primitive \u2014 deterministic template, clauses as typed doors, any degraded input names itself or suppresses the all-clear \u2014 and adopt it as the opener on approvals ('3 waiting; nearest expires in 40m; one approved action never ran'), sessions (failure clustering), notifications (windowed by_butler verdict), qa (staffer masthead from fields already fetched), spend (pace + confidence + top mover), ingestion, issues, butler-detail, and calendar. No LLM cost \u2014 pure composition from data each page already fetches.",
    "why": "The north star's signature property \u2014 'every surface opens with the system's own synthesized verdict, and every clause of that prose is a door' \u2014 landed on exactly two surfaces. Nine agents independently proposed the same move because the inputs are already on the wire and, on qa/spend/notifications, literally discarded.",
    "evidence": "types.ts:4967-4990 (staffer_status/last/next patrol/breaker/credentials fetched, unrendered at QaOverviewPage.tsx:239-247); SpendPage.tsx:80 (projection_confidence discarded before render); notification-stats-bar.tsx:90-99 (by_butler thrown away); EducationPage.tsx:78-80 (static tagline).",
    "cost": "L",
    "kind": "ux",
    "slice_plan": "1) Extract primitive + variants (skeleton/all-clear/problems) with the isError-suppression contract from move 1; 2) adopt on qa + approvals + spend (highest-stakes, data-ready); 3) sessions/notifications (needs small aggregate facets); 4) ingestion/issues/calendar/butler-detail; each adoption is an independent S/M PR."
   },
   {
    "rank": 10,
    "title": "One overlay contract + shell announcer: fix the fake-modal command palette",
    "what": "Extract useModalChoreography (focus-in, trap, Escape, focus-restore, sr-only status region) from the ingestion EventDrawer and apply it to EntityFinder \u2014 currently a raw div overlay with no dialog role that leaks Tab focus behind its own scrim and never restores focus \u2014 plus TimelineEventDrawer, CalendarAgendaView (aria-modal with no trap: worse than no ARIA), and ButlerManagementTab. Add one shell-level sr-only aria-live region announcing stream state edges, route changes, and new-event counts (and render LiveIndicator on mobile, where the degraded-stream signal currently vanishes). Lint hand-rolled fixed-inset overlays; generalize the real-page axe pattern into a route-registry-driven sweep over all 38 pages.",
    "why": "The console's single most important surface fails basic dialog semantics, and 'every degraded source names itself' is currently visual-only \u2014 SR users never hear the fleet stream drop. The correct implementations exist in-repo once each; this makes them mechanisms.",
    "evidence": "EntityFinder.tsx:431-453 (no role=dialog/aria-modal, Tab leak when activeResult null, no focus restore); CalendarAgendaView.tsx:44-48; LiveIndicator.tsx:56-69 (silent swaps, hidden sm:inline-flex); 3 of 38 pages have axe tests (ButlersPage/ButlerDetailPage/TimelineTab only); EventDrawer.tsx:554-575 as the gold-standard donor.",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) Extract hook + migrate EntityFinder (keep Tab=hop on entity rows only, fix activeResult tracking); 2) migrate the three other overlays; 3) shell announcer fed by useEventStream edges + Page title effect + NewEventsPill; 4) overlay lint entry; 5) registry-driven axe suite with skip-manifest burn-down."
   },
   {
    "rank": 11,
    "title": "Shortcut registry + fleet palette adoption: an unadvertised shortcut becomes structurally impossible",
    "what": "Add binding metadata to PaletteCommand and ship useRegisterShortcut \u2014 one hook that both installs page-scoped keys (with the editable-field guard, subsuming bu-5o22a, extended to SELECT/contentEditable/open-modal suspension) and publishes them to the '?' sheet ('On this page' section) and the palette's inline kbd column. Make the palette browsable (recents + verbs at empty query, shared fuzzy scorer), migrate the approvals j/k/a/d/x and ChatPanel chords onto it, delete MemorySearch's dead colliding '/' handler, extract useListTriage from Approvals for Dashboard/Issues/Notifications rows, and register verbs on the ten verb-mute surfaces (calendar, memory, secrets, qa, spend, health, education, butler-detail, settings, chronicles).",
    "why": "'The first use needs no docs, the tenth needs no mouse': the one-command-spine landed as infrastructure and 2 of ~25 pages adopted it; the product's best interaction (approvals triage) is a secret advertised nowhere; the fleet's richest workspace (calendar) has one keydown handler in 6,472 lines.",
    "evidence": "command-registry.tsx:36-45 (no binding field) and :104 (2 importers); ApprovalsPage.tsx:1332-1374 (keys, zero hints anywhere); use-keyboard-shortcuts.ts:41-45 vs MemorySearch.tsx:82-96 ('/' double-fire); EntityFinder.tsx:366-375 (verbs hidden until first keystroke, no recents); CalendarWorkspacePage.tsx:2017 (sole keydown).",
    "cost": "L",
    "kind": "ux",
    "slice_plan": "1) binding field + useRegisterShortcut + help-sheet section + palette kbd column; 2) migrate approvals/chat, delete the '/' collision, add u=undo; 3) recents store + fuzzy scorer + empty-query verbs; 4) useListTriage extraction + Dashboard/Issues/Notifications adoption with a shared footer hint strip; 5) per-page verb registration sweep (each page an S PR); complements in-flight bu-86c4c.15 (verbs) without overlap \u2014 this is reachability."
   },
   {
    "rank": 12,
    "title": "API-direct inference lane + purpose-tagged spend attribution",
    "what": "Add an 'api' runtime adapter (direct provider tool-use call, no CLI subprocess) and flip the discretion/classification catalog tiers onto it \u2014 connector screening and switchboard routing return in model-time instead of paying cold CLI+MCP bootstrap, halving the two-serial-spawn latency of every interactive reply. Simultaneously make that spend honest: add a purpose dimension to public.token_usage_ledger, fix the classifier's trigger_source 'tick'\u2192'classification' (it currently makes spend rules targeting {trigger:'route'} silently never match), and give discretion calls per-connector identity instead of '__discretion__' with NULL session_id.",
    "why": "A JARVIS answers in conversational time and accounts for every token: today every thought is a heavyweight subprocess and the highest-volume calls are the least attributed \u2014 /spend cannot price one inbound Telegram message, and classification spend masquerades as scheduler ticks. Vision rule 4 intact: judgment stays in LLM calls, only the transport gets cheap.",
    "evidence": "core/runtimes/claude_code.py:439 (subprocess per invoke); discretion_dispatcher.py:226-239 (1-turn screens on the same path, mcp_servers={}), :106/:251-258 ('__discretion__', session_id=None); modules/pipeline.py:1979-1984 (trigger_source='tick' on classification); model_routing.py:481-483 (trigger constraints fail-closed on None).",
    "cost": "M",
    "kind": "ecosystem",
    "slice_plan": "1) Adapter + tests behind an unreferenced catalog entry; 2) flip discretion tiers, verify connector screening; 3) classifier structured-tool-use fast lane with CLI fallback; 4) ledger purpose column + both write paths + trigger_source rename with fixture sweep; 5) surface purpose in /spend drill-down and rules."
   },
   {
    "rank": 13,
    "title": "URL-backed filters + predicate-carrying inbound doors on the triage surfaces",
    "what": "Migrate filter state to useSearchParams on notifications, timeline (facets/view/butlers \u2014 the drawer's ?event already is), issues (plus a default 7d window and severity/butler pills with a capped CTE), and health measurements (?type=); collapse audit-log's dual filter state (the ?actor param silently overriding the visible input) into one URL-serialized state. Then make the system's own inbound clauses carry their predicates: dashboard 'N failed notifications' \u2192 /notifications?status=failed, Trust Console 'Full audit log' \u2192 ?kind=privileged, stat tiles become filter anchors, and off-page ?event deep links resolve honestly instead of rendering nothing.",
    "why": "Clause-is-a-door fails inbound across six surfaces: the console hands the owner links that dump them on unfiltered streams, filtered views are unshareable and palette-unaddressable, and one page shows data its own controls deny. Cheap, mechanical, and it unlocks every future deep-link (chat widget, Telegram alerts).",
    "evidence": "NotificationsPage.tsx:75 (useState-only); model.ts:511,587-599 (bare /notifications hrefs); TimelinePage.tsx:76-78 + TimelineLedger.tsx:302-311 (?event silently renders nothing off-page); AuditLogPage.tsx:40-72 (URL overrides input); issues.py:64 (unbounded all-time CTE, no window).",
    "cost": "M",
    "kind": "ux",
    "slice_plan": "1) Notifications params + dashboard/tile predicate links; 2) timeline facets \u2192 URL + off-page ?event resolution (single-event lookup or explicit notice); 3) issues 7d window + pills + LIMIT; 4) audit single filter state + action-cell pivot; 5) health ?type= feeding the one-scope page."
   },
   {
    "rank": 14,
    "title": "Interaction-speed consolidation: one socket, poll-policy tokens, intent prefetch, never-blank floor",
    "what": "Wrap the singleton event stream in an EventBusProvider with a subscribe(type, cb) API and retire the three legacy per-page WebSockets (approvals stream currently double-invalidates the same keys as the registry); replace the refetchInterval zoo with named policy tokens (bus-covered \u2192 5-min reconcile per the blessed Approvals pattern) enforced by lint; add usePrefetchOnIntent to RowLink/DisclosureRow/palette-highlight via a route-registry prefetch map (zero prefetch call sites exist today); extend placeholderData+FetchingDim to windowed lists (chronicles day-step currently blanks the entire page, calendar week-nav, finance, search); and add a registry\u2194hook coverage test so a surface can never silently go event-dead again (the notifications-page miss is the proven instance).",
    "why": "'State arrives as a live event stream' and 'moves at the speed of thought': the primitives landed (bus, speed policy, FetchingDim) but adoption stopped, leaving duplicate refetches, 15-30s polls beside the bus, cold drill-downs everywhere, and full-page skeleton flashes on the archive's hottest path.",
    "evidence": "use-approvals-stream.ts:147-150 duplicating event-cache-registry.ts:55-61; use-butler-status-board.ts:191 (30s poll on a bus-patched key); repo-wide grep: zero prefetchQuery sites; use-chronicles-briefing.ts:33-43 (no placeholderData \u2192 WorkspaceSkeleton per day-step); event-cache-registry.ts:100-104 (misses ['notifications']/['notification-stats']).",
    "cost": "L",
    "kind": "ux",
    "slice_plan": "1) Provider + subscribe API + delete useApprovalsStream (pure win); 2) port spend ticker + settings console, delete their sockets; 3) POLL tokens + demote the four bus-covered surfaces + lint on numeric intervals; 4) prefetch hook wired to sessions/timeline/approvals rows then palette; 5) placeholderData sweep (search, chronicles, calendar, finance) + registry coverage test."
   },
   {
    "rank": 15,
    "title": "Turn on the cross-butler knowledge plane: memory_catalog enable + backfill",
    "what": "Flip enable_shared_catalog on fleet-wide (or change the default), add a backfill job upserting the ~3,600 existing facts/rules into public.memory_catalog via its UNIQUE(source_schema,source_table,source_id) key, and land the first consumers: catalog search in briefing context assembly and a fleet-knowledge search surface on the dashboard memory router.",
    "why": "There is currently no way for one butler to read another's knowledge short of a full Switchboard LLM round-trip, yet the sanctioned discovery plane is fully built \u2014 spec, table, tools \u2014 and switched off with 0 rows. Cheapest durable capability in the ecosystem backlog; also the prerequisite sequencing step before delegation/subscriptions are worth their L costs.",
    "evidence": "src/butlers/modules/memory/__init__.py:185 (default False; zero toml enables fleet-wide); live dev DB: SELECT count(*) FROM public.memory_catalog = 0; tools/reading.py:113 (memory_catalog_search returns empty for every butler); write-behind already implemented at tools/writing.py:381,531.",
    "cost": "M",
    "kind": "ecosystem",
    "slice_plan": "1) Flip default + toml enables (write-behind starts populating); 2) idempotent backfill job + test; 3) briefing context consumption + dashboard cross-butler search; spec delta making catalog default-on with a backfill requirement."
   }
  ],
  "dropped": [
   "Cross-butler delegation ask/answer ledger \u2014 strongest new capability proposal but L-cost and correctly sequenced after the memory_catalog flip (move 15); carry to next run.",
   "Domain-event subscriptions + cross-domain case files (eco:cross-butler) \u2014 premature before catalog/delegation land; both L-cost with dependency chains.",
   "Deterministic precondition gates on scheduled prompt tasks (eco:inference) \u2014 real recurring spend waste, but efficiency-only; below the trust-weighted cutoff this run.",
   "Latency spine per-phase spawn timings (eco:inference, S) \u2014 narrowly cut; file as a standalone bead alongside move 12, it makes that move measurable.",
   "New connectors (simplefin, flightstatus, activitywatch, weather, parcels) \u2014 sound, manifesto-grounded perception expansions; defer as a dedicated connector epic rather than consuming top-15 slots against trust defects.",
   "Speculative reply drafting, wake-anchored delivery, per-category insight feedback (eco:proactivity) \u2014 fold behind move 8's attention ledger and bu-24lu6's decision loop as later slices.",
   "Secrets scheduled verification sweep + event-bus credential state ('earned green') \u2014 valuable L; below cutoff, uncovered by any bead, file for next run.",
   "Dashboard clause-doored LLM verdict with anchor spans (L) \u2014 subsumed by move 9's deterministic verdict primitive; the LLM-elaboration half deferred.",
   "Butler-detail run-stays-on-console drawer \u2014 page-local polish; verdict opener folds into move 9, palette verbs into move 11.",
   "Health one-scope measurements page, daily_avg lead, condition-color inversion, six-sibling dialect \u2014 page-local M work; dialect largely covered by move 7's base flip; file as a health bead cluster.",
   "Education tutor hero / Dispatch reskin / event-bus / node dossier \u2014 reskin covered by move 7, bus by move 14, palette by move 11, verdict by move 9; node-dossier drill-down deferred.",
   "Chronicles day-scoped evidence coverage strip + keyboard SVG timeline marks \u2014 coordinate with the in-flight Chronicler IEA epic (bu-jc6htw/bu-8whey5) so the Day Ribbon inherits the fixes rather than duplicating them.",
   "Calendar overlay link_ref drill-down, always-on counts masthead, one-timezone sweep, now-line \u2014 real gaps but routed under the calendar roadmap epic bu-l3k0zg; keymap/bus halves covered by moves 11/14.",
   "Plex server-side attention verdict, optimistic retier undo, find fall-through, canvas keyboard parity \u2014 verdict folds into move 9; the rest are page-local S/M beads for the fleet.",
   "Timeline server-side heartbeat exclusion + saved-view divergence honesty + view-delete undo \u2014 below cutoff; deep-link half covered by move 13.",
   "Spend butler-hue on CostStripeChart + keyboard money controls + verdict line \u2014 hue folds into move 7's chart registry, keyboard into move 11, verdict into move 9.",
   "Destructive-without-recovery trio (webhook delete, spend-rule delete, timeline saved-view delete) \u2014 file as one S bead applying the models-page DeleteConfirmDialog / undo-toast pattern.",
   "Settings model-verification evidence spine + DB size-history endpoint + permissions inherited-cell operability \u2014 good M beads, below cutoff; permissions spec self-contradiction routes to bu-9q1dx reconciliation.",
   "QA rail truncation count / j-k rail / claim-evidence focusability \u2014 truncation and status-mapping land in move 2; keyboard halves land via move 11's registry adoption.",
   "New inert-KPI instances (memory overture/pipeline stats, chronicles KPI cells, secrets KPI cells, butler-detail config KVs) \u2014 extend bu-9d5vp's scope note rather than a new move.",
   "Circles arbitrary-hex badge contrast + system text-amber-600 raw utility \u2014 extend bu-kx3xc's site list; audit DisclosureRow anchoring coordinates with bu-f310e (move 6 notes this).",
   "Editable-field guard gaps (SELECT, contentEditable, modal suspension) \u2014 scope input to bu-5o22a, subsumed by move 11's useRegisterShortcut guard.",
   "rule_applications spec drift + 'memory butler' stale references + dashboard-domain-pages vs design-language spec conflict \u2014 route to bu-17axsl doctrine-spec-code reconciliation.",
   "Sessions drawer null-butler hard-fail + ToolCallTimeline raw palette \u2014 folded into move 5's dossier merge and move 7's token sweep respectively.",
   "Entity-detail per-entity duplicates endpoint + two-h1 headline + Telegram wizard relocation \u2014 real (duplicates is the page's worst lie) but page-local; file as an entity-detail bead cluster, duplicates endpoint first.",
   "Ingestion frozen-window aggregates fix \u2014 S and real, but ingestion-local; file as a bead (it narrowly lost the last hotfix-batch slot to fleet-visible lies).",
   "Attention-strip inert links on ingestion timeline + bidirectional trace on event drawer \u2014 trace half overlaps move 5/6's spine work; file the one-import link fix as an S bead."
  ]
 }
}