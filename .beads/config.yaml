# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Issue prefix for this repository (used by bd init)
# If not set, bd init will auto-detect from directory name
# Example: issue-prefix: "myproject" creates issues like "myproject-1", "myproject-2", etc.
# issue-prefix: ""

# NOTE: This repo runs bd v1.0.x in Dolt-native mode (see sync.mode / dolt.*
# below). The shared Dolt server (dolt.parrot-hen.ts.net:3307, db `butlers`) is the source of
# truth — there is NO SQLite database and NO `bd sync` command. The legacy
# SQLite/JSONL settings below (no-db, sync-branch) are inert in this mode and
# kept only as documented defaults.
#
# Legacy no-db toggle (SQLite vs JSONL); ignored in Dolt-native mode.
no-db: false

# Disable daemon for RPC communication (forces direct database access)
# no-daemon: false

# Disable auto-flush of database to JSONL after mutations
# no-auto-flush: false

# Disable auto-import from JSONL when it's newer than database
no-auto-import: true

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by BD_ACTOR or --actor)
# actor: ""

# Path to database (overridden by BEADS_DB or --db)
# db: ""

# Auto-start daemon if not running (can also use BEADS_AUTO_START_DAEMON)
# auto-start-daemon: true

# Debounce interval for auto-flush (can also use BEADS_FLUSH_DEBOUNCE)
# flush-debounce: "5s"

# Export events (audit trail) to .beads/events.jsonl on each flush/sync
# When enabled, new events are appended incrementally using a high-water mark.
# Use 'bd export --events' to trigger manually regardless of this setting.
# events-export: false

# Legacy sync-branch setting — inert in Dolt-native mode (there is no `bd sync`
# command and no beads-sync branch). Left commented out for historical reference.
# sync-branch: "beads-sync"

# Multi-repo configuration (experimental - bd-307)
# Allows hydrating from multiple repositories and routing writes to the correct JSONL
# repos:
#   primary: "."  # Primary repo (where this database lives)
#   additional:   # Additional repos to hydrate from (read-only)
#     - ~/beads-planning  # Personal planning repo
#     - ~/work-planning   # Work planning repo

# Integration settings (access with 'bd config get/set')
# These are stored in the database, not in this file:
# - jira.url
# - jira.project
# - linear.url
# - linear.api-key
# - github.org
# - github.repo

# Active backend: Dolt-native. Mutations write directly to the shared Dolt
# server and auto-commit to its history (durable without any sync/flush step).
sync.mode: "dolt-native"
dolt.host: dolt.parrot-hen.ts.net
dolt.port: 3307

# Git-tracked JSONL mirror. NEVER point this at issues.jsonl — on bd 1.0.4 that
# triggers a full-reimport loop on every write. `bd export` refreshes this file.
export.path: "issues.export.jsonl"

export.git-add: false

# Disable the post-merge/post-checkout JSONL->Dolt auto-import (bd PR #3730,
# GH#3729, default true). That feature exists to plug a data-loss gap in
# JSONL-in-git sync topologies with no shared Dolt server: without it, issues
# created on another machine could enter this machine's Dolt only via a
# manual `bd import`, so an untouched local Dolt could silently overwrite the
# richer git-tracked JSONL on the next auto-export.
#
# That scenario cannot happen here: this repo runs Dolt-native shared-server
# mode (see sync.mode / dolt.* above), so every write already lands directly
# on the one shared server at dolt.port — there is no "other machine's Dolt"
# to reconcile, and issues.export.jsonl is a pure downstream export, never an
# input. With import.auto left at its default, every `git checkout <branch>`
# and `git pull`/`git merge` re-imports the full ~9MB issues.export.jsonl via
# `bd import --quiet`, which upserts ~5.6k issues row-by-row and took 5-8
# minutes per checkout while contending the shared :3307 server (bu-dna8i).
# Note: no-auto-import (above) does NOT gate this — that flag guards a
# different, older stale-DB auto-import path (maybeAutoImportJSONL);
# import.auto is the key PR #3730 actually checks.
#
# CAVEAT (confirmed empirically in PR #3144 review): on bd v1.0.4 this line
# is a no-op. bd's YAML config loader does not wire up `import.auto` —
# `bd config get import.auto` reports "(not set)" and `bd config show`
# reports "import.auto = true (default)" even when this file is the one bd
# loads (confirmed via `bd -v config get`, which prints the config.yaml path
# it read). The BD_IMPORT_AUTO env var IS honored, so the actual fix lives in
# .beads/hooks/post-checkout and .beads/hooks/post-merge (each exports
# BD_IMPORT_AUTO=false outside the managed BEADS INTEGRATION markers, so it
# survives `bd doctor --fix` hook regeneration). This YAML line is kept as
# forward-compatible documentation of intent in case a future bd version
# wires the key up — do not rely on it alone.
import.auto: false
