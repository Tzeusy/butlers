# Evidence Map

| Topic | Class | Required depth | Confidence | Why it matters here | Repo evidence | When necessary |
|---|---|---:|---|---|---|---|
| MCP/FastMCP tool boundaries | foundation | working | direct | Butlers expose tool surfaces over MCP, and spawned runtimes call only their butler's tools. | `README.md`, `docs/concepts/mcp-model.md`, `src/butlers/core/spawner.py`, `tests/core/test_spawner_mcp_config.py` | Before reading code |
| Long-running daemons and ephemeral LLM subprocesses | system-model | working | direct | Durable orchestration and nondeterministic reasoning are intentionally separated. | `docs/architecture/butler-daemon.md`, `docs/runtime/spawner.md`, `src/butlers/core/runtimes/` | Before reading code |
| Modules vs connectors | system-model | implementation | direct | Modules add in-process tools; connectors normalize external transports into ingress. | `docs/concepts/modules-and-connectors.md`, `src/butlers/modules/`, `src/butlers/connectors/` | Before changing capabilities or ingress |
| Switchboard routing, envelopes, and request context | system-model | implementation | direct | Message routing, provenance, trace context, and prompt-injection boundaries depend on durable envelopes. | `docs/concepts/switchboard-routing.md`, `docs/architecture/routing.md`, `src/butlers/core_tools/_routing.py`, `tests/contracts/test_routing_pipeline.py` | Before changing routing |
| `asyncio` task lifecycle and cancellation | foundation | implementation | direct | Sessions, scheduler loops, connectors, queues, and shutdown are cancellation-sensitive. | `src/butlers/core/spawner.py`, `src/butlers/background.py`, `src/butlers/lifecycle.py`, `tests/core/test_buffer.py` | Before changing runtime code |
| Bounded queues, backpressure, retry, and crash recovery | foundation | implementation | direct | The system assumes some work is at-least-once and recoverable through durable storage. | `src/butlers/core/buffer.py`, `roster/messenger/tools/reliability/`, `tests/core/test_dsa4_validation.py` | Before changing ingestion or delivery |
| Idempotency and deduplication | foundation | implementation | direct | Replays, retries, duplicate external events, and manual data entry need stable keys and careful conflict behavior. | `src/butlers/core_tools/_routing.py`, `src/butlers/modules/calendar.py`, `roster/finance/tools/transactions.py` | Before changing write paths |
| PostgreSQL schemas, roles, and `search_path` | foundation | implementation | direct | One database contains isolated butler schemas plus shared `public` data. | `docs/architecture/database-design.md`, `src/butlers/db.py`, `scripts/init-db.sql`, `tests/integration/test_schema_isolation.py` | Before changing SQL |
| Alembic multi-chain migrations | system-model | implementation | direct | Core, module, and roster migration chains run programmatically and must stay branch-safe. | `docs/data_and_storage/migration-patterns.md`, `src/butlers/migrations.py`, `alembic/versions/core/`, `tests/config/test_migrations.py` | Before adding tables |
| JSONB and application-layer validation | foundation | working | direct | Tool calls, state, route envelopes, job args, and scheduler results use flexible JSON data. | `docs/architecture/database-design.md`, `docs/data_and_storage/state-store.md`, `src/butlers/core/scheduler.py` | Before changing persisted payloads |
| Identity, contacts, owner roles, and entity anchoring | system-model | implementation | direct | Routing, OAuth, approvals, memory, and notifications depend on canonical owner/contact identity. | `docs/concepts/identity-model.md`, `src/butlers/identity.py`, `src/butlers/lifecycle.py` | Before changing user-facing flows |
| Credential stores, OAuth, and secret masking | foundation | implementation | direct | Integration credentials are DB-first and high-risk. | `docs/data_and_storage/credential-store.md`, `docs/identity_and_secrets/oauth-flows.md`, `src/butlers/credential_store.py`, `src/butlers/api/routers/oauth.py` | Before changing integrations |
| Approval gates and tool sensitivity | system-model | implementation | direct | Side-effecting tools must be gated, audited, and redacted. | `docs/modules/approvals.md`, `src/butlers/modules/approvals/`, `tests/api/test_api_approvals.py` | Before changing outbound actions |
| Cron, recurrence, calendar projection, and timezones | foundation | implementation | direct | Autonomous tasks, reminders, and calendar views require correct time semantics. | `docs/runtime/scheduler-execution.md`, `docs/modules/calendar.md`, `src/butlers/core/scheduler.py`, `tests/modules/test_module_calendar.py` | Before changing schedules/calendar |
| Runtime model catalog, quotas, token ledger, and timeouts | system-model | implementation | direct | Model/runtime behavior is DB-authoritative and cost-aware. | `model_catalog_defaults.toml`, `src/butlers/core/model_routing.py`, `tests/core/test_spawner_quota_enforcement.py` | Before changing runtime selection |
| OpenTelemetry, structured logs, metrics, trace context | foundation | working | direct | Debugging depends on traces crossing daemons, subprocesses, and MCP tool calls. | `docs/architecture/observability.md`, `src/butlers/core/telemetry.py`, `src/butlers/core/metrics.py` | Before changing runtime or ops |
| Docker Compose orchestration and network isolation | repo-orientation | working | direct | Local/prod workflows depend on service health, OAuth gates, connector startup, and restart policies. | `docker-compose.yml`, `scripts/compose.sh`, `scripts/egress-firewall.sh` | Before changing deployment |
| pytest-asyncio, xdist, testcontainers, and E2E cost | foundation | working | direct | Tests encode contracts but have async loop and Docker constraints. | `pyproject.toml`, `conftest.py`, `docs/testing/`, `tests/` | Before changing tests |
| Memory facts, provenance, embeddings, vector search | system-model | implementation | direct | Memory is structured, provenance-aware, and retrieval-backed, not a simple note store. | `docs/modules/memory.md`, `src/butlers/modules/memory/`, `openspec/specs/module-memory/spec.md` | Before changing memory |
| S3-compatible blob storage and attachment policy | foundation | working | direct | Attachments/imports depend on object refs, MIME/size policy, and not-found semantics. | `docs/data_and_storage/blob-storage.md`, `src/butlers/storage/blobs.py`, `tests/core/test_blob_storage.py` | Before changing attachments/imports |
| Frontend/API server-state contracts | repo-orientation | working | direct | Dashboard changes must preserve REST payloads, polling, and cache behavior. | `docs/frontend/backend-api-contract.md`, `frontend/package.json`, `frontend/src/api/client.ts` | Before changing UI/API |
| Doctrine/OpenSpec traceability | repo-orientation | working | direct | Major changes are governed by specs, doctrine, and contract tests. | `about/`, `openspec/`, `tests/contracts/` | Before planning nontrivial changes |
| WhatsApp Go sidecar and Unix-socket IPC | repo-orientation | glossary | direct | A narrow connector uses a separate Go bridge and subprocess/IPC model. | `whatsapp-bridge/`, `openspec/changes/whatsapp-connector/design.md` | Nice later unless changing WhatsApp |
