## 1. Contract and owner gates

- [ ] 1.1 Obtain written provider, privacy/data-retention, security, product/operations, and implementation-owner approval for RFC 0024; do not treat this planning PR as execution authorization.
- [ ] 1.2 Resolve and document the Messenger-initiated provider-native send that returns the exact stable reference directly, the fenced principal/dispatch/send-report/confirmation-report idempotency contract, the exact-reference Sent proof, OAuth scope, canonical account reference, authenticated ingress binding, transient provider-age assertion source and receipt-delay/clock-skew budget, confirmation deadline, and full-window coverage semantics before enabling any account.
- [ ] 1.3 Decide active-literal `has-email` only versus an explicit versioned provider peer-alias authority; if the latter is approved, document its source/writer, exact canonical-account/entity/peer binding, issuance/expiry/revocation/purge lifecycle, evidence-time/query-time validity, and no-auto-renewal rule.  In this v1 an active alias can support only a positive result: because output is entity-wide and 180-day authority is half-open/retained, any active alias forces `null` absent `true`.  Provider account aliases remain out of scope unless a separate approved change defines them.
- [ ] 1.3a Obtain an owner-approved static complete canonical provider/account universe for negative evidence, including disabled, unsupported, and unproven accounts.  Document its configuration writer, no mailbox/provider-list/backfill discovery rule, rolling continuity interval, identical-member-set-only rollover, and change/gap/expiry invalidation; do not enable `false` until that internal interval naturally spans 180 days.
- [ ] 1.4 Update RFC 0024, the affected OpenSpec specs, Messenger/Gmail/Relationship documentation, and topology notes if the approved provider contract differs from this plan.

## 2. Private Messenger storage and access controls

- [ ] 2.1 Add `roster/messenger/migrations/004_email_correspondence.py` with the private allowlisted four-state ledger, qualified inbound observations with a non-reversible account-scoped unique source-deduplication token, coverage epochs, complete account-universe memberships with rolling continuity bounds/checkpoint/state, bounded native-send dispatch/report and confirmation lease/report fences, optional versioned peer-alias authority, each record's no-later-than-180-day anchor, and no JSONB/free-text/content columns.
- [ ] 2.1a Add the fixed Messenger-owned, non-generic `SECURITY DEFINER` projection function `messenger.record_qualified_email_ingress(text, text, text, uuid, timestamptz, bytea)`, with fixed derived provider/canonical-account/normalized-peer/ingress-epoch/server-receipt/dedup-token inputs and `recorded|duplicate` output, migration-managed definer owner, `SET search_path = pg_catalog`, explicit schema-qualified references, `REVOKE ALL ON FUNCTION messenger.record_qualified_email_ingress(text, text, text, uuid, timestamptz, bytea) FROM PUBLIC`, and only `GRANT USAGE ON SCHEMA messenger` plus exact function `GRANT EXECUTE` to `butler_switchboard_rw`; deny that role all other Messenger object access and deny connector/dashboard/Relationship/other roles schema usage, execute, and all direct table access.  It accepts no raw source-event ID, atomically deduplicates/projections with the qualifying accepted-ingress transaction, and resists caller-controlled search paths; a projection failure leaves correspondence evidence/coverage unavailable rather than falling back to best effort.
- [ ] 2.1b Add only the fixed Messenger-owned, non-generic `SECURITY DEFINER` function `messenger.close_qualified_email_coverage(text, text, uuid, text, timestamptz)`, with broker-derived provider/canonical-account/ingress-epoch/fixed-category/server-closure-time inputs and `closed|already_closed` output.  Permit only `age_invalid`, `checkpoint_gap`, `reauth`, `rebind`, and `principal_mismatch`; use the same migration-managed owner, `SET search_path = pg_catalog`, schema-qualified body, `REVOKE ALL ON FUNCTION messenger.close_qualified_email_coverage(text, text, uuid, text, timestamptz) FROM PUBLIC`, minimal schema `USAGE`, and exact `GRANT EXECUTE` only to `butler_switchboard_rw`.  It accepts no peer/raw source ID/provider time/content/free text/caller principal, is idempotent, and atomically closes or blocks the matching coverage epoch in the authenticated ingress/control transaction.
- [ ] 2.2 Add a migration-admin-owned idempotent post-chain activation routine in `src/butlers/migrations.py`, invoked after relevant chains in `src/butlers/lifecycle.py` and after `src/butlers/cli.py::_migrate_all`; it creates/enables `public.relationship_email_correspondence_summary(uuid[])` only after Messenger tables and `relationship.entity_facts` exist and owner/ACL checks pass.
- [ ] 2.3 Implement downgrade/feature-disable safeguards: revoke the aggregate first; partial Messenger-first/Relationship-first/missing-schema topology remains aggregate-disabled/unknown; refuse destructive private-table drop while data exists; retain metadata until its approved expiry or a separate retention decision.
- [ ] 2.4 Add migrated PostgreSQL schema/ACL/topology tests for role isolation, both ingress-function signatures/owner/search path, input bound, output shape, direct-select denial, narrow projection/coverage-close execute grants, no public/connector/dashboard access, state/retention constraints, immutable deduplicated receipt time/coverage, each invalid-age/checkpoint-gap/re-auth/rebind/principal-mismatch closure, complete-universe identical-set rollover versus change/gap invalidation, messenger-first/relationship-first activation, and empty-only rollback.

## 3. Correspondence admission and provider outcomes

- [ ] 3.1 Add a focused correspondence service (for example `src/butlers/modules/email_correspondence.py`) with typed content-free intent, provider outcome, native dispatch/fence/send-report, confirmation lease/fence/report, qualified inbound, coverage, and aggregate-result values; Messenger alone atomically consumes each fenced report and records its outcome.
- [ ] 3.2 Refactor `src/butlers/modules/email.py` so Messenger commits/locks intent after approval and before egress; returns categorical outcomes; replaces recipient/subject/raw-error `gmail_send` audit writes; keeps SMTP `accepted -> unknown`/unleased; and accepts an exact reference only from the approved native-send response.
- [ ] 3.3 Update `src/butlers/core_tools/_routing.py` so trusted routing derives a stable opaque key and non-caller-spoofable content-free correspondence candidate before route invocation, carries it through immediate/replay/validation/network/no-admission paths, and categorizes errors before generic persistence.
- [ ] 3.4 Add the disabled-by-default Messenger-native provider send dispatcher, one-time broker binding, and transient-only content route; Gmail reports its direct send outcome/reference only through the principal/dispatch/fence-bound idempotent `send.report` before confirmation leasing, cannot initiate generic egress, and cannot manufacture a lease-eligible reference.
- [ ] 3.5 Add unit/behavior tests for commit-before-send, intent-write failure blocking egress, duplicate keys, concurrent callers, known rejection, accepted SMTP, post-dispatch crash/timeout to `unknown`, confirmation deadline expiry, allowed provider-idempotent retry, prohibited blind retry, native dispatch/send-report binding and replay/crash order, confirmation report replay, and pre-admission privacy failures.

## 4. Private notification and provider reconciliation boundaries

- [ ] 4.1 Update `roster/switchboard/tools/notification/deliver.py`, `log.py`, and the route-error seam so trusted pre-route correspondence candidates suppress content-bearing notification/outbound-inbox/audit mirrors and convert validation/network/no-admission/provider errors to categories before generic persistence; preserve normal non-email behavior and prevent caller spoofing.
- [ ] 4.2 Modify `src/butlers/connectors/mcp_client.py` (or its safe framework boundary) to attach a secret-authority managed scoped credential without logging it; register `correspondence.send.dispatch`, `correspondence.send.report`, `correspondence.confirmation.poll`, and `correspondence.confirmation.report` in `src/butlers/core_tools/_switchboard.py` and implement a focused Switchboard broker.
- [ ] 4.3 Make Switchboard derive connector/provider/endpoint/account principal server-side, issue at most one opaque native dispatch ID/fence/deterministic send-report ID and at most one later confirmation lease ID/fence/deterministic report ID, forward report data through the scrubbed route without separate consumption, and let Messenger atomically validate/consume/report. Identical retried reports return their categorical result; stale/conflicting/cross-account/fence-altered reports fail closed.
- [ ] 4.4 Extend `src/butlers/connectors/gmail.py` behind disabled-by-default account configuration to accept only Messenger-issued native sends for its verified principal, return its direct stable reference through the fenced `send.report`, and reconcile one recorded accepted leased reference using the documented exact metadata operation, without Sent enumeration, search, history traversal, mailbox scan, backfill, content fetch, reconciliation mutation, or `_sent_ids_cache` evidence beyond the approved native send.
- [ ] 4.5 Authenticate Gmail ingress at Switchboard, derive/verify provider+endpoint against the principal and canonical account, derive and discard a transient typed provider-age assertion at the authenticated adapter seam, and create private qualified-inbound observations only from post-enforcement unbroken epochs/committed checkpoints.  In the same qualifying accepted-ingress transaction, derive an account-scoped non-reversible source-deduplication token and call only `messenger.record_qualified_email_ingress(text, text, text, uuid, timestamptz, bytea)` under its exact schema-usage/function-execute grant so retry cannot replace server `received_at` or advance coverage.  On invalid age, checkpoint gap, re-auth, rebind, or principal mismatch, atomically call `messenger.close_qualified_email_coverage(text, text, uuid, text, timestamptz)` with its fixed category before the generic ingress/control path returns, so no old watermark remains fresh.  Use server `received_at` (not raw-envelope observed time) for the 180-day window. Add outbound coverage only after ledger/native-path enforcement for every eligible send, advance it from committed health checkpoints, and close it on fallback/disable/gap.
- [ ] 4.6 Add provider/broker/ingress fakes proving native-send dispatch/send-report binding and crash/replay, exact-reference confirmation, account/reference mismatch rejection, disabled/insufficient-scope/timeout/revocation outcomes, no list/search/backfill calls, no raw response/credential/error or transient-age assertion persistence, no provider mutation beyond the approved native send, no direct Messenger access, confirmation report replay/fence behavior, historical envelope exclusion, delayed/future/absent-age rejection, accepted-ingress duplicate/reset/gap/re-auth coverage handling, and atomic coverage close/no-stale-watermark behavior for every fixed close category.

## 5. Correlation, aggregate consumption, retention, and freshness

- [ ] 5.1 Implement the aggregate's exact same authenticated-canonical-account mapping and exact peer resolution from active literal `relationship.entity_facts` `has-email` values or the approved explicit provider peer-alias authority; reject inferred, provider-account-alias, expired, revoked, newly-issued-after-evidence, inactive, envelope-only, and no-peer identities.  Require an alias to remain active at query time and cover each positive evidence time.  For `false`, accept literal peers only and only when there is no active alias authority for the entity, one owner-approved complete account-universe continuity interval spans the query window, and every universe member's inbound/outbound coverage proves no leg for every literal peer; alias-derived negatives are always `null` in this v1.
- [ ] 5.2 Add a separate `email_correspondence_enrichment` known-entity job that submits at most 100 resolved entity IDs to the aggregate and consumes only its tri-state/freshness result.  Register its Relationship wrapper in `src/butlers/scheduled_jobs.py`; seed exactly one daily `35 6 * * *` `roster/relationship/butler.toml` schedule with `dispatch_mode="job"` and `job_name="email_correspondence_enrichment"`.  The handler is deterministic and zero-LLM, with no MCP/API/on-demand/interactive aggregate path, prompt, provider call, raw-ledger access, or public tool.  Preserve `run_email_identity_enrichment` as unresolved-sender inbound-only discovery/proposal work.
- [ ] 5.2a Before that job is registered, add a scheduler-level protected-job registry keyed by the normalized Relationship `source='toml'` schedule identity: `name="email-correspondence-enrichment"`, `cron="35 6 * * *"`, job dispatch mode, `job_name="email_correspondence_enrichment"`, and configuration-declared arguments.  In `src/butlers/core_tools/_scheduling.py`, reject generic `schedule_trigger` before dispatch.  In `src/butlers/core/scheduler.py`, reject generic `schedule_create` aliases/runtime copies and `schedule_update` attempts to mutate the protected identity or convert another row into it before persistence.  Keep trusted TOML configuration synchronization and a due system tick as the sole invocation path; do not add a generic runtime alias or a manual override.  Each rejection returns an auditable bounded category and emits a content-free metric/security audit event.  Any durable enforcement metadata or constraint is migration-managed, reversible, and owned by the canonical roster configuration rather than interactive CRUD.
- [ ] 5.3 Register Messenger's deterministic, zero-LLM/non-egress retention, deadline, native-dispatch/send-report and confirmation-lease/report-fence, coverage-close/rolling-successor, complete-universe continuity/rollover, and purge maintenance handler in `src/butlers/scheduled_jobs.py` and `roster/messenger/butler.toml`; update `roster/messenger/MANIFESTO.md`/`AGENTS.md` to state the narrow maintenance exception.
- [ ] 5.4 Add tests for authenticated same account+peer inside 180 days, different/envelope-only/provider-account-alias account, unmatched/inactive/newer-than-evidence/expired-or-revoked alias peer, no-resolvable-peer null, alias-only and literal-plus-active-alias null absent true, inbound-only recurrence, confirmed-outbound-only evidence, partial versus contiguous full coverage, current-but-new/incomplete/gapped/member-changed account-universe null, all-member complete-universe full-coverage false, stale/unknown watermarks, transient receipt-delay/future-skew rejection, deduplicated ingress receipt time, capped aggregates, every 180-day hard-delete anchor, and no fact/log materialization of raw metadata.  Add scheduler tests proving the protected identity rejects generic `schedule_trigger` before handler dispatch, rejects `schedule_create` aliases and `schedule_update` cron/job/argument mutation before persistence, emits only the bounded audit/metric category, and still permits the exact TOML configuration sync plus due system tick.  Add registry/TOML tests proving the fixed job and cron resolve to a callable handler, no public MCP/API/on-demand/interactive aggregate path exists, and the static packet contract preserves the RFC 0010 exception boundary.

## 6. Verification, rollout, and rollback

- [ ] 6.1 Run focused unit, behavior, migrated-PostgreSQL ACL/schema/topology, provider-native-send/broker/ingress fake, privacy non-materialization (including pre-admission failure), Relationship known-entity job, and notification-route tests from the exact implementation head.
- [ ] 6.2 Run `openspec validate true-bidirectional-email-correspondence --strict`, formatting/lint/type checks for touched code, `git diff --check`, documentation-link/guard checks, and independent exact-head review.
- [ ] 6.3 Deploy only after all owner gates approve a disabled-by-default canary for one provider/account; monitor only unlinkable categorical confirmation/freshness/coverage/retention counters without content or identifiers.
- [ ] 6.4 Roll back by disabling capability and revoking the aggregate so Relationship returns unknown; do not delete retained ledger data unless the empty-only migration guard passes or a separate owner retention decision authorizes it.
