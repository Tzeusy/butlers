## 1. Database contract and role boundary

- [ ] 1.1 Add failing real-PostgreSQL migration tests in `tests/migrations/test_codex_auth_generation_provenance_migration.py` for the additive `core_199` / `core_198` chain, a fresh database, an upgraded database with a valid legacy shared row, present malformed legacy content, a missing/mismatched binding, direct reserved-row DML, value-free provenance columns, and an intentionally irreversible downgrade that changes no catalog/data/ACL state.
- [ ] 1.2 Extend the privileged `scripts/init-db.sql` bootstrap with a fixed no-argument installer/finalizer and membership-free NOLOGIN provenance owner, then add `core_199_codex_auth_generation_provenance.py`, which catalog-verifies and invokes that installer rather than creating or owning protected objects. The installer creates the singleton authority state, opaque generation and operation tables, nullable `public.butler_secrets.codex_auth_generation_id` binding, constraints/indexes, fixed-search-path prepare/mark/complete/abandon/owner/cleanup operations, and direct-DML compatibility guard without copying or parsing a credential.
- [ ] 1.3 Add real effective-role ACL tests proving `PUBLIC`, connectors, unrelated runtime roles, and the normal migration login cannot write/read the provenance boundary, read/update `codex_auth_generation_id`, assume the owner, retain protected-schema `CREATE`, or invoke the installer/finalizer. Finalization must repair every broad-grant bootstrap rerun with FORCE RLS, the exact `codex_auth_non_reserved_secrets` and `codex_auth_reserved_owner` policies, and precise legacy-column grants. Prove a role allowed normal runtime preparation cannot invoke device-auth bootstrap, the dashboard no-`SET ROLE` path can use only its fixed definers, and ordinary non-Codex CRUD remains unchanged.
- [ ] 1.4 Add fresh/core-only and upgraded-database ordering tests proving migration-before-bootstrap fails closed, privileged-bootstrap-then-migration succeeds, no owner membership/direct DML survives finalization, no reserved raw row at all is the only first-bootstrap absence, a present malformed/unbound row fails closed until direct owner replacement, post-initialization legacy writes stay unprovable, and no orphaned `current_generation_id`/secret-binding state is possible.
- [ ] 1.5 Add `tests/config/test_codex_auth_generation_concurrency_integration.py` with behavior-executing real PostgreSQL races for two conditional successors, owner replacement versus completion, revoke versus device auth, duplicate completion, and production effective roles. Assert exactly one winner, one terminal result per operation, and no stale health write, extra generation/raw mutation, or orphan pointer; mocks may supplement but not substitute.

## 2. Typed system-global authority repository

- [ ] 2.1 Add failing unit tests in `tests/config/test_credential_store.py` for opaque one-time legacy adoption, complete-current reads, exact operation preparation/launch, conditional successor completion, health-only finalization, explicit abandonment from prepared and contained-launched states, direct owner replace/revoke precedence, duplicate completion/abandonment, safe malformed/unavailable results, and no secret/provenance leakage from repr/logging.
- [ ] 2.2 Add value-redacted `CodexAuthLaunchLease` and `CodexAuthBootstrapLease`, safe outcome/result/closed-abandon-reason enums, a normal `prepare_codex_auth_operation` with no bootstrap selector, a separate dashboard-only `prepare_codex_auth_device_bootstrap`, and `abandon_codex_auth_operation` to `src/butlers/credential_store.py`; keep a normal-operation raw document transient and `repr=False`, make the device-auth definer require physical row absence, preserve all non-Codex credential semantics, and remove Codex raw-value CAS as authority identity.
- [ ] 2.3 Implement the short transactions that lock the singleton state, validate the existing raw-row/generation binding, prepare/mark/complete exactly one operation, atomically write an accepted successor, record safe health, supersede stale operations, and revoke without a local-file fallback.
- [ ] 2.4 Implement deterministic expiry/cleanup in the core maintenance path with database-time expiry and terminal-operation-only 90-day pruning; retain current/referenced opaque generations and never include raw error/provider output in terminal reasons.

## 3. Runtime and local-stage conversion

- [ ] 3.1 Add failing adapter and sandbox tests in `tests/adapters/test_codex_auth_sync.py`, `tests/adapters/test_codex_refresh_lock.py`, `tests/adapters/test_codex_adapter.py`, and `tests/cli/test_runtime_cli_sandbox.py` for exact-generation launch revalidation, private stage attribution, behavior-executing peer read/write denial across two concurrent children, stale dashboard replacement, stale health withholding, malformed stage discard, no unlocked projection launch, and restart orphan non-promotion.
- [ ] 3.2 Refactor `src/butlers/core/runtimes/_codex_auth_sync.py` to remove content-digest/process-cache local successor inference; retain only strict parsing, atomic database-originated projection where required, and value-free safe diagnostics.
- [ ] 3.3 Refactor `src/butlers/core/runtimes/codex.py` plus the shared CLI-auth sandbox primitives so invocation and prewarm prepare/mark/complete/abandon their exact durable operations, use a unique leased outer UID/GID and Bubblewrap user/mount/PID/IPC/UTS namespaces around every child, carry one existing absolute deadline, and discard all stale/unprovable finalization. The Codex `--dangerously-bypass-approvals-and-sandbox` flag remains inside this outer kernel boundary.
- [ ] 3.4 Update `src/butlers/core/spawner.py`, `src/butlers/connectors/discretion_dispatcher.py`, and their focused tests so every Codex adapter construction receives the typed system-global authority boundary and unavailable provenance refuses only the affected Codex launch.

## 4. Dashboard, device-auth, and startup callers

- [ ] 4.1 Add failing device-auth tests in `tests/cli/test_cli_auth.py`, `tests/cli/test_runtime_cli_sandbox.py`, `tests/api/test_secrets_v2_cli_reauthorize.py`, and `tests/api/test_secrets_v2_codex_authority.py` for the separately authorized bootstrap, owner replacement/revoke racing a device session, the two-phase prepared-sandbox launch, containment/parser failure, stale completion, stage cleanup, and value-free session results.
- [ ] 4.2 Refactor `src/butlers/cli_auth/persistence.py`, `src/butlers/cli_auth/session.py`, `src/butlers/cli_auth/sandbox.py`, `src/butlers/cli_auth/sandbox_platform.py`, and `src/butlers/api/routers/cli_auth.py` so Codex preparation returns a redacted launch context, the platform prepares and seeds only that context's kernel-isolated private stage, a successful operation mark is required immediately before child creation, and the same lease survives containment/finalization. Prepared-stage failure, cancellation, timeout, failed marking, launch failure, containment/parser failure, and persistence failure invoke guarded abandonment with closed reasons; launched stages are removed only after namespace PID1 death. Other providers' persistence remains unchanged.
- [ ] 4.3 Add failing backend and frontend contract tests in `tests/api/test_secrets_v2_cli_mutations.py`, `tests/api/test_secrets_v2_cli_reauthorize.py`, `tests/api/test_secrets_v2_codex_authority.py`, `frontend/src/api/client.secrets-v2.test.ts`, and `frontend/src/components/secrets/passport/PageCli.actions.test.tsx` for direct owner replacement/revoke precedence, probe health withholding, the preserved Codex rotate `{fingerprint,value}` response, inventory/detail display-only fingerprints, response/audit redaction, and no exposure of operation/generation/lineage fields.
- [ ] 4.4 Refactor `src/butlers/api/routers/secrets_v2.py`, `src/butlers/api/app.py`, `src/butlers/api/routers/model_settings.py`, and `src/butlers/jobs/model_verify.py` to use the guarded owner/probe paths and safe health classifications while deliberately preserving the documented owner-only rotate raw-value-once response and on-read inventory/detail fingerprint contract.
- [ ] 4.5 Add/update startup coverage in `tests/daemon/test_startup_coverage_gaps.py` and connector restoration tests, then refactor `src/butlers/daemon.py`, `src/butlers/lifecycle.py`, and `src/butlers/cli_auth/persistence.py` restoration helpers so a restart uses the read-only complete-current-binding query, creates no prepared operation merely to project, and never promotes local state.

## 5. Documentation, completeness checks, and integration evidence

- [ ] 5.1 Add a source-completeness regression that enumerates every Codex raw load/store/CAS, subprocess/prewarm, dashboard/device-auth, startup, model-verify, direct-dispatcher, and connector-restoration call site and fails if it bypasses the typed generation-fenced boundary.
- [ ] 5.2 Update `docs/data_and_storage/credential-store.md`, `docs/identity_and_secrets/cli-runtime-auth.md`, `docs/concepts/butler-lifecycle.md`, `docs/architecture/butler-daemon.md`, and `docs/runtime/spawner.md` with the opaque-generation, restart, private-stage, precedence, redaction, and fail-closed contract; do not include credential-shaped examples or provenance identifiers.
- [ ] 5.3 After `harden-runtime-auth-and-breaker-attention` lands its `core-credentials` replacement, rebase and verify the canonical wording. Reconcile the `core-credentials`, `core-daemon`, `dashboard-api`, and `database-security` additions plus this change's full `MODIFIED` replacement of `core-spawner` requirement `Pre-Launch and Prewarm Codex Auth Synchronization`; remove the old local-fallback scenarios and add requirement-ID citations to behavior-executing tests.
- [ ] 5.4 Before implementation capture `IMPLEMENTATION_REVIEW_BASE="$(git rev-parse HEAD)"`. Run focused unit, API, adapter, behavior-executing sandbox, migration, effective-role, real-PostgreSQL concurrency, restart, and source-completeness suites; then run Ruff, format, strict OpenSpec validation, `make test-qg`, `git diff --check "${IMPLEMENTATION_REVIEW_BASE}...HEAD"`, and a targeted/stat diff over that same captured-base range before deployment authorization.
