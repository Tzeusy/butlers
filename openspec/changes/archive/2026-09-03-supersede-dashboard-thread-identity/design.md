# Design

The successor requirement retains the existing behavior under a new name and
replaces `event.external_thread_id` with `event.external_conversation_id` plus
`event.reply_target_ref`. Removing and adding through OpenSpec records why the
archived legacy clause is no longer present.
