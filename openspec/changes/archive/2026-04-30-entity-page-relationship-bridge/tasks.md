## 1. Frontend — EntityDetailPage header link

- [ ] 1.1 Modify `frontend/src/pages/EntityDetailPage.tsx`: in the main entity card's `CardContent` (lines 1254–1466), add a "View relationship activity →" `<Link>` rendered conditionally on `entity.entity_type === 'person'`. Target: `/butlers/relationship/entities/${entity.id}`. Styling: `text-primary text-sm font-medium hover:underline`. Position: directly after the Roles block (which closes around line 1380) and before the Source Provenance block (which begins around line 1382). Do NOT render any link or placeholder when the predicate is false (mirror the conditional-render convention used by "Mark as confirmed" at line 1462).
- [ ] 1.2 Add a unit/component test under `frontend/src/pages/__tests__/EntityDetailPage.test.tsx` (create if absent) covering: (a) link renders for `entity_type='person'` with correct href, (b) link is absent for `entity_type='organization'`, (c) link is absent for `entity_type='place'`, (d) link is absent for `entity_type='other'`. Use the existing test fixture pattern from `EntityDetailView.test.tsx` for entity payloads. The four entity types listed are the complete set permitted by the `chk_entities_entity_type` CHECK constraint at `entity-identity/spec.md:19-20` — no other types need testing.

## 2. Frontend — EntitiesPage row action

- [ ] 2.1 Modify `frontend/src/pages/EntitiesPage.tsx`: in the row actions cluster (lines 808–920), add an icon button immediately adjacent to the existing `EditIcon` button (currently at line 838). Use `lucide-react` `Activity` (or substitute if already imported and clearer). Wrap with the existing `Tooltip`/`TooltipTrigger`/`TooltipContent` pattern. Implement the button using the **always-rendered, conditionally-disabled** pattern that the existing "View contact" `UserIcon` button uses at lines 811–832: enabled with tooltip "Open relationship activity" and `onClick={() => navigate(\`/butlers/relationship/entities/${entity.id}\`)}` when `entity.entity_type === 'person'`; rendered disabled (no `onClick`) with tooltip "Not a person entity" otherwise. Do NOT omit the button for non-person rows — that breaks visual rhythm across the table.
- [ ] 2.2 Extend the existing `EntitiesPage` tests (`frontend/src/pages/__tests__/EntitiesPage.test.tsx` if present, else create) to cover: (a) the action button renders enabled for a row with `entity_type='person'` and the tooltip text matches, (b) the action button renders disabled for a row with `entity_type='organization'`, `entity_type='place'`, and `entity_type='other'` (parametrize over all three), (c) clicking the enabled button calls `navigate` with `/butlers/relationship/entities/<id>`, (d) clicking the disabled button does not navigate.

## 3. Verification and report

- [ ] 3.1 Run quality gates per `CLAUDE.md`: `uv run ruff check src/ tests/ roster/ conftest.py --output-format concise`, `uv run ruff format --check src/ tests/ roster/ conftest.py -q`, frontend `npm run lint` and `npm run test` for the touched files. All MUST pass.
- [ ] 3.2 Manual verification on the dev environment: navigate to `/butlers-dev/entities/<owner-entity-id>` (a `person` entity), confirm the "View relationship activity →" link appears between the Roles block and the Source Provenance block, and that clicking it lands on `/butlers-dev/relationship/entities/<owner-entity-id>` with all five tabs visible. Navigate to a non-person entity (an `organization`, `place`, or `other` row from `/butlers-dev/entities`) and confirm the link is absent on its detail page. On `/butlers-dev/entities`, verify the new row action is enabled on person rows (with the "Open relationship activity" tooltip) and disabled on non-person rows (with the "Not a person entity" tooltip), and that clicking the disabled button does nothing.
- [ ] 3.3 Run `openspec validate entity-page-relationship-bridge --strict`. After tasks 1–2 are merged and tests pass, run `openspec apply entity-page-relationship-bridge` and archive the change.
