# CRUD-to-SPO Migration: Coverage Checklist

Generated by bu-gjb1.4 verification audit. Last updated: 2026-03-15.

---

## Summary

The crud-to-spo-migration is **complete** across all four phases. All domain
butler tools have corresponding `store_fact()`-backed implementations, all
backfill scripts are implemented and idempotent, and all predicates are seeded
into the registry.

---

## Phase 1: Health Butler (bu-ddb.2)

| Item | Status | Evidence |
|---|---|---|
| Health predicate migration | DONE | `src/butlers/modules/memory/migrations/009_health_predicates.py` |
| `measurement_log` / `_history` / `_latest` → fact wrappers | DONE | `roster/health/tools/facts.py` |
| `symptom_log` / `_history` / `_search` → fact wrappers | DONE | `roster/health/tools/facts.py` |
| `medication_log_dose` / `_history` → fact wrappers | DONE | `roster/health/tools/facts.py` |
| `medication_add` / `_list` → property-fact wrappers | DONE | `roster/health/tools/facts.py` |
| `condition_add` / `_list` / `_update` → property-fact wrappers | DONE | `roster/health/tools/facts.py` |
| `research_save` / `_search` → property-fact wrappers | DONE | `roster/health/tools/facts.py` |
| `health_summary` / `trend_report` → fact queries | DONE | `roster/health/tools/facts.py` |
| Health butler tests updated | DONE | `roster/health/tests/test_facts.py` |
| Phase 1 backfill script | DONE | `src/butlers/scripts/backfill_facts.py::backfill_health()` |

---

## Phase 2: Relationship Butler (bu-ddb.3)

| Item | Status | Evidence |
|---|---|---|
| Relationship predicate migration | DONE | `src/butlers/modules/memory/migrations/011_relationship_predicates.py` |
| `interaction_log` / `_list` → temporal fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| `note_create` / `_list` / `_search` → temporal fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| `quick_facts` (fact_set, fact_list) → property facts | DONE | `roster/relationship/tools/facts.py` |
| `gift_add` / `_update_status` / `_list` → property-fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| `loan_create` / `_settle` / `_list` → property-fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| `reminder_create` / `_list` / `_dismiss` → property-fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| `feed_get` → temporal fact wrapper | DONE | `roster/relationship/tools/facts.py` |
| Life event tools → temporal fact wrappers | DONE | `roster/relationship/tools/facts.py` |
| Relationship butler tests updated | DONE | `roster/relationship/tests/test_facts.py` |
| Phase 2 backfill script | DONE | `src/butlers/scripts/backfill_facts.py::backfill_relationship()` |

---

## Phase 3: Finance Butler (bu-ddb.4)

| Item | Status | Evidence |
|---|---|---|
| Finance predicate migration | DONE | `src/butlers/modules/memory/migrations/010_finance_predicates.py` |
| `record_transaction` / `list_transactions` → temporal fact wrappers with dedup | DONE | `roster/finance/tools/facts.py` — `record_transaction_fact()`, `list_transaction_facts()` |
| `track_subscription` / `upcoming_bills` → property-fact wrappers | DONE | `roster/finance/tools/facts.py` — `track_subscription_fact()` |
| `track_bill` → property-fact wrapper | DONE | `roster/finance/tools/facts.py` — `track_bill_fact()` |
| `account` property fact for account tracking | DONE | `roster/finance/tools/facts.py` — `track_account_fact()` |
| `spending_summary` → JSONB aggregation query on facts | DONE | `roster/finance/tools/facts.py` — `spending_summary_facts()` |
| Finance MCP tools registered | DONE | `roster/finance/modules/tools.py` — all `*_fact` variants registered as `@mcp.tool()` |
| Finance butler tests updated | DONE | `roster/finance/tests/test_facts.py` |
| Phase 3 backfill script | DONE | `src/butlers/scripts/backfill_facts.py::backfill_finance()` |

### Finance Tool Inventory (Phase 3 fact-layer)

| MCP Tool | Implementation | Predicate | Type |
|---|---|---|---|
| `record_transaction_fact` | `facts.record_transaction_fact()` | `transaction_debit` / `transaction_credit` | Temporal |
| `list_transaction_facts` | `facts.list_transaction_facts()` | `transaction_debit` / `transaction_credit` | Read |
| `track_account_fact` | `facts.track_account_fact()` | `account` | Property |
| `track_subscription_fact` | `facts.track_subscription_fact()` | `subscription` | Property |
| `track_bill_fact` | `facts.track_bill_fact()` | `bill` | Property |
| `spending_summary_facts` | `facts.spending_summary_facts()` | `transaction_debit` (debit-only aggregation) | Aggregation |

### Finance Deduplication Contract (D7)

`record_transaction_fact()` implements deduplication via pre-insert query on
`(entity_id, predicate, valid_at, metadata->>'source_message_id')` when
`source_message_id` is provided. Race conditions are handled by the
`idempotency_key` mechanism in `store_fact()` (SHA-256 hash of
`entity_id|predicate|posted_at|merchant|amount|currency`).

---

## Phase 4: Home Butler (bu-ddb.5)

| Item | Status | Evidence |
|---|---|---|
| Home `ha_state` predicate migration | DONE | `roster/home/migrations/002_seed_ha_state_predicate.py` |
| `ha_entity_snapshot` persistence → `ha_state` property fact with supersession | DONE | `roster/home/modules/__init__.py` — `_persist_entity_snapshot()` |
| HA device entity lazy-create/lookup pattern | DONE | Inline UPSERT on `shared.entities (tenant_id, canonical_name, entity_type)` in `_persist_entity_snapshot()` |
| Home butler tests updated | DONE | `tests/modules/test_module_home_assistant.py` |
| Phase 4 backfill script | DONE | `src/butlers/scripts/backfill_facts.py::backfill_home()` |

### Home Implementation Notes

- Entity resolution uses `UPSERT ... ON CONFLICT (tenant_id, canonical_name, entity_type) DO UPDATE SET updated_at = now()` — consistent with D8 specification.
- `valid_at=None` is passed to `store_fact()` — correct property-fact semantics. Each new snapshot supersedes the previous active `ha_state` fact.
- `permanence="volatile"` — consistent with short-lived device state.
- Backfill script uses `predicate="status"` (generic) rather than `"ha_state"` for legacy `ha_entity_snapshot` rows, since the predicate registry entry `ha_state` was added post-migration. This is intentional — it avoids mixing legacy snapshots with live `ha_state` facts in the same query plane.

---

## Cross-Cutting (bu-ddb.6)

| Item | Status | Evidence |
|---|---|---|
| GIN index on `facts.metadata` | DONE | `src/butlers/modules/memory/migrations/012_facts_gin_indexes.py` — `idx_facts_metadata_gin` |
| Partial B-tree for meal/nutrition aggregation | DONE | `012_facts_gin_indexes.py` — `idx_facts_meal_predicate_valid` |
| Partial B-tree for transaction/spending aggregation | DONE | `012_facts_gin_indexes.py` — `idx_facts_transaction_predicate_valid` |
| Partial B-tree for measurement/health trend queries | DONE | `012_facts_gin_indexes.py` — `idx_facts_measurement_predicate_valid` |

---

## Backfill Scripts (bu-ddb.7)

Single unified script: `src/butlers/scripts/backfill_facts.py`

| Phase | Entry point | Tables covered | Idempotency |
|---|---|---|---|
| health | `backfill_health()` | measurements, symptoms, medication_doses, medications, conditions, research | `backfill_source` key in `facts.metadata` |
| relationship | `backfill_relationship()` | quick_facts, interactions, life_events, notes, gifts, loans, tasks, reminders, activity_feed | same |
| finance | `backfill_finance()` | transactions, accounts, subscriptions, bills | same |
| home | `backfill_home()` | ha_entity_snapshot | same |

Usage:
```bash
python -m butlers.scripts.backfill_facts --phase all
python -m butlers.scripts.backfill_facts --phase finance --dry-run
```

All phases skip rows that already have a corresponding fact in the `facts`
table (checked via `metadata->>'backfill_source' = '<table>:<id>'`).
Tables that do not exist are gracefully skipped with a warning log.

---

## Entity Resolution Contract Verification (bu-ddb.8)

### D1: No Bare String Subjects

| Domain | Implementation | Compliant |
|---|---|---|
| Finance | `_get_owner_entity_id(pool)` fetches `shared.entities WHERE 'owner' = ANY(roles)`. When resolved, `entity_id` is always passed to `store_fact()`. When owner entity is absent (new install), facts are stored with `entity_id=None` (graceful degradation, not a bare string). | YES |
| Home | Inline UPSERT on `shared.entities` resolves/creates a device entity per HA entity ID. The returned UUID is always passed as `entity_id`. | YES |
| Health | Owner entity resolved at module startup and cached. All health facts carry owner `entity_id`. | YES |
| Relationship | Contact entity resolved via `shared.contacts.entity_id` FK. If NULL, `memory_entity_create()` is called and `shared.contacts.entity_id` is backfilled. | YES |

### Predicate Registry Coverage

All Phase 1-4 predicates are registered:

| Phase | Migration | Predicates seeded |
|---|---|---|
| Phase 1 (health) | `mem_009` | measurement_weight, measurement_blood_pressure, measurement_heart_rate, measurement_blood_sugar, measurement_temperature, symptom, took_dose, medication, condition, research |
| Phase 2 (relationship) | `mem_011` | interaction_call, interaction_meeting, interaction_message, interaction_email, interaction_other, life_event, contact_note, activity, gift, loan, contact_task, reminder |
| Phase 3 (finance) | `mem_010` | transaction_debit, transaction_credit, account, subscription, bill |
| Phase 4 (home) | `home_assistant_002` | ha_state |

### Scope Field Compliance

| Domain | Scope value | Where enforced |
|---|---|---|
| Health | `health` | All `store_fact()` calls in health fact tools |
| Relationship | `relationship` | All `store_fact()` calls in relationship fact tools |
| Finance | `finance` | `_store_fact()` wrapper in `roster/finance/tools/facts.py` |
| Home | `home` | `store_fact()` call in `_persist_entity_snapshot()` |

---

## Gap Analysis

No gaps found. All tasks from `tasks.md` phases 1-4 and the backfill section
are implemented and verifiable in the codebase. The only remaining item is
**bu-ddb.6** benchmarking (performance validation of aggregation queries against
10k facts), which is a runtime acceptance test, not a code change.

### Legacy CRUD Tools Retained (Non-Goals)

Per the design, the original CRUD-table tools (`record_transaction`,
`track_subscription`, `track_bill`, `list_transactions`, `spending_summary`)
are **retained** alongside the new `*_fact` variants. They continue to write to
the deprecated `transactions`, `subscriptions`, `bills` tables. This is
intentional — no breaking changes to existing callers. The deprecated tables
will be dropped in a future cleanup epic.
